# AGENTS.md

This repository's instructions for coding agents (Codex, OpenCode, Pi and the rest) live in one file, `CLAUDE.md` at the repository root. Read it before working here; everything in it applies to you, including the rule that `Scripts/ci.sh` and `Scripts/ci-linux.sh` run only on the user's explicit request.
