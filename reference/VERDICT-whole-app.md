COMPILES: yes
RENDERS: yes
EXERCISED: Launched `./target/debug/tiller` after fixing the machine's disk space (real ENOSPC, distinct from the noted transient os error 35). Single window confirmed via `osascript 'count windows'` = 1, geometry 985,304 / 1470×834 (ref is 1470×833, 1pt noise). Drove it entirely with cliclick: expanded/collapsed the "tiller" project in the sidebar (real git-status dot icons, colored per project) — worked. Switched Chat↔Terminal tabs — worked, but registration is fragile: `cliclick c:` (combined down+up) missed the hit-test repeatedly across many attempts; explicit `dd:`/`du:` with a pause was needed to land reliably. Typed a real shell command and confirmed a live PTY (a stray `[` keystroke sent while testing a keyboard shortcut triggered an actual pyenv-rehash error from the real shell); a tab that looked frozen (stale clock, typed "echo hello-critic-test-2" sitting unexecuted for minutes, no output) recovered cleanly on its own after switching away and back — clock resumed ticking and the pane came back clean. Toggled right panel Files↔Changes — real live git diff of the outer Swift repo (3 changed files: App/ContentView.swift, App/DividerCursorStrip.swift, AppTests/DividerCursorStripTests.swift) rendered correctly both times. Opened the "+" new-tab menu (New Terminal/Claude Code/Codex/OpenCode/Pi/Oh-My-Pi/New Browser/New Chat) — opens fine when clicked at its *current* screen position (it shifts right as tabs are added, and I lost time clicking a stale coordinate before realizing this was my error, not the app's), but **Esc does not dismiss it**: pressing Esc fired the default "New Terminal" entry instead of closing the menu, spawning an unwanted tab — reproduced twice independently (once at session-restore where the menu was already open, once deliberately reproduced later). This is a clean, deterministic defect, not flakiness. Sent a real message to a real Claude Code agent over ACP: first attempt failed with a red banner "could not launch ACP agent: ACP Startup timed out after 15s", after which the status pill stuck on "offline" and the Send button became permanently inert for the rest of that session — retyping the message, clicking Send, clicking the agent pill, and waiting 15+ seconds all did nothing; there was no visible retry affordance. On a fresh app launch (lower system load, no other cargo builds running), retried and got a full successful round trip: "what is 2+2? Answer in one word." → "4", streamed and rendered correctly with the "working"→"Ask" status-pill transition, matching the reference's turn structure closely. Also hit a genuinely orphaned second process/window mid-session: a `pkill -f target/debug/tiller` silently failed to kill one instance, and the next launch showed a ghost overlapping window (with unrelated fixture files `alpha.txt`/`beta.txt` in its Changes list, not part of this repo) composited on top of the real one; `ps aux` confirmed two live PIDs even though System Events reported only 1 window. Resolved by killing both PIDs explicitly and relaunching. I'm treating this as an artifact of my own process management under repeated relaunches rather than a first-class app defect, since a normal user quitting via Cmd+Q would not hit it — but I can't fully rule out a teardown bug given accessibility only ever reported one window even while two processes were alive. Quick source-level check against Zed conventions: 12,395 LOC across 8 crates (tiller, tiller_terminal, tiller_persistence, tiller_theme, tiller_project, tiller_ui, tiller_acp, tiller_git, tiller_agents); `main.rs` is a thin 473-line entry point, not a god-file; only 12 `.unwrap()` calls in non-test source, concentrated in `tiller_project/src/workspace.rs` (7) and `tiller_terminal/src/lib.rs` (4); the `block_on`/`thread::sleep` calls in `tiller_acp/src/lib.rs` and `tiller_project/src/git.rs` are inside dedicated spawned worker threads (`thread::Builder::new().spawn(...)`), not the render/main thread — no obvious main-thread blocking found by this pass, though I did not do a full manual audit of every render path.
BLIND PICK: indistinguishable on the static chrome — `reference/probe.py px` on sidebar bg, centre bg, divider, and tab accent all matched the reference within 0–1 per channel (sidebar `#141416` exact, centre `#1a1a1d` exact, divider `#000000` exact pure black, tab accent `#8495fc` vs ref `#8595fc`, 1 unit off, inside the documented noise band). Layout geometry (columns, tab bar, composer, right panel, status bar) matches structurally. The completed-chat-turn screenshot matches reference/shots/17-chat-response.png point for point in structure: user bubble, plain-text reply, timestamp, centered-timestamp divider, "Ask"-pill + agent-picker composer. Where it loses, concretely: the reference's sidebar shows a small green dot next to the active worktree ("main") and a checkmark on the completed Chat tab/Activity row signaling a finished turn; my post-response screenshot of ours shows neither — no live-session dot on the worktree row, no checkmark on the tab or Activity entry. That's a real, visible parity miss a side-by-side viewer would notice within a few seconds.
BIGGEST GAP: when ACP startup fails once, there is no recovery path — the composer's Send control goes permanently inert with no retry affordance, so the single most important loop in the app (talk to an agent) can dead-end for the rest of the session with no way out short of quitting and relaunching Tiller. Everything else I found (Esc/menu bug, click-registration fragility, missing status-dot/checkmark) is a rough edge; this one silently kills the app's core purpose with no on-screen indication of how to recover.
EVIDENCE: `cd rust && cargo build -j 2` → `cargo build: 0 errors, 1 warnings (122 crates)`; `ls -la target/debug/tiller` → 755 target/debug/tiller 60.2M. `osascript -e 'tell application "System Events" to tell process "tiller" to get {position, size} of window 1'` → `985, 304, 1470, 834`. `python3 reference/probe.py px /tmp/ours-01-chat-empty-833.png 10 400` → `#141416` (ref `#141416`); same file `700 300` → `#1a1a1d` (ref `#1a1a1d`); `325 300` → `#000000` (ref `#000000`); `380 36` → `#8495fc` (ref `#8595fc`). `python3 reference/probe.py diff /tmp/ours-01-chat-empty-833.png reference/shots/01-chat-empty.png` → `mean_delta=5.42 max_delta=230`, worst rows y=0, 821, 236-239, 818-820 (status bar and sidebar-row bands — content differences from a different project set and a different git branch/path string, not color drift). Screenshots taken myself via `screencapture -x -R985,304,1470,834 <path>` after `osascript ... set frontmost to true` before every capture, window count and PIDs re-verified before each: `/tmp/ours-01-chat-empty-833.png` (idle Chat, cropped 1470×833 to match ref for diffing), `/tmp/probe_chat_final.png` (project-expanded idle state), `/tmp/probe_send_final1.png` → `/tmp/probe_send_final2.png` (message sent, "working" pill) → `/tmp/probe_send_final3.png` (completed turn, reply "4", "Ask" pill), `/tmp/probe_afteresc.png` (Esc-triggers-New-Terminal reproduction, 4 tabs visible), `/tmp/probe_send1.png` / `/tmp/probe_chat_retry.png` (first-attempt ACP timeout banner + stuck-offline composer), `/tmp/probe_cleanfinal.png` (post-orphan-process cleanup, single window confirmed).
