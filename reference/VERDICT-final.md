COMPILES: yes
RENDERS: yes

EXERCISED:
- Built fresh: `cd rust && cargo build -j 2` → `cargo build: 0 errors, 1 warnings (407 crates)` (the one warning is the pre-existing `block` crate future-incompat notice, unrelated).
- Launched `./target/debug/tiller`, raised with `osascript ... set frontmost to true` before every capture, killed stray copies before each relaunch (needed repeatedly — see note on environment below).
- Pixel-sampled the idle 3-column view against `reference/shots/04-terminal-pane.png` with `probe.py px`: sidebar (150,400) `#141416`/`#141416`, divider (325,400) `#000000`/`#000000`, centre pane (700,400) `#191a1d`/`#1a1a1d` (1-unit, noise), right-panel divider (1064,400) `#000000`/`#000000`, right panel (1200,400) `#1a1a1d`/`#1a1a1d`. Active-tab accent line (500,36): ours `#8495fc`, reference `#8595fc` — 1-unit green-channel drift, inside the documented 1-3 capture-noise band. Column geometry and the two 1pt-black seams are exact.
- Typed a real command into the Terminal pane (`echo hello_from_critic`), pressed Return, got real PTY output back (`hello_from_critic`) and a live shell prompt with git-branch decoration, matching the reference's prompt richness.
- Switched Chat → Terminal → Chat; terminal output and scrollback survived the round trip.
- Opened the "+" new-tab menu (New Terminal / Claude Code / Codex / OpenCode / Pi / Oh-My-Pi / New Browser / New Chat all present), pressed Escape: menu closed cleanly, tab count unchanged at 3 (Chat, Terminal, Terminal) before and after. No new tab was spawned. This directly re-tests the previously-reported "Escape fires New Terminal" bug — not reproduced.
- Reproduced the ACP dead-end scenario directly: fresh Chat tab showed `could not launch ACP agent: ACP Startup timed out after 15s` with a **Retry** button, composer still accepting text (not locked out). On a later launch the same agent connected cleanly ("idle", green dot, no error) — confirming the timeout is genuinely this machine's load, exactly as the task's machine note predicted, not a code defect.
- With a live connection: sent a real message ("Say hi in one word.") to the actual Claude Code agent over ACP. It streamed back "Hi." for real. After the turn completed, all three status signals appeared together and agreed: a green dot on the `main` worktree row in the sidebar, a green checkmark on the `Chat` tab title, and a colour change on the Chat row in the Activity list (amber while streaming, back to idle-with-mark after). This directly re-tests the previously-reported "reference shows a dot/checkmark, ours showed neither" bug — not reproduced; all three now agree, live, off a real turn.
- Ran the regression test that specifically covers the dead-end bug: `cargo test -j 2 -p tiller_ui failed_launch_can_retry_and_complete` → `cargo test: 1 passed (1 suite, 0.66s)`. Read it: it asserts `"a failed launch must leave Send usable"` after a bad agent command, then swaps in a working stub agent, sends, and asserts the turn completes. This is exactly the bug, exercised, not just eyeballed.
- Attempted: sidebar filter, Files/Changes toggle, stage/discard-plus-git-status, and Escape-cancels-a-live-stream. Sidebar filter and Files/Changes toggle attempts were inconclusive — my clicks kept landing during windows where this machine had killed my process or stolen window focus (see below), and I ran out of stable time before getting a clean repro. Stage/discard: this checkout's `git status --short` is clean, so there was nothing to stage or discard, and my instructions here restrict me to writing under `/tmp` only, so I did not manufacture a dirty file in the checkout to force the test. I did not get a clean run of Escape-cancels-mid-stream before running out of time.
- Read `TillerWorkspace::new` in `crates/tiller/src/main.rs`: 11 positional arguments (titlebar, sidebar, tab_bar, status_bar, right_panel, tabs, active_tab, working_directory, actions, session, cx), no builder. This is the literal example CRITIC.md names as a gap.
- Grepped for the other named smells: no blocking filesystem/process calls in a render path (the one `std::fs::read_dir` in `right_panel.rs` runs once at construction before the window is visible; all later refreshes go through `cx.background_spawn`); `main.rs`'s five `.expect()`/`.unwrap()` calls are all at startup-fatal points (spawn a PTY, read cwd, index a static catalog by known ID) in the same spirit as Zed's own window-init panics, not swallowed I/O errors; `.clone()` counts in render are high (23-41 per file) but nearly all are `Entity<T>` handle clones, which is normal, idiomatic GPUI/Zed style, not the "cloning heavy data every frame" smell.

ENVIRONMENT NOTE, not a code finding: this box is under heavy, sustained load from other concurrent agents on the same machine. My `tiller` process was killed out from under me (SIGKILL, not a crash — no diagnostic report, log empty) more than a dozen times across this session, sometimes mid-click; my window was repeatedly stolen or repositioned by other apps; the disk hit ENOSPC twice, including once while I was just running `df`. I did not count any of that against the app. Every functional finding above is from a run where I confirmed the process was alive and frontmost immediately before and after the action.

BLIND PICK: indistinguishable, for the parts I could put side by side — the sampled pixels are exact at every column/divider/pane point I checked, the terminal prompt styling (glyph badges, git branch, dirty marker, timestamp) matches the reference's richness, and the status-signal trio now matches `17-chat-response.png`'s description point for point.

BIGGEST GAP: the sidebar tree under a worktree only ever shows the `Chat` row — across every screenshot I took, with 2 or 3 Terminal tabs open the whole time, no `Terminal` row ever appeared under `main` in the sidebar, while the reference (`04-terminal-pane.png`) shows both `Chat` and `Terminal` rows there; the Activity list on the right does list every open tab correctly, so the two lists disagree with each other, which is the same "one piece of state, several views" problem the status-signal work was supposed to have put to rest.

EVIDENCE:
- Build: `cd rust && cargo build -j 2` → `cargo build: 0 errors, 1 warnings (407 crates)`
- Test: `cargo test -j 2 -p tiller_ui failed_launch_can_retry_and_complete` → `cargo test: 1 passed (1 suite, 0.66s)`
- Pixel probe: `python3 reference/probe.py px /tmp/critic_pre_menu.png 150 400 325 400 700 400` and the matching reference call, output quoted above
- Screenshots (all real, taken by me with `screencapture -x -R 40,60,1470,834`, window raised via `osascript ... set frontmost to true` immediately before each):
  - `/tmp/critic_menu_open.png` — ACP failed state, Retry button, composer still live
  - `/tmp/critic_turn_done.png` — completed real turn: "Hi." reply, green dot on `main` in the sidebar, green checkmark on the `Chat` tab
  - `/tmp/critic_menu_open3.png` — "+" menu open, all 8 entries, tab count 3
  - `/tmp/critic_after_esc.png` — same window after Escape: menu gone, tab count still 3
  - `/tmp/critic_sent.png` — message sent, composer shows "working", Activity dot for Chat turns amber
- Code read: `crates/tiller/src/main.rs:73-84` (`TillerWorkspace::new`, 11 positional args); `crates/tiller_ui/src/chat.rs:1499-1544` (the retry regression test)
