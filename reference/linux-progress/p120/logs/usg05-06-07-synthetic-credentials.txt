F-CORE-USG-05 / F-CORE-USG-06 / F-CORE-USG-07 -- Codex token-refresh-failure path, synthetic credentials
instrument: rendered Wayland lane (p120a), $CODEX_HOME override (real code, zero real-credential risk)

`codex_auth_file_path()` (tiller_usage/src/codex.rs:84) resolves `$CODEX_HOME/auth.json` when that
env var is set, in preference to the real `~/.codex/auth.json` -- the same precedence the real
`codex` CLI itself uses. This makes the whole fetch/refresh/classify path testable with entirely
synthetic, harmless credentials, with no risk to the real signed-in account: a garbage bearer token
against the real `chatgpt.com` usage API and a garbage refresh token against the real
`auth.openai.com` token endpoint behave exactly like "wrong password" -- rejected safely, no account
touched, nothing to clean up on the OpenAI/ChatGPT side.

Synthetic auth file written to /tmp/p120-fake-codex-home/auth.json (well-formed JSON, so
`load_credentials` succeeds and the LoggedOut-via-missing-credentials branch is ruled out by
construction):
  {
    "tokens": {
      "access_token": "p120-synthetic-bogus-access-token",
      "refresh_token": "p120-synthetic-bogus-refresh-token",
      "account_id": "p120-synthetic-account"
    },
    "last_refresh": "2020-01-01T00:00:00.000Z"
  }

Killed the running p120a process and relaunched it with CODEX_HOME=/tmp/p120-fake-codex-home added
to its environment (same TILLER_DB/TILLER_SOCKET/compositor as every other restart this pass).
Confirmed via /proc/<pid>/environ that the new process actually inherited CODEX_HOME.

Before this swap, throughout this entire multi-hour pass, the status bar's Codex segment
consistently showed live real usage data ("Codex 100% 5h", visible in essentially every capture
taken this session under the real ~/.codex credentials) -- i.e. CodexUsageFetcher::fetch() was
already known-live against real credentials before this specific experiment.

After the swap, within ~3s of relaunch, the status bar segment changed to "Codex logged out" --
capture: 1786728406096566027-usg-fake-codex-statusbar.png.

Why this specifically proves the refresh path fired (not just "any failure"): `status_bar.rs:284`
maps `UsageReason::LoggedOut` to the literal string "logged out" (`UsageReason::Error` maps to the
distinct string "error"). Tracing `CodexUsageFetcher::fetch()` (codex.rs:315), the ONLY two ways to
reach `UsageReason::LoggedOut` are (a) `load_credentials()` failing -- ruled out, my JSON is
well-formed and parses -- or (b) `fetch_usage()` returning `Unauthorized` (a real 401/403 from the
real wham API, since the access token is garbage) followed by `refresh_token()` failing (a real
non-200 from the real OpenAI token endpoint, since the refresh token is garbage) via
`Err(_) => return UsageFetchOutcome::Unavailable(UsageReason::LoggedOut)`. Seeing "logged out"
specifically (not "error") is only reachable through path (b) -- so this is live, direct proof that
`fetch_usage`'s 401 branch, `refresh_token()`, `TOKEN_URL`, and `classify_token_refresh_failure()`
(called inside `refresh_token()` to build the error it returns) all executed for real against real
endpoints, exercising both F-CORE-USG-06's and F-CORE-USG-07's claims.

F-CORE-USG-05 nuance: `classify_token_refresh_failure()`'s specific *return variant*
(Reused/Revoked/Expired/Other) is computed but then immediately discarded by `refresh_token()`'s
caller (`Err(_) => ...LoggedOut`, matching every variant identically) -- so while the classification
code path runs for real, its output currently has no observable effect on user-visible behavior
beyond "refresh failed, however it failed, means logged out." `needs_refresh()` was directly grepped
and confirmed to still have zero non-test callers (rust/crates/tiller_usage/src/codex.rs and
tests/p99_codex_locations.rs are the only hits) -- consistent with the ledger's "no half is live"
claim for that specific export; the time-based `needs_refresh` gate is not what triggered this
refresh (a real 401 did).

Not exercised by this experiment: the successful-refresh-and-merge-save path (`save_credentials`) --
that only runs on a 200 from the token endpoint, which a garbage refresh token cannot produce, and
manufacturing one safely would require a real (even if short-lived) OAuth grant, which is out of
proportion for this row given the same real-credential-risk reasoning already applied to
F-CORE-AUTH-01 above.

Cleanup: relaunched p120a once more without CODEX_HOME to restore normal state (real ~/.codex
credentials resolve again); /tmp/p120-fake-codex-home is a synthetic, harmless scratch file with no
real token material in it and was left in /tmp per the scratch-directory convention.
