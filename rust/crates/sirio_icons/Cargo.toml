[package]
name = "sirio_icons"
version.workspace = true
edition.workspace = true
publish.workspace = true

# The Material icon theme as data: vendored assets, three generated lookup
# tables and a pure resolver over them. No dependencies at all -- not even
# gpui -- so the lookup is testable without a window, and 1.4 MB of embedded
# assets stay out of sirio_ui's rebuild loop.
[dependencies]
