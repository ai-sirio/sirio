[package]
name = "sirio_usage"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
libc = "0.2"
serde_json = "1"
chrono = { version = "0.4", default-features = false, features = ["clock"] }
