# P93 accounts tier implementation plan

> Execute from `/home/enzopalmisano/Scrivania/Progetti/tiller-linux` on
> `linux/gpui-waku`. Preserve all pre-existing worktree changes; never use
> `git add -A`. Do not edit `chat.rs`, `composer.rs`, or `tiller_acp/**` while
> P91 is active.

## 1. Establish the safe edit boundary

- Record the current status and diffs for `settings.rs`, `status_bar.rs`, and
  `tiller_usage/src/model.rs`.
- Treat the existing browser-grants changes in `settings.rs` as user work;
  patch only the provider/account sections and do not reset or reformat the
  unrelated hunks.
- Confirm P91-owned paths remain untouched before and after every commit.

## 2. Add failing tests first

- In `tiller_usage/src/model.rs`, add a reducer assertion that a timeout with no
  previous usage produces a distinct `TimedOut` reason rather than `Error`.
- In `tiller_ui/src/status_bar.rs`, add/extend rendering tests for the four
  unavailable labels: `not found`, `logged out`, `timed out`, and `error`.
- In `settings.rs`, add pure seams/tests for provider identity discovery and
  login command selection. Cover Claude's parsed email/organization, Codex's
  parsed login-status identity, the Linux OpenCode Go unknown state, and all
  three provider login command mappings.
- Run the focused package tests and confirm the new tests fail for the missing
  behavior before implementation.

## 3. Implement account identity and actions

- Extend the Settings provider account view model with optional display
  identity while retaining honest `Signed in`, `Not signed in`, and `Unknown`
  state labels.
- Read Claude credentials through the public usage helpers and parse them with
  `AgentAccountIdentity::parse_claude_json`.
- Check Codex's local credential state and parse `codex login status` output with
  `parse_codex_identity`; never display token material.
- Render the discovered identity in the provider card's System default row.
- Add a pure provider-to-login-command mapping and an external-terminal
  launcher for Claude, Codex, and OpenCode. Keep the existing host callback
  override for tests/embedders; the production fallback must make the existing
  running-app button actionable.
- Refresh provider discovery after the external login process exits. Surface
  an explicit unavailable/error message if the terminal or CLI cannot spawn,
  rather than claiming that login succeeded.

## 4. Implement all F-SET-11 unavailable visuals

- Add `UsageReason::TimedOut` in the usage model.
- Preserve `Stale` when a timeout has a previous good value; map a timeout with
  no previous value to `Unavailable(TimedOut)`.
- Render and test all four reason-specific labels distinctly, with the same
  dimming rule as other non-fresh states.
- Update exhaustive matches and comments/tests affected by the new enum case.

## 5. Verify and exercise

- Run focused `tiller_usage` and `tiller_ui` tests, then the relevant workspace
  tests. If P91's active ACP changes prevent the workspace build, record the
  exact compiler failure without touching those paths.
- Check formatting/lints proportionally and run `git diff --check`.
- Take `/tmp/tiller-drive-1.lockd` using the documented recipe, launch the Linux
  app, open Settings, click an Add Account button, observe the external
  terminal/login flow and its cancel/exit behavior, then release the lock.
- Verify with `git status --short` that unrelated dirty work, including P91's
  files, remains present and that no ledger file was edited.
