# Tab Bar Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Tab bar orizzontale sopra l'area contenuto (terminali/chat/markdown), sincronizzata con la sidebar; riordino drag & drop su entrambe; scorciatoie ⌘1–⌘9 e ⌃Tab.

**Architecture:** SwiftUI puro. Zero stato nuovo: tab bar e sidebar leggono `tabs[worktreeId]` / `activeTabId[worktreeId]` da `AppModel` (`@MainActor @Observable`) — sync strutturale. Logica pura (riordino, ciclo, selezione per indice) in TillerCore come funzioni statiche su `[WorkspaceTab]`, testate con swift-testing. Spec: `docs/superpowers/specs/2026-07-18-tab-bar-design.md`.

**Tech Stack:** Swift 6, SwiftUI (macOS 15+), swift-testing (`@Test`/`#expect`), XcodeGen.

## Global Constraints

- Dopo aver aggiunto file nuovi sotto `App/` o `Packages/`: eseguire `xcodegen generate` prima di buildare (CLAUDE.md).
- Mai modificare `Tiller.xcodeproj` a mano.
- Test TillerCore: `cd Packages/TillerCore && swift test` (swift-testing, non XCTest).
- Build App: `xcodegen generate && xcodebuild -project Tiller.xcodeproj -scheme Tiller -configuration Debug build -quiet` (exit 0 = ok).
- Gate finale: `Scripts/ci.sh` deve stampare `CI OK`. Nota: i test PTY di TillerTerminal sono flaky — ritentare fino a 5–6 volte se fallisce `spawnCapturesOutput`.
- Commit: Conventional Commits, imperativo minuscolo (`feat:`, `refactor:`, `test:`, `docs:`).
- UI copy in italiano (come l'esistente: "Nuova tab", "Chiudi", "Rinomina").
- Value types; nessuna nuova classe.

---

### Task 1: TillerCore — TabOrdering (logica pura + test)

**Files:**
- Create: `Packages/TillerCore/Sources/TillerCore/TabOrdering.swift`
- Test: `Packages/TillerCore/Tests/TillerCoreTests/TabOrderingTests.swift`

**Interfaces:**
- Consumes: `WorkspaceTab` (esistente, `Packages/TillerCore/Sources/TillerCore/WorkspaceTab.swift`).
- Produces (usate dal Task 2):
  - `TabOrdering.moving(_ list: [WorkspaceTab], id: UUID, before targetId: UUID?) -> [WorkspaceTab]`
  - `TabOrdering.cycledIndex(current: Int?, forward: Bool, count: Int) -> Int?`
  - `TabOrdering.selectionIndex(number: Int, count: Int) -> Int?`

Semantica:
- `moving`: rimuove `id` e lo reinserisce immediatamente prima di `targetId`; `targetId == nil` → in coda. `id` sconosciuto, `targetId` sconosciuto, o `id == targetId` → lista invariata. Ordine relativo delle altre tab stabile.
- `cycledIndex`: indice successivo (forward) o precedente con wrap-around; `current == nil` → `0` (forward) / `count-1` (backward); `count == 0` → `nil`.
- `selectionIndex`: ⌘n → indice. `number == 9` → ultima tab (`count-1`, stile browser). `1 <= number <= count` → `number-1`. Altrimenti (`number < 1`, `number > count && number != 9`, `count == 0`) → `nil`.

- [ ] **Step 1: Scrivi i test (falliranno: tipo inesistente)**

```swift
// Packages/TillerCore/Tests/TillerCoreTests/TabOrderingTests.swift
import Testing
import Foundation
@testable import TillerCore

private func tabs(_ n: Int) -> [WorkspaceTab] {
    (0..<n).map { WorkspaceTab(id: UUID(), title: "T\($0)", tree: .leaf(id: UUID())) }
}

@Suite struct TabOrderingMovingTests {
    @Test func movesBeforeTarget() {
        let list = tabs(4)
        let moved = TabOrdering.moving(list, id: list[3].id, before: list[1].id)
        #expect(moved.map(\.id) == [list[0].id, list[3].id, list[1].id, list[2].id])
    }

    @Test func nilTargetMovesToEnd() {
        let list = tabs(3)
        let moved = TabOrdering.moving(list, id: list[0].id, before: nil)
        #expect(moved.map(\.id) == [list[1].id, list[2].id, list[0].id])
    }

    @Test func moveBeforeItselfIsNoOp() {
        let list = tabs(3)
        #expect(TabOrdering.moving(list, id: list[1].id, before: list[1].id).map(\.id) == list.map(\.id))
    }

    @Test func unknownIdIsNoOp() {
        let list = tabs(2)
        #expect(TabOrdering.moving(list, id: UUID(), before: list[0].id).map(\.id) == list.map(\.id))
    }

    @Test func unknownTargetIsNoOp() {
        let list = tabs(2)
        #expect(TabOrdering.moving(list, id: list[0].id, before: UUID()).map(\.id) == list.map(\.id))
    }

    @Test func moveForwardBeforeLaterTarget() {
        // Sposta la prima tab prima della terza: il target index va calcolato
        // DOPO la rimozione, non prima.
        let list = tabs(3)
        let moved = TabOrdering.moving(list, id: list[0].id, before: list[2].id)
        #expect(moved.map(\.id) == [list[1].id, list[0].id, list[2].id])
    }
}

@Suite struct TabOrderingCycleTests {
    @Test func forwardWraps() {
        #expect(TabOrdering.cycledIndex(current: 2, forward: true, count: 3) == 0)
        #expect(TabOrdering.cycledIndex(current: 0, forward: true, count: 3) == 1)
    }

    @Test func backwardWraps() {
        #expect(TabOrdering.cycledIndex(current: 0, forward: false, count: 3) == 2)
        #expect(TabOrdering.cycledIndex(current: 2, forward: false, count: 3) == 1)
    }

    @Test func nilCurrentStartsAtEdge() {
        #expect(TabOrdering.cycledIndex(current: nil, forward: true, count: 3) == 0)
        #expect(TabOrdering.cycledIndex(current: nil, forward: false, count: 3) == 2)
    }

    @Test func emptyListReturnsNil() {
        #expect(TabOrdering.cycledIndex(current: nil, forward: true, count: 0) == nil)
    }
}

@Suite struct TabOrderingSelectionTests {
    @Test func numberMapsToIndex() {
        #expect(TabOrdering.selectionIndex(number: 1, count: 5) == 0)
        #expect(TabOrdering.selectionIndex(number: 5, count: 5) == 4)
    }

    @Test func nineIsAlwaysLast() {
        #expect(TabOrdering.selectionIndex(number: 9, count: 3) == 2)
        #expect(TabOrdering.selectionIndex(number: 9, count: 12) == 11)
    }

    @Test func outOfRangeIsNil() {
        #expect(TabOrdering.selectionIndex(number: 5, count: 3) == nil)
        #expect(TabOrdering.selectionIndex(number: 0, count: 3) == nil)
        #expect(TabOrdering.selectionIndex(number: 1, count: 0) == nil)
        #expect(TabOrdering.selectionIndex(number: 9, count: 0) == nil)
    }
}
```

- [ ] **Step 2: Verifica che falliscano**

Run: `cd Packages/TillerCore && swift test --filter TabOrdering`
Expected: errore di compilazione "cannot find 'TabOrdering' in scope".

- [ ] **Step 3: Implementa**

```swift
// Packages/TillerCore/Sources/TillerCore/TabOrdering.swift
import Foundation

/// Operazioni pure sull'ordine delle tab di un worktree: riordino drag&drop,
/// ciclo ⌃Tab e selezione ⌘n. Nessuno stato: AppModel applica il risultato.
public enum TabOrdering {
    /// Rimuove `id` e lo reinserisce immediatamente prima di `targetId`
    /// (`nil` = in coda). Id o target sconosciuti → lista invariata.
    public static func moving(
        _ list: [WorkspaceTab], id: UUID, before targetId: UUID?
    ) -> [WorkspaceTab] {
        guard id != targetId,
              let sourceIndex = list.firstIndex(where: { $0.id == id })
        else { return list }
        if let targetId, !list.contains(where: { $0.id == targetId }) { return list }
        var result = list
        let tab = result.remove(at: sourceIndex)
        // Il target va ricercato dopo la rimozione: se la sorgente stava
        // prima del target, l'indice del target è slittato di uno.
        if let targetId, let targetIndex = result.firstIndex(where: { $0.id == targetId }) {
            result.insert(tab, at: targetIndex)
        } else {
            result.append(tab)
        }
        return result
    }

    /// Indice successivo/precedente con wrap-around; `current == nil` parte
    /// dal bordo. Lista vuota → nil.
    public static func cycledIndex(current: Int?, forward: Bool, count: Int) -> Int? {
        guard count > 0 else { return nil }
        guard let current else { return forward ? 0 : count - 1 }
        return forward ? (current + 1) % count : (current - 1 + count) % count
    }

    /// ⌘n → indice tab: 9 = ultima (stile browser), n entro il conteggio →
    /// n-1, altrimenti nil (no-op).
    public static func selectionIndex(number: Int, count: Int) -> Int? {
        guard count > 0 else { return nil }
        if number == 9 { return count - 1 }
        guard number >= 1, number <= count else { return nil }
        return number - 1
    }
}
```

- [ ] **Step 4: Verifica che passino**

Run: `cd Packages/TillerCore && swift test --filter TabOrdering`
Expected: tutti i test PASS.

- [ ] **Step 5: Commit**

```bash
git add Packages/TillerCore/Sources/TillerCore/TabOrdering.swift Packages/TillerCore/Tests/TillerCoreTests/TabOrderingTests.swift
git commit -m "feat: add pure tab ordering logic in tillercore"
```

---

### Task 2: AppModel wrappers + scorciatoie ⌘1–⌘9 / ⌃Tab

**Files:**
- Modify: `App/AppModel.swift` (dopo `renameTab`, ~riga 909)
- Modify: `App/TillerApp.swift` (blocco `.commands`, ~riga 39)

**Interfaces:**
- Consumes: `TabOrdering` (Task 1); esistenti `activateTab(_:in:)`, `activeTab(for:)`, `persistTabs(for:)`, `selectedWorktree`, `tabs`.
- Produces (usate dai Task 4–7):
  - `AppModel.moveTab(_ tabId: UUID, before targetId: UUID?, in worktreeId: UUID)`
  - `AppModel.selectTab(number: Int)`
  - `AppModel.cycleTab(forward: Bool)`

- [ ] **Step 1: Aggiungi i wrapper in AppModel**

Inserire subito dopo `renameTab` (dopo la riga `persistTabs(for: worktreeId)` che chiude il metodo):

```swift
    /// Riordina la tab prima di `targetId` (nil = in coda). Usato dal drag
    /// & drop di tab bar e sidebar; l'ordine persiste nello snapshot già
    /// serializzato da persistTabs.
    func moveTab(_ tabId: UUID, before targetId: UUID?, in worktreeId: UUID) {
        guard let list = tabs[worktreeId] else { return }
        let moved = TabOrdering.moving(list, id: tabId, before: targetId)
        guard moved.map(\.id) != list.map(\.id) else { return }
        tabs[worktreeId] = moved
        persistTabs(for: worktreeId)
    }

    /// ⌘n: attiva la tab n del worktree selezionato (9 = ultima). Out of
    /// range = no-op.
    func selectTab(number: Int) {
        guard let worktree = selectedWorktree,
              let list = tabs[worktree.id],
              let index = TabOrdering.selectionIndex(number: number, count: list.count)
        else { return }
        activateTab(list[index].id, in: worktree.id)
    }

    /// ⌃Tab / ⌃⇧Tab: cicla le tab del worktree selezionato con wrap-around.
    func cycleTab(forward: Bool) {
        guard let worktree = selectedWorktree, let list = tabs[worktree.id] else { return }
        let current = list.firstIndex { $0.id == activeTab(for: worktree.id)?.id }
        guard let index = TabOrdering.cycledIndex(
            current: current, forward: forward, count: list.count
        ) else { return }
        activateTab(list[index].id, in: worktree.id)
    }
```

- [ ] **Step 2: Aggiungi il menu Tab in TillerApp**

In `TillerApp.swift`, dentro `.commands { ... }`, dopo il blocco `CommandMenu("History") { ... }` aggiungi:

```swift
            CommandMenu("Tab") {
                Button("Tab successiva") { model.cycleTab(forward: true) }
                    .keyboardShortcut(.tab, modifiers: .control)
                Button("Tab precedente") { model.cycleTab(forward: false) }
                    .keyboardShortcut(.tab, modifiers: [.control, .shift])
                Divider()
                ForEach(1...9, id: \.self) { number in
                    Button(number == 9 ? "Ultima tab" : "Tab \(number)") {
                        model.selectTab(number: number)
                    }
                    .keyboardShortcut(
                        KeyEquivalent(Character("\(number)")), modifiers: .command
                    )
                }
            }
```

- [ ] **Step 3: Build**

Run: `xcodebuild -project Tiller.xcodeproj -scheme Tiller -configuration Debug build -quiet`
Expected: exit 0, nessun errore. (Nessun file nuovo: xcodegen non serve.)

- [ ] **Step 4: Commit**

```bash
git add App/AppModel.swift App/TillerApp.swift
git commit -m "feat: add tab move/select/cycle with cmd-digits and ctrl-tab"
```

---

### Task 3: Componenti condivisi — NewTabMenuItems e WorkspaceTabIcon

Estrae dal `SidebarView` due pezzi che la tab bar riuserà, senza cambi di comportamento.

**Files:**
- Create: `App/NewTabMenuItems.swift`
- Create: `App/WorkspaceTabIcon.swift`
- Modify: `App/SidebarView.swift` (menu "+" a ~riga 388–427 e menu "+" del footer a ~riga 385–426 — cercare `Label("Nuovo Terminale"`, le occorrenze sono due; e la computed `icon` di `TabRow` a ~riga 626)

**Interfaces:**
- Consumes: `AgentCatalog.all`, `AppModel.acpAgents()`, `model.newShellTab(in:)`, `model.spawnAgent(_:in:)`, `model.openChatTab(agentId:in:)`, `AgentMenuIconCache`, `AgentIcon`, `AppTheme` (tutti esistenti).
- Produces (usati dal Task 4):
  - `struct NewTabMenuItems: View` — `init(model: AppModel, worktree: Worktree)`; SOLO il contenuto del menu (i `Button`/`Divider`), il chiamante fornisce il `Menu { }` wrapper.
  - `struct WorkspaceTabIcon: View` — `init(model: AppModel, tab: WorkspaceTab)`.

- [ ] **Step 1: Crea NewTabMenuItems**

```swift
// App/NewTabMenuItems.swift
import SwiftUI
import TillerCore
import TillerAgents

/// Contenuto del menu "+" (nuova shell, spawn agenti terminale, chat ACP),
/// condiviso tra il "+" della sidebar e quello della tab bar. Il chiamante
/// fornisce il Menu wrapper.
struct NewTabMenuItems: View {
    @Bindable var model: AppModel
    let worktree: Worktree

    var body: some View {
        Button {
            model.newShellTab(in: worktree)
        } label: {
            Label("Nuovo Terminale", systemImage: "terminal")
        }
        Divider()
        ForEach(AgentCatalog.all, id: \.id) { adapter in
            Button {
                Task { await model.spawnAgent(adapter, in: worktree) }
            } label: {
                if let icon = AgentMenuIconCache.image(for: adapter.id) {
                    Label {
                        Text(adapter.displayName)
                    } icon: {
                        Image(nsImage: icon)
                    }
                } else {
                    Text(adapter.displayName)
                }
            }
        }
        Divider()
        ForEach(AppModel.acpAgents(), id: \.id) { adapter in
            Button {
                model.openChatTab(agentId: adapter.id, in: worktree)
            } label: {
                Label("Chat · \(adapter.displayName)",
                      systemImage: "bubble.left.and.text.bubble.right")
            }
        }
    }
}
```

Nota: verificare gli import effettivi guardando la testa di `SidebarView.swift` — se `AgentCatalog` arriva da un modulo diverso da `TillerAgents`, copiare gli stessi import di `SidebarView.swift`.

- [ ] **Step 2: Crea WorkspaceTabIcon**

Contenuto = l'attuale computed `icon` privata di `TabRow` (SidebarView.swift ~626):

```swift
// App/WorkspaceTabIcon.swift
import SwiftUI
import TillerCore

/// Icona per tipo di tab (markdown / chat / terminale, con icona agente
/// quando un agente è stato rilevato nei pane). Condivisa tra la TabRow
/// della sidebar e la tab bar.
struct WorkspaceTabIcon: View {
    @Bindable var model: AppModel
    let tab: WorkspaceTab

    var body: some View {
        if tab.markdownFileURL != nil {
            Image(systemName: "doc.text")
                .font(.system(size: 10))
                .foregroundStyle(AppTheme.meta)
        } else if let agentId = tab.chatAgentId {
            AgentIcon(agentId: agentId, size: 12)
        } else if let agentId = tab.leafIds.compactMap({ model.agentActivity.paneAgents[$0] }).first {
            AgentIcon(agentId: agentId, size: 12)
        } else {
            Image(systemName: "terminal")
                .font(.system(size: 10))
                .foregroundStyle(AppTheme.meta)
        }
    }
}
```

- [ ] **Step 3: Usa i componenti in SidebarView**

1. In ENTRAMBE le occorrenze del menu "+" (cercare `Label("Nuovo Terminale"`): sostituire l'intero contenuto del `Menu { ... }` (i Button + Divider + i due ForEach) con:

```swift
                Menu {
                    NewTabMenuItems(model: model, worktree: worktree)
                } label: {
```

(il `label:` e i modifier esistenti — `.buttonStyle(.plain)`, `.menuIndicator(.hidden)`, `.help(...)` — restano invariati).

2. In `TabRow`: eliminare la computed `@ViewBuilder private var icon` e sostituire il call site `icon` (riga ~540) con:

```swift
            WorkspaceTabIcon(model: model, tab: tab)
                .frame(width: 14)
```

- [ ] **Step 4: Rigenera progetto e builda**

Run: `xcodegen generate && xcodebuild -project Tiller.xcodeproj -scheme Tiller -configuration Debug build -quiet`
Expected: exit 0.

- [ ] **Step 5: Commit**

```bash
git add App/NewTabMenuItems.swift App/WorkspaceTabIcon.swift App/SidebarView.swift
git commit -m "refactor: extract shared new-tab menu and tab icon components"
```

---

### Task 4: TabBarView core + mount in ContentView

Tab bar con: item cliccabili (icona, titolo, dirty dot, indicatore attività, × on-hover), pulsante "+", montata sopra `terminalStack`. Rename/menu contestuale/overflow/drag arrivano nei task successivi.

**Files:**
- Create: `App/TabBarView.swift`
- Modify: `App/ContentView.swift` (VStack a ~riga 164)

**Interfaces:**
- Consumes: `model.tabs`, `model.activeTabId`, `model.activeTab(for:)`, `model.activateTab(_:in:)`, `model.closeTab(_:in:)`, `model.markdownDocuments`, `model.agentActivity.statusForWorktree(paneIds:)`, `model.agentActivity.paneAgents`, `NewTabMenuItems`, `WorkspaceTabIcon`, `WorktreeStatusGlyph`, `HoverIconButtonStyle`, `AppTheme`.
- Produces: `struct TabBarView: View` — `init(model: AppModel, worktree: Worktree)`; `struct TabBarItem: View` (stesso file, interna) estesa dai Task 5–7.

- [ ] **Step 1: Crea TabBarView**

```swift
// App/TabBarView.swift
import SwiftUI
import TillerCore

/// Tab bar orizzontale sopra l'area contenuto: le tab del worktree
/// selezionato, sempre in sync con la sidebar (stessa source of truth:
/// AppModel.tabs / activeTabId). Sempre visibile quando un worktree è
/// selezionato; con zero tab mostra solo il "+".
struct TabBarView: View {
    @Bindable var model: AppModel
    let worktree: Worktree

    var body: some View {
        HStack(spacing: 4) {
            ScrollViewReader { proxy in
                ScrollView(.horizontal, showsIndicators: false) {
                    HStack(spacing: 2) {
                        ForEach(model.tabs[worktree.id] ?? []) { tab in
                            TabBarItem(model: model, worktree: worktree, tab: tab)
                                .id(tab.id)
                        }
                    }
                    .padding(.leading, 6)
                }
                .onChange(of: model.activeTabId[worktree.id]) { _, newValue in
                    guard let id = newValue else { return }
                    withAnimation(.easeInOut(duration: 0.15)) { proxy.scrollTo(id) }
                }
            }

            Spacer(minLength: 0)

            Menu {
                NewTabMenuItems(model: model, worktree: worktree)
            } label: {
                Image(systemName: "plus")
                    .font(.system(size: 11))
                    .foregroundStyle(AppTheme.meta)
            }
            .buttonStyle(.plain)
            .menuIndicator(.hidden)
            .help("Nuova tab (⌘T)")
            .padding(.trailing, 8)
        }
        .frame(height: 32)
    }
}

/// Una tab nella bar: icona tipo, titolo, dirty dot markdown, indicatore
/// attività agente, × on-hover. Click = attiva.
struct TabBarItem: View {
    @Bindable var model: AppModel
    let worktree: Worktree
    let tab: WorkspaceTab
    @State private var hovering = false

    private var isActive: Bool {
        model.activeTab(for: worktree.id)?.id == tab.id
    }

    private var status: AgentStatus? {
        model.agentActivity.statusForWorktree(paneIds: tab.activityPaneIds)
    }

    private var agentId: String? {
        tab.chatAgentId
            ?? tab.leafIds.compactMap { model.agentActivity.paneAgents[$0] }.first
    }

    var body: some View {
        HStack(spacing: 6) {
            WorkspaceTabIcon(model: model, tab: tab)
                .frame(width: 14)

            Text(tab.title)
                .font(.system(size: 12))
                .foregroundStyle(isActive ? AppTheme.titleSelected : AppTheme.subtitle)
                .lineLimit(1)
                .truncationMode(.tail)

            if model.markdownDocuments[tab.id]?.isDirty == true {
                Circle().fill(.secondary).frame(width: 5, height: 5)
            }

            if status != nil {
                WorktreeStatusGlyph(status: status, agentId: agentId)
                    .scaleEffect(0.8)
                    .frame(width: 14, height: 14)
            }

            if hovering {
                Button {
                    model.closeTab(tab.id, in: worktree)
                } label: {
                    Image(systemName: "xmark")
                        .font(.system(size: 9, weight: .bold))
                        .foregroundStyle(AppTheme.meta)
                }
                .buttonStyle(HoverIconButtonStyle())
                .help("Chiudi tab (⌘W)")
            } else {
                // Placeholder della × per evitare che la tab cambi larghezza
                // in hover (jitter durante il mouse-over).
                Color.clear.frame(width: 16, height: 16)
            }
        }
        .padding(.horizontal, 9)
        .padding(.vertical, 5)
        .contentShape(Rectangle())
        .background {
            if isActive {
                RoundedRectangle(cornerRadius: 7)
                    .fill(AppTheme.selectionFill)
                    .overlay(
                        RoundedRectangle(cornerRadius: 7)
                            .stroke(AppTheme.selectionRing, lineWidth: 1)
                    )
            } else if hovering {
                RoundedRectangle(cornerRadius: 7).fill(AppTheme.rowHover)
            }
        }
        .onHover { hovering = $0 }
        .onTapGesture { model.activateTab(tab.id, in: worktree.id) }
    }
}
```

- [ ] **Step 2: Monta in ContentView**

In `ContentView.swift`, nel `VStack(spacing: 0)` della colonna centrale (~riga 164), prima di `terminalStack`:

```swift
            VStack(spacing: 0) {
                if let worktree = model.selectedWorktree {
                    TabBarView(model: model, worktree: worktree)
                    Divider()
                }
                terminalStack
                if showUsageBar {
                    Divider()
                    UsageBarView(store: model.usage, worktree: model.selectedWorktree)
                }
            }
```

- [ ] **Step 3: Rigenera progetto e builda**

Run: `xcodegen generate && xcodebuild -project Tiller.xcodeproj -scheme Tiller -configuration Debug build -quiet`
Expected: exit 0.

- [ ] **Step 4: Verifica manuale rapida (se possibile lanciare l'app)**

Aprire l'app, selezionare un worktree con più tab: la bar appare sopra il terminale, click su una tab la attiva (e la sidebar evidenzia la stessa), × chiude, "+" apre il menu con shell/agenti/chat.

- [ ] **Step 5: Commit**

```bash
git add App/TabBarView.swift App/ContentView.swift
git commit -m "feat: add tab bar above content area synced with sidebar"
```

---

### Task 5: Rename inline, menu contestuale, Chiudi altre / a destra

**Files:**
- Modify: `App/AppModel.swift` (dopo `moveTab` del Task 2)
- Modify: `App/TabBarView.swift` (`TabBarItem`)

**Interfaces:**
- Consumes: `closeTab(_:in:)` (esistente — gestisce già la conferma markdown dirty), `renameTab(_:in:to:)` (esistente).
- Produces:
  - `AppModel.closeOtherTabs(_ tabId: UUID, in worktree: Worktree)`
  - `AppModel.closeTabsToRight(of tabId: UUID, in worktree: Worktree)`

- [ ] **Step 1: Aggiungi i metodi di chiusura multipla in AppModel**

Dopo `moveTab`:

```swift
    /// Chiude tutte le tab del worktree tranne quella indicata. Passa dal
    /// percorso closeTab singolo: le conferme markdown-dirty appaiono una
    /// alla volta e un annulla lascia la tab aperta.
    func closeOtherTabs(_ tabId: UUID, in worktree: Worktree) {
        let ids = (tabs[worktree.id] ?? []).map(\.id).filter { $0 != tabId }
        for id in ids { closeTab(id, in: worktree) }
    }

    /// Chiude le tab a destra di quella indicata (stesso percorso singolo).
    func closeTabsToRight(of tabId: UUID, in worktree: Worktree) {
        guard let list = tabs[worktree.id],
              let index = list.firstIndex(where: { $0.id == tabId }) else { return }
        for id in list.suffix(from: index + 1).map(\.id) {
            closeTab(id, in: worktree)
        }
    }
```

- [ ] **Step 2: Rename inline + context menu in TabBarItem**

In `TabBarItem` aggiungi state e sostituisci il `Text(tab.title)`:

```swift
    @State private var renaming = false
    @State private var draftTitle = ""
    @FocusState private var renameFieldFocused: Bool
```

```swift
            if renaming {
                TextField("", text: $draftTitle)
                    .textFieldStyle(.plain)
                    .font(.system(size: 12))
                    .frame(width: 120)
                    .focused($renameFieldFocused)
                    .onSubmit {
                        model.renameTab(tab.id, in: worktree.id, to: draftTitle)
                        renaming = false
                    }
                    .onExitCommand { renaming = false }
            } else {
                Text(tab.title)
                    .font(.system(size: 12))
                    .foregroundStyle(isActive ? AppTheme.titleSelected : AppTheme.subtitle)
                    .lineLimit(1)
                    .truncationMode(.tail)
                    // Doppio click SOLO sul titolo (stesso pattern di TabRow):
                    // un count:2 sull'intera riga inghiottirebbe i click sulla ×.
                    .onTapGesture(count: 2) {
                        draftTitle = tab.title
                        renaming = true
                        renameFieldFocused = true
                    }
            }
```

E dopo `.onTapGesture { model.activateTab(...) }` aggiungi:

```swift
        .contextMenu {
            Button("Rinomina") {
                draftTitle = tab.title
                renaming = true
                renameFieldFocused = true
            }
            Divider()
            Button("Chiudi") { model.closeTab(tab.id, in: worktree) }
            Button("Chiudi altre") { model.closeOtherTabs(tab.id, in: worktree) }
                .disabled((model.tabs[worktree.id]?.count ?? 0) <= 1)
            Button("Chiudi a destra") { model.closeTabsToRight(of: tab.id, in: worktree) }
                .disabled(model.tabs[worktree.id]?.last?.id == tab.id)
        }
```

- [ ] **Step 3: Build**

Run: `xcodebuild -project Tiller.xcodeproj -scheme Tiller -configuration Debug build -quiet`
Expected: exit 0.

- [ ] **Step 4: Commit**

```bash
git add App/AppModel.swift App/TabBarView.swift
git commit -m "feat: add tab rename and context menu with close others/right"
```

---

### Task 6: Overflow — auto-scroll + menu elenco tab

Chevron con menu di tutte le tab, visibile solo quando le tab eccedono la larghezza disponibile.

**Files:**
- Modify: `App/TabBarView.swift` (`TabBarView`)

**Interfaces:**
- Consumes: `model.activateTab(_:in:)`, `model.activeTab(for:)`, `WorkspaceTabIcon`.
- Produces: nulla di nuovo per altri task.

- [ ] **Step 1: Misura overflow e aggiungi il chevron**

In `TabBarView` aggiungi:

```swift
    @State private var contentWidth: CGFloat = 0
    @State private var viewportWidth: CGFloat = 0

    private var isOverflowing: Bool { contentWidth > viewportWidth + 1 }
```

Sull'`HStack(spacing: 2)` dentro la ScrollView (dopo `.padding(.leading, 6)`):

```swift
                    .onGeometryChange(for: CGFloat.self) { $0.size.width } action: {
                        contentWidth = $0
                    }
```

Sulla `ScrollView` (dopo `.onChange(of: model.activeTabId[worktree.id])`):

```swift
                .onGeometryChange(for: CGFloat.self) { $0.size.width } action: {
                    viewportWidth = $0
                }
```

Tra la `Spacer(minLength: 0)` e il Menu "+", aggiungi:

```swift
            if isOverflowing {
                Menu {
                    ForEach(model.tabs[worktree.id] ?? []) { tab in
                        Button {
                            model.activateTab(tab.id, in: worktree.id)
                        } label: {
                            if model.activeTab(for: worktree.id)?.id == tab.id {
                                Label(tab.title, systemImage: "checkmark")
                            } else {
                                Text(tab.title)
                            }
                        }
                    }
                } label: {
                    Image(systemName: "chevron.down")
                        .font(.system(size: 10))
                        .foregroundStyle(AppTheme.meta)
                }
                .buttonStyle(.plain)
                .menuIndicator(.hidden)
                .help("Tutte le tab")
            }
```

- [ ] **Step 2: Build**

Run: `xcodebuild -project Tiller.xcodeproj -scheme Tiller -configuration Debug build -quiet`
Expected: exit 0.

- [ ] **Step 3: Commit**

```bash
git add App/TabBarView.swift
git commit -m "feat: add tab bar overflow menu and active-tab autoscroll"
```

---

### Task 7: Drag & drop riordino — tab bar e sidebar

**Files:**
- Create: `App/TabDragPayload.swift`
- Modify: `App/TabBarView.swift` (`TabBarItem` + coda della bar)
- Modify: `App/SidebarView.swift` (`TabRow`)

**Interfaces:**
- Consumes: `AppModel.moveTab(_:before:in:)` (Task 2).
- Produces: `struct TabDragPayload: Codable, Transferable { let tabId: UUID; let worktreeId: UUID }`.

- [ ] **Step 1: Crea il payload Transferable**

```swift
// App/TabDragPayload.swift
import CoreTransferable
import UniformTypeIdentifiers

extension UTType {
    /// Tipo interno per il drag delle tab: mai esportato fuori dall'app.
    static let tillerTabDrag = UTType(exportedAs: "it.tiller.tab-drag")
}

/// Payload del drag di una tab: porta anche il worktree di origine così i
/// drop target rifiutano il cross-worktree.
struct TabDragPayload: Codable, Transferable {
    let tabId: UUID
    let worktreeId: UUID

    static var transferRepresentation: some TransferRepresentation {
        CodableRepresentation(contentType: .tillerTabDrag)
    }
}
```

Nota: `UTType(exportedAs:)` senza dichiarazione in Info.plist funziona per drag in-app. Se in verifica manuale il drop non si attiva, aggiungere la dichiarazione `UTExportedTypeDeclarations` in `project.yml` → `info.properties` e rieseguire `xcodegen generate`.

- [ ] **Step 2: Drag & drop su TabBarItem**

In `TabBarItem`, aggiungi state e modifier dopo `.contextMenu { ... }`:

```swift
    @State private var dropTargeted = false
```

```swift
        .draggable(TabDragPayload(tabId: tab.id, worktreeId: worktree.id))
        .dropDestination(for: TabDragPayload.self) { payloads, _ in
            guard let payload = payloads.first,
                  payload.worktreeId == worktree.id,
                  payload.tabId != tab.id else { return false }
            model.moveTab(payload.tabId, before: tab.id, in: worktree.id)
            return true
        } isTargeted: { dropTargeted = $0 }
        .overlay(alignment: .leading) {
            if dropTargeted {
                RoundedRectangle(cornerRadius: 1)
                    .fill(AppTheme.selectionRing)
                    .frame(width: 2)
            }
        }
```

- [ ] **Step 3: Drop "in coda" sulla bar**

In `TabBarView`, sull'`HStack(spacing: 2)` interno alla ScrollView (dopo l'`onGeometryChange` del Task 6):

```swift
                    .dropDestination(for: TabDragPayload.self) { payloads, _ in
                        // Drop nello spazio vuoto dopo l'ultima tab → in coda.
                        guard let payload = payloads.first,
                              payload.worktreeId == worktree.id else { return false }
                        model.moveTab(payload.tabId, before: nil, in: worktree.id)
                        return true
                    }
```

(SwiftUI dà priorità al `dropDestination` più interno: il drop su un item inserisce prima di quello, il drop sullo sfondo mette in coda.)

- [ ] **Step 4: Drag & drop su TabRow in sidebar**

In `SidebarView.swift`, dentro `TabRow`, aggiungi lo state `@State private var dropTargeted = false` e, dopo `.contextMenu { ... }` (prima di `.alert`), gli stessi modifier:

```swift
        .draggable(TabDragPayload(tabId: tab.id, worktreeId: worktree.id))
        .dropDestination(for: TabDragPayload.self) { payloads, _ in
            guard let payload = payloads.first,
                  payload.worktreeId == worktree.id,
                  payload.tabId != tab.id else { return false }
            model.moveTab(payload.tabId, before: tab.id, in: worktree.id)
            return true
        } isTargeted: { dropTargeted = $0 }
        .overlay(alignment: .top) {
            if dropTargeted {
                RoundedRectangle(cornerRadius: 1)
                    .fill(AppTheme.selectionRing)
                    .frame(height: 2)
                    .padding(.leading, 44)
            }
        }
```

- [ ] **Step 5: Rigenera progetto e builda**

Run: `xcodegen generate && xcodebuild -project Tiller.xcodeproj -scheme Tiller -configuration Debug build -quiet`
Expected: exit 0.

- [ ] **Step 6: Commit**

```bash
git add App/TabDragPayload.swift App/TabBarView.swift App/SidebarView.swift
git commit -m "feat: add drag and drop tab reordering in tab bar and sidebar"
```

---

### Task 8: Smoke checklist + gate CI completo

**Files:**
- Create: `docs/superpowers/plans/2026-07-18-tab-bar-smoke-checklist.md`

**Interfaces:** nessuna.

- [ ] **Step 1: Scrivi la checklist**

```markdown
# Tab bar — smoke checklist manuale

Prerequisito: app avviata, un progetto con un worktree, 3+ tab miste
(terminale, chat, markdown).

- [ ] La tab bar appare sopra il terminale col worktree selezionato; con
      zero tab mostra solo il "+"; senza worktree selezionato è nascosta.
- [ ] Click su una tab nella bar → contenuto cambia e la sidebar evidenzia
      la stessa tab; click su una TabRow in sidebar → la bar si aggiorna.
- [ ] × in hover chiude la tab; su markdown dirty appare la conferma.
- [ ] "+" apre il menu (Nuovo Terminale / agenti / Chat · agente) e le voci
      creano la tab giusta.
- [ ] Doppio click sul titolo → rename inline (Invio conferma, Esc annulla);
      il nuovo titolo appare anche in sidebar.
- [ ] Context menu: Rinomina, Chiudi, Chiudi altre (disabilitato con 1 tab),
      Chiudi a destra (disabilitato sull'ultima).
- [ ] Drag di una tab nella bar → riordino; stesso drag su TabRow in
      sidebar; l'ordine coincide tra bar e sidebar e sopravvive al riavvio.
- [ ] Drag di una tab su un worktree diverso in sidebar → nessun drop.
- [ ] Con molte tab: la bar scrolla, la tab attiva torna visibile quando
      selezionata da sidebar/⌘n, appare il chevron con l'elenco completo.
- [ ] ⌘1..⌘n attiva la tab n; ⌘9 l'ultima; ⌃Tab / ⌃⇧Tab cicla con wrap.
- [ ] Agente in esecuzione in una tab non attiva → indicatore attività
      sulla tab (running dots / dot needs-input).
- [ ] Cambio tab ripetuto: i PTY restano vivi (processi non riavviati).
```

- [ ] **Step 2: Gate CI completo**

Run: `Scripts/ci.sh`
Expected: stampa `CI OK`. I test PTY di TillerTerminal sono flaky: se fallisce `spawnCapturesOutput`, rilanciare (fino a 5–6 volte).

- [ ] **Step 3: Commit**

```bash
git add docs/superpowers/plans/2026-07-18-tab-bar-smoke-checklist.md
git commit -m "docs: add tab bar manual smoke checklist"
```
