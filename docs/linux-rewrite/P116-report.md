# P116 report

## Slice C — headless rows

Headless instance: `env -u DISPLAY -u WAYLAND_DISPLAY TILLER_SOCKET=/tmp/pi.sock TILLER_DB=/tmp/pi.sqlite rust/target/debug/tiller`; it listened on `/tmp/pi.sock`. All captures below are socket/CLI transcripts.

### Batch 1 (rows 1–10)

- `F-CORE-ACT-19` — Drove `session-ref` and attempted an activity `notify` against a live socket-created terminal panel. The CLI rejected the combined agent-status and user-notification arguments as “ambiguous notify modes”; no notification was delivered. The control capability list has `notify` and `notification.*`, but no activity-policy/visibility control. Capture: `/tmp/p116-c2-control.txt`.
- `F-CORE-ACT-20` — The same live panel was used to probe status notification delivery. The attempted working/idle calls were rejected before dispatch because the title/body notification arguments were mixed with `--session/--status`; `list-notifications --json` remained `[]`. Capture: `/tmp/p116-c2-control.txt`.
- `F-CORE-ACT-24` — Queried the live socket’s complete capability list and exercised `session-ref` on `pane-2157148-2`; it returned success. No control method exposes `AgentSessionRestorePlan` inputs or a stable-content-ID restore result, so that plan could not be driven through this socket. Capture: `/tmp/p116-c1-control.txt`, `/tmp/p116-c2-control.txt`.
- `F-CORE-ACT-25` — Added the project and observed both discovered worktrees live; the selected worktree was `p-c1fd7a5bbfd541af-wt-1`. No socket command accepts bootstrap priority/deferred inputs or returns the `BootstrapRestoreOrder` partition. Capture: `/tmp/p116-c1-control.txt`.
- `F-CORE-ACT-26` — Mounted/listed the project worktrees and created/split live panels in the selected worktree. The control contract offers no mount-eviction command or reported eviction decision, so selected/active/unsaved protection was not observable. Capture: `/tmp/p116-c1-control.txt`, `/tmp/p116-c2-control.txt`.
- `F-CORE-FILE-03` — Drove a real PTY panel through `panel write`; the panel scrollback reported the base64 payload for `printf socket-write\r\npanel-ready`. The socket has no file-drop/external-path method, so terminal file-drop encoding could not be sent. Capture: `/tmp/p116-c2-control.txt`.
- `F-CORE-FILE-06` — Opened and read the Changes surface for the selected worktree; it reported zero staged, unstaged, and untracked files. No control method performs a file save or an external modify/delete/rename event, so the clean/dirty external-change cases were not driven. Capture: `/tmp/p116-c2-control.txt`.
- `F-CORE-FILE-08` — The headless socket exposed no file-icon or rendered-tree payload. `surface changes read` returned only counts; no named directory/file glyph mapping was observable without a rendered display. Capture: `/tmp/p116-c2-control.txt`.
- `F-CORE-USG-05` — Queried all live capabilities, opened General then Appearance settings, and read both surfaces. The protocol returns only `settings\t<section>` and has no usage-refresh/status-bar method; no usage refresh request could be issued. Capture: `/tmp/p116-c2-control.txt`.
- `F-CORE-USG-06` — With the same isolated instance, inspected settings and the live capabilities. There is no control request for Codex credential refresh or token-error classification, so the refresh failure branches were not observable through the specified headless interface. Capture: `/tmp/p116-c2-control.txt`.

Follow-up to the activity probes above: a fresh panel accepted `notify --status running`, `notify --status needs-input`, and `notify --status needs_input`; it rejected `idle`, `working`, and `stopped` as unknown. Capture: `/tmp/p116-c4-status.txt`.

### Batch 2 (rows 11–20)

- `F-CORE-USG-07` — On the restarted isolated instance, `capabilities` had no usage-fetch/status-bar method. The only user notification sent through the CLI appeared in `list-notifications`, then notifications were empty after restart; no Codex usage request/result could be invoked. Capture: `/tmp/p116-c3-session.txt`.
- `F-CORE-DOM-03` — Added both the assigned checkout and a scratch Git project through `project add <path>`. The socket reports the explicit supplied paths and no default-project-base path, and exposes no request that asks the domain for an implicit project location. Capture: `/tmp/p116-c1-control.txt`, `/tmp/p116-c6-git.txt`.
- `F-CORE-DOM-07` — `new-workspace --project p-bdcd4c1e4b1614f9` without `--branch` produced worktree `...-wt-1` on branch `wt-1786722152`; the returned workspace list showed the generated branch and path. Capture: `/tmp/p116-c6-git.txt`.
- `F-CORE-WSP-04` — Created a live control terminal and split it right; `panel list` showed the original and `sh (right)` pane under the selected worktree. The socket does not expose the broader `LayoutCommand` variants (move, divider, view-state, rename), so only the split command was driven. Capture: `/tmp/p116-c2-control.txt`.
- `F-CORE-WSP-08` — Before restart, the socket listed Chat, Terminal, Changes, and three control panes; after `quit` and relaunch against the same `/tmp/pi.sqlite`, `panel list` for that worktree returned no panes. This drive did not expose `WorkspaceTabViewState`; it showed that these live control panes were not restored. Capture: `/tmp/p116-c3-session.txt`.
- `F-CORE-SET-01` — Selected Appearance and read `settings\tAppearance` before restart. After relaunch with the same database, `surface settings read` returned `Settings surface is not open`; the socket has no settings-snapshot or malformed-data control. Capture: `/tmp/p116-c3-session.txt`.
- `F-CORE-AUTH-01` — Inspected the complete live capability list and settings surface. Neither exposes an authentication command, callback, credential state, nor return-url result, so the headless socket could not drive this authentication row. Capture: `/tmp/p116-c1-control.txt`, `/tmp/p116-c2-control.txt`.
- `F-AGENT-OMP-01` — Not re-driven: `ENVIRONMENT.md` documents that `oh-my-pi` dies upstream on untranspiled TypeScript and explicitly says not to re-drive these rows. No Tiller adapter change was made. Capture: `/tmp/p116-c3-session.txt` (isolated instance remains available; no OMP launch issued).
- `F-AGENT-OMP-02` — Not re-driven for the same documented upstream instrument reason; therefore no hook event was manufactured or treated as generated evidence. No Tiller adapter change was made. Capture: `/tmp/p116-c3-session.txt`.
- `F-AGENT-SESSION-01` — Set a native session reference on a live control panel, invoked `restore-session`, then quit and relaunched the same database. The post-relaunch worktree had no control panes to query for that reference. Capture: `/tmp/p116-c2-control.txt`, `/tmp/p116-c3-session.txt`.

### Batch 3 (rows 21–31)

- `F-AGENT-SESSION-02` — Drove the socket session-reference API and a full isolated restart; it accepted `p116-native-ref` while the panel was alive, but no transcript/recent-text API appears in `capabilities`, and the panel was absent after restart. Capture: `/tmp/p116-c2-control.txt`, `/tmp/p116-c3-session.txt`.
- `F-AGENT-API-01` — Queried the complete live control contract and exercised panel/session controls. It has no adapter-catalog, launch/resume-shape, or optional summarizer request, so no adapter API result was observable through the specified socket. Capture: `/tmp/p116-c7-terminal.txt`.
- `F-AGENT-SAFE-02` — Used only the isolated socket/database and a scratch Git project; no agent prepare/skill-provisioning method exists in the live contract, so no agent configuration was written or global configuration touched. Capture: `/tmp/p116-c6-git.txt`, `/tmp/p116-c7-terminal.txt`.
- `F-CTRL-CLI-02` — Drove the compiled `tillerctl` against `/tmp/pi.sock`: ping returned `pong`; project/workspace, panel, settings, changes, session, notification, and capability requests all returned structured responses. Two incorrect `notify` forms were rejected with explicit CLI errors rather than silently sent. Capture: `/tmp/p116-c1-control.txt`, `/tmp/p116-c2-control.txt`, `/tmp/p116-c7-terminal.txt`.
- `F-GIT-RUN-02` — Created a fresh `/tmp/p116-git` repository and invoked `new-workspace` through the live socket. The command returned the new worktree ID, but the control response carries no incremental Git-progress events; this drive could not observe the row’s streaming-progress clause. Capture: `/tmp/p116-c6-git.txt`.
- `F-GIT-BRANCH-01` — The same scratch-repository `new-workspace` invocation without `--branch` made `wt-1786722152`; the socket list and `git branch --all`/`git worktree list --porcelain` both showed that branch and its linked worktree. Capture: `/tmp/p116-c6-git.txt`.
- `F-TERM-PTY-06` — Created a terminal panel, wrote `printf terminal-control` plus Enter, sent a named Enter key, then read state and scrollback. It stayed `running` with 36 bytes of scrollback containing the command, CRLFs, and `pty-start`. Capture: `/tmp/p116-c7-terminal.txt`.
- `F-TERM-UI-02` — The no-display instance exposes PTY scrollback but no pointer/click or URL-open operation for a terminal link. No link activation was issued; this avoids treating a socket transcript as a platform-click result. Capture: `/tmp/p116-c7-terminal.txt`.
- `F-USE-01` — Opened/read General and Appearance through the socket and re-queried capabilities after restart. There is no status-bar refresh control in the protocol, so the refresh affordance was not observable headlessly. Capture: `/tmp/p116-c2-control.txt`, `/tmp/p116-c7-terminal.txt`.
- `F-USE-02` — The capability contract and settings reads contain no provider-segment tooltip method or returned unavailable reason. No tooltip result was observable without a rendered surface. Capture: `/tmp/p116-c7-terminal.txt`.
- `F-USE-03` — The headless control responses expose no status-bar provider segment, loading state, stale value, or error text. No status-bar state was inferred from source or adjacent controls. Capture: `/tmp/p116-c7-terminal.txt`.
