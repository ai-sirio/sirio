[
 {
  "id": "F-PRJ-07",
  "verdict": "half-proven",
  "why": "Anchored-popover input-delivery gap (shared with F-PRJ-06/09) still blocks reaching the correct-URL retry arm; prior invalid-URL proof stands. No new instrument found this pass.",
  "evidenceDiscriminates": true,
  "newEvidenceCell": "half-proven (unchanged, reconfirmed by critic): same anchored-popover input gap as F-PRJ-06/09 blocks the correct-URL retry arm; invalid-URL half stands proven.",
  "slice": "C-U"
 },
 {
  "id": "F-PRJ-14",
  "verdict": "half-proven",
  "why": "Favicon-domain arm proven live+persisted; PNG-upload arm blocked by native file dialog with no synthetic-input path on headless Wayland (no portal service) \u2014 a lane limitation, not a defect.",
  "evidenceDiscriminates": true,
  "newEvidenceCell": "half-proven (unchanged, reconfirmed): favicon-domain arm fully proven (live label + persisted restart); PNG-upload arm blocked by no-portal native file dialog, same class as F-BRW embedded-browser gap.",
  "slice": "C-U"
 },
 {
  "id": "F-CHG-22",
  "verdict": "half-proven",
  "why": "Prior pass correctly caught that the cited frames show only a cursor entering frame, not an actual expand; data-tier half remains proven, visual half remains unproven.",
  "evidenceDiscriminates": true,
  "newEvidenceCell": "half-proven (unchanged): cited frames re-examined, deltas explained by cursor movement not content change; visual expand/collapse half unproven, data-tier half stands proven from prior sweep.",
  "slice": "C-U"
 },
 {
  "id": "F-CHG-18",
  "verdict": "NOT EXERCISED",
  "why": "grep -n drag Scripts/wayland-drive.sh returns zero hits confirming no drag primitive exists on this lane; DISPLAY=:1 route is out of scope for this slice; drag code on both ends verified real and non-test.",
  "evidenceDiscriminates": true,
  "newEvidenceCell": "NOT EXERCISED (unchanged, independently re-confirmed): grep -n drag Scripts/wayland-drive.sh -> no match, confirming click/move/type/key/title/shot is the full vocabulary; drag source/target code real, non-test, but only reachable via X11 lane out of scope here.",
  "slice": "C-U"
 },
 {
  "id": "F-BRW-07",
  "verdict": "half-proven",
  "why": "grep -rn '.request_permission(' crates/ finds only the internal wrapper body plus two #[test] call sites; Browser::request_permission has zero production callers. Persisted-reload half stands proven.",
  "evidenceDiscriminates": true,
  "newEvidenceCell": "half-proven (unchanged, independently re-verified via grep): request_permission has exactly 3 call sites in the tree -- browser.rs:887 (its own wrapper body) and 2 #[test] sites (1711,1722) -- zero production callers. Persisted-reload half (main.rs) stands proven.",
  "slice": "C-U"
 },
 {
  "id": "F-SET-11",
  "verdict": "half-proven",
  "why": "Claude's remaining 3 states genuinely blocked by login_shell()'s dotfile re-sourcing (claude.rs:397, confirmed live-code); Codex half was never actually blocked (CODEX_HOME isolation already proven this sweep) -- prior pass's correction to the overclaim is preserved.",
  "evidenceDiscriminates": true,
  "newEvidenceCell": "half-proven (unchanged): Claude's 3 remaining states blocked by claude.rs:397 dotfile re-sourcing, confirmed live-code, no synthetic instrument reaches it. Codex half was mislabeled as blocked in an earlier record; CodexUsageFetcher is HTTP+auth.json-based and already proven reachable via CODEX_HOME isolation for F-CORE-USG-05/07 this sweep -- correction preserved.",
  "slice": "C-U"
 },
 {
  "id": "F-SET-24",
  "verdict": "half-proven",
  "why": "Same zero-production-caller finding for request_permission (browser.rs, shared crate with F-BRW-07); WAYLAND-LANE.md confirms webview content is X11-only; empty-state capture independently viewed as genuine. Grant/revoke round trip needs the X11 lane.",
  "evidenceDiscriminates": true,
  "newEvidenceCell": "half-proven (unchanged, reconfirmed): same request_permission zero-caller grep result as F-BRW-07 (same file). Empty-grant-list capture (04-03-settings-permissions.png) genuine. Grant/revoke round trip owed to X11 lane, out of scope here.",
  "slice": "C-U"
 },
 {
  "id": "F-TERM-06",
  "verdict": "half-proven",
  "why": "main.rs:2211-2212 explicitly routes CopyPaneId/CopyTerminalId (with Copy/Paste/CopyContext/ClearTerminal) to None in the ControlAction mapping -- confirmed no command-socket path exists; only the right-click context menu (context_menu.rs:59/64) reaches the clipboard-writing code (lib.rs:1138-1143).",
  "evidenceDiscriminates": true,
  "newEvidenceCell": "half-proven (unchanged, independently re-verified): CopyPaneId/CopyTerminalId reachable only via right-click menu (context_menu.rs:13-14,59,64 -> lib.rs:1138-1143 clipboard write); main.rs:2211-2212 confirms no ControlAction path exists. Items visible in p17-rclick-term.png. Click+clipboard-read remains owed, no new drive this pass.",
  "slice": "C-U"
 },
 {
  "id": "F-CORE-USG-07",
  "verdict": "half-proven",
  "why": "codex.rs's Err(_) => Unavailable(LoggedOut) branch confirmed present (now line 331, drift from originally-cited 319 due to unrelated intervening edits) -- a real HTTP+auth.json path independently proven reachable via CODEX_HOME isolation this sweep with a positive-control label pair. Refresh-needed/merge-save-on-success half remains owed.",
  "evidenceDiscriminates": true,
  "newEvidenceCell": "half-proven (unchanged, independently re-verified): Err(_) -> LoggedOut branch exists in tiller_usage/src/codex.rs (line 331, minor drift from cited 319, same branch). Positive-control label pair 'Codex 100% 5h' vs 'Codex logged out' (empty CODEX_HOME) stands proven. Refresh/merge-save-on-success half (shared w/ F-CORE-USG-05) still owed, no new instrument found.",
  "slice": "C-U"
 },
 {
  "id": "F-EDIT-08",
  "verdict": "NOT EXERCISED",
  "why": "Portal-based route unproven as before. New: found a second, real production dedup path (main.rs add_file_tab, distinct from the untested DocumentRegistry) reachable via Files-panel double-click; my own live attempt to drive it was mis-targeted/inconclusive, not a negative.",
  "evidenceDiscriminates": false,
  "newEvidenceCell": "NOT EXERCISED (unchanged). Builder landed nothing. Source read: DocumentRegistry (editor.rs, the component the row's comment names) has zero production callers, but add_file_tab (main.rs:4423) implements its own real inline dedup (scans open File tabs, focuses existing instead of duplicating) -- a second, wired, untried route via Files-panel double-click, not the ctrl+o portal. Attempted live double-click on f.txt; coordinates crossed a resolution toggle and landed on stray targets (opened a duplicate Changes tab, not the file) -- inconclusive, not proof either way.",
  "slice": "C-MAIN-3"
 },
 {
  "id": "F-GIT-BRANCH-01",
  "verdict": "NOT EXERCISED",
  "why": "GitBranches::list/list_branches still has zero callers outside tests; no wave-C commit touched branches.rs.",
  "evidenceDiscriminates": false,
  "newEvidenceCell": "NOT EXERCISED (unchanged). git log f3b6169..HEAD -- tiller_git/src/branches.rs is empty (no wave-C commit from any slice). grep re-confirms GitBranches::list/list_branches: zero callers outside tests/p99_git_rows.rs. Record stands.",
  "slice": "C-MAIN-3"
 },
 {
  "id": "F-PER-01",
  "verdict": "FAILED \u2014 defective",
  "why": "No in-range commit touched the chat persistence write path; pass-17's live restart evidence (0 rows chat_turn/session_ref after 2 real ACP exchanges) is still the freshest instrument.",
  "evidenceDiscriminates": true,
  "newEvidenceCell": "FAILED -- defective (unchanged). git log f3b6169..HEAD -- tiller_ui/src/chat.rs shows only the 3 C-CHAT-chain commits (status pill / AuthRequired banner / MCP warnings), none touching the persistence write path; tiller_acp/src/chat.rs untouched in range (confirmed via INTEGRATION.md cross-slice sweep). Pass-17's live restart finding (chat_turn=0, session_ref=0 rows after two real completed ACP exchanges, WAL-aware read) stands as the most recent evidence.",
  "slice": "C-MAIN-3"
 },
 {
  "id": "F-PER-08",
  "verdict": "half-proven",
  "why": "Re-confirmed via source: newly_allowed (main.rs:4645-4668) is the only synthesis site and is socket-only; no UI-settings route exists.",
  "evidenceDiscriminates": false,
  "newEvidenceCell": "half-proven (unchanged). grep re-confirms newly_allowed (main.rs:4645-4668) is the sole site that can synthesize a browser-origin permission grant, reachable only via browser.* socket calls, not any settings UI control. General-settings half stays proven; browser-origin half stays unreached on this lane.",
  "slice": "C-MAIN-3"
 },
 {
  "id": "F-PERSIST-DB-05",
  "verdict": "FAILED \u2014 defective",
  "why": "load_chat_transcript still has exactly 2 production callers (tiller_acp/src/chat.rs); no in-range commit touched that call graph; the app-level restart VERIFY remains unexerciseable on the live path.",
  "evidenceDiscriminates": true,
  "newEvidenceCell": "FAILED -- defective (unchanged). grep re-confirms load_chat_transcript's 2 production callers stay confined to tiller_acp/src/chat.rs; no wave-C commit (any slice) touched that call graph. Row's own VERIFY (create chat tabs, restart, inspect transcript restoration in-app) still unexerciseable on the path the app actually uses.",
  "slice": "C-MAIN-3"
 },
 {
  "id": "F-PRJ-03",
  "verdict": "FAILED \u2014 absent",
  "why": "Live-drove project.add on a genuine non-git folder: silent add, zero prompt (frame 09-nongit-add.png). The row's real VERIFY clause (Initialize Git / Add without Git / Cancel three-way prompt) is confirmed absent; grep shows only a post-add Initialize-Git context action, never a browse-time prompt.",
  "evidenceDiscriminates": true,
  "newEvidenceCell": "FAILED -- absent, evidence corrected. Live: ctl project.add path=/tmp/cmain3-nongit (real non-.git folder) -> ok:true, added SILENTLY, zero prompt (frame 09-nongit-add.png). grep confirms no browse-time Initialize-Git/Add-without-Git/Cancel prompt exists anywhere (only a post-add sidebar context action). Separately: the ledger's own evidence sentence (no Open Project entry exists) is FALSE and stale -- live frame 07-menuopen.png shows Open Project.../Clone Repository.../Create Project... all three, Open Project wired to a real cx.prompt_for_paths folder picker (commit 5c1248f, predates wave-C entirely). Verdict unchanged; evidence text corrected so it isn't propagated forward.",
  "slice": "C-MAIN-3"
 },
 {
  "id": "F-SET-04",
  "verdict": "FAILED \u2014 defective",
  "why": "A sibling slice's (C-MAIN-1) concurrent main.rs commit gave resume_command a real production caller, making the functional half directly traceable for the first time: resume_agent_sessions is read only for UI/DB round-trip and never gates the restore path anywhere in the call chain -- the setting is proven decorative, not merely unproven.",
  "evidenceDiscriminates": true,
  "newEvidenceCell": "UPGRADED half-proven -> FAILED -- defective. C-MAIN-1's commit 32fffaf added a real prod caller of resume_command (main.rs:7807, restored_agent_shell <- resumable_session_refs <- restore_tabs/restore_tabs_in_workspace). Traced the full chain: saved_session_refs = session_store.load_session_refs() (main.rs:8450) passed into restore_tabs UNCONDITIONALLY; grepped every .resume_agent_sessions site in main.rs + tiller_persistence/db.rs -- used only for settings snapshot/DB round-trip and system.capabilities reporting, never checked in the restore/resume path. Toggling the setting off has zero effect on whether a validated session gets --resume'd. DB half still holds; functional half is now concretely disproven, not just unreached.",
  "slice": "C-MAIN-3"
 },
 {
  "id": "F-SET-09",
  "verdict": "half-proven",
  "why": "grep re-confirms no InstallStatus/install-progress field exists anywhere in settings.rs; no in-range commit added one.",
  "evidenceDiscriminates": false,
  "newEvidenceCell": "half-proven (unchanged). grep re-confirms zero InstallStatus/install-progress field in settings.rs (only hit is a test name). Click-reaches-consumer half stays proven; feedback/control-state-change half stays structurally absent.",
  "slice": "C-MAIN-3"
 },
 {
  "id": "F-SET-10",
  "verdict": "half-proven",
  "why": "No in-range commit touched the cited wiring (main.rs settings conversion, session.rs usage-bar plumbing); the one in-range status_bar.rs commit is unrelated Codex refresh-copy work.",
  "evidenceDiscriminates": false,
  "newEvidenceCell": "half-proven (unchanged). git log f3b6169..HEAD -- tiller_ui/src/status_bar.rs shows only 8b7c265 (Codex refresh-classification copy), unrelated to this row's toggle/interval/Refresh-now clause. DB half stays proven from prior live kill/relaunch evidence; refresh-interval-change and Refresh-now-confirmed-update stay test-only.",
  "slice": "C-MAIN-3"
 },
 {
  "id": "F-SET-18",
  "verdict": "half-proven",
  "why": "grep re-confirms no Install/Update/Retry control anywhere in settings.rs agent rows; no in-range commit added one.",
  "evidenceDiscriminates": false,
  "newEvidenceCell": "half-proven (unchanged). grep re-confirms zero Install/Update/Retry control site in settings.rs's agent rows. No wave-C commit added one. Record stands.",
  "slice": "C-MAIN-3"
 },
 {
  "id": "F-SET-22",
  "verdict": "FAILED \u2014 defective",
  "why": "Re-confirmed directly in source: main.rs:8284 still carries the row's own tracking comment and hard-codes agent_colors to the default rather than reading AppSettings, which has no such field.",
  "evidenceDiscriminates": true,
  "newEvidenceCell": "FAILED -- defective (confirmed, unchanged). settings_snapshot_from_app_settings (main.rs:8284) still carries its own comment verbatim (F-SET-22 has no AppSettings field yet...) and hard-codes agent_colors: SettingsSnapshot::default().agent_colors. Grepped the whole AppSettings struct: no agent_colors field exists. Click-to-snapshot route is real (prior evidence); value dead-ends exactly where the row's own defect names.",
  "slice": "C-MAIN-3"
 },
 {
  "id": "F-SID-06",
  "verdict": "half-proven",
  "why": "No in-range sidebar.rs commit touches the collapsed-project descendant-badge path (the 3 in-range commits are New-Worktree-prompt fields and a GitHub avatar prefill fix).",
  "evidenceDiscriminates": false,
  "newEvidenceCell": "half-proven (unchanged). git log f3b6169..HEAD -- tiller_ui/src/sidebar.rs: 3 commits, all New-Worktree-prompt base/location fields + GitHub avatar prefill -- none touch collapsed-project badges. Standing P106 half-proven (dots shown live on worktree rows; collapsed-project badge not exercised, code gates dots to worktree rows) stands.",
  "slice": "C-MAIN-3"
 },
 {
  "id": "F-SID-11",
  "verdict": "half-proven",
  "why": "Same file-touch check as F-SID-06: no in-range sidebar.rs commit is relevant to the branch/folder/primary/comment/status display clause.",
  "evidenceDiscriminates": false,
  "newEvidenceCell": "half-proven (unchanged). Same sidebar.rs commit-touch check as F-SID-06 -- none relevant to branch/path/Primary/status/comment display. Standing P106 half-proven (branch/path/Primary/status shown live; no folder-worktree row exists, comment not rendered) stands.",
  "slice": "C-MAIN-3"
 },
 {
  "id": "F-SID-12",
  "verdict": "half-proven",
  "why": "No in-range commit touched wayland-virtual-pointer.c or the command-palette chord path; BTN_LEFT hardcoding and the ctrl-shift-p-gated alternate route both re-confirmed unchanged.",
  "evidenceDiscriminates": false,
  "newEvidenceCell": "half-proven (unchanged). No wave-C commit touched wayland-virtual-pointer.c. Re-confirmed via grep: sidebar.rs:762 'Set Primary' palette entry still requires the same out-of-scope ctrl-shift-p chord as the primary route. No chord-free path exists on this lane.",
  "slice": "C-MAIN-3"
 },
 {
  "id": "F-SID-15",
  "verdict": "FAILED \u2014 defective",
  "why": "Direct source read: no 'Remove Worktree' string anywhere in sidebar.rs (no context-menu entry); the only door, hover-x, calls remove_worktree_row directly with zero confirmation before spawning the actual on-disk delete.",
  "evidenceDiscriminates": true,
  "newEvidenceCell": "FAILED -- defective (confirmed, unchanged). grep: literal 'Remove Worktree' does not occur anywhere in sidebar.rs -- no menu entry. Hover-x on_click (sidebar.rs:2454-2472) calls remove_worktree_row(row_id,cx) directly with stop_propagation and no confirm state; remove_worktree_row (sidebar.rs:1525) spawns the real remove_worktree (deletes on-disk dir) immediately on the background executor. No wave-C commit touched this code. Destructive, no safety confirmation, no named route -- matches standing verdict exactly.",
  "slice": "C-MAIN-3"
 },
 {
  "id": "F-PRJ-13",
  "verdict": "PASSED",
  "why": "Live X11 drive: rclick project row -> Project Settings shows all 8 colour swatches wrapped onto two lines instead of clipped; picking the green git-branch icon glyph and clicking Close then shows the sidebar project row itself displaying the green git-branch glyph instead of the default orange folder -- both the clipping defect and the on_change(ProjectIcon)-unwired sidebar defect are confirmed fixed end to end.",
  "evidenceDiscriminates": true,
  "newEvidenceCell": "X11 drive: settings-sheet screenshot shows 8/8 swatches wrapped (6+2 rows); after picking green branch icon + Close, sidebar row icon is green branch glyph, not default orange folder -- full round trip confirmed live, exceeding builder's own unverified sidebar half",
  "slice": "C-P3"
 },
 {
  "id": "F-PRJ-17",
  "verdict": "PASSED",
  "why": "Live three-field drive (branch+base+location together, not just one field): typed base branch 'altbase' (a real second branch in the scratch repo with a distinguishing commit/file) into the base field; created worktree's git log shows that distinguishing commit as HEAD, proving the typed base -- not HEAD -- was used as ancestor.",
  "evidenceDiscriminates": true,
  "newEvidenceCell": "Scratch repo: branch 'altbase' has extra commit ec2f2cc (MARKER.txt) not on main. New Worktree base field typed 'altbase'; created worktree's git log HEAD = ec2f2cc, proving base branch honored, not HEAD default",
  "slice": "C-P3"
 },
 {
  "id": "F-PRJ-18",
  "verdict": "PASSED",
  "why": "Same live drive as F-PRJ-17: typed an absolute custom path into the location field; git worktree list shows the new worktree living at exactly that typed path, not the project's default sibling directory.",
  "evidenceDiscriminates": true,
  "newEvidenceCell": "Location field typed '/tmp/cp3-custom-location'; git worktree list confirms worktree at /tmp/cp3-custom-location/cp3-proj-critic-branch, not the default project-sibling path -- sidebar path label matches too",
  "slice": "C-P3"
 },
 {
  "id": "F-AGENT-SAFE-01",
  "verdict": "PASSED",
  "why": "X11 lane supports rclick (unlike the builder's Wayland lane), so the actual named route was driven live in both directions: happy path wrote a marker-bearing .claude/skills/tiller/SKILL.md and worktree-local .claude/settings.local.json hooks via a real right-click -> New Tab -> Claude Code launch; refusal path (pre-seeded unmanaged SKILL.md, repeated identical gesture) left the file byte-for-byte untouched and left settings.local.json's mtime unchanged, with the app log showing PrepareError::UnmanagedSkillFile fired for that exact path -- matching the row's actual VERIFY clause. One real but separately-scoped UX gap noted: the failure is only eprintln'd to the app log, never surfaced in the sidebar/pane, so the tab launches normally with silently-missing hooks.",
  "evidenceDiscriminates": true,
  "newEvidenceCell": "Real rclick->New Tab->Claude Code (X11 lane) wrote marker SKILL.md + settings.local.json hooks. Overwrote SKILL.md with unmarked text, repeated gesture: file unchanged byte-for-byte, settings.local.json mtime unchanged, app log shows PrepareError::UnmanagedSkillFile. Caveat: failure not surfaced to user UI, only app log",
  "slice": "C-P3"
 },
 {
  "id": "F-AGENT-API-01",
  "verdict": "PASSED",
  "why": "The real fix landed via the integrator in a foreign file (tiller_control/src/panel.rs commit 3fb90ec per INTEGRATION.md), not by this builder. Reran cargo test -p tiller_control clean (46/46). Live-reproduced the exact race the row names: opened a real OpenCode tab, panel.list showed it wired, and an immediately-following panel.read returned ok:true (empty scrollback) instead of the previously-reproduced UnknownPane error.",
  "evidenceDiscriminates": true,
  "newEvidenceCell": "Live OpenCode tab via rclick->New Tab->OpenCode; panel.list shows pane-2 agent=opencode wired; immediate panel.read id=pane-2 returns {ok:true,data:\"\"}, not unknown-pane -- confirms integrator's dual-map PaneRegistry::read fix (3fb90ec) live, plus tiller_control 46/46 rerun clean",
  "slice": "C-P3"
 },
 {
  "id": "F-AGENT-OMP-01",
  "verdict": "UNREACHABLE",
  "why": "Independently reran oh-my-pi --version and reproduced the identical upstream SyntaxError at bin/oh-my-pi.js:176 before any Tiller code runs. No session can ever exist to exercise this row's real behaviour; the blocker is upstream of Tiller and outside this pass's ability to route around, matching the ledger's own instrument-blocked framing. Graded UNREACHABLE rather than NOT EXERCISED because the behaviour genuinely cannot be reached, not merely deferred for time.",
  "evidenceDiscriminates": false,
  "newEvidenceCell": "Reconfirmed independently: oh-my-pi --version -> SyntaxError: Unexpected token ':' at bin/oh-my-pi.js:176, same as prior passes. Upstream-blocked, not exercisable",
  "slice": "C-P3"
 },
 {
  "id": "F-AGENT-OMP-02",
  "verdict": "UNREACHABLE",
  "why": "Same shared upstream blocker as OMP-01, independently reconfirmed with the same command. omp.rs's command/resume_command/prepare were read and look correct for a working upstream, but no session can be spawned locally to exercise them.",
  "evidenceDiscriminates": false,
  "newEvidenceCell": "Same upstream SyntaxError reconfirmed via oh-my-pi --version; no hook event ever fireable without a live session. Upstream-blocked, not exercisable",
  "slice": "C-P3"
 },
 {
  "id": "F-CORE-ACT-25",
  "verdict": "FAILED \u2014 absent",
  "why": "Re-grepped BootstrapRestoreOrder workspace-wide on current HEAD: only its own definition/impl, a pub use re-export, and its own test reference it. restore_tabs_in_workspace (main.rs:7967) still never calls it. No wave-C commit touched bootstrap.rs.",
  "evidenceDiscriminates": true,
  "newEvidenceCell": "Re-grepped today, zero callers outside own module/test, unchanged from prior record; no wave-C commit touched bootstrap.rs or the restore path.",
  "slice": "C-MAIN-2"
 },
 {
  "id": "F-CORE-ACT-26",
  "verdict": "FAILED \u2014 absent",
  "why": "Re-grepped ids_to_evict workspace-wide: only its own definition in mount.rs and its own test. Zero callers elsewhere; unchanged since prior pass, no wave-C commit touched mount.rs.",
  "evidenceDiscriminates": true,
  "newEvidenceCell": "Re-grepped today, zero callers outside own test, unchanged; no wave-C commit touched mount.rs.",
  "slice": "C-MAIN-2"
 },
 {
  "id": "F-CORE-DOM-03",
  "verdict": "UNREACHABLE",
  "why": "Live-drove the add-project-menu popover today (click 293,50 then 178,84 in one action block) and it opened reliably, showing Open Project/Clone Repository/Create Project \u2014 contradicting the prior 'never seen open' record, which was a coordinate/procedure artifact. Clicking Open Project invokes cx.prompt_for_paths, an xdg-desktop-portal file picker; no portal service runs in this lane's compositor and the project's own ENVIRONMENT.md states the portal picker is Wayland-side and invisible to X captures too \u2014 structurally unreachable by every harness this project has.",
  "evidenceDiscriminates": true,
  "newEvidenceCell": "Live click today (cmain2-dom-shots/02) shows the popover open with all 3 items \u2014 new proof the popover itself works, correcting the prior 'never seen open' claim. Selecting Open Project closes the menu with no portal window visible (cmain2-dom3-shots/02); per ENVIRONMENT.md the portal picker is invisible to every harness here, not an app defect. UNREACHABLE, not NOT EXERCISED or FAILED.",
  "slice": "C-MAIN-2"
 },
 {
  "id": "F-CORE-DOM-07",
  "verdict": "NOT EXERCISED",
  "why": "Re-grepped AutoNamingThrottle/should_request/record_request workspace-wide: only domain.rs (definition+own unit test) and its own integration test file reference it; zero callers in main.rs. Unchanged since prior pass; no wave-C commit touched domain.rs. Kept as NOT EXERCISED (not reclassified to FAILED) since nobody has driven a real transcript through the actual auto-naming flow to watch it fail to fire live \u2014 I did not attempt that live drive either, no obvious socket verb to grow a chat transcript.",
  "evidenceDiscriminates": true,
  "newEvidenceCell": "Re-grepped today: zero callers outside domain.rs/tests, unchanged. No live drive attempted this pass (no socket verb to grow a transcript past the throttle gate); gap recorded plainly rather than promoted to FAILED.",
  "slice": "C-MAIN-2"
 },
 {
  "id": "F-CORE-FILE-04",
  "verdict": "FAILED \u2014 defective",
  "why": "Re-grepped FileViewEvent: emitted at file_view.rs:379, subscribed only inside file_view.rs's own #[gpui::test] (line 2398). main.rs has zero references to FileViewEvent, though it does wire the sibling RightPanelEvent::OpenFile and ChatEvent::OpenFile. Unchanged from prior record. Attempted a live click-through but lost state twice to wayland-drive.sh's own-label kill-on-relaunch and a Loading-files race; did not chase further given the grep evidence is already unambiguous.",
  "evidenceDiscriminates": true,
  "newEvidenceCell": "Re-grepped today: FileViewEvent emitted at file_view.rs:379, its only subscriber is its own module test (line 2398); main.rs never references it (grep, 0 hits) though it wires the sibling RightPanelEvent/ChatEvent OpenFile variants. Live click-through attempted but not completed (state lost to harness relaunch behavior); code evidence stands unchanged.",
  "slice": "C-MAIN-2"
 },
 {
  "id": "F-CORE-SET-01",
  "verdict": "half-proven",
  "why": "No wave-C commit touched the settings load/clamp paths (only unrelated Translucency/account-identity fields were added by the integrator). Did not re-drive the 5 already-confirmed malformed fields since unchanged; did not attempt the remaining untested fields (mount cap, sidebar widths, TILLER_SOCKET_ENABLE override, summarizerAgent) since the harness's own readiness gate requires the control socket enabled to confirm startup, making the env-override case unreachable without extending the drive script.",
  "evidenceDiscriminates": false,
  "newEvidenceCell": "No wave-C commit touched settings load/clamp code. Did not re-drive the 5 already-proven fields (unchanged) or attempt the remaining untested ones (TILLER_SOCKET_ENABLE override is unreachable through wayland-drive.sh's own readiness gate without script changes). Coverage unchanged from prior pass.",
  "slice": "C-MAIN-2"
 },
 {
  "id": "F-CORE-WSP-04",
  "verdict": "NOT EXERCISED",
  "why": "Re-grepped LayoutCommand/classify_layout_command: only layout.rs (definition+own tests) and a lib.rs re-export; zero references in main.rs or tiller_ui. Unchanged since prior pass; no wave-C commit touched layout.rs. PaneRegistry::split (what P116 actually drove) remains confirmed as a distinct path.",
  "evidenceDiscriminates": true,
  "newEvidenceCell": "Re-grepped today: LayoutCommand/classify_layout_command have zero callers outside layout.rs/lib.rs re-export; main.rs never references them. Unchanged; no wave-C commit touched layout.rs.",
  "slice": "C-MAIN-2"
 },
 {
  "id": "F-CORE-WSP-08",
  "verdict": "NOT EXERCISED",
  "why": "Re-grepped WorkspaceTabViewState: only layout.rs and its lib.rs re-export; zero references in main.rs, session.rs, or tiller_ui. Unchanged since prior pass; no wave-C commit touched layout.rs or session.rs.",
  "evidenceDiscriminates": true,
  "newEvidenceCell": "Re-grepped today: WorkspaceTabViewState has zero callers outside layout.rs/lib.rs re-export. Unchanged; no wave-C commit touched layout.rs or session.rs.",
  "slice": "C-MAIN-2"
 },
 {
  "id": "F-CTRL-BROWSER-02",
  "verdict": "FAILED \u2014 defective",
  "why": "Live-drove browser.open on a genuinely fresh instance (zero prior project.add, workspace.current confirmed 'no current workspace') and browser.open url=https://example.com still succeeded with a real surface/url/title. No missing-workspace check exists; confirms the prior record with a fresh instrument on current HEAD.",
  "evidenceDiscriminates": true,
  "newEvidenceCell": "Live today on a genuinely fresh instance (cmain2fresh, no prior project.add): workspace.current -> 'no current workspace' error, then browser.open -> ok:true with a real surface returned. Confirms the recorded defect exactly, freshly re-driven; code unchanged.",
  "slice": "C-MAIN-2"
 },
 {
  "id": "F-CTRL-BROWSER-03",
  "verdict": "FAILED \u2014 absent",
  "why": "Recorded evidence (queued:true no-op at 'main.rs:4638-4643') is stale: that line range is drain_browser_events today; the fix that removed the no-op (commit 988d9e9, honest browser_request_error + real queue_action results) is an ancestor of wave C's own start boundary (f3b6169), i.e. predates the whole wave. Live-drove browser.get today: returns an explicit ok:false 'is unsupported on Linux: browser automation is not implemented', not a silent fake success. Capability still absent, now honestly reported.",
  "evidenceDiscriminates": true,
  "newEvidenceCell": "Live today (cmain2a): browser.get returned explicit ok:false 'unsupported on Linux: not implemented', not the recorded queued:true no-op. Traced the fix (988d9e9) to before wave C's start boundary via git merge-base \u2014 recorded evidence is stale. Reclassified defective->absent: capability still doesn't exist.",
  "slice": "C-MAIN-2"
 },
 {
  "id": "F-CTRL-BROWSER-04",
  "verdict": "FAILED \u2014 absent",
  "why": "Same stale-evidence correction as BROWSER-03. Live-drove browser.screenshot and browser.snapshot today: both return explicit ok:false 'unsupported on Linux: not implemented', not the recorded queued:true no-op with a missing file/generation.",
  "evidenceDiscriminates": true,
  "newEvidenceCell": "Live today (cmain2a): browser.screenshot and browser.snapshot both returned explicit ok:false 'unsupported on Linux: not implemented' \u2014 not the recorded queued:true no-op. Same 988d9e9 pre-wave-C fix as BROWSER-03. Reclassified defective->absent.",
  "slice": "C-MAIN-2"
 },
 {
  "id": "F-CTRL-BROWSER-05",
  "verdict": "FAILED \u2014 absent",
  "why": "Same stale-evidence correction as BROWSER-03. Live-drove browser.wait and browser.act today: browser.wait returns explicit ok:false 'unsupported on Linux: not implemented'; browser.act returns 'unsupported on Linux: only the driving flag is implemented' (unchanged \u2014 no click/fill/type/press/scroll exist).",
  "evidenceDiscriminates": true,
  "newEvidenceCell": "Live today (cmain2a): browser.wait -> explicit ok:false 'unsupported: not implemented'; browser.act -> explicit ok:false 'only the driving flag is implemented', none of click/fill/type/press/scroll exist (source-confirmed, single match-arm handles only 'driving'). Reclassified defective->absent for the not-implemented half.",
  "slice": "C-MAIN-2"
 },
 {
  "id": "F-CTRL-BROWSER-06",
  "verdict": "FAILED \u2014 absent",
  "why": "Same stale-evidence correction as BROWSER-03. Live-drove browser.eval and browser.console today: both return explicit ok:false 'unsupported on Linux: not implemented', not the recorded queued:true no-op.",
  "evidenceDiscriminates": true,
  "newEvidenceCell": "Live today (cmain2a): browser.eval and browser.console both returned explicit ok:false 'unsupported on Linux: not implemented' \u2014 not the recorded queued:true no-op. Same 988d9e9 pre-wave-C fix. Reclassified defective->absent.",
  "slice": "C-MAIN-2"
 },
 {
  "id": "F-CTRL-CLI-02",
  "verdict": "PASSED",
  "why": "Live-drove the tab-bar plain '+' menu -> 'Claude Code' item (no chord) today. The resulting new tab shows the real Claude Code CLI's own first-run trust prompt text (verified not present anywhere in the codebase via grep, so it can only be the actual claude binary running), and the freshly-written worktree's .claude/settings.local.json contains real notify hook entries pointing at the installed XDG tillerctl binary. Both halves the prior record left open (real-agent-hook wiring; the already-confirmed XDG install) are now proven live in one pass through exactly the plain-click path the prior critic named.",
  "evidenceDiscriminates": true,
  "newEvidenceCell": "Live today: tab-bar '+' -> Claude Code (no chord) spawned a tab showing the genuine Claude CLI first-run trust prompt ('Quick safety check... 1. Yes, I trust this folder'), text absent from the codebase (grep-confirmed, so it's the real binary). Worktree's .claude/settings.local.json (written by the app's own prepare step) has real notify hooks pointing at the installed XDG tillerctl. Both halves proven; upgraded half-proven -> PASSED.",
  "slice": "C-MAIN-2"
 },
 {
  "id": "F-CTRL-WORK-01",
  "verdict": "FAILED \u2014 defective",
  "why": "Source unchanged (main.rs:400-401 still doc-commented 'intentionally not persisted'; no wave-C commit touched the struct or set_worktree). Upgraded to a genuine kill+relaunch instrument: drive 1 set worktree.set comment=RESTART_PROOF_MARKER_9182 (echoed back correctly), the app process was then fully killed by the harness's own cleanup; drive 2 relaunched a fresh tiller process against the identical DB file (same label => same /tmp/<label>.sqlite) and read the comment back via worktree.set session=probe-readback (never touching comment itself) \u2014 it came back empty.",
  "evidenceDiscriminates": true,
  "newEvidenceCell": "Real restart today: set comment=RESTART_PROOF_MARKER_9182 (echoed live), fully killed the process (no TILLER_WL_KEEP), relaunched a fresh process against the identical DB file (same label), read comment back via a session-only worktree.set \u2014 came back empty. Direct restart proof, not just the source comment; verdict unchanged, evidence strengthened.",
  "slice": "C-MAIN-2"
 },
 {
  "id": "F-AGENT-OMP-03",
  "verdict": "half-proven",
  "why": "Command-string half stays proven (unit test + spec match). Live-run half independently reconfirmed still blocked: personally reran `oh-my-pi --print --no-tools 'echo hi'`, same upstream SyntaxError at oh-my-pi.js:176 before Tiller's command even matters. main.rs correctly has no adapter caller (F-SET-05's tracked gap, confirmed by grep, not this row's).",
  "evidenceDiscriminates": true,
  "newEvidenceCell": "Reran upstream oh-my-pi myself (not trusting report prose): same SyntaxError, node module-load crash, unrelated to Tiller's generated command.",
  "slice": "C-MAIN-1"
 },
 {
  "id": "F-AGENT-SAFE-02",
  "verdict": "PASSED",
  "why": "Live restart test isolated from any prepare() confound: fixture worktree with a stale-tillerctl-path settings.local.json, added as a project, NEVER had a pane opened in it this run (panel.list after relaunch confirms), yet after graceful quit+relaunch (same sqlite DB) the leading path was rewritten to the real resolved tillerctl path while pane-99 and a marker field in the JSON stayed byte-identical. ClaudeHookMigrator::migrate_file is genuinely invoked at startup across every known worktree.",
  "evidenceDiscriminates": true,
  "newEvidenceCell": "Real quit+relaunch, worktree with zero panes opened this run: only the leading tillerctl path changed in .claude/settings.local.json; pane-99 id and a marker field untouched.",
  "slice": "C-MAIN-1"
 },
 {
  "id": "F-AGENT-SESSION-01",
  "verdict": "PASSED",
  "why": "Same restart rig as F-CORE-ACT-24 isolates AgentSessionValidator::is_likely_valid specifically: a session ref with a matching ~/.claude/projects/<slug>/<ref>.jsonl fixture produced --resume; an otherwise-identical ref with NO matching file (negative control) fell back to plain claude. The validator's is_file() gate is now reachable from a real restart, not only its own unit tests.",
  "evidenceDiscriminates": true,
  "newEvidenceCell": "Positive+negative fixture-file pair through a real restart: matching .jsonl -> --resume fired; missing .jsonl -> fresh claude, no --resume.",
  "slice": "C-MAIN-1"
 },
 {
  "id": "F-AGENT-SESSION-02",
  "verdict": "PASSED",
  "why": "Live raw-socket round trip: session.transcript session=pane-9 agent=claude worktree=<fixture> against a fixture .jsonl with two marker strings returned {\"text\":\"...MARKER_HELLO\\n...MARKER_REPLY\"} sourced from ClaudeTranscriptSource. Two negative controls (unknown pane; ref exists but wrong worktree) returned typed failures, not silent empty success. Advertised in system.capabilities (code-read).",
  "evidenceDiscriminates": true,
  "newEvidenceCell": "Live socket call returned exact fixture marker text; unknown-pane and wrong-worktree calls returned typed error strings, not silent ok:true.",
  "slice": "C-MAIN-1"
 },
 {
  "id": "F-BRW-04",
  "verdict": "FAILED \u2014 defective",
  "why": "Re-read handle_browser_action in main.rs (owned by this slice, untouched by any C-MAIN-1 commit): browser.open validates only non-emptiness, no scheme check; browser.navigate's map_err only fires if the webview call itself errors, which per the ledger's live P96 evidence it does not for unreachable/malformed URLs. Same code paths as the recorded live evidence, confirmed present verbatim.",
  "evidenceDiscriminates": true,
  "newEvidenceCell": "Static re-read this pass (not re-driven live, file outside slice, no code change): same no-validation code paths P96 drove live are still present verbatim in main.rs.",
  "slice": "C-MAIN-1"
 },
 {
  "id": "F-BRW-05",
  "verdict": "half-proven",
  "why": "grep-reconfirmed browser.act (main.rs:4613) remains the sole caller of BrowserSurface::set_agent_driving. Render-wiring half stays proven from prior passes; no real ACP browser action sets this flag.",
  "evidenceDiscriminates": true,
  "newEvidenceCell": "grep-reconfirmed: browser.act still the only call site of set_agent_driving in the whole tree.",
  "slice": "C-MAIN-1"
 },
 {
  "id": "F-BRW-09",
  "verdict": "FAILED \u2014 defective",
  "why": "Re-read chat.rs (foreign file, untouched by this slice): Entry::User still uses render_plain_text (chat.rs:3954-3974), only Entry::Assistant uses render_markdown; both cx.open_url call sites (813, ~3676) still lack any modifier check or internal-tab routing.",
  "evidenceDiscriminates": true,
  "newEvidenceCell": "Direct re-read of chat.rs this pass: same render_plain_text/open_url code paths confirmed still present, unchanged from ledger's live evidence.",
  "slice": "C-MAIN-1"
 },
 {
  "id": "F-CHAT-34",
  "verdict": "FAILED \u2014 absent",
  "why": "Exhaustive grep -rn \"chat_sessions\" across the whole rust/crates workspace: the only non-test hit is the method's own definition in tiller_persistence/src/db.rs; every other occurrence is that crate's own integration test. Zero callers in main.rs or tiller_ui -- the browse/open/delete past-chats capability is structurally unreachable from the app, not merely untried.",
  "evidenceDiscriminates": true,
  "newEvidenceCell": "Whole-workspace grep: chat_sessions() has zero non-test callers anywhere outside tiller_persistence's own tests -- structurally absent, upgraded from NOT EXERCISED.",
  "slice": "C-MAIN-1"
 },
 {
  "id": "F-CHAT-35",
  "verdict": "FAILED \u2014 absent",
  "why": "grep-reconfirmed no no-past-chats/empty-state string anywhere in chat.rs, consistent with F-CHAT-34: the browse UI this state would belong to doesn't exist either.",
  "evidenceDiscriminates": true,
  "newEvidenceCell": "grep-reconfirmed: no empty-state string for past chats anywhere in chat.rs.",
  "slice": "C-MAIN-1"
 },
 {
  "id": "F-CHG-02",
  "verdict": "FAILED \u2014 absent",
  "why": "Live-reproduced the actual clause: fixture project, select worktree, workspace.close it, then surface.changes.open worktree=<same still-mounted worktree> now returns ok:true (14e9eee's control_state re-mark fix, live-confirmed) where it previously errored no current workspace -- a real, separate bug fixed. But the row's actual required behaviour, a visible no-worktree-selected empty state in the Changes surface/right panel itself, is still absent (grep-reconfirmed, both foreign files untouched). Different bugs; only the socket-layer one is closed.",
  "evidenceDiscriminates": true,
  "newEvidenceCell": "Live before/after: surface.changes.open on a just-closed worktree now ok:true (real fix). But no empty-state UI exists in changes.rs/right_panel.rs (grep-reconfirmed) -- row's actual clause still unmet.",
  "slice": "C-MAIN-1"
 },
 {
  "id": "F-CHG-13",
  "verdict": "FAILED \u2014 defective",
  "why": "Re-read main.rs's two OpenDiff handlers (2819, 4018 -- this slice's OWN owned file): both discard the emitted path and always call add_changes_tab (generic multi-file view). No add_diff_tab/single-file-view method exists anywhere (grep-confirmed). The builder's report claims this defect lives entirely in a foreign file (changes.rs); that's only half true -- main.rs throws the path away first, and main.rs is this slice's own file. Capability genuinely absent either way.",
  "evidenceDiscriminates": true,
  "newEvidenceCell": "main.rs:2819/4018 (this slice's own file) discard the OpenDiff path unconditionally; no single-file diff view exists anywhere. Builder's foreign-file-only scope claim is inaccurate.",
  "slice": "C-MAIN-1"
 },
 {
  "id": "F-CORE-ACT-10",
  "verdict": "NOT EXERCISED",
  "why": "panes.rs (likely home of real Layer-D wiring) is outside this slice's owned files and untouched by any C-MAIN-1 commit. Prior evidence already invalidated as confounds (tab-highlight, misrouted chat); not re-driven this pass given the file is out of scope and budget went to the six rows this slice's own commits could actually move.",
  "evidenceDiscriminates": false,
  "newEvidenceCell": "Not re-driven: panes.rs is outside this slice's owned files and received no commits; standing confound finding stands unchanged.",
  "slice": "C-MAIN-1"
 },
 {
  "id": "F-CORE-ACT-19",
  "verdict": "PASSED",
  "why": "Live D-Bus capture: spawned a real Claude Code pane via UI clicks (panel.list confirmed agent:claude), backgrounded via tab.select, dbus-monitor running throughout, tillerctl notify --status done. Captured real Notify call: title=\"Claude Code \u2014 tiller/linux/gpui-waku\" (agent + activity_label), not the old \"Claude Code \u2014 finished\" (agent + status). e17f3f7's fix is real and freshly live-confirmed, not inherited P120 evidence.",
  "evidenceDiscriminates": true,
  "newEvidenceCell": "Fresh dbus-monitor capture: real Notify call title=\"Claude Code \u2014 tiller/linux/gpui-waku\", agent+worktree-label, not agent+status.",
  "slice": "C-MAIN-1"
 },
 {
  "id": "F-CORE-ACT-20",
  "verdict": "PASSED",
  "why": "All four should_notify branches live-driven with the same rig, each checked against a fresh dbus-monitor window: hidden pane + fresh status fires (base case); identical status resent suppressed; no-agent pane suppressed; foreground/visible pane + fresh status suppressed. Closes both untested branches (no-agent-running, identical-status) the ledger recorded plus reconfirms the visibility gate both directions.",
  "evidenceDiscriminates": true,
  "newEvidenceCell": "Four dbus-monitor windows: hidden+fresh fires; identical-status, no-agent, and foreground+fresh all zero Notify calls -- all branches of should_notify exercised live.",
  "slice": "C-MAIN-1"
 },
 {
  "id": "F-CORE-ACT-24",
  "verdict": "PASSED",
  "why": "Real restart, real process tree. Attached a session ref (with matching fixture .jsonl) to a live Claude Code pane, graceful quit, relaunch against the same sqlite DB. pstree on the real relaunched tiller pid showed its restored terminal's direct child was literally `claude --resume <ref>`. Negative control: a ref with no matching transcript file restored as plain claude, no --resume. Directly refutes the ledger's prior 2-real-restarts-always-fresh finding.",
  "evidenceDiscriminates": true,
  "newEvidenceCell": "Real quit+relaunch, /proc pstree on the live tiller pid: restored pane's direct child process argv is literally 'claude --resume <ref>'; unmatched ref falls back to bare 'claude'.",
  "slice": "C-MAIN-1"
 },
 {
  "id": "F-CHAT-25",
  "verdict": "UNREACHABLE",
  "why": "Re-drove live (session critic7): switched to Manual mode via the mode picker, then explicitly asked the live Claude Code ACP agent to call its AskUserQuestion tool. The agent's own reply states it has no AskUserQuestion tool in its tool list or deferred-tool registry in this environment, so it cannot invoke it -- confirmed directly from the model, not inferred from an absent card. render_question_answer_row/parse_permission_question/AnswerTextInput are all present and structurally correct in source, and the identical Entry::Permission rendering path is proven live for the plain-gate case in F-CHAT-26/27. Only the CLI-side trigger is missing, which is outside this slice's owned files. Genuinely UNREACHABLE this pass, not merely untried.",
  "evidenceDiscriminates": true,
  "newEvidenceCell": "Live turn (critic7): asked installed Claude Code CLI to call AskUserQuestion directly under Manual mode; agent replied it has no such tool in its tool list/deferred-tool registry (frame 07-05-after-send1.png). Confirms the CLI-side blocker directly rather than by absence. Code path (chat.rs Entry::Permission, tiller_acp parse_permission_question) verified present and already proven live for the plain-gate variant (F-CHAT-26). Pi ui/select alternate route not attempted this pass (time).",
  "slice": "C-CHAT-2"
 },
 {
  "id": "F-CHAT-26",
  "verdict": "PASSED",
  "why": "Builder reported this blocked because two calibrated clicks on the mode pill did not open the picker. Independently re-drove on a fresh instance (critic3) using the same coordinates the builder used (530,864 @1715px) and the picker opened cleanly showing all six modes. Selected Manual (critic4/5), then sent a real Write-tool turn under Manual mode (critic6): a live Entry::Permission card rendered with Deny/Allow Once/Always Allow buttons, a bottom Question waiting bar, and the composer showing a Stop control in place of Send. The identical write action completed with no permission card under Auto mode elsewhere in this same pass and in the builder's own F-CHAT-32 evidence, so this is a state Auto never reaches on its own -- discriminating.",
  "evidenceDiscriminates": true,
  "newEvidenceCell": "Own live drive (critic3-6): mode picker opened on click at (530,864)@1715px (frame 05-03-after-click-530-864.png), contradicting the report's blocked claim; selected Manual (06-04-manual-selected.png); sent a real Write-tool turn; live pending-permission card with Deny/Allow Once/Always Allow rendered (critic6shots/08-06-pending.png), Question-waiting bar and Stop control visible. Same action under Auto mode elsewhere in this pass produced no permission card.",
  "slice": "C-CHAT-2"
 },
 {
  "id": "F-CHAT-27",
  "verdict": "PASSED",
  "why": "Continuation of the same live session (critic6) that proved F-CHAT-26: with the permission card still pending, called surface.chat.stop over the control socket. surface.chat.read immediately after shows the permission entry's status flip from pending to expired and the turn read 14:24 - cancelled. The capture from the same moment shows the permission card's buttons replaced by the exact string No answer -- the turn ended, matching chat.rs:4194 (expire_unanswered, invoked from the stop path) byte for byte. Both the data-model half and the render half are proven live in one continuous drive.",
  "evidenceDiscriminates": true,
  "newEvidenceCell": "Own live drive (critic6): surface.chat.stop called against a genuinely pending permission; surface.chat.read shows status pending to expired and turn cancelled; frame 09-07-after-stop.png shows No answer -- the turn ended replacing the button row, matching chat.rs:4194 verbatim. Full live proof of both data model and render.",
  "slice": "C-CHAT-2"
 },
 {
  "id": "F-CHAT-28",
  "verdict": "PASSED",
  "why": "Builder's own cited frames, opened directly and found genuine: 06-04-turn75s.png shows the collapsed purple Subagent card after a real Task-tool round trip; 07-05-expand-clicked.png (same session, click on the header) shows the chevron flipped and a nested Execute pwd -- Completed row rendered inside. The two states are visually distinct and the nested row only exists post-click -- discriminating. No re-drive needed; the builder's evidence for this row held up under direct inspection.",
  "evidenceDiscriminates": true,
  "newEvidenceCell": "Opened builder's own frames directly: f28c3shots/06-04-turn75s.png (collapsed) vs 07-05-expand-clicked.png (expanded, nested Execute pwd - Completed row visible). Both frames genuine and match the claim; no discrepancy found.",
  "slice": "C-CHAT-2"
 },
 {
  "id": "F-CHAT-29",
  "verdict": "PASSED",
  "why": "The builder's report cites proof frame 02-assistantcopy-clicked.png under session label f29c1, but /tmp/f29c1shots/ only contains six frames ending at 06-04-turn60s.png -- no click frame exists there, which would have made this an unverifiable claim if taken at face value. Searched the filesystem and found the real proof in an unmentioned sibling directory, /tmp/f29manual/ (same session, one minute later). Opened it directly: the frame shows the hover Copy pill now reading Copied for a plain-text assistant reply, a state the baseline frame never reaches unclicked.",
  "evidenceDiscriminates": true,
  "newEvidenceCell": "Builder's cited path (f29c1shots/) does not contain the claimed proof frame -- verified by listing the directory (stops at 06-04-turn60s.png). Located the real proof at /tmp/f29manual/02-assistantcopy-clicked.png (same session, later timestamp): hover Copy pill reads Copied. Opened and confirmed directly before crediting.",
  "slice": "C-CHAT-2"
 },
 {
  "id": "F-CHAT-30",
  "verdict": "PASSED",
  "why": "Same session and same directory-mismatch issue as F-CHAT-29 (report cites f29c1, real frame is in the unmentioned /tmp/f29manual/). Opened 01-codecopy-clicked.png directly: the code block's always-visible Copy button is mid-transition to Copied with the checkmark, cursor still over it, and the assistant-level Copy pill is also now visible in the same frame (confirming the two controls share one hover group as separately claimed). Discriminating against the pre-click baseline which shows plain Copy.",
  "evidenceDiscriminates": true,
  "newEvidenceCell": "Located and opened /tmp/f29manual/01-codecopy-clicked.png directly (not at the report's cited f29c1shots path): code-block Copy button mid-transition to Copied with cursor over it. Confirms the control live; report's directory citation for this row was also wrong but the underlying claim holds.",
  "slice": "C-CHAT-2"
 },
 {
  "id": "F-CHAT-32",
  "verdict": "PASSED",
  "why": "Builder's own frames, opened directly: Revert (unclicked) to Confirm Revert/Cancel to reverted (red text) across three sequential frames. Then independently verified on the actual filesystem rather than trusting the transcript label: git status --porcelain and ls on docs/scratch/ in the live worktree show the directory does not exist at all, confirming the probe file was genuinely deleted from disk, not just relabeled in the chat UI. This is the strongest instrument available for this row.",
  "evidenceDiscriminates": true,
  "newEvidenceCell": "Opened builder's frames (Revert to Confirm Revert/Cancel to reverted) and independently ran git status --porcelain plus ls docs/scratch/ on the actual worktree: directory does not exist, confirming real deletion from disk, not just a chat-transcript label change.",
  "slice": "C-CHAT-2"
 },
 {
  "id": "F-TAB-01",
  "verdict": "FAILED \u2014 defective",
  "why": "Live-driven, Wayland lane 2026-08-15: expanded docs (children rendered correctly), then clicked the child row 'superpowers' \u2014 the parent docs row collapsed instead, and the click was consumed by whatever row the list-reflow left under that pixel. Reproduces the builder's claimed defect exactly with a clean before/after pair.",
  "evidenceDiscriminates": true,
  "newEvidenceCell": "Wayland lane 2026-08-15: docs expanded (c4c/02) shows linux-rewrite/superpowers/visual-reviews children; click on child 'superpowers' (c4d/02) collapses docs instead, click lands on the row that shifted into that pixel after collapse (Packages). Confirms parent collapses on child click.",
  "slice": "C-MAIN-4"
 },
 {
  "id": "F-TAB-08",
  "verdict": "half-proven",
  "why": "Fresh source read 2026-08-15: tab_bar.rs render_chat_empty still has no .on_click/.hover, unlike sibling render_chat_agent_item. Unchanged from prior finding; not re-driven live this pass.",
  "evidenceDiscriminates": false,
  "newEvidenceCell": "Re-confirmed via source read only 2026-08-15 (unchanged code): render_chat_empty (tab_bar.rs) still lacks .on_click, matches prior click-to-settings-absent finding exactly.",
  "slice": "C-MAIN-4"
 },
 {
  "id": "F-TAB-11",
  "verdict": "FAILED \u2014 absent",
  "why": "Right-click menu now renders all 12 items incl. Split Left/Right/Above/Down (prior 'only Copy' evidence is stale, dated after the code that produces the full menu). But the clause is about a disabled item with an explanatory reason, and zero 'disabled'/'reason' concept exists anywhere in context_menu.rs or its render \u2014 every item is unconditionally enabled always. Capability confirmed absent.",
  "evidenceDiscriminates": true,
  "newEvidenceCell": "X11 lane 2026-08-15 (x2-rclick.png): right-click renders full 12-item menu (contra prior 'only Copy'). grep 'disabled|reason' in tiller_terminal/src/{lib,context_menu}.rs = zero hits; items() is a flat unconditional const array. Disabled-with-reason capability confirmed absent; menu-emptiness evidence was stale.",
  "slice": "C-MAIN-4"
 },
 {
  "id": "F-TAB-13",
  "verdict": "half-proven",
  "why": "add_group still #[cfg(test)]-only (tab_machinery.rs:150); all 12 live tab-creation sites in main.rs still hard-code active_group(). Structural defect unchanged; Z-order fix stands from prior pass.",
  "evidenceDiscriminates": false,
  "newEvidenceCell": "Re-confirmed via source read 2026-08-15: add_group still #[cfg(test)]-only, 12 active_group() call sites unchanged in main.rs. Group-attach still dead in production.",
  "slice": "C-MAIN-4"
 },
 {
  "id": "F-TAB-16",
  "verdict": "PASSED",
  "why": "Live X11 drive: ctrl-w on a dirty Terminal tab opened 'Close dirty tab? / Discard unsaved work in Terminal?' with Close/Cancel; Cancel left the tab open (verified tab count unchanged), Close actually removed it (verified tab count dropped). Clean discriminating positive control. Underlying fix predates both prior 'defective' evidence dates.",
  "evidenceDiscriminates": true,
  "newEvidenceCell": "X11 lane 2026-08-15: ctrl-w on dirty Terminal tab -> 'Close dirty tab? Discard unsaved work in Terminal?' dialog (x12). Cancel dismisses, tab count unchanged (x13). Re-run + Close: dialog dismisses, tab count drops by one, tab gone (x14). Upgrades from FAILED \u2014 defective; fix predates prior evidence dates (commit 5e55ed0, 2026-08-13).",
  "slice": "C-MAIN-4"
 },
 {
  "id": "F-TAB-23",
  "verdict": "FAILED \u2014 defective",
  "why": "Typed a unique marker into the sole terminal, right-clicked, clicked Split Left via the menu (previously-owed route): the new (unmarked, no shell-integration breadcrumb yet) terminal landed on the right regardless of 'Left' being clicked. Confirms the placement defect via the menu route too, not just the socket route already on record. Root cause of the mismatch between correct-looking placement code and wrong pixels not resolved this pass.",
  "evidenceDiscriminates": true,
  "newEvidenceCell": "X11 lane 2026-08-15: typed MARKER_ORIGINAL_PANE into sole terminal, right-click -> Split Left. New (breadcrumb-less) pane appeared on the right, marker-bearing original stayed left (x4, x5). Confirms Left/Right indistinguishable via the menu route, not just socket. delegated_terminal_context_action/split_focused_inner/render order all read correct on inspection \u2014 mismatch unexplained, flagged open.",
  "slice": "C-MAIN-4"
 },
 {
  "id": "F-TAB-28",
  "verdict": "PASSED",
  "why": "Live X11 drive: ctrl-w on a clean Chat tab closed it immediately with no dialog (tab count dropped by one). ctrl-w on a dirty Terminal tab opened the same confirm dialog as F-TAB-16. Directly contradicts prior 'zero observable effect' finding; underlying keybinding/handler predates that evidence date.",
  "evidenceDiscriminates": true,
  "newEvidenceCell": "X11 lane 2026-08-15: real xdotool ctrl-w on clean Chat tab -> closed immediately, no dialog, tab count -1 (x11). ctrl-w on dirty Terminal tab -> same 'Close dirty tab?' dialog as F-TAB-16 (x12). Upgrades from FAILED \u2014 defective.",
  "slice": "C-MAIN-4"
 },
 {
  "id": "F-TERM-08",
  "verdict": "FAILED \u2014 absent",
  "why": "requires_close_confirmation still has zero callers anywhere outside its own def/tests; close_focused_pane still has no confirmation gate. Matches prior finding exactly on unchanged code.",
  "evidenceDiscriminates": false,
  "newEvidenceCell": "Re-confirmed via grep 2026-08-15: requires_close_confirmation zero callers, close_focused_pane ungated. State half (ClosePane works) already stood; confirmation half re-confirmed absent.",
  "slice": "C-MAIN-4"
 },
 {
  "id": "F-TERM-09",
  "verdict": "FAILED \u2014 defective",
  "why": "Code (tiller_activity/src/{title,model}.rs) unchanged since before the prior pass's four-live-agent-launch evidence. Not re-driven live this pass \u2014 would require real agent CLI turns this pass's remaining budget didn't cover.",
  "evidenceDiscriminates": false,
  "newEvidenceCell": "Not re-driven this pass; source confirmed unchanged since prior evidence (last touch 2026-08-13, before pass-17 evidence). Carrying prior verdict forward explicitly un-reverified live here.",
  "slice": "C-MAIN-4"
 },
 {
  "id": "F-TERM-11",
  "verdict": "PASSED",
  "why": "Fresh-DB Wayland instance with zero projects added renders exactly the spec'd central-surface state: icon + 'No worktree selected' + 'Add a project, then select a worktree.' Fix (main.rs:6538-6558) dates to commit 7289c84, 2026-08-14T17:33 \u2014 after every prior census that recorded it absent that same day, which is why no pass had caught it since.",
  "evidenceDiscriminates": true,
  "newEvidenceCell": "Wayland lane 2026-08-15, fresh DB, zero projects: central terminal surface shows terminal glyph + 'No worktree selected' + 'Add a project, then select a worktree.' (c4b/02-noworktree.png). Fix landed 2026-08-14T17:33 (commit 7289c84), after that day's 'still absent' censuses. Upgrades from FAILED \u2014 absent.",
  "slice": "C-MAIN-4"
 },
 {
  "id": "F-TERM-SPLIT-01",
  "verdict": "FAILED \u2014 defective",
  "why": "Same live evidence and mechanism as F-TAB-23 (shared split_terminal_at_with_placement code path). Placement half defective via the menu route too; TerminalPaneCache integration not independently re-checked this pass.",
  "evidenceDiscriminates": true,
  "newEvidenceCell": "See F-TAB-23 \u2014 identical live evidence, same underlying split_terminal_at_with_placement path. Placement defect confirmed via menu route this pass, not just socket.",
  "slice": "C-MAIN-4"
 },
 {
  "id": "F-TERM-UI-01",
  "verdict": "half-proven",
  "why": "The prior blocker ('menu cannot be opened from this lane') is resolved: X11 rclick works. Hit-testing resolves the correct pane; the menu offers every action the clause enumerates. One action (Split Left) invoked end-to-end with a real observed effect. Remaining 11 items not individually invoked/inspected this pass, so short of the clause's literal 'invoke every menu item' ask; confirmation-dialog absence already independently proven via F-TERM-08.",
  "evidenceDiscriminates": true,
  "newEvidenceCell": "X11 lane 2026-08-15: rclick correctly hit-tests the focused terminal and renders all 12 spec'd actions (previously blocked, Wayland has no right-click device at all \u2014 confirmed via wayland-virtual-pointer.c hard-coding BTN_LEFT only). Split Left invoked, real effect observed. Not all 12 items individually inspected; verdict stays half-proven but the owed lane-blocker is resolved.",
  "slice": "C-MAIN-4"
 },
 {
  "id": "F-USE-06",
  "verdict": "half-proven",
  "why": "register_restored_agent still has exactly its 2 prior call sites, unchanged. A live positive-fire D-Bus test needs a real agent launch not covered this pass.",
  "evidenceDiscriminates": false,
  "newEvidenceCell": "Not re-driven this pass; register_restored_agent call sites (restore_tabs, restore_tabs_in_workspace) unchanged since prior finding. Carrying prior verdict forward, explicitly un-reverified live here.",
  "slice": "C-MAIN-4"
 },
 {
  "id": "F-WIN-07",
  "verdict": "half-proven",
  "why": "Fresh grep 2026-08-15: no History menu/previous-launch string anywhere in tiller/tiller_ui src. Matches prior finding on unchanged code; live socket half not re-hit this pass.",
  "evidenceDiscriminates": false,
  "newEvidenceCell": "Re-confirmed via grep 2026-08-15: zero History-menu hits in tiller/tiller_ui src. Matches prior finding exactly; restoredCount:0 socket half relied on prior pass's own reproduction, not re-hit here.",
  "slice": "C-MAIN-4"
 },
 {
  "id": "F-WIN-10",
  "verdict": "FAILED \u2014 absent",
  "why": "Fresh grep 2026-08-15: zero occurrences of 'toast' anywhere in main.rs. No toast mechanism exists at all; prior live-driven inline-error finding stands on unchanged code.",
  "evidenceDiscriminates": false,
  "newEvidenceCell": "Re-confirmed via grep 2026-08-15: zero 'toast' hits in main.rs. Prior finding (both real errors render as persistent inline text, no dismissable notification) stands unchanged.",
  "slice": "C-MAIN-4"
 },
 {
  "id": "F-CORE-AUTH-01",
  "verdict": "PASSED",
  "why": "Live-drove Settings > AI Providers on a fresh Wayland instance with the sandbox's genuinely-authenticated claude CLI. Frame shows 'Signed in e.palmisano@reply.it' on the Claude Code card, sourced from discover_claude_identity -> parse_claude_json on real `claude auth status` stdout (settings.rs:539-551), with Codex's 'Not signed in' as a live negative control on the same frame.",
  "evidenceDiscriminates": true,
  "newEvidenceCell": "Live: AI Providers frame shows 'Signed in e.palmisano@reply.it' from real claude auth status stdout; Codex card same frame reads 'Not signed in' (negative control). PASSED.",
  "slice": "C-CHAT-3"
 },
 {
  "id": "F-PERSIST-DB-06",
  "verdict": "PASSED",
  "why": "Builder's blocked verdict is stale -- integrator's foreign-file patch (ea5071b/6af7ff4) landed after the report and genuinely wires account_identity end to end. Live-drove AI Providers, read account_identity via python3's sqlite3 module directly on the DB file: real row ('claude','e.palmisano@reply.it',ts), not the builder's '0 rows'. Stronger test: relaunched the app against the SAME db with PATH stripped of the claude binary (live discovery structurally impossible) -- UI still showed 'Signed in e.palmisano@reply.it', proving the cached fallback (not a lucky live re-query) is what renders.",
  "evidenceDiscriminates": true,
  "newEvidenceCell": "Live: account_identity table has real row after discovery (sqlite3 module read). Relaunched with claude unresolvable via PATH (same DB) -- UI still shows cached 'Signed in e.palmisano@reply.it', proving fallback read path works, not just live shell-out. PASSED.",
  "slice": "C-CHAT-3"
 },
 {
  "id": "F-SET-14",
  "verdict": "PASSED",
  "why": "Live-drove real gestures via the persistent virtual pointer: clicked Codex Add Account, a real x-terminal-emulator+codex login spawned and detached into its own session (launcher pgid 778803 vs login pgid 778841, matching the original defect shape). Clicked Cancel; polled ps -p <login-pid> at 0.3s intervals -- process confirmed dead within ~1.6s. Clean revert to 'Add Account', no leftover process.",
  "evidenceDiscriminates": true,
  "newEvidenceCell": "Live: Codex Add Account spawned real detached-session login process (confirmed differing pgid via ps). Cancel click killed it within ~1.6s (ps polling every 0.3s). Clean UI revert, no leftover. PASSED.",
  "slice": "C-CHAT-3"
 },
 {
  "id": "F-SET-20",
  "verdict": "FAILED \u2014 defective",
  "why": "INTEGRATION.md claims this threaded end to end, and the struct/persistence layer genuinely is (SettingsSnapshot.translucency, main.rs round-trip, tiller_persistence load/save all real and tested). But surface.settings.select's wire payload is built by a THIRD, separate function (settings_report_pairs in main.rs:1864-1958) that hand-lists fields into a BTreeMap and was never updated to include translucency. Live-reproduced the row's own VERIFY clause exactly: queried ctl surface.settings.select before and after clicking the toggle (visually confirmed ON) -- translucency key absent from the full JSON payload both times, byte for byte.",
  "evidenceDiscriminates": true,
  "newEvidenceCell": "Live: toggled Translucency ON (visually confirmed via screenshot). ctl surface.settings.select payload has zero translucency key before AND after -- settings_report_pairs (main.rs) never copies snapshot.translucency into its hand-built values map. FAILED \u2014 defective, root cause pinpointed to exact function/lines.",
  "slice": "C-CHAT-3"
 },
 {
  "id": "F-CHAT-33",
  "verdict": "half-proven",
  "why": "Code presence confirmed (mcp_warnings_shown, surface_mcp_warnings, ErrorKind::McpWarning, wired into AcpEvent::TurnEnded) -- the prior 'code-absent' defect is genuinely fixed, and tiller_acp's own stderr-capture tests pass. But two independent live attempts (a fresh single-project instance with a broken project .mcp.json, plus running the exact SDK-bundled claude binary Tiller invokes directly with matching flags) both showed the real CLI never writes an mcp-warning-shaped line to stderr during an ordinary non-interactive turn for a broken stdio MCP server -- only the separate `claude mcp list` diagnostic subcommand surfaces that text. Could not observe the actual transcript error card fire live despite genuine effort with a real authenticated CLI.",
  "evidenceDiscriminates": false,
  "newEvidenceCell": "Code genuinely present & unit-tested (real fix from code-absent). Two live attempts (broken .mcp.json via fresh instance; direct SDK binary invocation) show the real claude CLI does not emit mcp-warning-shaped stderr during an ordinary turn for this failure mode -- transcript card never observed live. half-proven, not FAILED (code is real) and not PASSED (live behavior unconfirmed).",
  "slice": "C-CHAT-3"
 },
 {
  "id": "F-SET-15",
  "verdict": "UNREACHABLE",
  "why": "Reconfirmed by code inspection and live capture across multiple frames: every provider card renders exactly one hardwired-Active 'System default' Accounts row; there is no code path producing a second, independently-selectable account for any provider, by design (single credential slot). The VERIFY clause (select between two accounts, confirm badge moves) has no state to exercise on this build -- same disposition as the existing F-SET-21 N/A-by-design ruling. Marking UNREACHABLE (not 'not-attempted') since the ledger separates could-not-reach from ran-out-of-time and this is genuinely the former.",
  "evidenceDiscriminates": false,
  "newEvidenceCell": "Reconfirmed live: every provider card shows exactly one hardwired-Active account row across all captured frames; no code path exists for a second row. VERIFY clause unanswerable as designed. UNREACHABLE, same shape as F-SET-21.",
  "slice": "C-CHAT-3"
 },
 {
  "id": "F-CORE-USG-06",
  "verdict": "PASSED",
  "evidenceDiscriminates": true,
  "why": "Live end-to-end: backdated CODEX_HOME creds with a garbage refresh_token drove the real running binary through a genuine (non-mocked) 401 from auth.openai.com/oauth/token, logged 'token refresh failed: Expired', and the status bar rendered 'Codex token expired' instead of the old generic 'Codex logged out'. Independently reproduced the same 401/token_expired response with a bare curl call. Unit tests token_refresh_failure_routes_to_distinct_usage_reasons and token_refresh_reasons_render_distinct_text also pass.",
  "newEvidenceCell": "Live: real backdated CODEX_HOME creds drove a genuine 401 from auth.openai.com through the running binary; status bar read 'Codex token expired' not generic 'logged out'. Screenshot 01-usg06.png. Upgraded half-proven to PASSED.",
  "slice": "C-P2"
 },
 {
  "id": "F-CORE-USG-05",
  "verdict": "half-proven",
  "evidenceDiscriminates": true,
  "why": "Unit test a_real_refresh_success_merges_into_the_auth_file_without_losing_unrelated_fields drives a real local one-shot HTTP fixture through refresh_token_at + save_credentials_to and proves fields the fetcher never wrote survive the merge - genuine, discriminating. Live pass additionally confirmed the proactive needs_refresh gate itself genuinely fires in the running binary against real network (log: 'proactive token refresh failed: Expired'). The success-path merge inside the actual running binary remains unreachable: TOKEN_URL is a hardcoded constant, not env-overridable, so redirecting the live binary to a controlled 200 response needs host DNS/TLS surgery out of scope for a critic pass.",
  "newEvidenceCell": "Live-confirmed the proactive refresh gate fires for real (log: proactive token refresh failed: Expired) in the same run as USG-06. Merge-on-success proven only via real local-HTTP-fixture unit test, not the live binary (TOKEN_URL not overridable). Stays half-proven.",
  "slice": "C-P2"
 },
 {
  "id": "F-USE-02",
  "verdict": "PASSED",
  "evidenceDiscriminates": true,
  "why": "Code: tooltip_text is literally text.clone() of the same segment_text() string for every ProviderUsageState, so no separate vocabulary exists to diverge. Live: moved a real persistent virtual pointer over the Codex segment while it read 'Codex token expired' (a genuine Unavailable state) and captured the hover frame - tooltip box read the identical text. Combined with the previously-confirmed Loaded-segment tooltip match, both states the row's claim covers are now live-proven.",
  "newEvidenceCell": "Live hover (real synthetic wl_pointer) over the Unavailable 'Codex token expired' segment produced a tooltip reading the identical text. Screenshot 03-hover-attempt.png. Upgraded half-proven to PASSED.",
  "slice": "C-P2"
 },
 {
  "id": "F-USE-03",
  "verdict": "half-proven",
  "evidenceDiscriminates": true,
  "why": "Booted a fully fresh instance and captured a timed burst of frames. At ~3.4s post-launch the frame genuinely shows 'Claude ...' and 'Codex ...' (the exact Loading format), persisting well past a single-frame flash; by ~5.6s it had transitioned to real Loaded/Unavailable text - a real, observed, discriminating state transition, closing the prior pass's 'no OCR instrument' gap by reading the screenshot directly. The Stale half (force a fetch timeout after a prior success) was not attempted: Claude's fetch is a real hidden-PTY /usage drive, not an easily-interceptable network call, and this was out of budget this pass.",
  "newEvidenceCell": "Live-captured the genuine Loading state ('Claude ...' / 'Codex ...') persisting for seconds on a fresh launch, then its real transition to Loaded/Unavailable text. Frames fresh3-04-crop.png, fresh3-05-crop.png. Stale-after-timeout half still not attempted. Stays half-proven, stronger footing.",
  "slice": "C-P2"
 },
 {
  "id": "F-CORE-ACT-06",
  "verdict": "PASSED",
  "evidenceDiscriminates": true,
  "why": "Code: the clear-on-unmatched-title branch in handle_title_change is gated on title_owned_panes.contains(pane_id); a process-owned pane is never in that set. Live, one pane, three screenshots: (1) a safe synthetic foreground child (symlink named codex, /proc comm reads codex) detected by Layer D within one 500ms poll - tab dot went hollow to filled orange (Running). (2) with the process still alive, a real shell printf emitted a genuine unrelated OSC title into the same live pane - dot stayed filled orange, unchanged. (3) killed the process - dot cleared to hollow within one poll. Exactly the row's claim, discriminating at each step.",
  "newEvidenceCell": "Live full cycle on one pane: real /proc-detected foreground child sets Running glyph; a genuine unrelated OSC title mid-life leaves it unchanged; killing the process clears it. Frames 12/23/25-tabglyph-crop.png. Upgraded half-proven to PASSED.",
  "slice": "C-P2"
 },
 {
  "id": "F-CORE-ACT-07",
  "verdict": "PASSED",
  "evidenceDiscriminates": true,
  "why": "Live race on a process-owned (agent_id=codex) pane: ctl notify session=pane-1 status=needs-input (real Layer-A push), then ~200ms later a real printf OSC title mapping to Running for a registered codex pane - screenshot shows the tab still reading NeedsInput ('Terminal ?'), the contradicting title suppressed inside the 1500ms debounce. After the window elapsed (~20s gap, well past 1500ms), a fresh title was sent and the tab flipped to the filled Running dot - the same signal now applies once no longer suppressed. Both debounce halves proven live on the same pane.",
  "newEvidenceCell": "Live: ctl notify(needs-input) then a same-window contradicting OSC title left the glyph at NeedsInput (27-within-debounce.png); after the debounce elapsed a fresh title flipped it to Running (28-after-debounce.png). Upgraded NOT EXERCISED to PASSED.",
  "slice": "C-P2"
 },
 {
  "id": "F-CORE-ACT-11",
  "verdict": "half-proven",
  "evidenceDiscriminates": true,
  "why": "Real integration test each_ownership_kind_is_cleared_only_by_its_own_condition combines all three ownership kinds on three distinct panes in one AgentActivityModel and asserts independent clearing - exactly this row's claim at the model level, passing. Live: independently proved process-owned's full survive/clear cycle (see ACT-06) and, separately, spawn-owned's cycle - clicked the real + menu (directly refuting the builder's 'unreachable + menu' claim, screenshots 30/35), spawned a real Claude Code tab that registered Running at spawn time before the CLI even painted, then SIGTERM'd the real claude process (no further keystrokes, to avoid repeating an earlier login-flow mishap) and watched the glyph correctly flip to Error ('!', exit 143). What's missing for full PASSED: I did not get all three kinds coexisting on three panes simultaneously with a live cross-check that clearing one leaves the other two untouched - a separate attempt at the title-owned leg on a freshly-opened plain pane hit a flaky virtual-keyboard binding race and produced garbled, un-executed input, not salvaged within budget.",
  "newEvidenceCell": "Real 3-way integration test passes. Live-refuted the builder's spawn-owned blocker: + menu reachable, real Claude Code tab spawns Running and clears to Error on SIGTERM (36/37.png). Process-owned leg fully live-proven separately (ACT-06). Title-owned leg live attempt was inconclusive (input race on a fresh pane). Stays half-proven - simultaneous 3-pane cross-check not completed live.",
  "slice": "C-P2"
 },
 {
  "id": "F-CHAT-15",
  "verdict": "PASSED",
  "why": "Live drive on a real, authenticated claude-agent-acp session: after a completed real turn the status pill read a clickable 'Auto' (mode_selectable gate confirmed true only post-turn), and clicking it opened a genuine dropdown listing six live agent-advertised modes (Auto/Manual/Accept Edits/Plan Mode/Don't Ask/Bypass Permissions) with Auto highlighted as current -- this contradicts the builder's own claim that no live agent observed advertises modes. Fixture test also passes (58/58).",
  "evidenceDiscriminates": true,
  "newEvidenceCell": "Live: real mode dropdown opened from an authenticated claude-agent-acp session post-turn (6 real modes, frame 03-after-pill-click.png); pre-turn pill was plain non-clickable idle/connecting text. Fixture test still passes.",
  "slice": "C-CHAT-1"
 },
 {
  "id": "F-CHAT-02",
  "verdict": "PASSED",
  "why": "Live drive using the exact fixture ACP-agent stub (rejects session/new with -32000) as TILLER_ACP_PROGRAM, launched through the real New Chat UI menu (not the test harness): rendered card is amber, reads 'could not launch ACP agent: ACP agent requires authentication (Login)' plus the claimed 'Sign in from a terminal using this agent's own CLI... then Retry' guidance sentence. Status pill correctly reads offline.",
  "evidenceDiscriminates": true,
  "newEvidenceCell": "Live: real amber chat-auth-required-banner rendered via UI launch against a fixture auth-rejecting agent (frame 04-chat-view.png), matching claimed text verbatim.",
  "slice": "C-CHAT-1"
 },
 {
  "id": "F-CHAT-14",
  "verdict": "FAILED \u2014 defective",
  "why": "Integrator applied the builder's suggested foreign-file diff (620b9a3, adds a bind_chat loop to TillerWorkspace::new) and INTEGRATION.md claims this resolves the row. Live reproduction on a genuinely restored (process-killed-and-relaunched) chat tab with Follow Edited Files confirmed on ('Stop Following' label) and a real turn that really edited README.md (confirmed on disk + transcript + Files-panel Diff badge) still opened no file tab. Code reading found why: the applied fix only covers TillerWorkspace::new's restore_tabs path; a second, near-duplicate function restore_launch_snapshot (session.restore control method, explicitly named in the row's own report as an equally-valid drive) independently rebuilds TabContent::Chat via restore_tabs_in_workspace and never calls bind_chat -- same bug, unpatched second call site.",
  "evidenceDiscriminates": true,
  "newEvidenceCell": "Live: real restored chat tab, Follow ON, real Edit tool call completed and confirmed on disk, but tab strip still shows only Chat/Terminal -- no file tab opened (frame 02-after-turn2.png). Code read: restore_launch_snapshot (session.restore path) never gained the bind_chat loop the integrator added elsewhere.",
  "slice": "C-CHAT-1"
 },
 {
  "id": "F-CHAT-18",
  "verdict": "FAILED \u2014 absent",
  "why": "Re-confirmed directly against the vendored agent-client-protocol-schema-1.5.0 crate source (not just re-reading the builder's claim): UsageUpdate (v1 and v2) carries only used/size/cost; input/output/cache-token fields exist only on an unrelated, feature-gated Usage struct describing a different wire message. tiller_acp::ContextUsage mirrors UsageUpdate exactly. Genuine protocol ceiling, matches the report word for word.",
  "evidenceDiscriminates": true,
  "newEvidenceCell": "Static: read vendored crate source directly at v1/agent.rs:3371 and v2/agent.rs -- confirms input_tokens/output_tokens/cached_read_tokens live only on a separate #[cfg(unstable_end_turn_token_usage)] Usage struct, not UsageUpdate.",
  "slice": "C-CHAT-1"
 },
 {
  "id": "F-CHAT-05",
  "verdict": "half-proven",
  "why": "can_send() independently gates on pending_question().is_none() as claimed, re-confirmed by direct code read. This box has only two live ACP programs (claude-agent-acp authenticated, codex-acp not authenticated), so the specific live scenario the row's route requires (a real agent whose mode genuinely differs from Claude's global default, driven through a gating turn) remains undriveable here. Not re-attempted live this pass; holding the builder's own grading rather than weakening it.",
  "evidenceDiscriminates": false,
  "newEvidenceCell": "Unchanged: guard code re-confirmed at chat.rs:1628; no new live drive attempted (no available agent on this box exercises the specific scenario).",
  "slice": "C-CHAT-1"
 },
 {
  "id": "F-CHAT-13",
  "verdict": "UNREACHABLE",
  "why": "Re-confirmed via direct grep: neither wayland-virtual-pointer.c nor wayland-drive.sh's DSL implements an XDND drag-offer protocol, and ENVIRONMENT.md's XDND section still holds for X11. Harness limitation, not a code gap.",
  "evidenceDiscriminates": false,
  "newEvidenceCell": "Re-confirmed by fresh grep of Scripts/wayland-drive.sh and wayland-virtual-pointer.c -- zero XDND primitive, same as builder's record.",
  "slice": "C-CHAT-1"
 },
 {
  "id": "F-CHAT-20",
  "verdict": "UNREACHABLE",
  "why": "Re-confirmed via direct grep: no scroll/axis primitive in the Wayland drive DSL or its C helper; X11 lane explicitly out of scope for this slice per the row's own note.",
  "evidenceDiscriminates": false,
  "newEvidenceCell": "Re-confirmed by fresh grep -- zero scroll/axis primitive, same as builder's record.",
  "slice": "C-CHAT-1"
 },
 {
  "id": "F-CHG-20",
  "verdict": "PASSED",
  "why": "Real zero-tab worktree reached by closing both default Chat+Terminal tabs (confirming the dirty-terminal dialog), then expanding Activity via the header click. 'No activity' renders as clearly legible text, not the sub-pixel specks the original evidence photographed.",
  "evidenceDiscriminates": true,
  "newEvidenceCell": "Wayland lane (label cp4e): closed both default tabs to reach a genuine zero-tab 'No Terminals' worktree (not the app default), then expanded Activity via header click. 'No activity' is fully legible. Frame /tmp/cp4-shots/07-s6-final.png. Confirms fix.",
  "slice": "C-P4"
 },
 {
  "id": "F-CHG-03",
  "verdict": "PASSED",
  "why": "chmod 000 produced the Permission-denied state with Retry; chmod 755 back plus a 3s wait with zero clicks produced full self-heal (real file listing reappeared) in one continuous app session.",
  "evidenceDiscriminates": true,
  "newEvidenceCell": "Wayland lane (label cp4f), one continuous session: chmod 000 -> 'Files unavailable: Permission denied (os error 13)' (03-permission-denied.png); chmod 755 + sleep 3, zero clicks -> panel repopulated with note.txt/README.md unprompted (04-self-healed.png). Confirms fix.",
  "slice": "C-P4"
 },
 {
  "id": "F-BRW-01",
  "verdict": "FAILED \u2014 defective",
  "why": "Pixel-scanned bounding box of a Browser tab's page content (both a pre-existing tab and a brand-new one opened this session) is 729x729 offset 55px into the sidebar and 177px short of the Files panel edge, reproducing the original defect almost exactly. Contradicts the builder's already-correct claim.",
  "evidenceDiscriminates": true,
  "newEvidenceCell": "X11 lane, DISPLAY=:1: pixel scan of two Browser tabs (one pre-existing, one freshly opened via ctl browser.open+tab.select this session, surface:15) both show content x=331..1059 vs pane's true chrome bounds x=386..1236 (measured via background scan) - 55px bleed into sidebar, 177px unpainted gap before Files panel. Frames 01-browser-click.png, 02-fresh-browser.png. Builder's own 'no shrink, no sidebar coverage' claim does not survive measurement.",
  "slice": "C-P4"
 },
 {
  "id": "F-BRW-03",
  "verdict": "PASSED",
  "why": "ctrl+a then type via linux-drive.sh landed the exact URL string with no dropped/transposed characters.",
  "evidenceDiscriminates": true,
  "newEvidenceCell": "X11 lane: click address field, key ctrl+a, type 'https://claude.ai/test-cp4-marker' via linux-drive.sh - field shows exactly that string, caret at end, zero corruption. Frame 03-addr-typed.png. Confirms already-correct.",
  "slice": "C-P4"
 },
 {
  "id": "F-TERM-04",
  "verdict": "PASSED",
  "why": "shift+F10 on a focused terminal pane opened the full 12-item context menu with no right-click, matching context_menu.rs::ITEMS exactly.",
  "evidenceDiscriminates": true,
  "newEvidenceCell": "X11 lane: click terminal pane, key shift+F10 via linux-drive.sh - full 12-item menu opened (Copy...Close Terminal), no right-click. Frame 07-ctxmenu.png. Confirms already-correct. Separately noted (not graded here): a closed Browser tab's webview keeps painting on top of the window until full app relaunch - out of scope, flag for ledger.",
  "slice": "C-P4"
 },
 {
  "id": "F-CORE-FILE-06",
  "verdict": "PASSED",
  "why": "A genuine <100ms double-click (xdotool click --repeat 2 --delay 40) opened the editor; external overwrite produced the conflict banner over the editor content within 1.5s, unprompted.",
  "evidenceDiscriminates": true,
  "newEvidenceCell": "X11 lane: xdotool click --repeat 2 --delay 40 1 (true <100ms double-click) opened note.txt in FileView; typed to dirty it (12-dirty.png); externally overwrote via printf; within 1.5s, zero further clicks, conflict banner 'This file changed on disk.' with Reload/Keep appeared over editor content (13-conflict.png). Confirms already-correct.",
  "slice": "C-P4"
 },
 {
  "id": "F-CORE-FILE-03",
  "verdict": "FAILED \u2014 absent",
  "why": "Code reading shows render_file_row in right_panel.rs (owned by this slice) wires no .on_drag at all - only a test fixture proves the harness can express a payload drag. Live drive with the newly-added drag() helper confirms: dragging a Files-panel row onto a terminal pane inserts nothing, only a plain row-select fires.",
  "evidenceDiscriminates": true,
  "newEvidenceCell": "Code: grepped tiller_ui/tiller_terminal/tiller for .on_drag( outside tests - file rows (right_panel.rs render_file_row) wire only on_mouse_down; only PathBuf .on_drag is a test-only DragFixture, commented as harness-only, product wiring not yet built. Live: used Scripts/linux-drive.sh's new drag helper (e8f72a6, landed post-report) to drag README.md onto a Terminal pane - terminal received no text, only a row-select fired (14a/14-drag-result.png). Tooling gap now closed; feature genuinely absent, not just unexercised.",
  "slice": "C-P4"
 },
 {
  "id": "F-CORE-TERM-02",
  "verdict": "half-proven",
  "evidenceDiscriminates": false,
  "newEvidenceCell": "Critic re-ran shift_f10_opens_the_context_menu_from_the_keyboard (1 passed) and read it: real TerminalView with live PTY, real simulate_click focus, keystrokes driven through actual on_key_down dispatch, asserts terminal-context-item-0 drawn. Live chord instrument-blocked: wayland-drive.sh key action wraps wtype -k for one named key with no modifier composition.",
  "why": "",
  "slice": "C-P1"
 },
 {
  "id": "F-EDIT-12",
  "verdict": "NOT EXERCISED",
  "evidenceDiscriminates": false,
  "newEvidenceCell": "Critic re-ran a_drawn_change_row_drags_its_diff_payload_to_a_drop_target (1 passed); real ChangesTab over a real git repo, real MouseDown/Move/Up onto a DiffDropTargetFixture (stand-in only because tiller_ui cannot depend on tiller_terminal). No drag primitive of any kind exists in wayland-drive.sh, so un-driveable live on this lane.",
  "why": "",
  "slice": "C-P1"
 },
 {
  "id": "F-TERM-PTY-06",
  "verdict": "half-proven",
  "evidenceDiscriminates": false,
  "newEvidenceCell": "Critic re-ran a_drawn_terminal_accepts_a_real_external_paths_drop_with_several_files (1 passed) and confirmed no code changed. on_drop::<ExternalPaths> wired on the running-terminal branch; ENVIRONMENT.md records XDND as out of reach of the harness on any lane, broader than this Wayland lane.",
  "why": "",
  "slice": "C-P1"
 },
 {
  "id": "F-TERM-UI-02",
  "verdict": "NOT EXERCISED",
  "evidenceDiscriminates": false,
  "newEvidenceCell": "Critic re-ran platform_modifier_click_opens_a_terminal_link (1 passed) and read it: real PTY prints a URL, two real simulate_mouse_down at the link's actual position, Modifiers::none asserts no event and platform:true asserts the exact URL. wayland-drive.sh click has no modifier parameter.",
  "why": "",
  "slice": "C-P1"
 },
 {
  "id": "F-GIT-REMOTE-01",
  "verdict": "PASSED",
  "evidenceDiscriminates": true,
  "newEvidenceCell": "Critic drove it live end to end (labels cp1verify4/5): real repo with a github origin remote, ctl project.add, real gear-icon coordinate click, Avatar tab -- GitHub field pre-filled with the literal text octocat, not placeholder styling, contrasted against the still-placeholder Favicon field in the same frame. Clicked Use GitHub Avatar with zero typing and the panel updated to Current: GitHub avatar for octocat. Integrator had landed the requested sidebar.rs fix (fa1a330), confirmed at sidebar.rs:976.",
  "why": "",
  "slice": "C-P1"
 },
 {
  "id": "F-PRJ-06",
  "verdict": "half-proven",
  "evidenceDiscriminates": true,
  "newEvidenceCell": "Critic re-drove the real popover chrome (labels cp1verify7/8/9) rather than inheriting the prior claim, and narrowed it: text entry DOES land when the URL field is clicked first (destination auto-derived), but button clicks do not -- Cancel left the form open across two drives despite visible hover highlight, and Clone repository with a valid URL left status on Ready to clone. Guard itself proven by the_drawn_clone_button_cannot_start_a_second_clone via direct mount; popover submit-click delivery is the confirmed structural gap, in sidebar.rs which this slice does not own.",
  "why": "",
  "slice": "C-P1"
 },
 {
  "id": "F-PRJ-09",
  "verdict": "half-proven",
  "evidenceDiscriminates": false,
  "newEvidenceCell": "Critic re-ran the_drawn_create_button_cannot_start_a_second_creation (1 passed, direct-mounted CreateForm, two real create-submit clicks, exactly one Complete). Create popover not re-driven separately, but sidebar.rs render_project_form wraps Clone and Create in the identical absolute project-form-overlay whose submit-click delivery gap was just re-confirmed live for F-PRJ-06.",
  "why": "",
  "slice": "C-P1"
 }
]