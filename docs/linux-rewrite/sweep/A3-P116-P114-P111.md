# Adjudication slice A3-P116-P114-P111 — 12 rows already driven

Evidence source: `docs/linux-rewrite/P116-report.md`, `docs/linux-rewrite/P114-report.md`, `docs/linux-rewrite/P111-report.md`.

Every row below already has live drive evidence in that report. **No app launch is
needed for these** — read the report, open its captures, and judge. You did not
drive any of it and you did not build any of it; that is why you get to judge it.

| row | ledger line | current verdict | evidence recorded so far | source |
|---|---|---|---|---|
| `F-AGENT-OMP-01` | 469 | NOT EXERCISED | **P96 headless lane:** Tiller used the corrected `oh-my-pi --hook …` command, but the installed executable exited `code:1` before a session with `SyntaxError: Unexpected token ':'` at `function checkFile(path: string, label: string)`. Source still separates `id() == "omp"` from `executable_name() == "oh-my-pi"` and both builders use `oh-my-pi`; no live OMP session was reachable | P96 headless lane, 2026-08-14 |
| `F-AGENT-OMP-02` | 470 | NOT EXERCISED | **P96 headless lane:** the OMP process failed before startup, so no `session_start`, `turn_start`, `turn_end`, or `session_shutdown` hook events were observed. A follow-up status push also added no notification. The pre-existing stale `.tiller/omp-hook.ts` was not treated as generated evidence | P96 headless lane, 2026-08-14 |
| `F-AGENT-SESSION-01` | 474 | NOT EXERCISED | **subject-rule correction, 04:40** — the UNREACHABLE rested on "zero app callers", but this row's own VERIFY asks to *inspect the validator result*, which names the API, not a surface. App callers were never required, so this is not blocked on wiring: it is reachable and awaiting exercise. Demoting from PASSED was right; sending it to the build queue was not. Prior evidence: the pass-14 evidence stated "zero app c… | orchestrator subject-rule correction, 2026-08-14 |
| `F-AGENT-SESSION-02` | 475 | NOT EXERCISED | **subject-rule correction, 04:40** — the UNREACHABLE rested on "zero app callers", but this row's own VERIFY asks to *compare returned recent*, which names the API, not a surface. App callers were never required, so this is not blocked on wiring: it is reachable and awaiting exercise. Demoting from PASSED was right; sending it to the build queue was not. Prior evidence: same contradiction as `-01`. Re-swept 2026-0… | orchestrator subject-rule correction, 2026-08-14 |
| `F-CHAT-34` | 183 | NOT EXERCISED | RECENSUS Slice A PARTIAL: the persistence half exists: `Db::chat_sessions` returns `Vec<ChatSessionSummary>` "for the chat-history browser" (tiller_persistence/src/db.rs:523-548, model.rs:195-198, v12 migration migrations.rs:219-222) and `Db::delete_chat_session` db.rs:550 — but `delete_chat_session`'s only caller is a test (persistence_integration.rs:662) and `chat_sessions` has no production caller at all; the U… | P108 critic, 2026-08-14; RECENSUS + P106-report §Orchestrator correction |
| `F-CORE-DOM-07` | 375 | NOT EXERCISED | **subject-rule correction, 04:40** — the UNREACHABLE rested on "zero app callers", but this row's own VERIFY asks to *observing generated-name requests*, which names the API, not a surface. App callers were never required, so this is not blocked on wiring: it is reachable and awaiting exercise. Demoting from PASSED was right; sending it to the build queue was not. Prior evidence: "zero app callers — wiring owed" i… | orchestrator subject-rule correction, 2026-08-14 |
| `F-CORE-WSP-04` | 380 | NOT EXERCISED | RECENSUS Slice B PARTIAL: LayoutCommand at rust/crates/tiller_project/src/layout.rs:282 enumerates insert/split/move/close/activate/divider/view-state/rename, but classify_layout_command at :333 is test-only and no production executor applies commands. No source report exercised the clause’s user-visible/live result; census evidence cannot produce PASSED. | P108 critic, 2026-08-14; RECENSUS Slice B |
| `F-CORE-WSP-08` | 384 | NOT EXERCISED | RECENSUS Slice B PARTIAL: WorkspaceTabViewState at rust/crates/tiller_project/src/layout.rs:113 models editor/chat/terminal view state; production SessionTabState at tiller/src/session.rs:79 persists only pane events and scrollback. No source report exercised the clause’s user-visible/live result; census evidence cannot produce PASSED. | P108 critic, 2026-08-14; RECENSUS Slice B |
| `F-CTRL-CLI-02` | 451 | NOT EXERCISED | RECENSUS Slice B BUILT: resolve_tillerctl_path at rust/crates/tiller/src/main.rs:7785 resolves the XDG data destination, install_tillerctl at :7865 creates the symlink, and add_agent_tab passes it to hooks/commands at :4859. No source report exercised the clause’s user-visible/live result; census evidence cannot produce PASSED. | P108 critic, 2026-08-14; RECENSUS Slice B |
| `F-GIT-BRANCH-01` | 486 | NOT EXERCISED | **subject-rule correction, 04:40** — the UNREACHABLE rested on "zero app callers", but this row's own VERIFY asks to *list them through the Git layer*, which names the API, not a surface. App callers were never required, so this is not blocked on wiring: it is reachable and awaiting exercise. Demoting from PASSED was right; sending it to the build queue was not. Prior evidence: "zero app callers" is not a PASSED, … | orchestrator subject-rule correction, 2026-08-14 |
| `F-SET-10` | 298 | half-proven | UI half verified: drawn `refresh_now_re_runs_provider_discovery` + `settings_visibility_toggles_reach_the_usage_bar` + `usage_bar_consumes_visibility_and_interval_preferences` green (tiller_ui suite, pass 14); DB half absent — persisted schema lacks the keys (P58 handoff open) | pass 14 |
| `F-TERM-PTY-06` | 531 | NOT EXERCISED | P101 confirms the current target now calls `terminal_file_drop` from `TerminalView::receive_file_drop` and feeds bytes to the PTY without an Enter; that supersedes the old “zero callers” premise but is not a live drop. The required real-agent-shell drop, exact text/no implicit Enter check, and post-drop focus check could not be performed because the only `:1` official driver waited 900 s behind live `pid=657280 la… | P101 critic, 2026-08-14; static connection inspection; official-lock failure |
