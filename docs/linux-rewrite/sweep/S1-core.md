# Sweep slice S1-core — 22 rows to exercise

Families: F-CORE. Lane: wayland.

Every row below is `NOT EXERCISED` or `half-proven` in `INVENTORY-LEDGER.md`.
**Do not edit the ledger.** Produce evidence; a different agent adjudicates.
For a `half-proven` row the evidence column names the half already proven —
drive only the missing half and say which half you drove.

| row | ledger line | current verdict | evidence recorded so far | source |
|---|---|---|---|---|
| `F-CORE-ACT-02` | 338 | half-proven | sidebar half live (tab_status reads activity.status; FABLE-04 exoneration stands) and socket notify wired; the notification half is NO LONGER caller-less — as of 2026-08-14 main.rs:3892/3917 route transitions through should_notify→build_payload→notify-send (ACT-19/20 updated pass 17) — but that delivery has not been observed live, so the half stays unproven rather than absent | pass 17 |
| `F-CORE-ACT-06` | 342 | NOT EXERCISED | P115 replayed `panes::tests::process_owned_status_survives_title_and_child_exit_events` successfully in a fresh tree at `56977f2` (10/10 exact reruns). This removes the sole stated defective ground, but a passing unit test is not the required live gesture. Owed: identify one pane by title and another by process, replace each title with unrelated text, and observe that only title-owned state cle… | P115, 2026-08-14 |
| `F-CORE-ACT-07` | 343 | NOT EXERCISED | P115 replayed `panes::tests::real_pty_layer_a_debounce_suppresses_first_title_and_accepts_second` successfully in a fresh tree at `56977f2` (10/10 exact reruns). The ledger's unqualified name does not match with `--exact`; plain `cargo test -p tiller` supplied the qualified harness name. This removes the sole stated defective ground, but does not exercise the required live timing gesture. Owed:… | P115, 2026-08-14 |
| `F-CORE-ACT-11` | 347 | NOT EXERCISED | P115 replayed `panes::tests::process_owned_status_survives_title_and_child_exit_events` successfully in a fresh tree at `56977f2` (10/10 exact reruns). This removes the sole stated defective ground, but a passing unit test is not the required separate-pane lifecycle exercise. Owed: exercise title, process, and spawn detection on separate panes; alter or terminate one source at a time, then clos… | P115, 2026-08-14 |
| `F-CORE-ACT-19` | 355 | NOT EXERCISED | **P96's instrument reason is now stale in both halves.** (1) `notify-send` is no longer absent — `libnotify-bin` was installed 2026-08-14 and posts fine from the shell. (2) P96 read "it has no agent identity" as a property of a control-owned pane; it is not. `post_activity_notification` (`main.rs:3947`) returns early unless `activity.agent_id(pane)` is `Some`, and the backing map `pane_agents` … | orchestrator, code trace + live D-Bus, 2026-08-14 |
| `F-CORE-ACT-20` | 356 | NOT EXERCISED | same blocker as `F-CORE-ACT-19`, and the suppression gate is now read: `NotificationPolicy::should_notify` (`tiller_activity/src/notification.rs:19`) drops `new == Running` and `old == Some(new)`, then suppresses when `app_active && pane_visible` — with `app_active` hardcoded `true` at the call site, so **only a non-visible pane can ever notify**. Exercising this needs one spawned agent pane in… | orchestrator, code trace, 2026-08-14 |
| `F-CORE-ACT-24` | 360 | NOT EXERCISED | RECENSUS Slice B PARTIAL: AgentSessionRestorePlan::plan at rust/crates/tiller_activity/src/session.rs:52 distinguishes resumable/prunable stable content IDs, but its only callers are tests and the host persists raw pane-keyed refs. No source report exercised the clause’s user-visible/live result; census evidence cannot produce PASSED. | P108 critic, 2026-08-14; RECENSUS Slice B |
| `F-CORE-ACT-25` | 361 | NOT EXERCISED | RECENSUS Slice B PARTIAL: BootstrapRestoreOrder::partition at rust/crates/tiller_activity/src/bootstrap.rs:15 orders priority/deferred worktrees, but it is called only by activity_domain_integration tests and not launch restoration. No source report exercised the clause’s user-visible/live result; census evidence cannot produce PASSED. | P108 critic, 2026-08-14; RECENSUS Slice B |
| `F-CORE-ACT-26` | 362 | NOT EXERCISED | RECENSUS Slice B PARTIAL: WorktreeMountPolicy::ids_to_evict at rust/crates/tiller_activity/src/mount.rs:8 protects selected/active/unsaved worktrees, but only the integration test calls it and no mount eviction consumes it. No source report exercised the clause’s user-visible/live result; census evidence cannot produce PASSED. | P108 critic, 2026-08-14; RECENSUS Slice B |
| `F-CORE-AUTH-01` | 408 | NOT EXERCISED | **subject-rule correction, 04:40** — the UNREACHABLE rested on "zero app callers", but this row's own VERIFY asks to *inspect the parse*, which names the API, not a surface. App callers were never required, so this is not blocked on wiring: it is reachable and awaiting exercise. Demoting from PASSED was right; sending it to the build queue was not. Prior evidence: "zero app callers — wiring owe… | orchestrator subject-rule correction, 2026-08-14 |
| `F-CORE-DOM-03` | 371 | NOT EXERCISED | RECENSUS Slice B PARTIAL: default_project_base at rust/crates/tiller_project/src/domain.rs:6 implements TILLER_PROJECTS_DIR/XDG/HOME Linux policy, but its only callers are tests/export and production project creation does not use it. No source report exercised the clause’s user-visible/live result; census evidence cannot produce PASSED. | P108 critic, 2026-08-14; RECENSUS Slice B |
| `F-CORE-DOM-07` | 375 | NOT EXERCISED | **subject-rule correction, 04:40** — the UNREACHABLE rested on "zero app callers", but this row's own VERIFY asks to *observing generated-name requests*, which names the API, not a surface. App callers were never required, so this is not blocked on wiring: it is reachable and awaiting exercise. Demoting from PASSED was right; sending it to the build queue was not. Prior evidence: "zero app call… | orchestrator subject-rule correction, 2026-08-14 |
| `F-CORE-FILE-03` | 387 | NOT EXERCISED | P101 prepared real filenames with a space, apostrophe, and non-ASCII/double-quote under `/tmp/p101-fixture/drop-files`. Current code has `TerminalView::receive_file_drop` → `terminal_file_drop` → PTY input, but GPUI external drops are `ExternalPaths` while the terminal registers `PathBuf`; that static mismatch is not a replacement for the required real terminal drop. No file-manager/XDND gestur… | P101 critic, 2026-08-14; prepared fixture; official-lock failure |
| `F-CORE-FILE-06` | 391 | NOT EXERCISED | P101 independently traced the current `FileSystemEventMonitor` → `FileView::poll_file_system_events` → `check_external` connection, including Created/Modified/Removed/Renamed filtering. The clause requires live save, external modify/delete/rename both clean and dirty, and visible dirty/conflict/deleted states. None of those editor gestures ran: the official `:1` driver waited 900 s for live `pi… | P101 critic, 2026-08-14; static connection inspection only; official-lock failure |
| `F-CORE-FILE-08` | 393 | NOT EXERCISED | RECENSUS Slice B PARTIAL: file_glyph at rust/crates/tiller_ui/src/right_panel.rs:994 selects several extension/name glyphs and folder/file fallbacks; named directory icons, selected theme mapping, and SF fallback resolution are not integrated. No source report exercised the clause’s user-visible/live result; census evidence cannot produce PASSED. | P108 critic, 2026-08-14; RECENSUS Slice B |
| `F-CORE-SET-01` | 397 | NOT EXERCISED | RECENSUS Slice B PARTIAL: SettingsPolicy::from_values at rust/crates/tiller_project/src/settings.rs:37 clamps refresh and related Linux policy, with TILLER_SOCKET_ENABLE at :49; SettingsSnapshot at tiller_ui/src/settings.rs:330 omits persisted translucency/sidebar/right-panel widths and the host only applies the socket override. No source report exercised the clause’s user-visible/live result; … | P108 critic, 2026-08-14; RECENSUS Slice B |
| `F-CORE-TERM-02` | 395 | half-proven | upgraded from NOT EXERCISED without a new drive: `reference/linux-progress/p17-rclick-term.png` shows the real menu mounted on a live pane carrying **twelve** items — `Copy`, `Paste`, `Copy C…`, `Set Titl…`, `Copy P…`, `Copy T…`, all four `Split …`, `Clear T…`, `Close T…`. The action *set* is therefore confirmed live, which is more than the drawn test gave. **What remains** is the per-action ef… | orchestrator audit, existing frame applied, 2026-08-14 |
| `F-CORE-USG-05` | 403 | NOT EXERCISED | **subject-rule correction, 04:40** — the UNREACHABLE rested on "zero app callers", but this row's own VERIFY asks to *inspect load, refresh-needed*, which names the API, not a surface. App callers were never required, so this is not blocked on wiring: it is reachable and awaiting exercise. Demoting from PASSED was right; sending it to the build queue was not. Prior evidence: the row hedged that… | orchestrator subject-rule correction, 2026-08-14 |
| `F-CORE-USG-06` | 404 | NOT EXERCISED | RECENSUS Slice B BUILT: CodexUsageFetcher::fetch at rust/crates/tiller_usage/src/codex.rs:315 calls refresh_token at :240, posts TOKEN_URL at :27, and uses classify_token_refresh_failure at :69. No source report exercised the clause’s user-visible/live result; census evidence cannot produce PASSED. | P108 critic, 2026-08-14; RECENSUS Slice B |
| `F-CORE-USG-07` | 405 | NOT EXERCISED | RECENSUS Slice B BUILT: CodexUsageFetcher::fetch at rust/crates/tiller_usage/src/codex.rs:315 loads credentials, refreshes/retries bearer usage requests, parses windows, and maps logged-out/error outcomes; production caller is status_bar.rs:196. No source report exercised the clause’s user-visible/live result; census evidence cannot produce PASSED. | P108 critic, 2026-08-14; RECENSUS Slice B |
| `F-CORE-WSP-04` | 380 | NOT EXERCISED | RECENSUS Slice B PARTIAL: LayoutCommand at rust/crates/tiller_project/src/layout.rs:282 enumerates insert/split/move/close/activate/divider/view-state/rename, but classify_layout_command at :333 is test-only and no production executor applies commands. No source report exercised the clause’s user-visible/live result; census evidence cannot produce PASSED. | P108 critic, 2026-08-14; RECENSUS Slice B |
| `F-CORE-WSP-08` | 384 | NOT EXERCISED | RECENSUS Slice B PARTIAL: WorkspaceTabViewState at rust/crates/tiller_project/src/layout.rs:113 models editor/chat/terminal view state; production SessionTabState at tiller/src/session.rs:79 persists only pane events and scrollback. No source report exercised the clause’s user-visible/live result; census evidence cannot produce PASSED. | P108 critic, 2026-08-14; RECENSUS Slice B |
