# Sweep slice S6-changes — 12 rows to exercise

Families: F-CHG, F-GIT, F-PERSIST, F-CTRL. Lane: wayland.

Every row below is `NOT EXERCISED` or `half-proven` in `INVENTORY-LEDGER.md`.
**Do not edit the ledger.** Produce evidence; a different agent adjudicates.
For a `half-proven` row the evidence column names the half already proven —
drive only the missing half and say which half you drove.

| row | ledger line | current verdict | evidence recorded so far | source |
|---|---|---|---|---|
| `F-CHG-01` | 192 | half-proven | P106 fable §F-CHG-01: a live Changes tab opened and populated from the worktree, but the required right-panel Files/Changes switch is absent; titlebar toggle remains owed. | P108 critic, 2026-08-14; P106-report §F-CHG-01 |
| `F-CHG-06` | 197 | half-proven | pass 13: data half proven by socket transcript — `surface changes read` reports Staged/Changed/Untracked sections with correct per-file counts on a dirty fixture. Pixel half: three per-file status dots visible in the right-panel files tree (frame CHG-01). The symbol-per-status mapping and an explicit Files refresh were not exercised per-state, and no named drawn test covers the tree decorations… | pass 13 |
| `F-CHG-11` | 202 | half-proven | P104 §Group 6: Unstage all moved the staged file to Changed, confirmed by git status and delayed UI capture; Stage all and Discard all remain owed. | P108 critic, 2026-08-14; P104-report |
| `F-CHG-18` | 209 | NOT EXERCISED | P104 §Group 6 began a changed-row drag but released outside every target; the Diff chip appeared, yet no terminal/chat pane receipt was exercised. | P108 critic, 2026-08-14; P104-report |
| `F-CHG-20` | 211 | half-proven | P106 fable §F-CHG-20: live notify states showed running-count transitions (0/1/2 tab surfaces); No activity requires an unperformed Activity-header click. | P108 critic, 2026-08-14; P106-report §F-CHG-20 |
| `F-CHG-22` | 213 | half-proven | P104 §Group 5: live agent permission wait rendered ? needs-input and declined action returned idle; running, done and error status cases were not all exercised. | P108 critic, 2026-08-14; P104-report |
| `F-CTRL-CLI-02` | 451 | NOT EXERCISED | RECENSUS Slice B BUILT: resolve_tillerctl_path at rust/crates/tiller/src/main.rs:7785 resolves the XDG data destination, install_tillerctl at :7865 creates the symlink, and add_agent_tab passes it to hooks/commands at :4859. No source report exercised the clause’s user-visible/live result; census evidence cannot produce PASSED. | P108 critic, 2026-08-14; RECENSUS Slice B |
| `F-GIT-BRANCH-01` | 486 | NOT EXERCISED | **subject-rule correction, 04:40** — the UNREACHABLE rested on "zero app callers", but this row's own VERIFY asks to *list them through the Git layer*, which names the API, not a surface. App callers were never required, so this is not blocked on wiring: it is reachable and awaiting exercise. Demoting from PASSED was right; sending it to the build queue was not. Prior evidence: "zero app caller… | orchestrator subject-rule correction, 2026-08-14 |
| `F-GIT-REMOTE-01` | 489 | half-proven | genuinely half, unlike `F-CORE-ACT-22`/`-USG-05` whose hedges failed. Re-swept 2026-08-14: `GitRemote::project_name` is live with **8 app references** (called at `tiller_ui/src/project_forms.rs:704` to derive a project name from a clone URL, and that form was driven live at pass 17). `github_owner` (`lib.rs:62` — note the pass-12 note misnamed it `github_owner_from_url`) has **0 app references*… | orchestrator audit, grep evidence, 2026-08-14 |
| `F-GIT-RUN-02` | 484 | NOT EXERCISED | **subject-rule correction, 04:40** — the UNREACHABLE rested on "zero app callers", but this row's own VERIFY asks to *confirm progress arrives incrementally*, which names the API, not a surface. App callers were never required, so this is not blocked on wiring: it is reachable and awaiting exercise. Demoting from PASSED was right; sending it to the build queue was not. Prior evidence: "zero app… | orchestrator subject-rule correction, 2026-08-14 |
| `F-PERSIST-DB-06` | 509 | half-proven | the clause's subject is a conjunction — "**Agent account** *and* session records persist… VERIFY: …inspect restore planning *and* **account lookup**" — and only the session half is proven. Session refs: v5 upsert/load/delete + reopen replayed green (persistence_integration, pass 14); that half stands. The account half was never built: pass 14's own note, "agent-account rows deliberately absent … | orchestrator audit, 2026-08-14 |
| `F-PERSIST-DB-11` | 514 | half-proven | **the clause's harder conjunct has no test at all.** VERIFY asks to "*Open databases representing **earlier schema versions** and inspect that **each migration preserves data** and creates the expected current records*". The cited test does the second half only: it opens a **fresh** `TempDir` database, asserts `schema_version == CURRENT_SCHEMA_VERSION`, then checks the expected tables (`session… | orchestrator audit, 2026-08-14 |
