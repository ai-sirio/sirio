# Adjudication slice A2-P120 — 21 rows already driven

Evidence source: `docs/linux-rewrite/P120-report.md`.

Every row below already has live drive evidence in that report. **No app launch is
needed for these** — read the report, open its captures, and judge. You did not
drive any of it and you did not build any of it; that is why you get to judge it.

| row | ledger line | current verdict | evidence recorded so far | source |
|---|---|---|---|---|
| `F-AGENT-API-01` | 458 | NOT EXERCISED | RECENSUS Slice B PARTIAL: AgentAdapter at rust/crates/tiller_agents/src/lib.rs:107 exposes ID/display/hooks/prepare/launch/resume and ALL at :221 has the five adapters in order; no optional noninteractive summarizer method or command exists. No source report exercised the clause’s user-visible/live result; census evidence cannot produce PASSED. | P108 critic, 2026-08-14; RECENSUS Slice B |
| `F-AGENT-SAFE-02` | 473 | NOT EXERCISED | **subject-rule correction, 04:40** — the UNREACHABLE rested on "zero app callers", but this row's own VERIFY asks to *Run migration ... and diff every non-path field*, which names the API, not a surface. App callers were never required, so this is not blocked on wiring: it is reachable and awaiting exercise. Demoting from PASSED was right; sending it to the build queue was not. Prior evidence: the three migration … | orchestrator subject-rule correction, 2026-08-14 |
| `F-CORE-ACT-19` | 355 | NOT EXERCISED | **P96's instrument reason is now stale in both halves.** (1) `notify-send` is no longer absent — `libnotify-bin` was installed 2026-08-14 and posts fine from the shell. (2) P96 read "it has no agent identity" as a property of a control-owned pane; it is not. `post_activity_notification` (`main.rs:3947`) returns early unless `activity.agent_id(pane)` is `Some`, and the backing map `pane_agents` is written **only** … | orchestrator, code trace + live D-Bus, 2026-08-14 |
| `F-CORE-ACT-20` | 356 | NOT EXERCISED | same blocker as `F-CORE-ACT-19`, and the suppression gate is now read: `NotificationPolicy::should_notify` (`tiller_activity/src/notification.rs:19`) drops `new == Running` and `old == Some(new)`, then suppresses when `app_active && pane_visible` — with `app_active` hardcoded `true` at the call site, so **only a non-visible pane can ever notify**. Exercising this needs one spawned agent pane in a background tab; u… | orchestrator, code trace, 2026-08-14 |
| `F-CORE-ACT-24` | 360 | NOT EXERCISED | RECENSUS Slice B PARTIAL: AgentSessionRestorePlan::plan at rust/crates/tiller_activity/src/session.rs:52 distinguishes resumable/prunable stable content IDs, but its only callers are tests and the host persists raw pane-keyed refs. No source report exercised the clause’s user-visible/live result; census evidence cannot produce PASSED. | P108 critic, 2026-08-14; RECENSUS Slice B |
| `F-CORE-ACT-25` | 361 | NOT EXERCISED | RECENSUS Slice B PARTIAL: BootstrapRestoreOrder::partition at rust/crates/tiller_activity/src/bootstrap.rs:15 orders priority/deferred worktrees, but it is called only by activity_domain_integration tests and not launch restoration. No source report exercised the clause’s user-visible/live result; census evidence cannot produce PASSED. | P108 critic, 2026-08-14; RECENSUS Slice B |
| `F-CORE-ACT-26` | 362 | NOT EXERCISED | RECENSUS Slice B PARTIAL: WorktreeMountPolicy::ids_to_evict at rust/crates/tiller_activity/src/mount.rs:8 protects selected/active/unsaved worktrees, but only the integration test calls it and no mount eviction consumes it. No source report exercised the clause’s user-visible/live result; census evidence cannot produce PASSED. | P108 critic, 2026-08-14; RECENSUS Slice B |
| `F-CORE-AUTH-01` | 408 | NOT EXERCISED | **subject-rule correction, 04:40** — the UNREACHABLE rested on "zero app callers", but this row's own VERIFY asks to *inspect the parse*, which names the API, not a surface. App callers were never required, so this is not blocked on wiring: it is reachable and awaiting exercise. Demoting from PASSED was right; sending it to the build queue was not. Prior evidence: "zero app callers — wiring owed" is not a PASSED. … | orchestrator subject-rule correction, 2026-08-14 |
| `F-CORE-DOM-03` | 371 | NOT EXERCISED | RECENSUS Slice B PARTIAL: default_project_base at rust/crates/tiller_project/src/domain.rs:6 implements TILLER_PROJECTS_DIR/XDG/HOME Linux policy, but its only callers are tests/export and production project creation does not use it. No source report exercised the clause’s user-visible/live result; census evidence cannot produce PASSED. | P108 critic, 2026-08-14; RECENSUS Slice B |
| `F-CORE-FILE-03` | 387 | NOT EXERCISED | P101 prepared real filenames with a space, apostrophe, and non-ASCII/double-quote under `/tmp/p101-fixture/drop-files`. Current code has `TerminalView::receive_file_drop` → `terminal_file_drop` → PTY input, but GPUI external drops are `ExternalPaths` while the terminal registers `PathBuf`; that static mismatch is not a replacement for the required real terminal drop. No file-manager/XDND gesture could be driven: t… | P101 critic, 2026-08-14; prepared fixture; official-lock failure |
| `F-CORE-FILE-06` | 391 | NOT EXERCISED | P101 independently traced the current `FileSystemEventMonitor` → `FileView::poll_file_system_events` → `check_external` connection, including Created/Modified/Removed/Renamed filtering. The clause requires live save, external modify/delete/rename both clean and dirty, and visible dirty/conflict/deleted states. None of those editor gestures ran: the official `:1` driver waited 900 s for live `pid=657280 label=sonne… | P101 critic, 2026-08-14; static connection inspection only; official-lock failure |
| `F-CORE-FILE-08` | 393 | NOT EXERCISED | RECENSUS Slice B PARTIAL: file_glyph at rust/crates/tiller_ui/src/right_panel.rs:994 selects several extension/name glyphs and folder/file fallbacks; named directory icons, selected theme mapping, and SF fallback resolution are not integrated. No source report exercised the clause’s user-visible/live result; census evidence cannot produce PASSED. | P108 critic, 2026-08-14; RECENSUS Slice B |
| `F-CORE-SET-01` | 397 | NOT EXERCISED | RECENSUS Slice B PARTIAL: SettingsPolicy::from_values at rust/crates/tiller_project/src/settings.rs:37 clamps refresh and related Linux policy, with TILLER_SOCKET_ENABLE at :49; SettingsSnapshot at tiller_ui/src/settings.rs:330 omits persisted translucency/sidebar/right-panel widths and the host only applies the socket override. No source report exercised the clause’s user-visible/live result; census evidence cann… | P108 critic, 2026-08-14; RECENSUS Slice B |
| `F-CORE-USG-05` | 403 | NOT EXERCISED | **subject-rule correction, 04:40** — the UNREACHABLE rested on "zero app callers", but this row's own VERIFY asks to *inspect load, refresh-needed*, which names the API, not a surface. App callers were never required, so this is not blocked on wiring: it is reachable and awaiting exercise. Demoting from PASSED was right; sending it to the build queue was not. Prior evidence: the row hedged that only the refresh ha… | orchestrator subject-rule correction, 2026-08-14 |
| `F-CORE-USG-06` | 404 | NOT EXERCISED | RECENSUS Slice B BUILT: CodexUsageFetcher::fetch at rust/crates/tiller_usage/src/codex.rs:315 calls refresh_token at :240, posts TOKEN_URL at :27, and uses classify_token_refresh_failure at :69. No source report exercised the clause’s user-visible/live result; census evidence cannot produce PASSED. | P108 critic, 2026-08-14; RECENSUS Slice B |
| `F-CORE-USG-07` | 405 | NOT EXERCISED | RECENSUS Slice B BUILT: CodexUsageFetcher::fetch at rust/crates/tiller_usage/src/codex.rs:315 loads credentials, refreshes/retries bearer usage requests, parses windows, and maps logged-out/error outcomes; production caller is status_bar.rs:196. No source report exercised the clause’s user-visible/live result; census evidence cannot produce PASSED. | P108 critic, 2026-08-14; RECENSUS Slice B |
| `F-GIT-RUN-02` | 484 | NOT EXERCISED | **subject-rule correction, 04:40** — the UNREACHABLE rested on "zero app callers", but this row's own VERIFY asks to *confirm progress arrives incrementally*, which names the API, not a surface. App callers were never required, so this is not blocked on wiring: it is reachable and awaiting exercise. Demoting from PASSED was right; sending it to the build queue was not. Prior evidence: "zero app callers" is not a P… | orchestrator subject-rule correction, 2026-08-14 |
| `F-TERM-UI-02` | 536 | NOT EXERCISED | P101 independently found the current subscriber in `main.rs` (`TerminalLinkEvent` → `terminal_link_url_for_pane` → `cx.open_url`), so the old “zero subscribers” verdict is stale. I prepared a PATH-local `xdg-open` recorder for two distinct URLs, but could not perform either required Super/platform-click: the official `:1` driver waited 900 s behind live `pid=657280 label=sonnet` and timed out. No URL was opened in… | P101 critic, 2026-08-14; static subscriber inspection; official-lock failure |
| `F-USE-01` | 264 | NOT EXERCISED | RECENSUS Slice B PARTIAL: StatusBar::render at rust/crates/tiller_ui/src/status_bar.rs:262 renders the settings control at :326; on_refresh exists at :148 but no refresh button is rendered or wired. No source report exercised the clause’s user-visible/live result; census evidence cannot produce PASSED. | P108 critic, 2026-08-14; RECENSUS Slice B |
| `F-USE-02` | 265 | NOT EXERCISED | RECENSUS Slice B PARTIAL: StatusBar::render at rust/crates/tiller_ui/src/status_bar.rs:323 renders enabled provider segments and segment_text at :216 renders unavailable reasons; no unavailable tooltip is attached. No source report exercised the clause’s user-visible/live result; census evidence cannot produce PASSED. | P108 critic, 2026-08-14; RECENSUS Slice B |
| `F-USE-03` | 266 | NOT EXERCISED | RECENSUS Slice B BUILT: StatusBar::segment_text at rust/crates/tiller_ui/src/status_bar.rs:216 renders loading, loaded/stale values, logged-out/not-found/timed-out/error states, with dimming at :255. No source report exercised the clause’s user-visible/live result; census evidence cannot produce PASSED. | P108 critic, 2026-08-14; RECENSUS Slice B |
