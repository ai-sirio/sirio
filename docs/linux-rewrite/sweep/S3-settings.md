# Sweep slice S3-settings — 16 rows to exercise

Families: F-SET, F-USE. Lane: wayland.

Every row below is `NOT EXERCISED` or `half-proven` in `INVENTORY-LEDGER.md`.
**Do not edit the ledger.** Produce evidence; a different agent adjudicates.
For a `half-proven` row the evidence column names the half already proven —
drive only the missing half and say which half you drove.

| row | ledger line | current verdict | evidence recorded so far | source |
|---|---|---|---|---|
| `F-SET-04` | 292 | half-proven | UI half verified: drawn `general_surface_settings_flow_into_the_persistence_contract` green (tiller_ui suite, pass 14); DB half still absent — the persisted schema does not carry the surface-settings keys yet (main.rs test comment: loaded snapshots start at defaults; P58 handoff to codex11 open) | pass 14 |
| `F-SET-05` | 293 | half-proven | UI half verified: drawn `general_surface_settings_flow_into_the_persistence_contract` + `summarizer_picker_is_gated_on_auto_naming_and_selects` green (tiller_ui suite, pass 14); DB half absent — persisted schema lacks the key (P58 handoff open); rename behavior still has no consumer | pass 14 |
| `F-SET-06` | 294 | half-proven | UI half verified: drawn `general_surface_settings_flow_into_the_persistence_contract` green (tiller_ui suite, pass 14); DB half absent — persisted schema lacks the keys (P58 handoff open); trimming still has no consumer | pass 14 |
| `F-SET-07` | 295 | half-proven | UI half verified: drawn `general_surface_settings_flow_into_the_persistence_contract` green (tiller_ui suite, pass 14); DB half absent — persisted schema lacks the keys (P58 handoff open); eviction stays absent (ACT-26 is its own row) | pass 14 |
| `F-SET-09` | 297 | NOT EXERCISED | P101 independently found the current button handler calls `on_install_skill(agent_skill_install_command())`, and `main.rs` maps it to a terminal shell; headless `surface settings open --section general` also mounted General. Those are not feedback from clicking Install Skill. The required click and success/error/control-state observation could not run because the `:1` official-lock attempt wait… | P101 critic, 2026-08-14; `/tmp/p101h.sock`; official-lock failure |
| `F-SET-10` | 298 | half-proven | UI half verified: drawn `refresh_now_re_runs_provider_discovery` + `settings_visibility_toggles_reach_the_usage_bar` + `usage_bar_consumes_visibility_and_interval_preferences` green (tiller_ui suite, pass 14); DB half absent — persisted schema lacks the keys (P58 handoff open) | pass 14 |
| `F-SET-11` | 299 | NOT EXERCISED | RECENSUS Slice A BUILT: all seven states now render distinctly: `StatusBar::segment_text` tiller_ui/src/status_bar.rs:216 — Loading "…" :218, Loaded percentages :219-235, Stale = same values dimmed (`segment_dimmed` :257), and the four `Unavailable` reasons render "not found"/"logged out"/"timed out"/"error" :243-251 (comment names F-SET-11 and the old `_`-wildcard bug); production callers :337… | P108 critic, 2026-08-14; RECENSUS Slice A |
| `F-SET-15` | 303 | half-proven | P106 codex12 §F-SET-15: rendered System default/This device/Active state was observed, but no second account or selection control exists to move the active badge. | P108 critic, 2026-08-14; P106-report §F-SET-15 |
| `F-SET-16` | 304 | NOT EXERCISED | RECENSUS Slice A PARTIAL: search + refresh conjuncts now real (pass-12's dead chrome is stale): focusable `agent-search-field` settings.rs:1919-1957 with raw-key handler `on_agent_search_key` :1207-1220 filtering rows by name/description :1836-1846 (render comment names F-SET-16), "↻ Refresh" button :1959-1967 → `refresh_agent_availability` :1199-1203 re-running `discover_availability()`; the "… | P108 critic, 2026-08-14; RECENSUS Slice A |
| `F-SET-18` | 306 | half-proven | P106 codex12 §F-SET-18: reduced PATH live-rendered not-found and normal PATH rendered available/ACP states; Install/progress/Update/Retry controls were absent. | P108 critic, 2026-08-14; P106-report §F-SET-18 |
| `F-SET-19` | 307 | half-proven | pass 17 closes the pixel half for Light end-to-end through the user's own path: clicking the Light segment relit the entire surface live (p17-ae1), the DB then held `theme=light`, and a relaunch painted LIGHT at the FIRST frame with the settings page showing Light selected (p17-ag0-startup/ag1). Dark was the ambient state of every earlier frame (default). Still unexercised: choosing System and … | pass 17 |
| `F-SET-21` | 309 | NOT EXERCISED | RECENSUS Slice A PARTIAL: the chooser mechanism exists — segmented control wired at settings.rs:1448 over `file_icon_choices()` :162-168 with apply at :1028 — but on Linux there is exactly one option (`SEGMENTED_FILE_ICONS = ["Material"]` :42, the two-choice list :40 is macOS-only cfg), so the clause's "choose each option and confirm icons change" has nothing to choose between; unchanged since … | P108 critic, 2026-08-14; RECENSUS Slice A |
| `F-SET-24` | 312 | NOT EXERCISED | RECENSUS Slice A BUILT: the browser-origin permission surface exists end-to-end (P86 Part 4 landed; the orchestrator-probe evidence is stale): Permissions page render `render_permissions` dispatched settings.rs:2590, "Granted browser origins" card :2390-2395, empty state "No browser origins have been granted." (`settings-browser-grants-empty` :2396-2409), per-origin Revoke buttons :2419-2434 → … | P108 critic, 2026-08-14; RECENSUS Slice A |
| `F-USE-01` | 264 | NOT EXERCISED | RECENSUS Slice B PARTIAL: StatusBar::render at rust/crates/tiller_ui/src/status_bar.rs:262 renders the settings control at :326; on_refresh exists at :148 but no refresh button is rendered or wired. No source report exercised the clause’s user-visible/live result; census evidence cannot produce PASSED. | P108 critic, 2026-08-14; RECENSUS Slice B |
| `F-USE-02` | 265 | NOT EXERCISED | RECENSUS Slice B PARTIAL: StatusBar::render at rust/crates/tiller_ui/src/status_bar.rs:323 renders enabled provider segments and segment_text at :216 renders unavailable reasons; no unavailable tooltip is attached. No source report exercised the clause’s user-visible/live result; census evidence cannot produce PASSED. | P108 critic, 2026-08-14; RECENSUS Slice B |
| `F-USE-03` | 266 | NOT EXERCISED | RECENSUS Slice B BUILT: StatusBar::segment_text at rust/crates/tiller_ui/src/status_bar.rs:216 renders loading, loaded/stale values, logged-out/not-found/timed-out/error states, with dimming at :255. No source report exercised the clause’s user-visible/live result; census evidence cannot produce PASSED. | P108 critic, 2026-08-14; RECENSUS Slice B |
