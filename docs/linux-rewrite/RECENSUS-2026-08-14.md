# RECENSUS 2026-08-14 — re-census of the `FAILED — absent` bucket (P105)

Three-state census of the 86 `FAILED — absent` rows. **This is a census, not a verdict
sheet** — a row found BUILT here becomes `NOT EXERCISED` only when a critic exercises it.
`INVENTORY-LEDGER.md` is untouched by this pass.

States: **BUILT** (symbol + file:line, with at least one production call site — a test-only
caller is PARTIAL) · **ABSENT** (the needle that found nothing, quoted verbatim, run over all
of `rust/crates/`) · **PARTIAL** (the clause's conjuncts disagree — which half exists, which
does not). All paths below are relative to `rust/crates/`.

## Slice A (fable) — 30 rows

| row | state | evidence | needle |
|---|---|---|---|
| F-PRJ-03 | PARTIAL | initialize-git exists as a post-add action, not the clause's browse-time prompt: "Initialize Git repository" context-menu item at tiller_ui/src/sidebar.rs:676 (`SidebarContextAction::InitializeGit`, enabled only when !is_git), same action in the project-settings sheet at sidebar.rs:1971 and command palette tiller/src/command_palette.rs:414, backed by `init_repository` tiller_git/src/worktree.rs:128; add-without-git is the unconditional silent default (non-git projects are first-class, tiller_project/src/project.rs:9); the three-choice prompt (Initialize / Add without Git / Cancel) at browse time does not exist | `grep -rniE "initialize git|add without|without git|non.?git" rust/crates/ --include="*.rs"` — no prompt hit |
| F-PRJ-04 | BUILT | insertion errors now surface in the sidebar notice, not eprintln: `add_project` routes `Err(error)` and the duplicate case ("already tracked or nested: …") to `sidebar.set_notice` at tiller/src/main.rs:3226-3235; picker-open failure sets "could not open the folder picker: {error}" at tiller_ui/src/sidebar.rs:1002; notice rendered as `sidebar-notice` at sidebar.rs:2522-2531; production caller `SidebarEvent::AddProject` → `add_project` main.rs:2772; drawn test main.rs:9430 on top of the production route | `grep -n "\[projects\]" rust/crates/tiller/src/*.rs` — zero hits, the eprintln-only path is gone |
| F-PRJ-07 | BUILT | full clone-failure + retry surface: `CloneStatus::Failed` tiller_ui/src/project_forms.rs:24, `fail()` :93, error rendered "Clone failed: {error}" :722 (`clone_status_line`), submit button relabels "Retry clone" :287, `can_submit` deliberately true after failure :70-77 ("the same form is the retry surface"); production route: `+` menu → `start_clone_project` tiller_ui/src/sidebar.rs:1019, clicked at :1773 | `grep -n "CloneStatus::Failed\|Retry clone" rust/crates/tiller_ui/src/project_forms.rs` |
| F-PRJ-10 | BUILT | create-project failure surface: `CreateStatus::Failed` tiller_ui/src/project_forms.rs:410, `fail()` :461, rendered "Creation failed: {error}" :734 (`create_status_line`), button relabels "Retry creation" :596; production route: `+` menu → `start_create_project` tiller_ui/src/sidebar.rs:1037, clicked at :1782 | `grep -n "CreateStatus::Failed\|Retry creation" rust/crates/tiller_ui/src/project_forms.rs` |
| F-PRJ-11 | PARTIAL | delete-without-touching-disk exists, but not from Project Settings: context-menu "Remove Project" tiller_ui/src/sidebar.rs:687-692 with confirmation (`request_remove_project` :1055, F-SID-10 drawn test), removal is catalog-only (`remove_project` → `project_catalog.remove` tiller/src/main.rs:3261-3268, never disk); the sheet itself (`render_project_settings` sidebar.rs:1865-2020) has no trash/delete control — heading, path, repo line, display-name field, Initialize Git, icon picker, Close, id only | read of the full sheet render sidebar.rs:1865-2020 — no remove control present |
| F-PRJ-12 | PARTIAL | display-name edit exists end-to-end: editable field in the sheet sidebar.rs:1924-1960 (`project-display-name-field`, key handler `on_display_name_key` :852), emits `ProjectSettingsUpdate` :818, applied by `update_project_settings` tiller/src/main.rs:3239-3258, sidebar title updated at sidebar.rs:793-816; repository-type has only a one-way Folder→Git transition ("Initialize Git" button in the sheet :1962-1985) — the clause's Repository-Type switch is still the read-only text "Repository: Git/Folder" :1913-1920 | `grep -n "display_name\|Repository:" rust/crates/tiller_ui/src/sidebar.rs` |
| F-PRJ-17 | PARTIAL | a `default_worktree_base` field exists but is dormant: persisted (tiller_persistence/src/model.rs:28, migrations.rs:41, db.rs:1174) and modeled (tiller_project/src/project.rs:35) with **zero consumers** — nothing reads it and no UI sets it; the create-worktree flow always derives the parent (`derive_worktree_path` tiller_git/src/worktree.rs:163, `new_worktree_path` tiller/src/main.rs:3811); none of the four options (current/pinned/primary/no-primary) exist as controls | `grep -rn "default_worktree_base" rust/crates/ --include="*.rs"` — model/persistence rows only, no consumer |
| F-PRJ-18 | PARTIAL | `worktree_location_override` is persisted schema only (tiller_persistence/src/model.rs:29, migrations.rs:42, db.rs:1175) with zero consumers outside persistence; no chooser UI, no default-parent restore control anywhere in tiller_ui or tiller; the location is always derived | `grep -rn "worktree_location_override" rust/crates/ --include="*.rs"` — persistence only |
| F-CHAT-02 | PARTIAL | the Retry half exists, the authentication half does not: `Entry::Error{retryable, kind}` tiller_ui/src/chat.rs:196-199, retryable connection errors pushed at :1205/:1221/:1706, retry machinery `retry_pending_send` :690 → `start_connection` :2018 (drawn retry tests green, pass 12); `ErrorKind` has exactly one variant `Connection` (:399-401) — no Auth variant, no auth-required banner, no CLI guidance | `grep -niE "auth" rust/crates/tiller_ui/src/chat.rs rust/crates/tiller_acp/src/*.rs` → 1 hit, a test fixture's `"authMethods":[]` (chat.rs:6953) |
| F-CHAT-16 | PARTIAL | open + select conjuncts exist: `toggle_model_picker` chat.rs:1423 (pill-wired :3609), popover `model-picker` :3658, `select_model` :1449 clicked at :3700, empty state "The connected agent did not report any models." :3683; search input, no-match state and Recommended labels do not exist — the popover is a flat `available_models` list and `ModelOption` is {id, name, description} with no recommended flag (tiller_acp/src/lib.rs:142-149) | `grep -niE "recommended\|no match\|search" rust/crates/tiller_ui/src/chat.rs` → only slash-popup test ids (:7022, :7031) |
| F-CHAT-18 | PARTIAL | popover exists with percent/tokens/cost: `toggle_context_popover` chat.rs:1436, "{percent}% of context used" :3910, "{used} / {size} tokens" :3917, optional "Cost: {amount} {currency}" :3899-3901, empty state :3934, escape-dismiss :1990; the clause's input/output/cache breakdown rows do not exist and cannot — `ContextUsage` is {used, size, cost} only (tiller_acp/src/lib.rs:164-171) | `grep -n "cache" rust/crates/tiller_ui/src/chat.rs` → no popover breakdown hit |
| F-CHAT-21 | BUILT | `Entry::Thought{expanded}` with `toggle_thought_expanded` tiller_ui/src/chat.rs:865, click-wired :3006, drawn test :6535 (settled in P105 brief, spot-check 2026-08-14) | settled in brief — no needle spent |
| F-CHAT-22 | BUILT | `toggle_tool_call_group_expanded` tiller_ui/src/chat.rs:885 (`group_expanded` on `Entry::ToolCall`), click-wired :3468, drawn test :6806 (settled in P105 brief, spot-check 2026-08-14) | settled in brief — no needle spent |
| F-CHAT-23 | PARTIAL | expand + output-inspect conjuncts BUILT: `Entry::ToolCall{content, locations, raw_input, raw_output, expanded, group_expanded}` chat.rs:133-171, `toggle_tool_call_expanded` :874, click-wired :3367, drawn test :6719; the Dismiss-unrenderable-permission-call conjunct is ABSENT — the only `dismiss` in chat.rs is the slash popup's (:709-1754) | settled in brief (`grep -n dismiss chat.rs` — slash popup only) |
| F-CHAT-28 | ABSENT | `grep -niE "subagent\|task_card\|TaskCard" rust/crates/tiller_ui/src/chat.rs` → zero hits (settled in brief; assigned to pi as P102) | — |
| F-CHAT-29 | ABSENT | `grep -niE "copy" rust/crates/tiller_ui/src/chat.rs` → only the transcript-wide `CopyTranscript` action (:58, bindings :911-912, `copy_transcript` :1390) — no per-message hover control, no confirmation checkmark; same needle over `rust/crates/tiller_markdown/src/` → derive(Copy) + the copied-text model fn only (matches pass-17 live finding) | — |
| F-CHAT-30 | ABSENT | `grep -rniE "copy" rust/crates/tiller_ui/src/chat.rs rust/crates/tiller_markdown/src/` → no code-block Copy control anywhere — only `CopyTranscript` and tiller_markdown/src/model.rs:11 (a copied-text model fn with no render control attached); matches pass-17 live paste-check | — |
| F-CHAT-31 | PARTIAL | the diff-preview half exists: `render_tool_diff` chat.rs:2864 (comment names F-CHAT-31), `DiffLine::Added/Removed` colored rows :2888-2900, path header :2884, cap + "… N more lines" :2915-2923, rendered from `ToolCallContentInfo::Diff` :3391-3393; the open-a-file half does not — location chips render as inert text with no on_click (:3397-3412), and additions/removals are unnumbered, unselectable (`diff_preview_lines` emits prefix+text only) | `grep -n "on_click"` over chat.rs:3397-3412 region — none on locations |
| F-CHAT-32 | ABSENT | `grep -rniE "revert\|edit.?summar" rust/crates/ --include="*.rs"` → sole production hit is an unrelated optimistic-UI comment (chat.rs:1651, plan-approval rejection); no edit-summary card, no Open file / Revert actions, no reverted/error states. (`Entry::ToolCall` doc :149 name-drops F-CHAT-32 for its diff/raw surface, but no summary card exists) | — |
| F-CHAT-34 | PARTIAL | the persistence half exists: `Db::chat_sessions` returns `Vec<ChatSessionSummary>` "for the chat-history browser" (tiller_persistence/src/db.rs:523-548, model.rs:195-198, v12 migration migrations.rs:219-222) and `Db::delete_chat_session` db.rs:550 — but `delete_chat_session`'s only caller is a test (persistence_integration.rs:662) and `chat_sessions` has no production caller at all; the UI half is resume-most-recent only — "Resume Chat" palette/menu entries (command_palette.rs:384-391, main.rs:5934) → `handle_resume_chat` main.rs:6711 resumes `retained_chats.first()`; no browsable session list, no open-arbitrary, no delete-with-confirmation | `grep -rn "Chat History" rust/crates/ --include="*.rs"` → zero hits |
| F-CHAT-35 | ABSENT | `grep -rniE "no past" rust/crates/ --include="*.rs"` → zero hits; there is no history list surface to carry an empty state (see F-CHAT-34) — the nearest behavior is the palette's Resume Chat entry gating on `has_retained_chat` (main.rs:6742), which hides rather than explains | — |

## Slice B (codex12) — 40 rows

| row | state | evidence | needle |
|---|---|---|---|
| F-WIN-01 | BUILT | TillerWorkspace::open_settings at rust/crates/tiller/src/main.rs:4930 — switches into the Settings route, while render keeps the main workspace route | "open_settings" |
| F-WIN-07 | PARTIAL | restore_launch_snapshot at rust/crates/tiller/src/main.rs:3921 and ControlAction::RestoreSession at :2669 restore the saved snapshot; the History menu and ⇧⌘O entry do not exist | "restore-session" |
| F-WIN-10 | PARTIAL | Sidebar::set_notice at rust/crates/tiller_ui/src/sidebar.rs:942 and sidebar-notice at :2522 render operation errors; no transient informational toast with auto-dismiss exists | "sidebar-notice" |
| F-SID-06 | PARTIAL | Sidebar::set_worktree_status at rust/crates/tiller_ui/src/sidebar.rs:1166 and status_dot_color at :2056 render worktree activity; project descendant badges are absent | "status_dot_color" |
| F-SID-11 | PARTIAL | SidebarRow at rust/crates/tiller_ui/src/sidebar.rs:96 and render_row at :2024 show branch/title, path, Primary, and agent status; no worktree comment field is present | "agent_status" |
| F-SID-15 | PARTIAL | remove_worktree_row at rust/crates/tiller_ui/src/sidebar.rs:1438 is wired to the hover close control at :2285; context_menu_items at :660 has no Remove Worktree item | "Remove Worktree" |
| F-SID-16 | BUILT | Sidebar::row_drag at rust/crates/tiller_ui/src/sidebar.rs:527 — production project-row drag payloads use ReorderScope::Projects and commit through main.rs:3269 | "row_drag" |
| F-SID-17 | BUILT | Sidebar::row_drag at rust/crates/tiller_ui/src/sidebar.rs:527 — worktree drag scopes and project-group checks reach production reorder_sidebar at main.rs:3281 | "reorder_worktrees" |
| F-SID-18 | ABSENT | "no terminals" | — |
| F-TAB-01 | BUILT | TillerWorkspace::render_open_tab at rust/crates/tiller/src/main.rs:5565 — production tab rows render icon, status, close control, and dirty indicator | "render_open_tab" |
| F-TAB-11 | PARTIAL | split_disabled_reason at rust/crates/tiller/src/panes.rs:218 models sole-tab/too-small reasons but its callers are tests only; TerminalContextItem at context_menu.rs:30 has no disabled-reason wiring | "split_disabled_reason" |
| F-TAB-18 | BUILT | TillerWorkspace::render_open_tab at rust/crates/tiller/src/main.rs:5565 — production .on_drag at :5629 previews tab reordering through preview_tab_reorder at :5634 | "preview_tab_reorder" |
| F-TAB-23 | BUILT | TerminalContextAction::SplitLeft at rust/crates/tiller_terminal/src/context_menu.rs:15 and label at :68 — delegated split commands are wired in tiller/src/main.rs:2463 | "SplitLeft" |
| F-TAB-24 | PARTIAL | tab drag is production-wired by render_open_tab at rust/crates/tiller/src/main.rs:5629; no Escape cancellation handler or drag-cancel state exists | "tab.*drag.*Escape" |
| F-TAB-25 | ABSENT | "Attach to current terminal" | — |
| F-TAB-28 | BUILT | TillerWorkspace::new at rust/crates/tiller/src/main.rs:2581 binds cmd-w/ctrl-w and handle_close_tab at :6641 closes the active/menu tab | "cmd-w" |
| F-CHG-01 | PARTIAL | RightPanel::render_header at rust/crates/tiller_ui/src/right_panel.rs:603 renders Files, and TabContent::Changes is a production surface at tiller/src/main.rs:5388; no Files/Changes switch exists inside the right panel | "right-panel-changes" |
| F-CHG-02 | ABSENT | "No worktree selected" | — |
| F-CHG-20 | BUILT | RightPanel::render_activity at rust/crates/tiller_ui/src/right_panel.rs:758 — production Activity renders No activity and the running-count element at :810 | "activity-running-count" |
| F-PER-07 | PARTIAL | update_project_settings at rust/crates/tiller/src/main.rs:3239 persists display name/icon through schedule_catalog; default_worktree_base/worktree_location_override have persistence fields but no project-settings UI/update door | "default_worktree_base" |
| F-BRW-08 | BUILT | Settings::render_browser_grants at rust/crates/tiller_ui/src/settings.rs:2361 — production Revoke and Revoke all controls call the per-origin/all grant callbacks | "Revoke all" |
| F-USE-01 | PARTIAL | StatusBar::render at rust/crates/tiller_ui/src/status_bar.rs:262 renders the settings control at :326; on_refresh exists at :148 but no refresh button is rendered or wired | "on_refresh" |
| F-USE-02 | PARTIAL | StatusBar::render at rust/crates/tiller_ui/src/status_bar.rs:323 renders enabled provider segments and segment_text at :216 renders unavailable reasons; no unavailable tooltip is attached | "provider_segment" |
| F-USE-03 | BUILT | StatusBar::segment_text at rust/crates/tiller_ui/src/status_bar.rs:216 renders loading, loaded/stale values, logged-out/not-found/timed-out/error states, with dimming at :255 | "UsageReason::LoggedOut" |
| F-TERM-11 | ABSENT | "No worktree selected" | — |
| F-CORE-ACT-24 | PARTIAL | AgentSessionRestorePlan::plan at rust/crates/tiller_activity/src/session.rs:52 distinguishes resumable/prunable stable content IDs, but its only callers are tests and the host persists raw pane-keyed refs | "AgentSessionRestorePlan::plan" |
| F-CORE-ACT-25 | PARTIAL | BootstrapRestoreOrder::partition at rust/crates/tiller_activity/src/bootstrap.rs:15 orders priority/deferred worktrees, but it is called only by activity_domain_integration tests and not launch restoration | "BootstrapRestoreOrder::partition" |
| F-CORE-ACT-26 | PARTIAL | WorktreeMountPolicy::ids_to_evict at rust/crates/tiller_activity/src/mount.rs:8 protects selected/active/unsaved worktrees, but only the integration test calls it and no mount eviction consumes it | "WorktreeMountPolicy::ids_to_evict" |
| F-CORE-DOM-03 | PARTIAL | default_project_base at rust/crates/tiller_project/src/domain.rs:6 implements TILLER_PROJECTS_DIR/XDG/HOME Linux policy, but its only callers are tests/export and production project creation does not use it | "default_project_base" |
| F-CORE-WSP-04 | PARTIAL | LayoutCommand at rust/crates/tiller_project/src/layout.rs:282 enumerates insert/split/move/close/activate/divider/view-state/rename, but classify_layout_command at :333 is test-only and no production executor applies commands | "classify_layout_command" |
| F-CORE-WSP-08 | PARTIAL | WorkspaceTabViewState at rust/crates/tiller_project/src/layout.rs:113 models editor/chat/terminal view state; production SessionTabState at tiller/src/session.rs:79 persists only pane events and scrollback | "WorkspaceTabViewState" |
| F-CORE-FILE-08 | PARTIAL | file_glyph at rust/crates/tiller_ui/src/right_panel.rs:994 selects several extension/name glyphs and folder/file fallbacks; named directory icons, selected theme mapping, and SF fallback resolution are not integrated | "file_glyph" |
| F-CORE-SET-01 | PARTIAL | SettingsPolicy::from_values at rust/crates/tiller_project/src/settings.rs:37 clamps refresh and related Linux policy, with TILLER_SOCKET_ENABLE at :49; SettingsSnapshot at tiller_ui/src/settings.rs:330 omits persisted translucency/sidebar/right-panel widths and the host only applies the socket override | "TILLER_SOCKET_ENABLE" |
| F-CORE-USG-06 | BUILT | CodexUsageFetcher::fetch at rust/crates/tiller_usage/src/codex.rs:315 calls refresh_token at :240, posts TOKEN_URL at :27, and uses classify_token_refresh_failure at :69 | "classify_token_refresh_failure" |
| F-CORE-USG-07 | BUILT | CodexUsageFetcher::fetch at rust/crates/tiller_usage/src/codex.rs:315 loads credentials, refreshes/retries bearer usage requests, parses windows, and maps logged-out/error outcomes; production caller is status_bar.rs:196 | "CodexUsageFetcher::fetch" |
| F-CTRL-CLI-02 | BUILT | resolve_tillerctl_path at rust/crates/tiller/src/main.rs:7785 resolves the XDG data destination, install_tillerctl at :7865 creates the symlink, and add_agent_tab passes it to hooks/commands at :4859 | "resolve_tillerctl_path" |
| F-AGENT-API-01 | PARTIAL | AgentAdapter at rust/crates/tiller_agents/src/lib.rs:107 exposes ID/display/hooks/prepare/launch/resume and ALL at :221 has the five adapters in order; no optional noninteractive summarizer method or command exists | "AgentAdapter" |
| F-AGENT-OPENCODE-03 | ABSENT | "opencode run --pure" | — |
| F-AGENT-OMP-03 | ABSENT | "omp --print --no-tools" | — |
| F-TERM-SPLIT-01 | PARTIAL | render_pane_tree at rust/crates/tiller/src/main.rs:5362 plus PaneNode's 0.5 split, 6-pixel divider, 160-pixel minimum, pruning, and focus replacement are production-wired; TerminalPaneCache at tiller_terminal/src/lifecycle.rs:63 has only test callers and is not integrated | "TerminalPaneCache" |
