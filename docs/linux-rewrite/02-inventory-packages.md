## TillerCore

- [ ] F-CORE-ACT-01 | The domain status vocabulary has four observable agent states: running, needs-input, done, and error; each renders a human label and has a defined priority where error outranks needs-input, which outranks running, which outranks done. | VERIFY: Start agents in turn, make each emit the corresponding lifecycle event or exit result, and confirm the worktree status and ordering follow that priority. | SRC: AgentActivityModel.swift:30
- [ ] F-CORE-ACT-02 | A hook notification for a pane immediately records the supplied agent status and timestamp and returns an AgentTransition identifying the pane and old/new status. | VERIFY: With a real hooked agent in a pane, send `tillerctl notify --session <pane-id> --status running`, then `needs-input`, and observe the sidebar state and transition-driven notification behavior. | SRC: AgentActivityModel.swift:53
- [ ] F-CORE-ACT-03 | A spawned agent is registered as running, but spawning alone does not emit a transition notification. | VERIFY: Launch an agent through Tiller and observe that its pane becomes running without a completion/attention notification caused solely by launch. | SRC: AgentActivityModel.swift:69
- [ ] F-CORE-ACT-04 | When a spawn-owned agent exits with code 0 it becomes done; a nonzero exit becomes error. | VERIFY: Launch a shell command that exits 0 and another that exits nonzero through a pane, then observe the two distinct terminal statuses. | SRC: AgentActivityModel.swift:95
- [ ] F-CORE-ACT-05 | A title signal can assign an unregistered pane an agent identity from a recognized title and status, defaulting to running when the identity is recognized but the title has no status match; this ownership is title-owned. | VERIFY: Open a pane whose real agent emits a recognized title without a hook notification, wait for title processing, and confirm the pane acquires that agent and a status without requiring process-name detection. | SRC: AgentActivityModel.swift:112
- [ ] F-CORE-ACT-06 | A title status change clears a title-owned pane only when the title stops matching that agent's conventions; it does not clear process-owned or spawn-owned state. | VERIFY: First identify a pane by title, then change only its title to unrelated text and confirm title-owned state clears; repeat with a process-identified pane and confirm unrelated title text does not clear it. | SRC: AgentActivityModel.swift:135
- [ ] F-CORE-ACT-07 | A recent hook update suppresses title-derived updates for a 1.5-second debounce window; after that window title updates are eligible again. | VERIFY: Send a hook status, immediately force a contradictory recognized title, and verify the hook status remains; repeat after more than 1.5 seconds and verify the title can change it. | SRC: AgentSignalMerger.swift:10
- [ ] F-CORE-ACT-08 | Content matching is evaluated after output settles, changes the known agent status without the hook debounce, and returns a transition when the status changes. | VERIFY: In a real agent pane, produce the exact waiting prompt or running marker in scrollback, wait for output to settle, and observe the status change even immediately after a hook update. | SRC: AgentActivityModel.swift:160
- [ ] F-CORE-ACT-09 | Foreground-process identification marks an otherwise unknown pane running and makes that pane process-owned; process disappearance clears only process-owned state. PLATFORM: the reference implementation uses macOS libproc; Linux needs a `/proc`-based parent/child process inspection equivalent. | VERIFY: Run a supported native agent binary with no usable title or hook, observe the pane become running, terminate that process, and observe the process-owned status clear. | SRC: AgentActivityModel.swift:182
- [ ] F-CORE-ACT-10 | Process scanning walks direct shell children and descendants with a maximum depth of five and 50 visited processes, and matches catalog agent command names; node/bun-hosted CLIs are not found by this layer. PLATFORM: `proc_listchildpids` and `proc_name` are macOS libproc calls; Linux needs an equivalent process-tree source. | VERIFY: Run a native-binary agent and a node/bun-hosted agent under shells, compare which one is detected by process inspection, and confirm the latter must rely on another layer. | SRC: ForegroundProcessAgent.swift:15
- [ ] F-CORE-ACT-11 | Pane ownership is separated into spawn-owned, title-owned, and process-owned sets so one evidence layer cannot clear another layer's state; closing a pane clears all associated status, identity, and ownership records. | VERIFY: Exercise title, process, and spawn detection on separate panes, alter or terminate one signal source at a time, close each pane, and confirm only the correct state is removed. | SRC: AgentActivityModel.swift:18
- [ ] F-CORE-ACT-12 | Agent title identity recognizes Claude from `✳` or `. `, Pi from `π`, Oh-My-Pi from `π:`, and word-boundary occurrences of codex, opencode, or omp; a bare braille spinner is deliberately not enough to assign identity. | VERIFY: Set or observe each exact title form in separate panes and check the assigned agent; use a bare braille spinner and confirm no agent identity is assigned from that text alone. | SRC: AgentTitleIdentity.swift:19
- [ ] F-CORE-ACT-13 | Title status detection maps Claude `✳` to needs-input and Claude `. ` or braille spinner to running; Pi and omp braille spinners mean running, while their ordinary `π` titles mean needs-input. | VERIFY: Send or observe each agent's exact idle and working title forms and compare the resulting pane status. | SRC: AgentTitleStatus.swift:17
- [ ] F-CORE-ACT-14 | Generic title status detection uses word-boundary agent names and waiting keywords such as permission, action required, and waiting for needs-input, or working keywords such as working, thinking, and running for running; an unmatched title produces no status. | VERIFY: Use titles containing each keyword both as a standalone word and inside a larger word, then confirm only the defined boundary matches affect status. | SRC: AgentTitleStatus.swift:47
- [ ] F-CORE-ACT-15 | Screen content detection recognizes Claude's `Do you want to proceed?` as needs-input and `esc to interrupt` as running; generic codex/opencode/pi/omp content uses y/n prompts and proceed/continue/allow/confirm questions as best-effort needs-input matches. | VERIFY: Put each literal prompt into pane scrollback, wait for output-settle processing, and verify the expected attention or running state; test a nonmatching prompt and verify it does not match. | SRC: ScreenManifest.swift:13
- [ ] F-CORE-ACT-16 | ANSI CSI and OSC escape sequences are stripped before screen-content matching and related text inspection. | VERIFY: Produce a prompt containing ANSI color and OSC title escapes, then confirm matching behaves as if those escape bytes were absent. | SRC: ANSI.swift:3
- [ ] F-CORE-ACT-17 | Worktree status is the priority result across its pane statuses, and the agent identity query returns the first matching agent in worktree tab/pane order. | VERIFY: Put conflicting statuses in multiple panes of one worktree and confirm the worktree row shows the highest-priority status and the first matching identity. | SRC: AgentActivityModel.swift:225
- [ ] F-CORE-ACT-18 | The running-agent list is de-duplicated and emitted in fixed AgentCatalog order, not discovery order. | VERIFY: Run multiple panes for repeated and different agents, then inspect the running-agent list and confirm each agent appears once in catalog order. | SRC: AgentActivityModel.swift:249
- [ ] F-CORE-ACT-19 | Notification payloads expose a title formatted as `<agent display name> — <human worktree label>` and a body containing the branch plus optional project and comment. | VERIFY: Trigger an attention transition in a named project/worktree with a comment and inspect the emitted notification title and body fields. | SRC: AgentActivityModel.swift:261
- [ ] F-CORE-ACT-20 | A transition notification is suppressed when no agent is running, when status did not change, or when the app is active and the pane is visible; otherwise it is allowed. PLATFORM: the reference delivery path is native macOS notification; Linux needs a desktop-notification backend. | VERIFY: Trigger identical and changed transitions while the app is foregrounded with the pane visible, hidden, and not running, and compare notification delivery. | SRC: NotificationPolicy.swift:14
- [ ] F-CORE-ACT-21 | Activity rows flatten all open worktree tabs, represent terminal leaves and chat tabs as activity sources, retain unrecognized terminal panes as idle, and omit document, diff, and browser content. | VERIFY: Open terminal splits, a chat tab, a document, a diff, and a browser tab, then inspect the activity list and confirm only the documented sources appear. | SRC: ActivityList.swift:34
- [ ] F-CORE-ACT-22 | Activity urgency sorting puts error first, then needs-input, then running, then done, while the urgent-first mode moves only error and needs-input ahead of manual order. | VERIFY: Create rows in mixed statuses and inspect both normal sorted order and urgent-first order, including ties. | SRC: AttentionSort.swift:3
- [ ] F-CORE-ACT-23 | Activity close confirmation is required for running, needs-input, and error states but not for done or idle. | VERIFY: Attempt to close panes in every status and compare whether a confirmation is requested. | SRC: ActivityStatus.swift:23
- [ ] F-CORE-ACT-24 | Agent session references distinguish a stable content ID from a live pane ID, and restore planning classifies captured references as resumable or prunable based on the live content IDs present. | VERIFY: Close and relaunch a worktree with a saved agent session reference, then observe whether the pane resumes or is pruned when its content ID is absent. | SRC: AgentSessionRestorePlan.swift:11
- [ ] F-CORE-ACT-25 | Launch restoration remounts selected/open worktrees before deferred worktrees and preserves a priority ordering for bootstrap work. | VERIFY: Relaunch with multiple worktrees, select one, and observe which terminal hosts mount first and which are deferred. | SRC: BootstrapRestoreOrder.swift:11
- [ ] F-CORE-ACT-26 | Worktree mount eviction occurs only above a positive cap, evicts eligible nonselected worktrees in open order, and never evicts a worktree with running or needs-input activity or unsaved changes. | VERIFY: Set a small mount cap, open more worktrees than the cap, and confirm only eligible done/error/idle clean worktrees are unmounted. | SRC: WorktreeMountPolicy.swift:10
- [ ] F-CORE-DOM-01 | A project stores identity, root, name, color, display name, icon/avatar, default worktree base, and optional location override; a worktree stores project, branch, path, comment, timestamps, and primary flag. | VERIFY: Create and edit a project and worktree, restart the app, and confirm every field survives and is visible through the corresponding domain/control listing. | SRC: Models.swift:3
- [ ] F-CORE-DOM-02 | Worktree defaults choose an explicit project base branch before a primary worktree branch, and choose an explicit location before a sibling of the project root. | VERIFY: Configure each combination of project base/primary branch and location override, create a worktree, and inspect its branch and path. | SRC: WorktreeDefaults.swift:5
- [ ] F-CORE-DOM-03 | The default project base is `~/Tiller/projects`. PLATFORM: this uses `NSHomeDirectory`; Linux needs an explicit home/config/data-root policy. | VERIFY: Start with no project defaults and observe the proposed project location; repeat with a Linux replacement root and confirm it is deterministic. | SRC: ProjectDefaults.swift:3
- [ ] F-CORE-DOM-04 | Sidebar filtering trims the query, matches case-insensitively against project names and branch names, and returns all projects for an empty query. | VERIFY: Enter empty, whitespace, case-varied, project-name, and branch-name queries and compare visible project rows. | SRC: SidebarFilter.swift:3
- [ ] F-CORE-DOM-05 | Manual ordering moves a known item before a target or to the end; unknown IDs and no-op moves leave the order unchanged. | VERIFY: Reorder known and unknown project/worktree IDs, including moving an item onto itself, and inspect the resulting sequence. | SRC: ManualOrder.swift:6
- [ ] F-CORE-DOM-06 | Tab keyboard ordering wraps moving tabs and accepts numeric selection 1 through count, with 9 selecting the last tab and invalid numbers ignored. | VERIFY: With several tabs, issue left/right moves and numeric selections at boundaries and confirm the active tab. | SRC: TabOrdering.swift:5
- [ ] F-CORE-DOM-07 | Automatic naming is throttled to at least 30 seconds and at least 200 characters of new transcript growth, except for the first run. | VERIFY: Feed transcript growth below each threshold and above both thresholds while observing generated-name requests. | SRC: AutoNamingThrottle.swift:7
- [ ] F-CORE-DOM-08 | A MainActor once-gate executes its closure once and ignores later fire calls. | VERIFY: Trigger the same one-shot restore or setup callback multiple times and confirm it has one observable effect. | SRC: OnceGate.swift:3
- [ ] F-CORE-WSP-01 | A legacy workspace tab can hold a terminal split tree, markdown document, code document, or chat with agent/session IDs; terminal activity exposes leaf pane IDs and chat exposes its tab ID, while documents expose no activity pane. | VERIFY: Create each content kind and inspect its content ID, title, and activity participation. | SRC: LegacyWorkspaceTab.swift:3
- [ ] F-CORE-WSP-02 | Workspace content kinds include terminal, chat, document, diff, and browser, with stable string IDs for worktree, tab, terminal content, document, and browser content; document IDs resolve symlinks and are worktree-scoped. | VERIFY: Create each kind, serialize its ID, reopen through a symlinked path, and confirm the stable identity rules. | SRC: WorkspaceContentRef.swift:3
- [ ] F-CORE-WSP-03 | Workspace layouts are binary groups along horizontal or vertical axes; a valid empty layout has one empty group, and each split stores a fraction between 0 and 1. | VERIFY: Create an empty layout, split panes horizontally and vertically, resize dividers to boundary values, and observe accepted/rejected states. | SRC: WorkspaceLayout.swift:3
- [ ] F-CORE-WSP-04 | Layout commands support insert, split, move, close, activate, divider-fraction update, view-state update, and rename operations. | VERIFY: Exercise each command through the workspace UI/control path and confirm tab placement, focus, closure, divider, view-state, and title changes. | SRC: WorkspaceLayoutCommand.swift:43
- [ ] F-CORE-WSP-05 | Structural layout commands report structural transitions and focus intent, while activation, fraction, view-state, and rename commands report nonstructural transitions. | VERIFY: Apply one command of each class and inspect whether the resulting transition requests structural rebuild and tab/divider focus. | SRC: WorkspaceLayoutTransition.swift:49
- [ ] F-CORE-WSP-06 | Layout validation rejects empty nonroot groups, orphan or unresolved tabs/content, duplicate group/split/tab/content IDs, invalid active references, and nonfinite or out-of-range fractions. | VERIFY: Load or construct snapshots containing each invalid condition and confirm the app rejects or quarantines them rather than displaying corrupted layout. | SRC: WorkspaceLayoutInvariants.swift:3
- [ ] F-CORE-WSP-07 | Workspace snapshots use schema version 1, canonical sorted JSON without escaped slashes, reject future or missing versions, and materialize malformed snapshots as an empty group registry. | VERIFY: Save a snapshot, compare canonical JSON stability, then feed malformed, missing-version, and future-version data and observe the fallback/error behavior. | SRC: WorkspaceSnapshot.swift:3
- [ ] F-CORE-WSP-08 | Workspace tab view state preserves editor caret/selection/scroll/folds, chat draft/attachments/transcript, follows-tail, and terminal viewport data. | VERIFY: Change each state, close/reopen the tab, and confirm the state is restored. | SRC: WorkspaceTab.swift:3
- [ ] F-CORE-FILE-01 | File-tree loading rejects absolute paths, `..` traversal, and symlink traversal, excludes `.git`, puts directories before files, and sorts names localized. | VERIFY: Load a worktree containing each path hazard, a `.git` directory, nested directories, and mixed-case files, and inspect the returned tree. | SRC: FileTree.swift:43
- [ ] F-CORE-FILE-02 | File drops accept png/jpg/jpeg/gif/webp files up to 10 MiB, classify in-worktree paths as relative and outside paths as absolute, and reject unsupported/oversize items. | VERIFY: Drop supported, unsupported, oversized, in-tree, and outside-tree files and inspect each classification. | SRC: FileDrop.swift:18
- [ ] F-CORE-FILE-03 | Accepted terminal file drops become one shell-quoted, space-separated path string with no trailing newline and are written to the pane. | VERIFY: Drop filenames containing spaces, quotes, and non-ASCII characters into a real terminal and verify the shell receives exactly the intended paths. | SRC: FileDrop.swift:56
- [ ] F-CORE-FILE-03A | Dropped item URLs are resolved from NSItemProvider objects sequentially, preserving the user's drop order, and byte counts can be queried before classification. PLATFORM: NSItemProvider is an Apple API; Linux needs a GPUI/file-manager drop provider. | VERIFY: Drop several files in a known order, including a provider that resolves slowly, and confirm classification and insertion preserve that order. | SRC: DroppedFileLoader.swift:3
- [ ] F-CORE-FILE-04 | Markdown/document file links remove trailing `:line[:column]`, accept file URLs, absolute paths, and worktree-relative paths, and reject other URI schemes; markdown extensions are recognized. | VERIFY: Cmd-click links in each supported syntax and confirm the resolved path and line/column target; try an unsupported scheme and confirm it is not opened. | SRC: FileLink.swift:3
- [ ] F-CORE-FILE-05 | Markdown editing can wrap or prefix selected lines while preserving and recalculating the selection range. | VERIFY: Select one and multiple lines, apply each markdown edit command, and confirm both text and selection remain correct. | SRC: MarkdownSyntax.swift:7
- [ ] F-CORE-FILE-06 | A markdown document loads UTF-8, tracks dirty/conflict/deleted state, saves atomically, reloads, and auto-reloads external changes unless local edits require a conflict; external deletion/rename is surfaced. PLATFORM: the reference watcher uses macOS `O_EVTONLY`/DispatchSource and FSEvents; Linux needs inotify/fanotify or equivalent. | VERIFY: Edit and save a document, modify/delete/rename it externally with and without local edits, and inspect dirty/conflict/deleted behavior. | SRC: MarkdownDocument.swift:4
- [ ] F-CORE-FILE-07 | File-system event streams report paths and events from the macOS FSEvents API. PLATFORM: `CoreServices` FSEvents is macOS-only; Linux needs a filesystem notification implementation. | VERIFY: Modify, create, remove, and rename a watched file and confirm the corresponding event reaches the document model. | SRC: FileSystemEventMonitor.swift:1
- [ ] F-CORE-FILE-08 | File icon lookup selects exact filename or extension icons for files and named icons for directories, with SF-symbol/material mappings and SF fallback. PLATFORM: SF Symbols and AppKit asset resolution are macOS UI dependencies; Linux needs an icon theme/resource mapping. | VERIFY: Open a tree containing representative filenames/extensions/directories and compare the selected icon keys. | SRC: FileIconKey.swift:3
- [ ] F-CORE-TERM-01 | Terminal keys map Enter, Tab, Escape, Backspace, Delete, and arrow keys to the documented CR/tab/ESC/DEL/CSI byte sequences. | VERIFY: Send every symbolic key through `tillerctl panel.key` to a shell program that echoes bytes and compare the received byte sequences. | SRC: TerminalKey.swift:3
- [ ] F-CORE-TERM-02 | Terminal context actions include copy, paste, copy context, set title, copy pane ID, copy terminal ID, split right/down, clear, and close. | VERIFY: Open a terminal context menu, invoke each action, and inspect clipboard, title, split, clear, and close effects. | SRC: TerminalContextMenu.swift:3
- [ ] F-CORE-TERM-03 | Split trees support immutable leaf/split construction, four-way splitting, leaf removal with parent collapse, and leaf ID enumeration. | VERIFY: Split a terminal repeatedly in each direction, remove leaves in the middle and at the root, and inspect the remaining tree and pane IDs. | SRC: SplitTree.swift:13
- [ ] F-CORE-SET-01 | Settings expose refresh interval clamped to 60–3600 seconds, control-socket enablement with `TILLER_SOCKET_ENABLE` environment overrides, resume/autoname/translucency options, session retention, mount cap, font sizes, sidebar, and right-panel widths. PLATFORM: the reference stores settings through macOS UserDefaults; Linux needs a configuration store. | VERIFY: Set defaults and environment values at each accepted/rejected boundary, restart, and inspect the effective settings. | SRC: AppSettings.swift:3
- [ ] F-CORE-SET-02 | Permission state models macOS TCC permission kinds, reports statuses, and exposes refresh and perform-action callbacks. PLATFORM: TCC and System Settings actions are macOS-only; Linux needs permission checks appropriate to PTY/filesystem/desktop integration. | VERIFY: Inspect each permission state and invoke its action, confirming the status is refreshed afterward. | SRC: PermissionsModel.swift:8
- [ ] F-CORE-USG-01 | Provider usage models session, weekly, monthly, and Fable windows with percentages clamped to 0–100; availability is true if any window is present. | VERIFY: Feed usage values below 0, above 100, and valid values for each window and inspect the normalized usage display. | SRC: ProviderUsage.swift:5
- [ ] F-CORE-USG-02 | Claude usage parsing strips ANSI, recognizes session 5h, weekly, and Fable labels, and converts a percentage-left display into usage percentage. | VERIFY: Feed captured Claude usage text containing each label, ANSI decoration, and malformed values, then inspect parsed windows and failures. | SRC: ProviderUsage.swift:38
- [ ] F-CORE-USG-03 | Ollama Cloud parsing is explicitly best-effort and returns only an unverified usage-percent session window when its expected field is present. | VERIFY: Feed representative Ollama Cloud payloads with and without the expected field and inspect whether usage is marked available or unavailable. | SRC: OllamaCloudUsageParser.swift:3
- [ ] F-CORE-USG-04 | OpenCode Go usage parsing normalizes bare cookie auth, extracts a workgroup ID, reads rolling/weekly/monthly usage, and converts reset seconds to reset times. | VERIFY: Supply cookies and usage payloads containing each window and reset value and inspect the resulting provider usage. | SRC: OpenCodeGoUsageParser.swift:3
- [ ] F-CORE-USG-05 | Codex credentials load from `$CODEX_HOME/auth.json` or `~/.codex/auth.json`, require access and refresh tokens, merge-save tokens and last-refresh time, and consider credentials refresh-needed after eight days. | VERIFY: Place valid, missing, and expired credential files at both supported locations and inspect load, refresh-needed, and save behavior. PLATFORM: the home/config paths are reference conventions; Linux needs its chosen XDG-compatible location. | SRC: CodexOAuthCredentials.swift:36
- [ ] F-CORE-USG-06 | Codex token refresh posts OAuth credentials to `auth.openai.com`, and classifies HTTP 401 as reused, revoked, or expired according to the response. | VERIFY: Exercise token refresh against controlled success, 401-reused, 401-revoked, 401-expired, and other-error responses and inspect the outcome. | SRC: CodexTokenRefresher.swift:11
- [ ] F-CORE-USG-07 | Codex usage fetching loads credentials, refreshes as needed, calls the ChatGPT backend with bearer/account headers, parses usage windows, and exposes mapped logged-out/error outcomes. | VERIFY: Run with valid, refresh-needed, missing, and rejected credentials and inspect the resulting usage state and window data. | SRC: CodexUsageFetcher.swift:3
- [ ] F-CORE-USG-08 | Claude usage can be fetched by spawning a hidden login-shell Claude PTY, waiting two seconds, sending `/usage`, handling a command-palette confirmation, and returning success/not-installed/logged-out/error/timed-out outcomes within 25 seconds. PLATFORM: the source documents this path as macOS-only and uses `/bin/zsh`; Linux needs a PTY and available-shell strategy. | VERIFY: Run with Claude installed/logged in, not installed, logged out, and a delayed response, then inspect each fetch outcome. | SRC: ClaudeUsageFetcher.swift:46
- [ ] F-CORE-USG-09 | Usage provider selection includes Claude, Codex, OpenCode Go, and Ollama Cloud, with per-provider enabled/disabled preference keys; reducer outcomes preserve stale usage on timeout rather than erasing it. | VERIFY: Enable and disable each provider, fetch successful/error/timed-out results, and inspect provider visibility and stale-value retention. | SRC: UsageProvider.swift:1
- [ ] F-CORE-AUTH-01 | Account identity parsing reports Claude logged-in/email/organization fields from JSON and uses the first nonempty Codex credential line as identity. | VERIFY: Provide representative Claude account JSON and Codex credential text, including empty fields, and inspect the parsed identity. | SRC: AgentAccountIdentity.swift:3
- [ ] F-CORE-AUTH-03 | KeychainCredentialStore gets, sets, and deletes usage credentials under the `com.tiller.usage` service. PLATFORM: the reference uses the macOS Security Keychain; Linux needs Secret Service/libsecret, a keyring, or an explicitly chosen encrypted store. | VERIFY: Save, read, replace, and delete a provider credential, restart, and confirm persistence and deletion. | SRC: KeychainCredentialStore.swift:1
- [ ] F-CORE-UI-01 | Appearance mode supports system, light, and dark choices and maps them to the corresponding native appearance. PLATFORM: NSAppearance is macOS-only; Linux needs a desktop/GPUI theme preference and system-theme integration. | VERIFY: Select each appearance mode, restart, and inspect effective theme selection and system-following behavior. | SRC: AppAppearance.swift:3
- [ ] F-CORE-UI-02 | Update state models checking, available, downloading, installing, up-to-date, failed, and idle transitions for the native updater. PLATFORM: the source's updater integration is macOS/Sparkle-oriented; Linux needs an update mechanism or explicit removal. | VERIFY: Feed update callbacks for each state and inspect the user-visible update state and failure handling. | SRC: UpdateState.swift:1
- [ ] F-CORE-AUTH-02 | A macOS skill-install action opens Terminal.app through AppleScript and runs `npx skills add e-palmisano/tiller --skill tiller -a claude-code,codex,opencode,pi -y`. PLATFORM: AppleScript and Terminal.app are macOS-only; Linux needs a non-GUI installer or terminal launch policy. | VERIFY: Invoke skill installation and confirm the exact command is offered/executed; note that the literal command omits omp. | SRC: AgentSkillInstall.swift:3
- [ ] F-CORE-PLAT-01 | The TillerCore package declares macOS 15 as its only package platform even though several domain utilities are otherwise portable. PLATFORM: Linux must remove or conditionalize this declaration and replace each Apple-only dependency recorded above. | VERIFY: Inspect the Linux build manifest and run the resulting app only after the rewrite defines its platform policy; this is a reference gap, not a current-build step. | SRC: Package.swift:6

## TillerTerminal

- [ ] F-TERM-PTY-01 | A PTY process spawns an executable through `forkpty`, applies a working directory, environment, and initial winsize, forwards writes, resizes with TIOCSWINSZ, streams output, and reports EOF/exit. PLATFORM: the reference uses Darwin APIs; Linux needs openpty/fork/exec/wait/ioctl equivalents. | VERIFY: Launch `/bin/sh -c 'printf ready; exit 7'` in a pane, read output, resize it, and confirm the observed output and exit code 7. | SRC: PtyProcess.swift:60
- [ ] F-TERM-PTY-02 | PTY termination reaps the child and maps a normal exit to its code and a signal termination to 128 plus the signal number. | VERIFY: Run one command that exits with a chosen code and one that self-terminates by signal, then inspect `panel.wait` results. | SRC: PtyProcess.swift:119
- [ ] F-TERM-PTY-03 | PTY environment always sets the pane ID as `TILLER_PANE_ID`, preserves the inherited environment, and can override `TERM` and add extra variables. | VERIFY: Run `env` inside a pane and compare `TILLER_PANE_ID`, `TERM`, inherited variables, and supplied extras. | SRC: PtyEnvironment.swift:3
- [ ] F-TERM-REG-01 | PaneRegistry is the actor-backed source of truth for live panes: it registers/unregisters panes, exposes shell PIDs and snapshots, writes bytes, cancels registration, waits for registration or exit, and reports timeout/cancellation. | VERIFY: Create, list, write to, wait on, close, and recreate panes rapidly; confirm registration, write success, exit, timeout, and cancellation results. | SRC: PaneRegistry.swift:31
- [ ] F-TERM-REG-02 | A pane registration wait is bounded and a close cancels pending registration; exit waiting returns an exit code, timeout, or cancellation without leaving a live registration. | VERIFY: Request `panel.create` with a command that never registers or exits, then close/wait and inspect bounded failure and cleanup. | SRC: PaneRegistry.swift:114
- [ ] F-TERM-SCR-01 | Each pane keeps a fixed-capacity scrollback buffer defaulting to 256 KiB, retaining the newest bytes when appended data exceeds capacity and returning oldest-to-newest snapshots/tails. | VERIFY: Write more than 256 KiB of numbered output, read the pane, and confirm only the newest bounded bytes remain in order. | SRC: ScrollbackBuffer.swift:4
- [ ] F-TERM-SCR-02 | Terminal output is forwarded to the activity model only after a 200 ms output-settle debounce, while resize application is debounced for 120 ms. | VERIFY: Produce bursts of output and resize events faster than the intervals and count status/resize callbacks to confirm settled rather than per-byte/per-event behavior. | SRC: OutputSettleDebouncer.swift:3
- [ ] F-TERM-PTY-04 | The terminal pane starts a shell in the worktree, prefers `$SHELL` then `/bin/zsh`, chooses `xterm-ghostty` when available otherwise `xterm-256color`, restores initial scrollback, and waits for a settled first resize before fallback sizing. PLATFORM: shell paths, AppKit sizing, and libghostty are macOS/reference dependencies; Linux needs native GPUI/terminal sizing and terminfo choices. | VERIFY: Start panes with and without `$SHELL`/ghostty terminfo, resize before launch, and inspect shell, TERM, restored output, and final dimensions. | SRC: PtyTerminalPane.swift:291
- [ ] F-TERM-PTY-05 | The runtime sends user input to the PTY, appends output to the session and scrollback, invokes content matching on the last 10 KiB and last 40 lines, and calls process/activity callbacks on pane exit. | VERIFY: Connect a real coding agent, send a prompt, inspect live output and status updates, then exit it and confirm the final lifecycle state. | SRC: PtyTerminalPane.swift:218
- [ ] F-TERM-PTY-06 | A terminal drop writes the classified file paths to the pane without a newline and returns focus to the terminal. | VERIFY: Drop files onto a live agent shell and confirm the agent receives the exact path text without an implicit Enter. | SRC: PtyTerminalPane.swift:150
- [ ] F-TERM-PTY-07 | TerminalSurfaceHost gives each terminal content ID a stable host while assigning a new generation when relaunched, and teardown terminates/unregisters its PTY. PLATFORM: the implementation uses AppKit/NSHostingView; Linux needs a GPUI surface lifecycle. | VERIFY: Move a live terminal, relaunch it, and close its host; confirm scrollback continuity across moves and a new generation after relaunch. | SRC: TerminalSurfaceHost.swift:48
- [ ] F-TERM-PTY-08 | TerminalPaneCache preserves pane controllers and PTYs when a pane moves within a worktree and restores focus by pane/content ID. PLATFORM: AppKit controller caching is macOS-only in the reference. | VERIFY: Move a live agent pane between split groups and confirm its process and scrollback continue uninterrupted. | SRC: TerminalPaneCache.swift:3
- [ ] F-TERM-SPLIT-01 | Terminal split rendering builds recursive leaf/split hosts with cached leaf controllers, 50/50 initial split fractions, a 6-pixel divider, and 160-pixel minimum pane dimensions; it prunes removed leaves and restores focus. PLATFORM: NSSplitView/SwiftUI/AppKit are macOS-only; Linux needs GPUI split containers. | VERIFY: Create nested left/right/above/below splits, resize to the minimum, close leaves, and confirm surviving PTYs and focus. | SRC: SplitViewRenderer.swift:207
- [ ] F-TERM-UI-01 | Terminal context-menu hit testing resolves the clicked pane and offers copy/paste/context/title/ID/split/clear/close actions; surface actions run directly while other actions delegate to the app. PLATFORM: AppKit menus and Ghostty surface APIs are macOS-only. | VERIFY: Right-click each pane, invoke every menu item, and inspect the corresponding terminal or app effect. | SRC: TerminalContextMenuHandler.swift:6
- [ ] F-TERM-UI-02 | Cmd-click terminal URLs are routed to the owning terminal view state rather than globally. PLATFORM: the reference uses AppKit/Ghostty object identity; Linux needs GPUI event routing. | VERIFY: Cmd-click URLs in two panes and confirm each opens through the clicked pane's router. | SRC: TerminalOpenURLRouter.swift:4
- [ ] F-TERM-USG-01 | Claude usage fetch in the terminal package uses an injectable PTY seam, a lock-guarded output buffer, and terminates the hidden PTY on completion or timeout. PLATFORM: `os` unfair locking and the documented macOS-only fetch path need Linux equivalents. | VERIFY: Run the usage fetch with a fake PTY that emits staged output and verify parsing, timeout, and termination. | SRC: ClaudeUsageFetcher.swift:7
- [ ] F-TERM-PLAT-01 | The TillerTerminal package declares macOS 15 and depends on libghostty-spm/GhosttyTerminal. PLATFORM: Linux needs a GPUI-compatible terminal renderer and a Linux PTY backend rather than the SwiftUI/AppKit surface layer. | VERIFY: Inspect the Linux rewrite dependency graph and confirm no webview/HTML renderer is introduced. | SRC: Package.swift:6

## TillerControl

- [ ] F-CTRL-WIRE-01 | Control requests and responses are newline-delimited JSON; requests contain an ID, method, and string-valued params, responses contain the same ID plus either `ok` and result or `ok:false` and error, and encoded requests append a newline with sorted keys. | VERIFY: Connect to the Unix socket, send one valid request and one malformed/partial request, and inspect one newline-framed response per request. | SRC: ControlProtocol.swift:3
- [ ] F-CTRL-WIRE-02 | The server listens on a Unix socket, removes a stale socket, creates parent directories, enforces a 0600 socket mode and owner check, accepts clients, caps a request line at 1 MiB, serializes response writes, and returns malformed/oversize errors. PLATFORM: the reference imports Darwin; Linux needs Unix-domain socket APIs and equivalent ownership/permission checks. | VERIFY: Start Tiller, inspect socket mode/path, send concurrent requests, an over-1-MiB line, and malformed JSON, and confirm bounded error responses. | SRC: ControlServer.swift:66
- [ ] F-CTRL-WIRE-03 | The client opens a fresh Unix-socket connection for each round trip, applies a receive timeout, writes the complete request, reads through the first newline, and decodes the matching response. PLATFORM: Darwin socket constants are reference-specific; Linux needs the equivalent Unix client. | VERIFY: Issue normal, timeout, server-disconnected, and fragmented-response requests and inspect the client result. | SRC: ControlClient.swift:18
- [ ] F-CTRL-WIRE-04 | The default control socket is `$TILLER_SOCKET` when set, otherwise `~/Library/Application Support/Tiller/control.sock`. PLATFORM: Application Support is macOS-specific; Linux needs an XDG runtime/config path and compatible override. | VERIFY: Start with and without `TILLER_SOCKET`, then confirm the client and server use the same resolved path. | SRC: ControlProtocol.swift:65
- [ ] F-CTRL-PANEL-01 | `panel.create` creates a terminal pane in a selected or explicitly named worktree, optionally starts a supplied command, waits for pane registration in universal mode, and returns the live pane ID; legacy mode creates and persists a terminal tab. | VERIFY: Call `tillerctl panel create --worktree <id> --cmd 'printf ready'`, inspect the returned ID and list, then repeat without a command and in a legacy workspace. | SRC: AppModel.swift:661
- [ ] F-CTRL-PANEL-02 | `panel.split` splits a source pane left/right/up/down, optionally launches a command in the new pane, waits for registration, and returns the new pane ID. | VERIFY: Create a pane, call each split direction with `--from <pane-id>`, and inspect layout, process, and returned ID. | SRC: AppModel.swift:769
- [ ] F-CTRL-PANEL-03 | `panel.list` returns pane rows for the selected or specified worktree, including ID, tab, title, agent, and active fields, as tab-separated CLI output or JSON. | VERIFY: Create multiple tabs/splits and run `tillerctl panel list` and `--json`; compare every returned row with visible panes. | SRC: AppModel.swift:897
- [ ] F-CTRL-PANEL-04 | `panel.write` writes UTF-8 input bytes to a live pane and reports failure for an unknown pane; `--enter` appends carriage return in the CLI. | VERIFY: Write `printf ready` without Enter and then with `--enter` to a shell pane, inspect when execution occurs, and try an unknown ID. | SRC: AppModel.swift:942
- [ ] F-CTRL-PANEL-05 | `panel.key` translates the nine symbolic keys accepted by tillerctl into terminal bytes and writes them to a live pane. | VERIFY: Send every accepted key to a byte-echo process and confirm the documented terminal sequence. | SRC: AppModel.swift:953
- [ ] F-CTRL-PANEL-06 | `panel.read` returns current pane scrollback base64-encoded; an unknown pane is an error. | VERIFY: Print known UTF-8 and binary-safe output, call `tillerctl panel read <id>`, decode the base64, and compare bytes. | SRC: AppModel.swift:965
- [ ] F-CTRL-PANEL-07 | `panel.wait` waits for a pane process to exit, accepts a nonnegative optional timeout, returns the exit code, and distinguishes unknown pane and timeout failures. | VERIFY: Wait on a short-lived command, a long-lived command with a short timeout, and an invalid ID; inspect code and error outcomes. | SRC: AppModel.swift:976
- [ ] F-CTRL-PANEL-08 | `panel.focus` activates a universal tab/worktree or focuses a legacy pane and returns success for a valid target. PLATFORM: foreground activation uses macOS `NSApp`; Linux needs a window/surface focus policy. | VERIFY: Focus panes in different worktrees and tabs and confirm both selected worktree and visible pane change. | SRC: AppModel.swift:998
- [ ] F-CTRL-PANEL-09 | `panel.close` closes a universal tab or legacy pane, cancels its registration/process, removes session references/commands, and prunes legacy pane cache state. | VERIFY: Close a live and an exited pane/tab, then inspect layout, process lifetime, scrollback/session records, and a subsequent list. | SRC: AppModel.swift:1030
- [ ] F-CTRL-NOTIFY-01 | `notify` accepts agent mode with session and status, or user mode with title/body; agent mode updates activity and can save an agent-session reference, while user mode posts a notification. | VERIFY: Send every valid agent status through a real agent hook and send a user title/body notification; inspect status, session persistence, and notification. | SRC: AppModel.swift:1080
- [ ] F-CTRL-NOTIFY-02 | Agent notification mode can read bounded JSON from stdin for up to five seconds or extract a session reference from known argument keys/rollout filenames; ambiguous or missing mode arguments are errors. | VERIFY: Invoke `tillerctl notify` with stdin JSON, each supported session-id key, a rollout path, ambiguous flags, and no mode, and inspect parsed request/error. | SRC: BoundedStdinReader.swift:4
- [ ] F-CTRL-SESSION-01 | `session.ref` saves a pane's stable agent ID/session reference pair; restore uses those references to resume or prune agent content. | VERIFY: Send `tillerctl session-ref --session <pane> --ref <real-ref>`, restart, and inspect the restored pane and persisted reference. | SRC: AppModel.swift:1094
- [ ] F-CTRL-WORK-01 | `worktree.set` resolves a worktree by UUID or path, persists a new comment, and resynchronizes the selected/worktree state. | VERIFY: Set a comment by UUID and by exact path, restart, and confirm the comment is persisted; use an unknown selector and inspect the error. | SRC: AppModel.swift:1102
- [ ] F-CTRL-SYS-01 | `system.ping` returns `{pong:true}` and `system.capabilities` returns the advertised method rows plus socket-enabled state. | VERIFY: Run `tillerctl ping` and `tillerctl capabilities --json` and compare the response fields with the actual method list. | SRC: AppModel+Control.swift:79
- [ ] F-CTRL-SYS-02 | `system.identify` returns project, branch, path, workspace ID, and optional surface/pane ID using explicit params first, pane environment context second, and selected worktree/active pane fallback third. | VERIFY: Run `tillerctl identify` inside a pane, outside a pane with a selected worktree, with explicit workspace/pane, and with no context, and inspect fields/errors. | SRC: AppModel+Control.swift:613
- [ ] F-CTRL-WORK-02 | `workspace.list` returns sorted project/worktree rows with ID, project, branch, path, and selected flag. | VERIFY: Create worktrees in multiple projects and compare CLI rows with sidebar order and selection. | SRC: AppModel+Control.swift:93
- [ ] F-CTRL-WORK-03 | `workspace.create` requires a project UUID/name, rejects a non-Git project, creates an optional branch or a generated `wt-yyyyMMdd-HHmmss` branch, and returns worktree ID/branch/path. | VERIFY: Create with an existing Git project, with an explicit branch, without a branch, and with a non-Git project; inspect the result and filesystem. | SRC: AppModel+Control.swift:133
- [ ] F-CTRL-WORK-04 | `workspace.select` selects a worktree by UUID or exact path and activates the app; `workspace.current` returns selected project/branch/path/ID or a no-selection error. PLATFORM: activation uses macOS NSApp; Linux needs a window activation equivalent. | VERIFY: Select each worktree by both selectors, call current, and repeat with no selection. | SRC: AppModel+Control.swift:100
- [ ] F-CTRL-WORK-05 | `workspace.close` unmounts the selected worktree's terminal host and removes it from open selection while leaving its worktree in the sidebar. | VERIFY: Close a worktree with a live agent, confirm its PTY is unmounted/terminated and its row remains available for reopening. | SRC: AppModel+Control.swift:120
- [ ] F-CTRL-NOTIFY-03 | `notification.create` requires title and body and posts a user notification; `notification.list` returns stored notification rows; `notification.clear` clears them. PLATFORM: native macOS user notifications need a Linux desktop-notification/store implementation. | VERIFY: Create two notifications, list them, clear them, and confirm both the system notification and list state. | SRC: AppModel+Control.swift:155
- [ ] F-CTRL-SESSION-02 | `session.restore` reapplies the launch snapshot, remounting worktrees and re-adding tabs without duplicating existing state, and returns the restored count. | VERIFY: Close tabs/worktrees after launch, call `tillerctl restore-session`, and compare restored tabs/worktrees with the launch snapshot. | SRC: AppModel+Control.swift:27
- [ ] F-CTRL-BROWSER-01 | The source control dispatcher accepts browser.open, navigate, get, screenshot, snapshot, act, wait, eval, console, and additionally browser.errors; browser.errors is not in `system.capabilities` and has no corresponding tillerctl builder command. PLATFORM: these handlers expose browser/webview/HTML behavior that conflicts with the Linux rewrite's no-webview/no-HTML/no-web-UI rule. | VERIFY: Query capabilities, call each advertised browser method, then call `browser.errors` directly and record the discrepancy and platform behavior. | SRC: AppModel+Control.swift:431
- [ ] F-CTRL-BROWSER-02 | `browser.open` requires a URL and workspace context, creates a browser tab/surface in the universal workspace, loads the URL, and returns surface identifier, URL, and title; it rejects missing context or unavailable adapter. PLATFORM: browser content is explicitly out of scope for the Linux rewrite unless separately decided. | VERIFY: Call the raw socket method with a URL, missing URL, missing workspace, and unavailable adapter and inspect tab/result/error behavior. | SRC: AppModel+Control.swift:195
- [ ] F-CTRL-BROWSER-03 | `browser.navigate` performs back, forward, or reload on a browser surface and returns URL/title; `browser.get` returns URL, rendered text, or HTML, including optional selector behavior. PLATFORM: returning HTML and using a browser adapter conflict with the no-HTML/no-webview constraint. | VERIFY: Open a controlled page, call each navigation and get variant, and record URL/title/text/HTML results or unsupported errors. | SRC: AppModel+Control.swift:245
- [ ] F-CTRL-BROWSER-04 | `browser.screenshot` writes a surface screenshot and returns its path; `browser.snapshot` returns generation and accessibility/node data. PLATFORM: screenshot/node APIs depend on the reference browser surface and need an explicit Linux decision. | VERIFY: Request both operations for a loaded surface and inspect the returned path, generation, and nodes. | SRC: AppModel+Control.swift:300
- [ ] F-CTRL-BROWSER-05 | `browser.act` supports click, fill, type, press, and scroll with selector/ref/value/key/generation parameters and can return a snapshot-after or delta; `browser.wait` supports selector, text, URL substring, load state, or function conditions with a positive timeout. PLATFORM: browser automation is outside the no-web-UI target. | VERIFY: Run one action and one wait for each supported condition on a controlled page and inspect success, elapsed time, and optional snapshot/delta. | SRC: AppModel+Control.swift:331
- [ ] F-CTRL-BROWSER-06 | `browser.eval` evaluates a supplied script and returns a value; `browser.console` returns console entries since an optional cursor. PLATFORM: script evaluation and HTML/browser console behavior are out of scope under the Linux rewrite constraints. | VERIFY: Call both methods on a controlled browser surface and record returned values/entries or the required unsupported response. | SRC: AppModel+Control.swift:384
- [ ] F-CTRL-CLI-01 | The tillerctl executable exposes panel create/split/list/write/key/read/wait/focus/close, notify, session-ref, worktree-set, ping, capabilities, identify, workspace, notification, restore-session, and browser subcommands, with `--socket` override and environment pane/worktree defaults where documented. | VERIFY: Run `tillerctl --help` and each command against a running app, confirming CLI output and socket method match. | SRC: Tillerctl.swift:8
- [ ] F-CTRL-CLI-02 | The tillerctl shim installs a `tillerctl` symlink under `~/Library/Application Support/Tiller/bin` and uses that path for generated agent hooks. PLATFORM: the path is macOS Application Support; Linux needs an XDG data/bin or PATH installation policy. | VERIFY: Install the shim, inspect the symlink target and executable resolution, then invoke it from a real agent hook. | SRC: TillerctlShim.swift:3
- [ ] F-CTRL-PLAT-01 | The TillerControl package declares macOS 15 and the server/client implementation imports Darwin while the wire protocol itself is Unix-socket compatible. PLATFORM: Linux needs a conditional socket implementation and a Linux socket path policy. | VERIFY: Inspect the Linux rewrite transport and verify the same NDJSON request/response contract works without macOS-only imports. | SRC: Package.swift:6

## TillerAgents

- [ ] F-AGENT-API-01 | Every adapter exposes an ID, display name, native-hook flag, worktree-local preparation, launch command, optional resume command, and optional noninteractive summarizer command; the fixed catalog contains Claude Code, Codex, OpenCode, Pi, and Oh-My-Pi in that order. | VERIFY: Enumerate the agent picker/catalog and inspect each adapter's launch, resume, hook flag, and summarizer behavior. | SRC: AgentAdapter.swift:3
- [ ] F-AGENT-CLAUDE-01 | Claude Code launches as `claude`, reports native hooks, and resumes as `claude --resume '<session-ref>'`. | VERIFY: Launch a real Claude Code pane, send a prompt, save its session reference, and resume it in a new pane; confirm the same session continues. | SRC: ClaudeCodeAdapter.swift:90
- [ ] F-AGENT-CLAUDE-02 | Claude preparation creates or merges `<worktree>/.claude/settings.local.json`, replacing only Stop, Notification, SessionStart, UserPromptSubmit, and SessionEnd hook arrays while preserving all other JSON keys; Stop/Notification/SessionStart notify needs-input, UserPromptSubmit running, and SessionEnd done. | VERIFY: Seed settings.local.json with unrelated keys and existing hooks, launch Claude preparation, inspect the five hook commands and verify unrelated keys survive. | SRC: ClaudeCodeAdapter.swift:5
- [ ] F-AGENT-CLAUDE-03 | Claude's optional noninteractive summarizer command is `claude -p '<prompt>'`. | VERIFY: Request a summary with a prompt containing shell metacharacters and inspect the generated command/quoted argument. | SRC: ClaudeCodeAdapter.swift:99
- [ ] F-AGENT-CODEX-01 | Codex reports native hooks, writes no file during preparation, launches as `codex -c '<notify=[...]}>'`, and resumes as that same notify override followed by `resume '<session-ref>'`; its override calls `tillerctl notify` with needs-input. | VERIFY: Launch a real Codex pane, inspect the exact command/config effect, send a prompt, and resume from a captured reference. | SRC: CodexAdapter.swift:17
- [ ] F-AGENT-CODEX-02 | Codex's noninteractive summarizer command is `codex exec --output-last-message /dev/stdout '<prompt>'`; the JSON string literal keeps slashes unescaped so the embedded value remains valid TOML. | VERIFY: Generate a command with a prompt/path containing slashes and inspect that no `\/` appears in the `-c` value; run it with a real Codex CLI if available. | SRC: ShellQuote.swift:9
- [ ] F-AGENT-OPENCODE-01 | OpenCode has no native lifecycle hooks, launches as `opencode`, resumes as `opencode --session '<session-ref>'`, and relies on pane exit for terminal status. | VERIFY: Launch a real OpenCode pane, send a message, exit it, and confirm status follows process exit rather than hook notifications; resume by session reference. | SRC: OpenCodeAdapter.swift:3
- [ ] F-AGENT-OPENCODE-02 | OpenCode preparation writes only `<worktree>/.opencode/plugin/tiller-session.js`; the plugin observes `session.updated` and calls `tillerctl session-ref` once per new session ID. | VERIFY: Inspect the generated plugin, run OpenCode through a real session update, and confirm one persisted session reference per new ID with no global config modification. | SRC: OpenCodeAdapter.swift:15
- [ ] F-AGENT-OPENCODE-03 | OpenCode's noninteractive summarizer command is `opencode run --pure '<prompt>'`. | VERIFY: Generate and run the summarizer command with a known prompt and inspect stdout. | SRC: OpenCodeAdapter.swift:53
- [ ] F-AGENT-PI-01 | Pi has no native lifecycle hooks, performs no preparation, launches as `pi`, resumes as `pi --session '<session-ref>'`, and relies on pane exit for lifecycle completion. | VERIFY: Launch a real Pi pane, send a message, exit, and resume from its session reference while checking status transitions. | SRC: PiAdapter.swift:3
- [ ] F-AGENT-PI-02 | Pi's noninteractive summarizer command is `pi --print --no-tools '<prompt>'`. | VERIFY: Generate and run the summarizer command and compare stdout with the requested prompt. | SRC: PiAdapter.swift:28
- [ ] F-AGENT-OMP-01 | Oh-My-Pi reports native hooks, prepares `<worktree>/.tiller/omp-hook.ts`, launches as `omp --hook '<worktree>/.tiller/omp-hook.ts'`, and resumes with the same hook plus `--resume='<session-ref>'`. | VERIFY: Launch a real omp pane, inspect the generated hook and command, send a message, and resume the captured session. | SRC: OhMyPiAdapter.swift:14
- [ ] F-AGENT-OMP-02 | The omp hook reports session_start as running with an optional agent-session reference, turn_start as running, turn_end as needs-input, and session_shutdown as done. | VERIFY: Run a real omp session through start, turn, and shutdown events and inspect status/reference notifications. | SRC: OhMyPiAdapter.swift:21
- [ ] F-AGENT-OMP-03 | Oh-My-Pi's noninteractive summarizer command is `omp --print --no-tools '<prompt>'`. | VERIFY: Generate and run the summarizer command with a known prompt and inspect stdout. | SRC: OhMyPiAdapter.swift:49
- [ ] F-AGENT-SAFE-01 | Adapter preparation writes only worktree-local hook/plugin/skill files and refuses to overwrite an existing unmanaged skill file; unsupported agent IDs and skill text without the management marker fail. | VERIFY: Prepare each adapter in a temporary worktree, inspect changed paths, pre-create managed and unmanaged skill files, and compare success/error behavior. | SRC: TillerSkillProvisioner.swift:11
- [ ] F-AGENT-SAFE-02 | Claude hook migration rewrites only a stale quoted leading tillerctl binary path in hook commands, preserving pane IDs, arguments, unrelated hooks, and other JSON keys; unchanged/unparseable files are no-ops. | VERIFY: Run migration against matching, stale, malformed, and unrelated hook files and diff every non-path field. | SRC: ClaudeHookMigrator.swift:3
- [ ] F-AGENT-SESSION-01 | Claude session validity checks for `<config>/projects/<slug>/<ref>.jsonl`, where non-alphanumeric path characters become hyphens; Codex checks for a reference-containing file below `<codexHome>/sessions`; other agents are trusted as worth trying. | VERIFY: Create and remove each expected session file and inspect the validator result for Claude, Codex, and an uncheckable agent. | SRC: AgentSessionValidator.swift:7
- [ ] F-AGENT-SESSION-02 | Claude transcript extraction reads JSONL under `~/.claude/projects/<worktree-slug>/<session>.jsonl` and joins message text or text blocks; Codex extraction recursively searches `~/.codex/sessions` for a filename ending in `-<session>.jsonl` and joins text blocks. | VERIFY: Place representative transcript files at each path, including string and block content, and compare returned recent text. | SRC: ClaudeTranscriptSource.swift:4
- [ ] F-AGENT-SESSION-03 | Shell arguments and generated JSON/TOML literals are quoted so arbitrary paths and prompts can be embedded; JSON literals intentionally use `.withoutEscapingSlashes`. | VERIFY: Generate launch/notification commands containing spaces, quotes, dollar signs, backslashes, and slashes, and inspect or execute them in a shell. | SRC: ShellQuote.swift:4
- [ ] F-AGENT-PLAT-01 | The TillerAgents package declares macOS 15 but its adapter commands are ordinary CLI processes and its file outputs are worktree-local. PLATFORM: Linux needs a platform-neutral package manifest and explicit home/config conventions for transcript/session validation. | VERIFY: Inspect the Linux adapter package and confirm it launches the same configured CLIs without writing user-global configuration. | SRC: Package.swift:6

## TillerPersistence

- [ ] F-PERSIST-DB-01 | AppDatabase opens a SQLite database at the supplied path, supports an in-memory database for isolated use, applies migrations through v18, and exposes read/write database access. | VERIFY: Start with an empty path, restart, and inspect persisted records; separately create an in-memory store and confirm it disappears after process exit. | SRC: AppDatabase.swift:4
- [ ] F-PERSIST-DB-02 | The schema persists projects, worktrees, pane scrollback, legacy terminal tabs, agent accounts/sessions, workspace layouts/tabs/terminal content, browser content, chat sessions/items, context usage, permissions, ordering, and quarantine records through migrations. PLATFORM: GRDB/SQLite is portable in principle, but the package declares macOS 15 and Linux needs a supported GRDB/build configuration. | VERIFY: Create one record of every supported entity, restart, and inspect both restored state and migration-created columns/tables. | SRC: AppDatabase.swift:70
- [ ] F-PERSIST-DB-03 | Project records persist identity/name/root/display name/color/icon/avatar/default worktree base/location and ordering; worktree records persist project/branch/path/comment/timestamps/primary/order. | VERIFY: Change each project/worktree field, restart, and compare values and ordering. | SRC: Records.swift:4
- [ ] F-PERSIST-DB-04 | Pane scrollback records persist worktree/pane identity and bounded terminal bytes so a remounted pane can reattach prior output. | VERIFY: Fill a pane, quit/reopen the app, remount the worktree, and compare restored scrollback with the saved tail. | SRC: Records.swift:56
- [ ] F-PERSIST-DB-05 | Legacy terminal tab records persist kind, title, auto-name state, file path, active state/order, and terminal content/pane IDs; chat sessions/items persist transcript metadata and content. | VERIFY: Create legacy terminal, markdown/code, and chat tabs, restart, and inspect title, active tab, file, pane, and transcript restoration. | SRC: Records.swift:77
- [ ] F-PERSIST-DB-06 | Agent account and session records persist agent identity, worktree/content/pane linkage, session references, and timestamps for restore and usage/account features. | VERIFY: Save references for each hook-capable/resumable agent, restart, and inspect restore planning and account lookup. | SRC: Records.swift:108
- [ ] F-PERSIST-DB-07 | Workspace snapshots persist layout schema/root/group/tab state and terminal/browser content records; malformed records can be quarantined rather than materialized. | VERIFY: Save a universal workspace, corrupt one serialized record, restart, and inspect whether valid siblings remain and the bad record is quarantined. | SRC: Records.swift:150
- [ ] F-PERSIST-DB-08 | ProjectStore loads projects in saved order and worktrees in saved order, supports add/remove/update for project and worktree fields, performs exact-path worktree lookup, and keeps the primary-worktree flag exclusive. | VERIFY: Exercise CRUD, reorder, exact path lookup, and primary toggling across multiple worktrees, then restart and compare. | SRC: ProjectStore.swift:17
- [ ] F-PERSIST-DB-09 | ProjectStore persists and restores legacy tabs by deleting/reinserting the tab set, restores active tab, skips corrupt tab records, and reconstructs terminal/markdown/code/chat kinds. | VERIFY: Save a mixed legacy tab set, corrupt one record, restart, and confirm valid tabs and active selection survive while the corrupt tab is skipped. | SRC: ProjectStore.swift:238
- [ ] F-PERSIST-DB-10 | ProjectStore upserts, loads, and deletes agent session references keyed by live content/pane identity. | VERIFY: Save, replace, load, and delete a session reference and inspect restore behavior after each operation. | SRC: ProjectStore.swift:347
- [ ] F-PERSIST-DB-11 | Database migrations add worktree/project ordering with rowid backfill, migrate chat/session fields, introduce universal workspace tables, rename legacy fields, and add browser content through v18. | VERIFY: Open databases representing earlier schema versions and inspect that each migration preserves data and creates the expected current records. | SRC: AppDatabase.swift:211
- [ ] F-PERSIST-DB-12 | Migration v17 renames `terminalTab` to `legacyTerminalTab_v15`, while the current ProjectStore legacy save/load paths still refer to `terminalTab`; this is an observable compatibility risk requiring verification. | VERIFY: Create a legacy tab on a v18 database, restart, and check whether it is saved/restored or whether the table mismatch produces a visible failure. | SRC: AppDatabase.swift:295
- [ ] F-PERSIST-PLAT-01 | The TillerPersistence package declares macOS 15 despite using SQLite/GRDB data operations that may be portable. PLATFORM: Linux needs an explicit SQLite location, locking/permissions policy, and supported GRDB target. | VERIFY: Inspect the Linux persistence implementation and verify database creation, migration, and concurrent access on the chosen Linux data path. | SRC: Package.swift:6

## TillerGit

- [ ] F-GIT-RUN-01 | GitRunner runs `/usr/bin/git` asynchronously in a requested working directory, captures stdout/stderr, accepts configured exit codes, enforces output limits, and reports command failure, launch failure, cancellation, timeout, or truncated-output conditions. PLATFORM: the absolute executable path is a reference assumption; Linux should resolve or configure git without assuming `/usr/bin/git`. | VERIFY: Run successful, failing, missing, cancelled, and output-flooding git commands and inspect exit/error/truncation behavior. | SRC: GitRunner.swift:20
- [ ] F-GIT-RUN-02 | GitRunner streaming emits stderr lines split on CR/LF while the command runs and reports the final result after process completion. | VERIFY: Run `git clone` or a command that emits progress and confirm progress arrives incrementally before completion. | SRC: GitRunner.swift:182
- [ ] F-GIT-REPO-01 | Repository detection accepts a `.git` directory or file, and `git rev-parse --git-dir` returns the resolved Git directory; HEAD presence is checked with an accepted 0/1 result. | VERIFY: Test a normal repository, linked-worktree `.git` file, nonrepository directory, and repository with no commit. | SRC: GitRepository.swift:3
- [ ] F-GIT-BRANCH-01 | Git branch listing returns local branch names from `git branch --list`. | VERIFY: Create local branches with spaces and ordinary names, list them through the Git layer, and compare exact names. | SRC: GitBranches.swift:3
- [ ] F-GIT-WT-01 | Git worktree listing parses porcelain output and identifies the first/main worktree, path, HEAD, and branch; adding creates a new worktree with `git worktree add -b <branch> <path> [base]`, and removal uses force. | VERIFY: List a repository's main and linked worktrees, add with and without a base, then remove a linked worktree and inspect filesystem/branch state. | SRC: GitWorktrees.swift:3
- [ ] F-GIT-CLONE-01 | Git clone runs with progress reporting, parses Receiving objects percentage, and returns completion/failure through the streaming runner. | VERIFY: Clone a real reachable repository, inspect incremental percentage output and final path, then repeat with an invalid URL. | SRC: GitClone.swift:3
- [ ] F-GIT-REMOTE-01 | Git remote parsing extracts GitHub owner from SSH or HTTPS GitHub URLs and derives project name by removing trailing slash and `.git`. | VERIFY: Supply representative GitHub SSH/HTTPS, non-GitHub, and trailing-slash URLs and compare owner/name results. | SRC: GitRemote.swift:3
- [ ] F-GIT-STATUS-01 | Git paths reject empty, NUL, absolute, and `..` traversal paths; porcelain-v1 `-z` status parsing returns staged, changed, untracked, conflict, rename, and copy entries with original paths. | VERIFY: Create staged/unstaged/untracked/renamed/copied/conflicted files and compare parsed entries, including invalid path inputs. | SRC: GitStatus.swift:3
- [ ] F-GIT-STATUS-02 | Directory status aggregates file states to ancestors with conflict taking precedence over changed, and changed taking precedence over untracked; rename original and destination paths both affect directories. | VERIFY: Modify files at multiple depths, include a conflict and rename, then inspect status markers on every ancestor directory. | SRC: DirectoryGitStatus.swift:3
- [ ] F-GIT-ACT-01 | Git actions stage all changes with `add -A`, unstage only staged paths using reset or cached removal when no HEAD exists, restore tracked paths, and clean untracked paths with `clean -fd`. | VERIFY: Prepare staged, unstaged, tracked, untracked, and no-HEAD cases, invoke each action, and inspect `git status` and file contents. | SRC: GitActions.swift:3
- [ ] F-GIT-ACT-02 | Git action validation rejects empty paths and conflicted/stale/duplicate mutation paths before executing destructive or state-changing operations. | VERIFY: Attempt actions with empty, conflicted, duplicate, and changed-since-snapshot paths and confirm errors leave the worktree unchanged. | SRC: GitActions.swift:3
- [ ] F-GIT-DIFF-01 | Git diff loading distinguishes tracked, untracked, binary, submodule, added/deleted/renamed, and no-HEAD files; tracked files compare against HEAD while untracked files use `git diff --no-index /dev/null`. | VERIFY: Create each file state and inspect the reported diff kind, old/new text, and path metadata. | SRC: GitDiff.swift:3
- [ ] F-GIT-DIFF-02 | Diff parsing returns hunks, context/add/delete lines, old/new line numbers, binary/submodule markers, and added/deleted counts, with output and line-size safety limits. | VERIFY: Generate a normal multi-hunk diff, binary change, submodule change, huge output, and long line, then inspect parsed result or bounded error. | SRC: GitDiff.swift:82
- [ ] F-GIT-DIFF-03 | Side-by-side diff pairs context lines, zips deletion/addition runs, preserves full-width hunk context, and omits metadata lines. | VERIFY: Open a diff containing context-only, pure deletion/addition, and replacement runs and compare left/right rows. | SRC: GitDiffSideBySide.swift:3
- [ ] F-GIT-DIFF-04 | Diff stats parse one `numstat -z` invocation, preserve rename destinations, represent binary counts, and count untracked disk lines up to 500,000 lines. | VERIFY: Request stats for tracked, renamed, binary, and untracked files including a large untracked file and inspect counts/bounds. | SRC: GitDiffStats.swift:3
- [ ] F-GIT-PLAT-01 | The TillerGit package declares macOS 15 although its Git subprocess and filesystem operations are largely cross-platform. PLATFORM: Linux needs an executable lookup/configuration policy, process-signal mapping, and filesystem permission behavior verified on Linux. | VERIFY: Inspect the Linux Git implementation and run repository/worktree/status/diff flows using the configured Linux git executable. | SRC: Package.swift:6

## Open questions

- All six requested package manifests declare macOS 15; the Linux rewrite must decide which declarations and Apple imports are removed, conditionalized, or replaced before implementation.
- The source includes extra packages outside this requested six-package scope, including browser-related code; this report records TillerControl/App browser behavior because it is exposed on the control socket, but does not inventory those extra packages as independent subsystems.
- Browser control is source-observable, including HTML return, script evaluation, screenshots, and browser automation, but directly conflicts with the stated Linux no-webview/no-HTML/no-web-UI rule. Decide whether every browser method is removed, rejected, or replaced by a non-web capability.
- `browser.errors` is accepted by the app dispatcher but absent from `cmuxMethods` and the TillerControl request builder/CLI; decide whether it is intentional, deprecated, or missing from the advertised contract.
- `AppModel` is required to turn several package protocols into observable effects, especially panel creation/splitting, foreground-process scanning, native notifications, and browser dispatch; the package-only source does not define a standalone Linux app behavior for those effects.
- Layer-B title conventions and Layer-C content manifests are empirical/heuristic for several agents; the source explicitly marks non-Claude content matching best-effort, and a critic should verify every literal with real current CLIs rather than infer behavior.
- Layer D cannot identify node/bun-hosted CLIs by process name; decide whether Linux process inspection, title parsing, hooks, or another signal is authoritative for Pi and Oh-My-Pi.
- Migration v17 renames `terminalTab` to `legacyTerminalTab_v15`, while ProjectStore legacy save/load code still names `terminalTab`; verify whether this path is reachable on the current schema and whether compatibility migration is missing.
- CodexAdapter's source comment says “Cursor's Codex CLI,” while its ID/commands are `codex`; confirm the intended CLI identity and current command-line/config syntax against the real agent.
- The global skill-install command explicitly lists claude-code, codex, opencode, and pi but omits omp even though Oh-My-Pi is in AgentCatalog; decide whether that omission is intentional.
- KeychainCredentialStore uses the macOS Security Keychain, TCC permissions use System Settings, FSEvents uses CoreServices, terminal surfaces use AppKit/libghostty, dropped files use NSItemProvider, and skill installation uses AppleScript/Terminal.app. Each needs an explicit Linux replacement or removal decision.
