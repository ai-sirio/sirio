# INVENTORY-LEDGER — per-entry status of all 388 entries

Written by pireview (the critic). Pass 9 built it; pass 10 converted the bulk of
`NOT EXERCISED` into real verdicts (see `CRITIC-baseline.md` PASS 10). Pass 10 snapshot:
whole tree copied to `/tmp/critic-pass10` at **2026-08-13T12:40:06Z**; build clean in
14.14 s; headless launch served `TILLER_SOCKET=/tmp/critic10.sock`; live exercises: real
claude usage fetch (Success 4%/30%), `sleep` pane surviving select-workspace away/back.
Pass 9 snapshot: whole tree copied to
`/tmp/critic-pass9` at 2026-08-13T12:27:17Z (14:27:17 CEST); `cargo build -p tiller
-p tiller_control` clean in 4.87 s; headless launch served `TILLER_SOCKET=/tmp/critic9.sock`
(ping pongs, 42 methods advertised, `browser.*` answers its specific unsupported error,
the P37 mutation doors `surface.changes.stage/unstage/discard/stage_all/discard_all` exist).

Pass 12 snapshot: whole tree copied to `/tmp/critic-pass12`; build clean with shared
`CARGO_TARGET_DIR=/tmp/critic-target`; all suites run from the snapshot. Pass 12 applied
`PASSED-AUDIT.md` (fable): 10 false PASSEDs confirmed and re-marked, 4 unreplayable rows
given named drawn tests or downgraded, 4 doubts resolved, `F-SID-14` and six `F-GIT` stale
FAILEDs flipped, Totals recomputed from the body. New critic tests added in the snapshot:
projects-header/add/rows render, filter narrows+restores, project chevron hide/restore
(all sidebar.rs). Two P50 bin tests fail deterministically (see findings log).
Pass 11 snapshot: whole tree copied to `/tmp/critic-pass11` at **2026-08-13T13:26:20Z**.
The live tree did not compile (builder mid-edit in `main.rs:3596`, split-path refactor —
repaired in the snapshot only, never the live tree). All quiet-crate suites green
(120 tiller_ui lib, 67 tiller bin, 27 tiller_usage, 46 tiller_git, persistence/activity/
project/agents/acp/markdown all green); a mid-pass disk outage invalidated some transient
results, all re-run green afterwards. Headless launches served `TILLER_SOCKET=/tmp/critic11.sock`
with throwaway `TILLER_DB`s. Live exercises: pane launch-error surface for a missing CLI,
close-cancels-registration (6 ms), socket disable effect, scrollback capture->quit->relaunch->replay
(restored pane showed the previous session's 15:54 prompt inside a 15:56 session), `$SHELL`
preference (sh prompt under SHELL=/bin/sh), 1 MiB line-cap boundary pinned. New critic tests
added in the snapshot: usage timeout+PTY-termination, activity-row click/close, remove-project
confirmation prompt, socket disable effect. Pass 11 converted the entire half-proven bucket
(see `CRITIC-findings-log.md` PASS 11).

One row per entry from both inventory files, in inventory order. This ledger consolidates
critic passes 1–8 from `CRITIC-baseline.md`; pass 9 exercised nothing new beyond the
snapshot health check and the environment fact that `opencode`/`omp` are absent from PATH.
**The ledger wins over `INVENTORY-STATUS.md` wherever they disagree.**

Verdict vocabulary: `PASSED` · `FAILED — absent` · `FAILED — defective` ·
`UNREACHABLE` (with reason) · `N/A — platform` · `NOT EXERCISED — blocked on display`
(appearance only) · `NOT EXERCISED` · `half-proven` (with which half).
`judged` names the critic pass that established the verdict, or
`builder-claimed, unverified` / `never claimed` where no independent critic pass has
touched the entry — those rows do **not** count toward done.

## App target — `01-inventory-app.md` (217)

### Window and application shell (12)

| id | verdict | evidence | judged |
|---|---|---|---|
| `F-WIN-01` | PASSED | Live-driven: no titlebar gear exists (confirmed by full-strip crop); the status-bar gear at (27,948) fully replaces the workspace with a Back/Settings header + General section. Clicking Back fully restores sidebar, tab strip, and Files panel. Literal ⌘, chord untested (no macOS binding on this platform) but the functional route-swap is fully confirmed both directions. shots/173,174,175. | sweep A1-P109, 2026-08-14 |
| `F-WIN-02` | PASSED | ctrl-t chord (main.rs:115) dispatches `NewTerminalTab` → `NewTabAction::NewTerminal`; replayed `linux_window_command_chords_dispatch_typed_shell_actions` (pass 14) plus live tab-strip exercise this pass | pass 14 |
| `F-WIN-03` | PASSED | replayed `linux_window_command_chords_dispatch_typed_shell_actions` and `save_command_is_disabled_without_an_active_file_and_explains_why` — Ctrl+O opens the picker and Ctrl+S routes through `handle_save_file` with typed `NoActiveFile` gating (pass 14) | pass 14 |
| `F-WIN-04` | PASSED | replayed `titlebar_controls_emit_shell_visibility_events` (tiller_ui, green) and the chord test — Ctrl+Shift+S dispatches `ToggleSidebar`; caveat: the cited palette test is in the currently-failing tiller-bin cluster (owner codex12, see findings log pass 14), the sidebar route itself is independently green | pass 14 |
| `F-WIN-05` | PASSED | replayed `linux_window_command_chords_dispatch_typed_shell_actions` + `titlebar_controls_emit_shell_visibility_events` — Ctrl+Shift+I dispatches `ToggleRightPanel` (pass 14) | pass 14 |
| `F-WIN-06` | PASSED | PASSED (was FAILED — defective). Critic re-grepped main.rs and confirmed the NewBrowser match arm (main.rs:4663) is real, non-empty, and predates the stale evidence's cited empty-arm line (main.rs:4215). Captures confirm a live tab-strip New Browser menu entry and, after clicking, a real Browser tab with full working chrome; the blank content is the lane's documented Wayland webview-handle limitation (verified verbatim against WAYLAND-LANE.md), not a defect in the New Browser action. | sweep W13-git+per+persist+win, 2026-08-14 |
| `F-WIN-07` | PASSED | Live (Wayland lane): ctrl-k opened the palette (only after focusing a non-terminal surface — a real app test shows ctrl-k is intentionally inert with a terminal focused) and typing 'history' filtered to the single row 'History: Restore Previous Launch Ctrl+Shift+O'. Sending ctrl+shift+o directly, and separately clicking the titlebar RefreshCw button, each produced the identical live error notice 'could not restore the previous launch: unknown worktree: ...' proving all three routes reach the same handler and its Err now surfaces via sidebar.set_notice. | sweep D-MAIN-8, 2026-08-15 |
| `F-WIN-08` | PASSED | Independently reproduced with a fresh wayland-drive.sh instance (label h3critwin, not the builder's session): action block sent a real swaymsg kill (xdg_toplevel close) against the mapped window, then shot before/after. The script's post-action kill -0 $APP_PID guard did not fire, and the two screenshots are byte-identical (md5 match) — process survived a genuine close request with unchanged rendered content. Re-show half is the same code path proven by F-USE-05's click test above, not separately screenshotted against a minimized COSMIC window — same gap the builder flagged, still open. | sweep H3-tray, 2026-08-16 |
| `F-WIN-09` | PASSED | Own live X11-lane drive (linux-drive.sh, DISPLAY=:1): after gsettings set action-double-click-titlebar minimize and a harness-correct window-relative double-click, WM_STATE flipped Normal to Iconic and _NET_WM_STATE gained _NET_WM_STATE_HIDDEN -- genuine ICCCM/EWMH minimize signal, not a pixel diff. A first attempt using raw xdotool --window targeting produced no change at all, independently confirming the harness's documented reason for using WIN_X/WIN_Y absolute-coordinate clicks instead. gsetting restored afterward. | sweep I4-settle, 2026-08-16 |
| `F-WIN-10` | PASSED | Live (Wayland lane): drove the real sidebar + -> Create Project UI gesture to nest a new folder inside an already-tracked $HOME, hitting add_project's Ok(false) branch. Captured frame shows both the persistent inline red sidebar notice and a separate bordered #workspace-toast card floating bottom-right simultaneously; a repaint 5s later shows the inline notice still present while the floating card is gone, discriminating the toast's auto-dismiss from the persistent banner. | sweep D-MAIN-8, 2026-08-15 |
| `F-WIN-11` | PASSED | Drove all 5 non-idle states plus dismiss live in one wayland-drive.sh invocation with values I chose myself (version=9.9.9-critic, percent=42, message=CRITIC_FAIL_TEST/2), not matching the builder's report, so captures could only come from this drive: Available/Downloading-with-bar/Installing/UpToDate/Failed each rendered the correct toast text and control. A real click on the drawn update-toast-dismiss control removed the toast and the capture's color count reverted to exact idle baseline (6777), confirming a true Idle transition, not just visual similarity. cargo test -p tiller update_event and drawn_update_toast both pass in isolation. | sweep H4-window, 2026-08-16 |
| `F-WIN-12` | N/A — platform | TCC onboarding sheet | pass 8 |

### Projects and sidebar (19)

| id | verdict | evidence | judged |
|---|---|---|---|
| `F-SID-01` | PASSED | drawn: `projects_header_add_control_and_project_rows_render` green — now a **live-repo test** (sidebar.rs; re-established pass 13 after the pass-12 copy died in /tmp with the reboot) — the Projects header's + control and all four fixture project rows render, and clicking + opens the platform picker. Pixel half: frames A2-02 (nine row text-bands), S-plus-clicked (+ hover + ashpd FileChooser request created on the D-Bus) | pass 13 |
| `F-SID-02` | PASSED | drawn: `filter_narrows_rows_and_clearing_restores_them` green — live-repo test (sidebar.rs, pass 13): keystrokes reach the filter state, matching row stays while eight others disappear, clearing restores all four project rows. Display half: frames A2-03/A2-04 — typing `waku` narrows 9 text-bands to 4, backspace restores 9 | pass 13 |
| `F-SID-03` | PASSED | **live end-to-end (P92)**: sidebar + opens a two-mode menu (Clone / Create, p92-a0/a1); Clone form accepted a typed `file:///tmp/p92src` URL (typed-path mode avoids the portal wall), completed the clone, and the project row appeared **without refresh** (p92-a2/a3/a4); checkout verified on disk with correct origin remote. Drawn dir-picker dispatch remains for the picker mode (pass 8) | pass 19 (P92 live drive) |
| `F-SID-04` | PASSED | drawn: `project_chevron_hides_and_restores_children` green — live-repo test (sidebar.rs, pass 13): chevron click reveals a collapsed project's worktree/tab rows, second click hides them. Display half: frames P1-collapsed/E1-expand — the same row toggles 9 text-bands ↔ 4 on the live display | pass 13 |
| `F-SID-05` | PASSED | drawn selection event + live select-workspace mounts tabs | pass 8 |
| `F-SID-06` | PASSED | Live: notify status=error on a worktree pane flipped its row dot orange->red; collapsing the parent project row then showed the identical red dot on the collapsed project row itself (no dot there before the notify). | sweep D-MAIN-5, 2026-08-15 |
| `F-SID-07` | PASSED | Live re-drive (own drive, not a report read): right-click opened Project Settings; typed F2CRIT-RENAMED into Display name (landed in full, header live-updated) and clicked a different icon (header icon swapped live). After returning to the sidebar the project row itself now reads F2CRIT-RENAMED with the new icon, proving persistence into the sidebar, not just the settings header (02-90-check-sidebar-after-reset.png). | sweep F2-project, 2026-08-16 |
| `F-SID-08` | PASSED | Live re-drive against a freshly-created genuinely non-git folder: Initialize Git repository was enabled, clicked it, and a real filesystem read-back confirms .git now exists with git status reporting On branch master / No commits yet. Sidebar worktree row also updated live from bare path to a master branch row (03-103-after-init-git.png), and a re-opened menu now shows the item disabled as already-initialized. | sweep F2-project, 2026-08-16 |
| `F-SID-09` | PASSED | Live re-drive: right-click -> Show in File Manager on the test project. Instrument: host process tree via ps aux, which showed a real spawned process /usr/bin/cosmic-files /tmp/f2crit-nongit-proj with the exact project path as argv -- stronger than the original critic-reading-a-report evidence. | sweep F2-project, 2026-08-16 |
| `F-SID-10` | PASSED | pass 8 remove flow + shell remove_project real; pass 13 drawn (live-repo): `remove_project_context_item_confirms_before_emitting` green (sidebar.rs) — right-click project row → Remove Project → platform prompt up with nothing emitted, Cancel emits nothing, "Remove from Tiller" emits `RemoveProject` with the project's id (replaces the pass-11 auto-confirming-prompt test, which lived only in a wiped snapshot) | pass 13 |
| `F-SID-11` | PASSED | Two separate wayland-drive.sh invocations sharing one sqlite DB, fresh tiller process each (confirmed by differing bash-pane timestamps): worktree.set comment=CRITICMARK77 in run 1, then run 2 with NO worktree.set at all shows the comment truncated as CRITIC... on the very first captured frame. Matches git show 8860603's fix of seeding the initial Sidebar entity from persisted control_state instead of an empty comment map. | sweep G6-sidebar, 2026-08-16 |
| `F-SID-12` | PASSED | Live: right-click a non-primary worktree row shows Set Primary as the first item, no chord; clicking it moved the Primary badge onto that row in the next frame. Discriminates from the default primary. | sweep D-MAIN-6, 2026-08-15 |
| `F-SID-13` | PASSED | **live (P92)**: New Worktree… → branch prompt → typed `p92wt` → worktree row appeared in the sidebar and `git worktree list` shows `/home/enzopalmisano/p92src-p92wt` on branch p92wt (frames p92-b0/b2/b3/b4). Anomaly for the terminal owner: the worktree's terminal spawned with the app's cwd on creation but the correct worktree cwd on restore-respawn | pass 19 (P92 live drive) |
| `F-SID-14` | PASSED | pass 12: drawn `right_click_context_menu_dispatches_a_typed_worktree_action` green (sidebar.rs:2125) — right-click draws the menu, New Terminal click emits typed `ContextAction{Worktree, NewTab}`; shell subscribes → `handle_sidebar_context_action` → select worktree if needed → `open_action` (main.rs:2101-2103, 2514-2584); palette route emits the same typed event (main.rs:5086-5094); open_action tab creation proven by F-TAB-03/04/05. Stale FAILED flipped — same defect as a false PASSED with the sign reversed | pass 12 |
| `F-SID-15` | PASSED | Live (2 runs): rightclick worktree row, settle, click Remove Worktree -> real 'Remove worktree?' dialog (Remove Worktree/Cancel) appeared centred, menu closed; Cancel run confirmed no deletion. New collateral finding: same click also opens the New Worktree inline form underneath (click-fan-out past the deferred menu) -- doesn't violate this row's own pass clause, so still PASSED. | sweep E-C-4, 2026-08-16 |
| `F-SID-16` | PASSED | Two real press-move-release drags driven; order flipped in a later capture (after a hover-only event, which cannot itself cause a reorder — only force a repaint of one already committed). Clause's literal ask (order changes) satisfied; repaint lag noted as a secondary defect. shots/119. | sweep A1-P109, 2026-08-14 |
| `F-SID-17` | PASSED | Real fine-grained drag (30x2px steps): mid-drag preview (x11-mid-drag3.png) and post-mouseup order (x11-after-drag3.png) show linux/gpui-waku, rust/gpui-rewrite, New Worktree... — correct, matches fixed reorder_rows() fallback at sidebar.rs:622-635 (commit 2df5dca). Coarse-waypoint negative control reproduced old failure, confirming it was a harness gesture issue, not the feature. | sweep B3-sidebar, 2026-08-14 |
| `F-SID-18` | PASSED | PASSED (was FAILED — absent; driver's could-not-reach also overturned). Critic zoomed driver's own capture to find the true close-button pixel (556,50 at 1715x972) and live-drove the full flow: Terminal-tab close needs a 'Close dirty tab?' confirm (explaining the driver's stalled panel.list), then Chat-tab close reaches the empty state with 'No Terminals'/'New Terminal' exactly per clause, then New Terminal produces a genuinely new pane. Captures: reference/linux-progress/wavea-W11-ctrl+brw+sid/sid18-critic-verify/. | sweep W11-ctrl+brw+sid, 2026-08-14 |
| `F-SID-19` | PASSED | Live drive 2026-08-14: reached the real 'No Terminals' empty state (03-empty-retest.png) then pressed ctrl-t via a correctly-initialized virtual keyboard (a bare wtype call silently no-ops without a prior type/key action per wayland-drive.sh) -- a fresh Terminal tab replaced the empty state (04-ctrlt-with-keyboard-init.png). The new pane's own internal clock (22:12:45) differs from the earlier mouse-opened pane's clock (22:12:17), independently confirming a genuinely new pane was created, not a stale repaint. Positive control (mouse New Terminal) also verified working. Both empty-state precondition and ctrl-t replacement exercised live and observed working. | sweep E10-persist+sid+agent+per, 2026-08-14 |

### Project creation and project settings (18)

| id | verdict | evidence | judged |
|---|---|---|---|
| `F-PRJ-01` | PASSED | 03-add-project-menu-open.png: opaque add-project card, no compositing of Filter field or tiller row beneath it, in the exact region prior evidence showed bleed-through. | sweep B3-sidebar, 2026-08-14 |
| `F-PRJ-02` | PASSED | pass 13, exercised: `tillerctl project add` fixture-proj over the socket → catalog row persisted (project p-5c1c99d8fc9dca99, worktreeCount 2) and the sidebar renders the new project (frame A2-02). The picker→event half is the green drawn `add_project_picker_reports_the_chosen_directory`; the event→shell→catalog subscription is the same proven route as F-SID-03 | pass 13 |
| `F-PRJ-03` | PASSED | cargo test -p tiller_ui open_project_initialize_git_creates_a_real_repo_then_adds passes at HEAD; read it — simulates real clicks through +/Open/Initialize-Git controls, injects the non-git folder via simulate_path_prompt_response (the only deterministic way around this lane's picker limitation), asserts a real on-disk .git dir plus a real AddProject event. | sweep E-C-3, 2026-08-16 |
| `F-PRJ-04` | PASSED | OR clause: folder-selection arm has no UI door (same as F-PRJ-03), but insertion-fails arm is directly satisfied — live clone and create failures both show real, visible, non-silent error text (cross-ref F-PRJ-07/10 evidence, same report). shots/05a,09b. | sweep A1-P109, 2026-08-14 |
| `F-PRJ-05` | PASSED | Contradicts the ledger's FAILED — defective: drove the exact claimed failure gesture (one zero-delay wtype call into the URL field, confirmed wtype's default -d is 0ms) six independent times with zero character drops, then completed the full VERIFY flow — typed URL, clicked Clone repository, and a real git clone appeared on disk at /home/enzopalmisano/Hello-World with correct remote origin in .git/config, plus a new Primary worktree in the sidebar. project_forms.rs is unchanged at HEAD (last commit 4370eea), so the current code works when driven this way; could not reproduce the recorded drop despite trying. | sweep G6-sidebar, 2026-08-16 |
| `F-PRJ-06` | PASSED | Prior critic pass (committed b3a9997) reproduced the ledger's char-drop both ways with identical actions differing only in post-type settle time, proving it a lane-pacing artifact, not a code defect (project_forms.rs diff empty, confirmed via git show). Live-drove both real VERIFY halves: empty URL keeps Clone button dimmed/disabled, and a sleep-shimmed git proved the double-submit guard live (two clicks -> one clone-start line, button muted mid-clone). This session confirmed clone_state_disables_empty_url_and_double_submission still exists at project_forms.rs:896 and the tree has no uncommitted diff. | sweep E-P3, 2026-08-16 |
| `F-PRJ-07` | PASSED | Live-drove failure (real git error, invalid host) then edited URL and retried successfully: sidebar gained a real Hello-World project with an actual git clone on disk (README, .git present, verified then deleted). Both invalid-URL and correct-URL-retry arms now proven; overturns the prior anchored-popover input-gap claim. | sweep D-U, 2026-08-15 |
| `F-PRJ-08` | PASSED | Live re-drive, fresh instance: Create Project form opens with every claimed element (name field placeholder, Parent location, live Creates preview, Create/Cancel). Discriminator: typed a distinguishing name and watched the preview live-update and the Create button go from disabled to enabled (frames 03-51-create-form.png, 05-63-typed.png). | sweep F2-project, 2026-08-16 |
| `F-PRJ-09` | PASSED | Same overlay family, opposite result: typed 'mytestproject' (13 chars) into the real Create-project popover's name field, landed in full on the first attempt, clicked Create project, popover closed, sidebar gained a new Primary project row, and /home/enzopalmisano/mytestproject exists as a real directory on disk (checked with ls, not just the UI). Full click-to-filesystem round trip confirmed live. Test artifact removed afterward. | sweep D-P1, 2026-08-15 |
| `F-PRJ-10` | PASSED | Live-driven: colliding project name submitted, verbatim visible red failure text shown ('could not create ...: File exists'). Clause has no retry requirement — fully satisfied. shots/09b. | sweep A1-P109, 2026-08-14 |
| `F-PRJ-11` | PASSED | 09-project-settings-opened.png shows Remove Project control in sheet. Cancel path (10,12) returns to normal; Accept path on throwaway project (41,43) removes it from sidebar while ls confirms directory + .git still exist on disk. | sweep B3-sidebar, 2026-08-14 |
| `F-PRJ-12` | PASSED | 16-after-init-git.png: open sheet updates live to Repository: Git, Initialize Git button gone, without closing/reopening. git status on disk confirms real repo created. 17-closed-after-init.png shows sidebar row updated to worktree/master badge. | sweep B3-sidebar, 2026-08-14 |
| `F-PRJ-13` | PASSED | X11 drive: settings-sheet screenshot shows 8/8 swatches wrapped (6+2 rows); after picking green branch icon + Close, sidebar row icon is green branch glyph, not default orange folder -- full round trip confirmed live, exceeding builder's own unverified sidebar half | sweep C-P3, 2026-08-15 |
| `F-PRJ-14` | PASSED | Prior critic pass (committed b3a9997) bypassed the undrivable native file dialog by seeding a project row's icon_value directly in the DB, then relaunched and captured discriminating before/after frames: sidebar row glyph flips from folder-outline to a solid-red filled disc, and the Project Settings Avatar-tab preview renders the same 48x48 red image above Current:<filename>. This session confirmed both call sites are real: gpui::img(path.clone()) at project_identity.rs:1012 and sidebar.rs:2462, plus avatar_mode_local_png_validates_signature_and_size_before_committing at project_identity.rs:1378. GitHub/Favicon avatar arms remain caption-only (no HTTP client anywhere in the app) — confirmed genuine unbuilt gap, matching builder's scoped claim, not a regression. | sweep E-P3, 2026-08-16 |
| `F-PRJ-15` | PASSED | PASSED (was FAILED — defective, stale). Live re-drive at HEAD 4073297 confirms P97 fix 28a41fa works: picked git-branch glyph, Close -> sidebar row shows glyph in green immediately, survives a full kill+relaunch against the same TILLER_DB (round-tripped through SQLite). Default icon is orange folder, so this could not arrive any other way. Two earlier attempts at the same click flaked by dismissing the sheet instead of selecting (recorded, not reproduced on the drive carried to Close). | sweep W07-prj, 2026-08-14 |
| `F-PRJ-16` | PASSED | 32-picker-open2.png: picker grid appears on click with field empty (previously unreached). 36-emoji-search-autofocus.png: search narrows grid to a single swatch. 37-emoji-committed.png and 38-sidebar-after-emoji.png: swatch commits and sidebar row updates, confirming host round-trip. | sweep B3-sidebar, 2026-08-14 |
| `F-PRJ-17` | PASSED | Scratch repo: branch 'altbase' has extra commit ec2f2cc (MARKER.txt) not on main. New Worktree base field typed 'altbase'; created worktree's git log HEAD = ec2f2cc, proving base branch honored, not HEAD default | sweep C-P3, 2026-08-15 |
| `F-PRJ-18` | PASSED | Location field typed '/tmp/cp3-custom-location'; git worktree list confirms worktree at /tmp/cp3-custom-location/cp3-proj-critic-branch, not the default project-sibling path -- sidebar path label matches too | sweep C-P3, 2026-08-15 |

### Tabs, panes, navigation (28)

| id | verdict | evidence | judged |
|---|---|---|---|
| `F-TAB-01` | PASSED | Live: expanded docs in Files panel, then 4 consecutive settling shots (~4s, spanning multiple 1s periodic-refresh ticks) with no other input -- docs stayed expanded with linux-rewrite/superpowers/visual-reviews visible throughout. Discriminates cleanly against the pre-fix collapse-on-next-tick symptom. | sweep E-C-4, 2026-08-16 |
| `F-TAB-02` | PASSED | Live-driven: All Tabs panel now paints over centre-surface (04-overflow-open.png); 5px-step width scan (1505-1548) across crossover shows clean flip w=1515->4 tabs flush, w=1510->3 tabs+chevron, no stale-chevron state (32/33-montage.png). | sweep B1-tabbar-zorder, 2026-08-14 |
| `F-TAB-03` | PASSED | drawn +-menu dispatches New Terminal; shell creates real tab | pass 8 |
| `F-TAB-04` | PASSED | drawn +-menu New Terminal action; shell open_action creates tab | pass 8 |
| `F-TAB-05` | PASSED | menu lists all 5 adapters -> add_agent_tab (pass 8); pass 11 live: a pane launched for a missing CLI reports the launch error in its terminal (opencode: command not found in pane scrollback) | pass 11 |
| `F-TAB-06` | PASSED | Live gesture drive (label f3crit03): tab-bar + -> New Browser created a real Browser tab (globe icon) in both tab bar and sidebar tree under linux/gpui-waku; clicking it showed the address bar at https://example.com and the documented Wayland-lane no-content banner. Frame: /tmp/f3crit03/02-browser-selected.png, read directly. | sweep F3-browser, 2026-08-16 |
| `F-TAB-07` | PASSED | live, pass 14+15: New Chat → picker lists exactly the installed ACP agents (Claude Code, Codex — pi/opencode/omp correctly excluded by `is_available() && acp_program().is_some()`, tab_bar.rs:467; frame pass14/m2-01-picker-open.png); choosing either opens a chat tab, and both tabs completed real agent turns over ACP (codex/claude session JSONL nonces) | pass 15 |
| `F-TAB-08` | UNREACHABLE | tab_bar.rs's render_chat_empty now wires .on_click through TabBarEvent::OpenAgentSettings to the working open_settings call — reads correctly in source. Could not drive live: this environment has Claude Code and Codex genuinely on PATH, so the no-agent empty state cannot be produced with any documented lane mechanism. | sweep D-MAIN-6, 2026-08-15 |
| `F-TAB-09` | PASSED | P65: pane context menu has Open File → picker → `add_file_tab` editor; replayed `drawn_tab_context_open_file_uses_the_picker_and_adds_an_editor_tab` (main.rs:7787) | pass 14 |
| `F-TAB-10` | PASSED | chords + live split (pass 8, pane-3 created); pass 12: the clause's pane-menu route now exists — drawn `right_click_resolves_this_terminal_and_draws_all_context_actions` green (tiller_terminal lib.rs:1751): right-click draws all 10 items, Split Right click emits typed `TerminalContextEvent` with real pane/terminal ids; shell subscription routes to `split_terminal_at` (main.rs:2325-2341); mapping test `terminal_context_app_actions_have_workspace_routes` green | pass 12 |
| `F-TAB-11` | PASSED | Live: split a pane to 77pt wide, right-clicked it — menu anchored exactly at the click point, Split Left/Right dimmed with the pane-too-narrow reason text, other items normal; clicking the dimmed Split Left row produced no new pane in the next capture. cargo test -p tiller_terminal --lib 44/44 and the two new tiller SoleTabInGroup tests (a_solo_tab_is_pushed_as_the_sole_tab_in_its_group, two_tabs_sharing_a_group_are_not_pushed_as_sole) all green. | sweep H2-ctxmenu, 2026-08-16 |
| `F-TAB-12` | PASSED | Right-click menu on-top (11a-menu.png); Move Later shifts tab right (11-move-later.png), Move Earlier reverts (12-move-earlier.png); first tab shows Move Earlier disabled with reason, Move Later enabled (13-first-tab-menu.png). | sweep B1-tabbar-zorder, 2026-08-14 |
| `F-TAB-13` | PASSED | Live single-invocation drive (rightclick+click in one eval block): Move to New Pane enabled with 2 tabs in one group; clicking it removed Chat from the tab strip (only Terminal remained) and produced a real two-region split. Discriminates cleanly from the always-both-tabs default. | sweep D-MAIN-6, 2026-08-15 |
| `F-TAB-14` | PASSED | Rename menu item opens inline editable field pre-filled with 'Terminal'; typed text commits on Enter, title updates in strip and sidebar to 'bcdef' (10-rename5.png). xdotool input-fidelity issue noted as harness artifact, not app defect. grep confirmed no double-click-to-rename handler exists anywhere. | sweep B1-tabbar-zorder, 2026-08-14 |
| `F-TAB-15` | PASSED | Close menu item raises 'Close dirty tab? Discard unsaved work in Terminal?' dialog, itself correctly layered (22-close-single.png); confirming drops sidebar tab count by exactly one (23-close-confirmed.png). | sweep B1-tabbar-zorder, 2026-08-14 |
| `F-TAB-16` | PASSED | X11 lane 2026-08-15: ctrl-w on dirty Terminal tab -> 'Close dirty tab? Discard unsaved work in Terminal?' dialog (x12). Cancel dismisses, tab count unchanged (x13). Re-run + Close: dialog dismisses, tab count drops by one, tab gone (x14). Upgrades from FAILED — defective; fix predates prior evidence dates (commit 5e55ed0, 2026-08-13). | sweep C-MAIN-4, 2026-08-15 |
| `F-TAB-17` | PASSED | Close Tabs to the Right: bulk dirty-confirm dialog lists 3 correct tabs (25-close-right-clicked.png), confirming leaves exactly 2 tabs (26-close-right-done.png). Close Others: bulk confirm dialog (28-close-others-confirm.png), confirming leaves exactly 1 tab (29-close-others-done.png). | sweep B1-tabbar-zorder, 2026-08-14 |
| `F-TAB-18` | PASSED | Live-driven: real drag() with 12 interpolated steps moved Claude Code from 3rd to 1st slot; strip reordered Chat/Terminal/Claude Code → Chat/Claude Code/Terminal, sidebar tab list updated to match. shots/151. | sweep A1-P109, 2026-08-14 |
| `F-TAB-19` | PASSED | ctrl-tab/ctrl-shift-tab bound; handler = live tab.cycle; chord fixture green | pass 8 |
| `F-TAB-20` | PASSED | ctrl-1..9 bound; handler = live tab.select; chord fixture green | pass 8 |
| `F-TAB-21` | PASSED | Tab-strip right-click opened the on-top menu reliably across ~10 separate rclick actions, never dead. Terminal-body right-click opens its own distinct menu (Copy/Paste/Split/Clear/Close, 14-terminal-body-menu.png), confirming the two context-menu paths are independent. | sweep B1-tabbar-zorder, 2026-08-14 |
| `F-TAB-22` | PASSED | ctrl-alt-arrows bound; live pane.focus moved focus | pass 8 |
| `F-TAB-23` | PASSED | Live Wayland drive: typed CRIT_MARKER_LEFT, ctl pane.split direction=left in one action block; new empty pane landed left, marker pane right (discriminating vs the prior right-always defect). Source confirms pane.split now computes SplitPlacement from direction. | sweep D-MAIN-7, 2026-08-15 |
| `F-TAB-24` | PASSED | Live-driven: drag_escape() (mousedown, move, Escape, mouseup) at a comparable target to F-TAB-18's successful reorder — order held unchanged. Discriminating because F-TAB-18 proves this gesture class normally commits a reorder when not escaped. shots/152. | sweep A1-P109, 2026-08-14 |
| `F-TAB-25` | PASSED | Attach to Current Terminal enabled on non-current tab (19-attach-menu.png); clicking merges it into active tab as a third split pane and drops sidebar count 8->7 (20-attach-done.png). Right-clicking the active tab itself shows the action disabled with reason 'select another terminal tab' (21-attach-self-disabled.png), matching exercise text exactly. | sweep B1-tabbar-zorder, 2026-08-14 |
| `F-TAB-26` | PASSED | pass 12: drawn `right_click_resolves_this_terminal_and_draws_all_context_actions` green (tiller_terminal lib.rs:1751) — menu draws all 10 items, clicks emit typed events with resolved pane/terminal ids; main-shell subscription routes clicked-pane split/close events (main.rs:2325-2341); mapping test `terminal_context_app_actions_have_workspace_routes` green | pass 12 |
| `F-TAB-27` | PASSED | P65: context menu Resume Chat (disabled when no retained chat) → `resume_chat` reopens a chat tab and restores the retained transcript; replayed `drawn_tab_context_resume_chat_reopens_the_retained_session` (main.rs:7833). Pass-14 caveat CLOSED by P73 (live pass 16): tab.agent_id is stored ('codex' in the DB) and restore/resume relaunch the retained agent's adapter — after quit+relaunch the Codex tab spawned codex-acp, not claude-agent-acp (frames/ps pass16/p73-*) | pass 16 |
| `F-TAB-28` | PASSED | X11 lane 2026-08-15: real xdotool ctrl-w on clean Chat tab -> closed immediately, no dialog, tab count -1 (x11). ctrl-w on dirty Terminal tab -> same 'Close dirty tab?' dialog as F-TAB-16 (x12). Upgrades from FAILED — defective. | sweep C-MAIN-4, 2026-08-15 |

### Chat surface over ACP (37)

| id | verdict | evidence | judged |
|---|---|---|---|
| `F-CHAT-01` | PASSED | P53 socket door replayed: `chat_door_streams_stops_and_restores_transcript_over_a_real_socket` green (control_integration, 45 green) — ChatSession start/send/readback with incremental ACP output over a real socket; UI wiring exercised live pass 14 (picker → chat → agent reply) | pass 14 |
| `F-CHAT-02` | PASSED | Live: real amber chat-auth-required-banner rendered via UI launch against a fixture auth-rejecting agent (frame 04-chat-view.png), matching claimed text verbatim. | sweep C-CHAT-1, 2026-08-15 |
| `F-CHAT-03` | PASSED | exercised live (pass 17, fixture DB): kill -9 of the ACP bridge subtree mid-turn → red error entry with the transport reason JSON (`Incoming transport closed … session/prompt`), composer pill flips to `● offline`, tab ✓→○ (frames p17-aj6, p17-ak1); death→UI transition measured in (2s, 13s] (aj3 fresh at +2s via advancing stream, offline observed at +13s). Clicking **Retry** attempts the restart as the clause asks: banner clears, a NEW `npm exec @agentclientprotocol/claude-agent-acp` subtree appears under the app pid, pill returns to idle (p17-al2, scratch `p17-artifacts/aj-kill.txt`). Caveat beyond the clause: Retry recovers the session but silently DROPS the failed turn — no re-send, idle at +20s (findings log pass 17) | pass 17 |
| `F-CHAT-04` | PASSED | drawn enter_sends_and_shift_return_inserts_a_newline green: Return sends, Shift+Return inserts a newline and does not send (snapshot 2026-08-13T13:26Z; composer is pi's live file) | pass 11 |
| `F-CHAT-05` | half-proven | Offline half now live-proven and reference-literal. The composer is disabled while the agent is offline, matching the Swift original's ChatComposerView.canInteract, which excludes .disconnected and has no retry-and-resend feature — the earlier proposal to reword this row rested on a premise the reference contradicts. Critic drove it with its own markers (ZQX-CRITIC-MARKER-8271-unsent, ZQX-CRITIC-PREDATES-OFFLINE-3f9d) through real wtype keystrokes rather than control-socket shortcuts: the composer refuses input (empty composerText, unchanged transcript) and a pre-seeded draft survives byte-for-byte through a real click plus Return and an explicit Retry, so disabling did not reintroduce draft loss. Held at half-proven because the row's permission-wait clause stays environment-blocked and unexercised: Claude Code's own settings set permissions defaultMode to auto so it never emits a real permission_request, Codex is logged out, and OpenCode/Pi/omp expose no ACP program — all rechecked directly this pass rather than cited from earlier reports. | sweep J-portability critic, 2026-08-16 |
| `F-CHAT-06` | PASSED | **live vs real ACP agent (P92, claude-agent-acp)**: mid-stream the composer showed "Type to queue for the next turn…" (frame p92-i1), second message typed while status ● working (p92-i2 — turn 1 still active at Enter), queue drained automatically into turn 2 (p92-i3) and delivered **exactly once** — one "Say only: ok" bubble, one "ok" reply (p92-i5) and exactly one occurrence in the agent-side session JSONL (`~/.claude/projects/-home-enzopalmisano-p92src-p92wt/3e9c7455*.jsonl`). Drawn queue tests remain (pass 14) | pass 19 (P92 live drive) |
| `F-CHAT-07` | PASSED | **live, both conjuncts separately (P92)**: trial 1 — count-to-1000 stream stopped by ■ click at 262/1000, transcript divider states "cancelled", no further growth across consecutive frames (p92-m1/m2/m2b); trial 2 — second stream cancelled by Escape at ~173 (p92-m4b). Payload proof: session JSONL shows both prompts each followed by `[Request interrupted by user]`. Note: streams for ≤200-line counts complete in <5 s — a first stop attempt missed the window (harmless no-op on empty composer, p92-k1) | pass 19 (P92 live drive) |
| `F-CHAT-08` | PASSED | live, pass 15: streaming observed (20 transcript bands 7s after send), the round control at the composer's right accepted a click (9.2k px state change) and the transcript settled (19 bands, stable); the agent was too fast to catch an active-stream cancel live (session JSONL shows the full 200-line reply) — the cancel path rests on drawn `stop_click_cancels_the_stream_and_the_transcript_states_it` + the P53 socket-door stop test (both green) | pass 15 |
| `F-CHAT-09` | PASSED | **live, per-conjunct (P92)**: `/` opened the popup (p92-n1), "com" filtered it to 4 real agent-advertised commands (p92-n2), arrow-down moved the keyboard highlight (p92-n3), clicking `/compact` inserted a chip with × (p92-n4), Enter sent it and **the agent executed the command** — transcript "Compacting…/completed" (p92-n5b) and session JSONL records `<command-name>/compact</command-name>` plus a `compact_boundary` system record with `trigger: 'manual'` | pass 19 (P92 live drive) |
| `F-CHAT-10` | PASSED | **live, per-conjunct (P92)**: `@` listed the real workspace files (p92-o1), choosing alpha.txt inserted a `▤ alpha.txt ×` chip and consumed the filter text (p92-o4b), and the sent payload carried the file — session JSONL shows a second content part `[@alpha.txt](file:///home/enzopalmisano/p92src-p92wt/alpha.txt)` alongside the text (p92-q3/q4). Instrument fact: the popup's filter repaints one frame late (p92-o2 shows the pre-filter list; p92-q2 the filtered single row) | pass 19 (P92 live drive) |
| `F-CHAT-11` | PASSED | combination evidence (P92): **dispatch half live** — clicking + fired a real XDG portal call captured on the session bus: `org.freedesktop.portal.FileChooser.OpenFile`, parent `x11:400001` (the app window), `accept_label: "Attach image"`, **`multiple: false`** (multi-selection made impossible in the dialog itself, as chat.rs:1230's comment claims); selection/validation halves NOT EXERCISED live — the portal dialog is Wayland-side, invisible to X capture and undrivable by xdotool (ENVIRONMENT.md instrument wall; portal `Request.Close` from a third party is Access denied) — they stay on the drawn `attach_control_accepts_one_image_and_rejects_the_rest` (pass 14). D-Bus capture: `/tmp/p92session/dbus-filechooser.log` | pass 19 (P92 live drive) |
| `F-CHAT-12` | PASSED | Drove a real @-mention chip, clicked its x removal control, typed STILLFOCUSEDPROBE with no intervening click; surface.chat.read immediately showed composerText: STILLFOCUSEDPROBE and the frame confirms it rendered. Screenshots f12-01 through f12-03. | sweep B2-chat, 2026-08-14 |
| `F-CHAT-13` | UNREACHABLE | Re-confirmed by fresh grep of Scripts/wayland-drive.sh and wayland-virtual-pointer.c -- zero XDND primitive, same as builder's record. | sweep C-CHAT-1, 2026-08-15 |
| `F-CHAT-14` | PASSED | Live restart (real process kill+relaunch against the persisted DB) restored a Chat tab already selected and reconnecting. Turned Follow Edited Files on live, sent a real message, and a real claude CLI Write tool call caused a new file tab to open automatically in the tab strip -- confirmed the file's on-disk content matches. Previously-silent no-op now works, live. | sweep D-P1, 2026-08-15 |
| `F-CHAT-15` | PASSED | Live: real mode dropdown opened from an authenticated claude-agent-acp session post-turn (6 real modes, frame 03-after-pill-click.png); pre-turn pill was plain non-clickable idle/connecting text. Fixture test still passes. | sweep C-CHAT-1, 2026-08-15 |
| `F-CHAT-16` | PASSED | Clicked model pill (search + list + Recommended badge render); typed son (narrows to Sonnet + Opus Plan Mode, badge gone); typed nonsense (No models match renders); 20x Backspace restores full six-row list and badge. surface.chat.read's composerText stayed empty the entire time, confirming the search query never touched the draft. Screenshots f16-01 through f16-04. | sweep B2-chat, 2026-08-14 |
| `F-CHAT-17` | PASSED | **live (P92)**: the chip is an inert agent-badge pre-turn (two clicks no-op, p92-j1/j1b — the `has_completed_turn && !available_models.is_empty()` gate seen live); after the first completed turn it upgrades to the picker chip with host-supplied effort (p92-i3: "Model Opus Plan Mode XHIGH ⌄"); picker offers all six effort choices (Default/Low/Medium/High/Xhigh/Max, p92-r1b) and the chip label tracked four transitions XHIGH→HIGH→MAX→XHIGH (p92-r2/r5/chip-crop); 3 of 6 values driven, the rest share the same `select_effort` handler; effort survived a New Conversation reset. Note: the "Max" choice sits at the panel's clipped right edge — clicking it also triggers mouse-down-out and closes the picker | pass 19 (P92 live drive) |
| `F-CHAT-18` | PASSED | Sent a real one-line turn to the real claude CLI, then clicked the context ring: popover shows 5% of context used / 53477/1000000 tokens / Cost $0.15, then Input: 2 tokens / Output: 4 tokens / Cache read: 30418 tokens. A control click before any turn read 'The agent has not reported context usage yet', proving the popover distinguishes real states. Closes the builder's own caveat that live claude support was unconfirmed. | sweep D-P1, 2026-08-15 |
| `F-CHAT-19` | PASSED | combination evidence (P92): **the host-supplied half is live** — the ring rendered real `ContextUsage` events from two different agents (claude: 0%→23% mid-turn→5%→4% after /compact, frames p92-h1b/i1/i3/o3; codex: 0%→7%, p92-t2b/t3b), so the number the 80% threshold compares against is genuinely supplied and updates live (not the F-SET-22 shape); the >80% warning colour itself stays on the drawn `context_ring_warns_above_eighty_percent` (pass 14) — filling a real session past 80% was not driven (cost-prohibitive, the threshold arithmetic is pure) | pass 19 (P92 live drive) |
| `F-CHAT-20` | PASSED | Own drive of the ticker fixture: away and stillaway frames are byte-identical MD5 (LINE 0001-0031 both times) despite ~4s of continued streaming underneath, proving decouple; back (LINE 0089-0121) and repinned (LINE 0112-0144, a strictly later window) have different MD5s, proving re-pin tracks the live tail forward. Both halves independently reproduced byte-for-byte with my own scroll timing. | sweep H6-instruments, 2026-08-16 |
| `F-CHAT-21` | UNREACHABLE | UNREACHABLE: depends on a rendered Thought entry; transcript rendered nothing for any turn attempted in this report (F-CHAT-02). No disclosure control was ever reached. | sweep A1-P109, 2026-08-14 |
| `F-CHAT-22` | UNREACHABLE | UNREACHABLE: no grouped-step or older-turn entry was ever rendered in any send driven (plain send empty per F-CHAT-02; tool-call send stalled pre-completion per F-CHAT-23). | sweep A1-P109, 2026-08-14 |
| `F-CHAT-23` | UNREACHABLE | UNREACHABLE: write-tool send produced a stuck 'Question waiting' banner — Show/body-click/Stop all no-op across 3 isolated tries; fs confirms write never ran. No completed tool row ever produced. shots/71,77. | sweep A1-P109, 2026-08-14 |
| `F-CHAT-24` | PASSED | PASSED (was NOT EXERCISED). Live: Plan card renders the 'Ready to code?' approval attached to its own bottom (not a separate card), and resolving one option ('auto-accept edits', per pixel/resolved-text check -- differs from the driver's stated 'manually approve edits') both updates that card in place and lets the plan advance to a fresh Write tool card with its own pending permission. Mechanism proven live; only 1 of 4 decision options trialed (shares one generic handler, chat.rs:2185, so this is non-discriminating for correctness, per F-CHAT-17 precedent). | sweep E03-chat, 2026-08-14 |
| `F-CHAT-25` | UNREACHABLE | Live turn (critic7): asked installed Claude Code CLI to call AskUserQuestion directly under Manual mode; agent replied it has no such tool in its tool list/deferred-tool registry (frame 07-05-after-send1.png). Confirms the CLI-side blocker directly rather than by absence. Code path (chat.rs Entry::Permission, tiller_acp parse_permission_question) verified present and already proven live for the plain-gate variant (F-CHAT-26). Pi ui/select alternate route not attempted this pass (time). | sweep C-CHAT-2, 2026-08-15 |
| `F-CHAT-26` | PASSED | Own live drive (critic3-6): mode picker opened on click at (530,864)@1715px (frame 05-03-after-click-530-864.png), contradicting the report's blocked claim; selected Manual (06-04-manual-selected.png); sent a real Write-tool turn; live pending-permission card with Deny/Allow Once/Always Allow rendered (critic6shots/08-06-pending.png), Question-waiting bar and Stop control visible. Same action under Auto mode elsewhere in this pass produced no permission card. | sweep C-CHAT-2, 2026-08-15 |
| `F-CHAT-27` | PASSED | Own live drive (critic6): surface.chat.stop called against a genuinely pending permission; surface.chat.read shows status pending to expired and turn cancelled; frame 09-07-after-stop.png shows No answer -- the turn ended replacing the button row, matching chat.rs:4194 verbatim. Full live proof of both data model and render. | sweep C-CHAT-2, 2026-08-15 |
| `F-CHAT-28` | PASSED | Opened builder's own frames directly: f28c3shots/06-04-turn75s.png (collapsed) vs 07-05-expand-clicked.png (expanded, nested Execute pwd - Completed row visible). Both frames genuine and match the claim; no discrepancy found. | sweep C-CHAT-2, 2026-08-15 |
| `F-CHAT-29` | PASSED | Builder's cited path (f29c1shots/) does not contain the claimed proof frame -- verified by listing the directory (stops at 06-04-turn60s.png). Located the real proof at /tmp/f29manual/02-assistantcopy-clicked.png (same session, later timestamp): hover Copy pill reads Copied. Opened and confirmed directly before crediting. | sweep C-CHAT-2, 2026-08-15 |
| `F-CHAT-30` | PASSED | Located and opened /tmp/f29manual/01-codecopy-clicked.png directly (not at the report's cited f29c1shots path): code-block Copy button mid-transition to Copied with cursor over it. Confirms the control live; report's directory citation for this row was also wrong but the underlying claim holds. | sweep C-CHAT-2, 2026-08-15 |
| `F-CHAT-31` | UNREACHABLE | UNREACHABLE: same stuck approval-gate send as F-CHAT-23 — no diff/tool result was ever produced before the flow stalled. Same shots/71,77. | sweep A1-P109, 2026-08-14 |
| `F-CHAT-32` | PASSED | Opened builder's frames (Revert to Confirm Revert/Cancel to reverted) and independently ran git status --porcelain plus ls docs/scratch/ on the actual worktree: directory does not exist, confirming real deletion from disk, not just a chat-transcript label change. | sweep C-CHAT-2, 2026-08-15 |
| `F-CHAT-33` | UNREACHABLE | Reproduced the builder's regression test live (11.63s, real network agent). Additionally wrote and ran my own from-scratch JSON-RPC driver against npx claude-agent-acp with a fresh unapproved scratch repo and broken .mcp.json: continuous stderr pumping showed zero lines across the whole exchange, while the agent's own Bash tool call (claude mcp list) surfaced the broken-server as Pending approval via session/update chat content -- confirming both the trust-gate silence and the wrong-channel finding independently. Codex's ACP adapter (untested this pass) is the only remaining candidate agent; opencode has no ACP path (acp_program() is None). | sweep I4-settle, 2026-08-16 |
| `F-CHAT-34` | PASSED | Real Claude agent turn completed, ctl tab.select fired schedule_save, app killed and restarted twice against the same sqlite DB file; Chat History popover (not just the live transcript) listed the session 'Chat' with a Delete affordance instead of 'No past chats'. Instrument: real process restarts, Scripts/wayland-drive.sh, /tmp/ec1g-shots/04-after-history-click.png. | sweep E-C-1, 2026-08-16 |
| `F-CHAT-35` | PASSED | Live: a fresh chat tab with zero chat_turn rows (confirmed via the sqlite file) shows the Chat History popover with exactly 'No past chats', matching the Swift original's string. | sweep D-MAIN-1, 2026-08-15 |
| `F-CHAT-36` | PASSED | recharacterized live (P92): the badge is real and inert as the **pre-turn** rendering for both real agents (claude "Opus Plan Mode" p92-h1b, codex "GPT-5.6-Luna" p92-t2b — clicks no-op); but the clause's trigger — an agent that **exposes no model list** — is unreachable with the current roster: after one completed turn BOTH agents upgraded to a working picker (claude 6 models + effort p92-r1b; codex 6 GPT models, no effort section, p92-t4). The no-models-after-turn state therefore stays on the drawn `no_models_fallback_shows_a_plain_agent_badge` (pass 14); both branches of the gate (`has_completed_turn && !available_models.is_empty()`) were observed live from both sides | pass 19 (P92 live drive) |
| `F-CHAT-37` | PASSED | empty transcript + usable composer | pass 1 |

### Files, changes, activity (22)

| id | verdict | evidence | judged |
|---|---|---|---|
| `F-CHG-01` | PASSED | PASSED (was FAILED — absent, reclassified): live tab.select walk (4 captures, wavea-W10-chg) shows Chat/Terminal/Changes as 3 visibly distinct peer tabs in one strip; right_panel.rs re-grepped independently — Files header hardcodes "Files" (line 641), no toggle/enum switches it to a Changes mode anywhere in the file. Architecture pivot to a peer-tab model confirmed live + in code. | sweep W10-chg, 2026-08-14 |
| `F-CHG-02` | PASSED | Superseded by integrator commit fb86099 (landed after the builder's blocked report), still present at HEAD. Live: surface.changes.open on a clean worktree renders a centered No changes message (id changes-empty), not a blank panel (chg-shots 04-c-changes-open.png). | sweep D-MAIN-2, 2026-08-15 |
| `F-CHG-03` | PASSED | Wayland lane (label cp4f), one continuous session: chmod 000 -> 'Files unavailable: Permission denied (os error 13)' (03-permission-denied.png); chmod 755 + sleep 3, zero clicks -> panel repopulated with note.txt/README.md unprompted (04-self-healed.png). Confirms fix. | sweep C-P4, 2026-08-15 |
| `F-CHG-04` | PASSED | drawn clicking_a_drawn_directory_row_expands_and_collapses_it green (real mouse clicks; collapse assert run_until_parked-hardened) + double_clicking_a_drawn_file_row_emits_open_file green | pass 11 |
| `F-CHG-05` | PASSED | PASSED (was FAILED — defective, reclassified): sighted confirmation the driver's own text-only evidence could not supply -- post-Enter capture shows a genuine new editor tab 'untracked.txt' with real file content rendered (numbered source, plain-text badge), not just a generic large repaint. Zero-floor control re-verified AE=0; click-to-open diff re-verified AE=154,544. | sweep W10-chg, 2026-08-14 |
| `F-CHG-06` | PASSED | PASSED: sighted crop of chg06-before/after-1715.png confirms color-coded status icon per section (green/Staged, amber/Changed, blue/Untracked, same glyph shape) and untracked.txt's icon flips blue->green live when the app's own 1s refresh reclassifies it after an external git add -- both previously-unverified halves (glyph mapping, explicit refresh) now directly observed. Files-tree panel dots are uniform amber and do not carry a status signal. | sweep E08-chg, 2026-08-14 |
| `F-CHG-07` | PASSED | clean/empty tree 0/0/0 ready=true matches porcelain | pass 5 |
| `F-CHG-08` | PASSED | sections+counts match porcelain through MM/RM/UU/binary | pass 5 |
| `F-CHG-09` | PASSED | drawn error/Retry click recovers from removed .git; green ×3 | pass 7 |
| `F-CHG-10` | PASSED | drawn stage/unstage mutates real checkout; green ×3 (pass 8) | pass 8 |
| `F-CHG-11` | PASSED | PASSED: sighted read confirms chg11-after-stageall.png shows Staged(3) with all three files; chg11-after-discardall.png shows only Untracked(2) remaining (changed.txt/staged.txt cleared, untracked.txt/untracked2.txt intact) -- matches git restore --worktree semantics exactly. All three bulk controls (stage_all, unstage_all, discard_all) now proven live. | sweep E08-chg, 2026-08-14 |
| `F-CHG-12` | PASSED | drawn section/row/context-band clicks + diff data tier | pass 7 |
| `F-CHG-13` | PASSED | Live click on the Files-panel Diff affordance for a real modified file opened the Changes tab with that file's diff already expanded (real hunk shown) while the other file stayed collapsed, discriminating against wave-D's neither-expanded evidence from the identical gesture. Instrument: Scripts/wayland-drive.sh, /tmp/ec1d-shots/03-after-diff-click3.png. | sweep E-C-1, 2026-08-16 |
| `F-CHG-14` | PASSED | drawn discard + confirmation; green ×3 (pass 8) | pass 8 |
| `F-CHG-15` | PASSED | binary counts match numstat (pass 5); unavailable/retry halves now built and green: broken-repo retry recovers, error state renders + Retry clickable, diff-unavailable row test | pass 11 |
| `F-CHG-16` | PASSED | Created a genuine UU merge conflict in a throwaway repo (two branches editing the same line, merged mid-conflict). The Changes tab shows the conflicted file with conflict-red styling; expanding the row shows the real conflict-marker diff plus a Resolve in terminal action. Clicking it opened and focused a new terminal tab showing diff --cc output for the exact conflicted file and a live shell prompt, independently reproducing the wiring described by the prior evidence against a conflict I created myself. | sweep F1-editor, 2026-08-16 |
| `F-CHG-17` | PASSED | numbered lines + hunk headers match real file (data tier) | pass 5 |
| `F-CHG-18` | PASSED | Live, single invocation: opened Changes, right-clicked the Terminal tab, clicked Move to New Pane (fired, split created), dragged the changed-file row onto the new terminal pane — next capture shows a Dropped diff: SCRATCH_DRAG_TEST.md pill on the breadcrumb. A first same-invocation attempt with zero settle between rightclick and click failed to fire (menu stayed open, item only hover-highlighted) — reproduced and diagnosed as a synthetic double-IPC race faster than any human gesture, not a product defect, once a repaint settle was inserted between the two and the identical click then fired correctly. | sweep H2-ctxmenu, 2026-08-16 |
| `F-CHG-19` | PASSED | **live (P92)**: Activity section expanded (frames p92-d1/f1), rows list the open worktree's chat/terminal/file tabs, clicking a row switched the active tab (p92-d2), collapse works (p92-f2/f3); "every open worktree" resolved by ps probe — exactly one bash child at all times, single-open-worktree model, so the section trivially covers all open worktrees (model caveat). Earlier drawn: SelectActivity/CloseActivity emit (pass 11) | pass 19 (P92 live drive) |
| `F-CHG-20` | PASSED | Wayland lane (label cp4e): closed both default tabs to reach a genuine zero-tab 'No Terminals' worktree (not the app default), then expanded Activity via header click. 'No activity' is fully legible. Frame /tmp/cp4-shots/07-s6-final.png. Confirms fix. | sweep C-P4, 2026-08-15 |
| `F-CHG-21` | PASSED | **live (P92)**: the code-verified host half now ran — activity row × clicked in the real app closed the tab: strip, sidebar and Activity all dropped it (frames p92-d3/d4; d3 was paint-lagged, d4 proves the close landed) | pass 19 (P92 live drive) |
| `F-CHG-22` | PASSED | Reproduced live twice: shot-then-click-then-shot in one wayland-drive.sh invocation (no intervening shot) reliably expands the Activity header -- chevron flips and real Chat/Terminal/Changes rows render. Root cause of prior misses (wave-D's and my own first two clicks) is a lane hazard: shot alternates output resolution, invalidating coordinates from a separate invocation or a click separated from its confirming shot by another shot -- not an app defect. Frames /tmp/critic-ep1-chg22/02-07-fresh-before.png and 03-08-fresh-after.png. | sweep E-P1, 2026-08-16 |

### Documents and editors (13)

| id | verdict | evidence | judged |
|---|---|---|---|
| `F-EDIT-01` | PASSED | regression held on a live pass-18 drive (fresh fixture DB): double-clicking `aaa-f09-scratch.md` in the Files panel (1380,840) opened rendered Preview — styled H1, no line numbers (`f09-d1.png`); `Code` (1029,103) switched to numbered raw source (`f09-d2.png`); Preview re-entered live through the large-file `Render preview` control (`f09-g3.png`). Supersedes orch7-*, which stands | fable drive, 2026-08-14, pass 18 |
| `F-EDIT-02` | PASSED | Live double-click-word then Bold/Italic: **wordtobold** / *example* wrapped exactly the touched word, not whole line/EOF -- reverses P104 evidence. (Ctrl+Z/undo unimplemented anywhere in editor -- separate, out-of-scope gap, noted not blocking.) | sweep B7-editor-persist, 2026-08-14 |
| `F-EDIT-03` | PASSED | the pass-3 row was stale — the manual-preview state now exists and its control works. A 396021-byte `aaa-f09-large.md` (threshold `MARKDOWN_PREVIEW_THRESHOLD` = 256 KiB, editor.rs:83) opens as source with its full path in the header and a `Large file — manual preview` bar carrying `Render preview` (`f09-g1.png`); clicking it (1170,203) rendered the real preview — styled H1, bold resolved, `Preview` pill active, bar gone (`f09-g3.png`). Replay caution: `f09-g2.png` at +4 s still showed source — rendering 396 KB takes seconds (second-capture rule). Drawn test: `a_large_markdown_file_opens_in_code_with_manual_preview_and_unlocks` (file_view.rs:1671) | fable drive, 2026-08-14, pass 18 |
| `F-EDIT-04` | PASSED | the character path itself is now exercised (pass-14 evidence proved only the chord wiring, and until tonight the editor accepted five gestures while typing did nothing — the exact trap FABLE-09 named). Live, per-gesture, each step `ctrl-s`'d and `cat`'d from disk: typed `QWJX17` reached disk only after `ctrl-s` (`f09-d-predisk.txt` lacks it, `f09-d-postdisk.txt` has it); clicking inside a line (560,269) visibly selects the whole line (`select_source_line`, `f09-e2.png`) and typing replaces it (`REPLACEME selection target` → `ZK`, `f09-e1-clicktype.txt`); `Home` collapses to a caret and typing inserts (`ZK` → `ABZK`); `Return` splits the line without submitting (`AB`/`CDZK`, `f09-e2-enter.txt`); `BackSpace` deletes one char (`CDZK` → `CZK`), `Delete` forward-deletes (`CZK` → `ZK`), `Delete` across a `shift+End` selection empties the line (`f09-e3/e4/e5-*.txt`); dirty dot appears on edit, clears on save (`f09-d4.png`/`f09-d5.png`). Retention conjunct: drive E's saved `AB`/`**TYPED-OVER**` were still the content when drives F and H relaunched the app and reopened the file (`f09-h-recreated.txt`, `f09-h2.png`) — retained across full app restarts, which subsumes close/reopen | fable drive, 2026-08-14, pass 18 |
| `F-EDIT-05` | PASSED | Full conflict cycle driven live, verified by buffer content not just clicks. Dirty tab + external append produced exact banner This file changed on disk. with Reload/Keep; clean-tab control reloaded silently with no banner, proving the dirty-state gate. Keep: local edit preserved, disk change not pulled in. Reload: buffer replaced with disk content incl. external line, edit discarded. Both clause halves closed live. | sweep E09-prj+edit, 2026-08-14 |
| `F-EDIT-06` | PASSED | re-anchored to the row's actual clause — the orch11 evidence proved plain save, which is `F-EDIT-04`'s clause, not this one (delete externally → edit → save recreates). Driven: file open in Code, `rm aaa-f09-scratch.md` from the Terminal tab, refocus editor (600,430) → `This file was deleted. Saving will recreate it.` banner with no Reload/Keep (the DeletedOnDisk variant — nothing to reload; `f09-h1.png`, where the Files panel row is gone too); clicked line 3 (560,292), typed `RECREATED-BY-SAVE`, `ctrl-s` → the file exists again on disk, 109 bytes, typed line at line 3 (`f09-h-recreated.txt`: `ls -la` + `cat`); banner cleared and the Files row returned (`f09-h2.png`) | fable drive, 2026-08-14, pass 18 |
| `F-EDIT-07` | PASSED | Independently reproduced the builder's route: README.md with a fenced bash block, opened in Preview, 4x zoomed crop shows the comment in dim gray, echo in plain white, and the "hello world" string in green — three distinct colors, confirming the prior flat-color defect is fixed. | sweep G4-chat, 2026-08-16 |
| `F-EDIT-08` | PASSED | Fresh independent live drive: two rapid clicks on the CLAUDE.md Files-panel row opened one distinct tab with real rendered file content; a second double-click on the same row left exactly one CLAUDE.md tab, no duplicate. Reproduces the builder's claim end to end with a new capture, not their screenshots. | sweep E-C-3, 2026-08-16 |
| `F-EDIT-09` | PASSED | **live (P92)**: the code-verified host half now ran — double-click on alpha.txt in the Files panel (raw `xdotool click --repeat 2 --delay 180`) opened an editor tab: path bar, plain-text badge and buffer visible, tab active (frame p92-d5) | pass 19 (P92 live drive) |
| `F-EDIT-10` | PASSED | First attempt gave a false FAILED because click coordinates were read from a screenshot at one output resolution and sent while the live frame was at another (wayland-drive.sh alternates resolution every shot() call) — the right-click landed in empty space, not the file row. Redone with resolution-matched coordinates: right-click on README.md draws a fully legible Open / Reveal in File Manager / Copy Path menu anchored at the pointer, and clicking Open genuinely opens the file in the editor tab. | sweep F1-editor, 2026-08-16 |
| `F-EDIT-11` | PASSED | Same calibration bug as F-EDIT-10 initially produced a false negative; also xclip cannot read this app's Wayland clipboard at all (it goes through smithay-clipboard, not X11 selections), and a first custom reader using core wl_data_device got nothing because that protocol only delivers selection events to a focused client. Rebuilt the reader on wlroots' data-control protocol (same one wl-paste uses) and, after clicking Copy Path at correctly calibrated coordinates, read back the exact absolute path of the file actually clicked. | sweep F1-editor, 2026-08-16 |
| `F-EDIT-12` | PASSED | Lane gained `drag` since last sweep. Added a throwaway README.md diff, split Changes into its own pane beside a real running Terminal via the app's own Move to New Pane, waited for the shell prompt, then dragged the README.md change row onto the terminal. Terminal showed a real toast 'Dropped diff: README.md' -- production on_drop::<(PathBuf,String)> at tiller_terminal/src/lib.rs:1551, not the test-only fixture. README.md reverted with git checkout after capture. | sweep D-P2, 2026-08-15 |
| `F-EDIT-13` | PASSED | drawn a_missing_file_tab_reports_the_specific_state green (missing path input shows the 'does not exist' message); unreadable distinct message + binary/too-large states tested at editor model | pass 11 |

### Persistence and lifecycle (8)

| id | verdict | evidence | judged |
|---|---|---|---|
| `F-PER-01` | PASSED | Headless-lane restart with a real ACP fixture backend: completed a turn (chat_turn/session_ref each went from 0 to 1 row in DB), killed the process fully, fresh process against same DB restored the full transcript via surface.chat.read with zero further calls, then a second turn on the restored tab grew chat_turn to 2 rows. | sweep D-MAIN-4, 2026-08-15 |
| `F-PER-02` | PASSED | select-workspace → DB write → relaunch → selection restored | pass 3 |
| `F-PER-03` | PASSED | 2 splits persist across quit/relaunch (pane-1/2/3 present) | pass 4 |
| `F-PER-04` | PASSED | pass 11 live end-to-end: terminal output captured into the session DB on quit and replayed on relaunch — the restored pane scrollback showed the previous session's 15:54 prompt inside a 15:56 session; replay test green | pass 11 |
| `F-PER-05` | PASSED | session.restore restoredCount 2 + worktree re-selected | pass 3 |
| `F-PER-06` | PASSED | Confirmed 915b0d0 present at HEAD, not reverted. Live process-tree test: spawned real job-control `sleep &` (own pgid 81691, distinct from shell pgid 80506) in a terminal pane, closed the tab via the app's Close-dirty-tab dialog, host ps confirmed the process fully terminated afterward. | sweep B7-editor-persist, 2026-08-14 |
| `F-PER-07` | PASSED | Full live round trip: display name + icon changed, sidebar updated immediately on sheet close; quit + relaunch (self-exit then clean relaunch on same DB); sidebar read the new name+icon with zero further interaction. shots/231,239. | sweep A1-P109, 2026-08-14 |
| `F-PER-08` | PASSED | Full restart roundtrip: grant written and confirmed in DB, process killed fully, fresh process against same DB navigated straight through (no doorhanger) to the previously-granted origin while a never-granted origin in the same restarted process correctly re-triggered the permission request -- positive and negative control both pass. | sweep D-MAIN-4, 2026-08-15 |

### Browser (9)

| id | verdict | evidence | judged |
|---|---|---|---|
| `F-BRW-01` | PASSED | Live X11 re-check, two independent fresh launches: ImageMagick pixel scan of the captured screenshot shows near-white webview content spanning x=386..1235 (850px, zero gaps) at both y=400 and y=850/500, and y=133..924 vertically at x=800 -- flush with the toolbar chrome span (x=386..1236) instead of the old shrunk x=331..1060. cargo test -p tiller_ui browser:: 12/12 pass. | sweep G1-browser, 2026-08-16 |
| `F-BRW-02` | PASSED | Clicked in-page 'Learn more' link (navigated to iana.org/help/example-domains), then clicked '<' back button (417,104); within 2s both address bar and rendered page content reverted to https://example.com/ / Example Domain. cx.notify() fix confirmed working. | sweep B6-brw-bar, 2026-08-14 |
| `F-BRW-03` | PASSED | X11 lane: click address field, key ctrl+a, type 'https://claude.ai/test-cp4-marker' via linux-drive.sh - field shows exactly that string, caret at end, zero corruption. Frame 03-addr-typed.png. Confirms already-correct. | sweep C-P4, 2026-08-15 |
| `F-BRW-04` | PASSED | Live: browser.open url=https:// -> ok:false with a real validation error; browser.navigate to a dead host -> ok:false with 'Could not reach ... Connessione rifiutata', identical text rendered in the Browser tab's red banner. Pre-fix default was silent ok:true. | sweep D-MAIN-1, 2026-08-15 |
| `F-BRW-05` | PASSED | Live: browser.act driving=true renders an 'Agent driving' pill next to Stop; driving=false clears it in the next frame. No pill exists by default in any other captured frame. | sweep D-MAIN-1, 2026-08-15 |
| `F-BRW-06` | UNREACHABLE | Independently re-confirmed by grep: request_permission (browser.rs:588/880) and the doorhanger render (:1242) exist and are complete, but the only caller anywhere in rust/ is the unit test at :1666. Zero production triggers. Same zero-app-callers pattern graded UNREACHABLE elsewhere in the ledger (F-TERM-02, F-CORE-ACT-17/18, F-GIT-STATUS-02). | sweep E06-brw, 2026-08-14 |
| `F-BRW-07` | PASSED | Live, single unbroken instance: driving=true + navigate to a new origin returns permission:requested with a rendered Allow/Deny doorhanger; a real synthetic click on Allow dismissed it; the identical navigate call repeated immediately after proceeded past the gate (DNS-failure on the fictitious domain, not permission:requested again), proving the grant took effect. | sweep D-MAIN-1, 2026-08-15 |
| `F-BRW-08` | PASSED | PASSED (was half-proven). Seeded-state and empty-state both shown in captures; DB re-query independently confirms 2->0 rows. Source confirms Revoke-all (settings.rs:3052-3057) and per-origin Revoke are structurally distinct handlers, so one click clearing both rows can only be Revoke-all. Resolution changes between captures are wayland-drive.sh's documented repaint-forcing alternation, not a methodology flaw. | sweep W11-ctrl+brw+sid, 2026-08-14 |
| `F-BRW-09` | PASSED | Wrote a minimal real ACP-protocol script (scratchpad only, not committed) to get an assistant-authored markdown link since neither the shipped fixture nor compose-posted text produce one. Live: plain click on the rendered link opened a new internal Browser tab at the exact URL; Shift+Logo (platform on Linux) held click on a second such link opened no internal tab. External-browser-open half not independently observable in this sandboxed lane (no xdg portal); corroborated by source read of the exhaustive if/else. | sweep D-MAIN-1, 2026-08-15 |

### Status bar / usage (6)

| id | verdict | evidence | judged |
|---|---|---|---|
| `F-USE-01` | PASSED | Refresh icon now present next to settings gear (was absent pre-fix). Settled state captured, click at (67,901) captured immediately showed both segments flip to loading state with pressed/hover styling in the very next frame; after 10s both segments repopulated with the same real values as the settled baseline. | sweep B6-brw-bar, 2026-08-14 |
| `F-USE-02` | PASSED | Live hover (real synthetic wl_pointer) over the Unavailable 'Codex token expired' segment produced a tooltip reading the identical text. Screenshot 03-hover-attempt.png. Upgraded half-proven to PASSED. | sweep C-P2, 2026-08-15 |
| `F-USE-03` | PASSED | Live positive/negative control: TILLER_USAGE_CLAUDE_TIMEOUT_MS=1200 renders Claude timed out dimmed grey; unset env, same route, renders a real successful fetch (Claude 4% 5h - 3% wk). Same coordinates, only the env var differs, both captured by this critic. | sweep H6-instruments, 2026-08-16 |
| `F-USE-04` | PASSED | Independent live drive against this machine's real StatusNotifierWatcher: a fresh tiller process (isolated TILLER_DB/TILLER_SOCKET) registered org.kde.StatusNotifierItem-<pid>-1; busctl GetLayout returned 'No active agents'+'Quit Tiller' when empty, then 'master — repo1 (needs input)' after a real project.add/panel.create/notify over the control socket. Reproduced with a different scratch repo and process than the builder used. | sweep H3-tray, 2026-08-16 |
| `F-USE-05` | PASSED | Independently reproduced live with fresh scratch repos: forced Chat frontmost, notified pane-1 needs-input, switched away to repo2 (screenshot confirms the single-flat-tab-list constraint -- tabs stay the same, just relabeled), then ctl tray.jump workspace=repo1 replied jumped:true tabId:1 tabTitle:Terminal and panel.list/screenshot confirmed Terminal flipped active:true while Chat flipped active:false. Read main.rs myself and confirmed the real D-Bus tray click arm calls the identical select_worktree_and_jump function tray.jump drives, so this proves the production path, not a stand-in; also confirmed the ledger already accepts the same single-open-worktree model caveat under F-CHG-19's PASSED verdict. | sweep I3-tray-jump, 2026-08-16 |
| `F-USE-06` | PASSED | Ran cargo test -p tiller restore_tabs_registers_restored_agent_identity myself: passes. git show a5d09d7 confirms register_restored_agent's wiring landed 2026-08-14, before Wave D — the builder's already-correct claim checks out against real commit history, not just prose. | sweep D-MAIN-7, 2026-08-15 |

### Control socket automation (9)

| id | verdict | evidence | judged |
|---|---|---|---|
| `F-AUTO-01` | PASSED | drawn toggle + resolved socket path (pass 7); pass 11: controller test green — enable listens (connect ok), disable refuses connections, re-enable listens again | pass 11 |
| `F-AUTO-02` | PASSED | panel.create live | pass 3 |
| `F-AUTO-03` | PASSED | panel split/list/write/key/read/wait/focus/close live | pass 3 |
| `F-AUTO-04` | PASSED | notify + worktree.set + session.ref live | pass 3 |
| `F-AUTO-05` | PASSED | workspace list/current/select/create/close live | pass 3 |
| `F-AUTO-06` | PASSED | PASSED 2026-08-14: live dbus-monitor capture during notification.create shows a real Notify(Tiller, W12test, hello) call; list/clear round-trip confirmed. record_notification (main.rs:717-744) calls notification_poster unconditionally. Fix commit a5d09d7 confirmed ancestor of HEAD. Ledger's prior 'empty dbus-monitor' basis is stale. | sweep W12-auto+edit+use+tab, 2026-08-14 |
| `F-AUTO-07` | PASSED | ping/identify/capabilities live | pass 3 |
| `F-AUTO-08` | PASSED | session.restore restoredCount 2 | pass 3 |
| `F-AUTO-09` | PASSED | PASSED 2026-08-14: live probe of 8/10 browser.* methods -- browser.open returns a real surface/url result, the other 7 return explicit typed unsupported errors (matches browser_request_error, main.rs:201-236). Fix commit 988d9e9 confirmed ancestor of HEAD. Ledger's prior bare-ok:true/queued basis is stale. | sweep W12-auto+edit+use+tab, 2026-08-14 |

### Settings (25)

| id | verdict | evidence | judged |
|---|---|---|---|
| `F-SET-01` | PASSED | categories render; live surface.settings.select | pass 8 |
| `F-SET-02` | PASSED | live, pass 15: Settings opened via the control socket (the status-bar gear of the recipe does not exist — on_settings is never wired to a rendered element, status_bar.rs), then Escape closed the surface and returned the main UI (634k px delta, frame pass15/j3-02). Caveat: the workspace-level drawn test `escape_closes_the_settings_surface` still fails in the tiller bin (the palette-test harness cluster — owner codex12), while the live feature works | pass 15 |
| `F-SET-03` | PASSED | reasoning re-checked (P92): the surface claim holds — "Check for Updates" is still not drawn and the absence test `general_settings_state_the_version_and_no_dead_controls` asserts it (settings.rs:3465); all "Sparkle" grep hits are the Sparkles icon glyph, no updater framework or channel code. **Stale sub-claim corrected**: "zero updater code in the tree" is no longer true — `tiller_project/src/ui.rs` now carries a transport-less `UpdateState`/`UpdateEvent` state machine ("independent of whichever Linux update transport is eventually selected") with zero consumers outside its own crate; scaffolding, not a control, so the removed-by-design verdict stands with the scaffold named | pass 19 (P92 re-check) |
| `F-SET-04` | PASSED | cargo test -p tiller restored_agent_shell_resumes_only_when_a_session_ref_is_supplied passes; independently re-read both restore call sites in main.rs (~4330, ~9513) — gate unconditional, empty BTreeMap passed when resume_agent_sessions is off, matching what the test proves is safe. | sweep E-C-3, 2026-08-16 |
| `F-SET-05` | PASSED | Both halves closed: UI via green drawn test + independently-viewed d1c/d1d screenshots showing genuine picker interaction; DB via this session's relaunch plus an independently-viewed earlier relaunch (d1e), both matching claimed values. Auto-rename execution itself still has no consumer (separate rows, already absent) — out of this row's scope | sweep E01-set, 2026-08-14 |
| `F-SET-06` | PASSED | Zero-unlimited clause vacuous (stepper floor is 5, confirmed in model.rs). DB half closed: this session's relaunch plus an independently-viewed earlier relaunch (d1e) both show 17 surviving restart. Trimming enforcement itself still has no consumer (noted, out of scope) | sweep E01-set, 2026-08-14 |
| `F-SET-07` | PASSED | Toggle/stepper/persistence closed live: this session's relaunch plus an independently-viewed earlier relaunch (d1e) both show 12/true surviving restart. Eviction behavior remains FAILED — absent per F-CORE-ACT-26 (already its own row, per this row's own prior verdict text) | sweep E01-set, 2026-08-14 |
| `F-SET-08` | PASSED | reasoning re-checked (P92): "no install mechanism exists" holds — the only .app-bundle path in the tree is a **test fixture** (session.rs:2045 constructs a fake `Applications/Tiller.app` path to prove installed binaries keep the stable DB path, not an installer); absence test `general_settings_state_the_version_and_no_dead_controls` asserts "Copy install command is not drawn" (settings.rs:3469); socket-toggle half keeps its pass-11 live evidence | pass 19 (P92 re-check) |
| `F-SET-09` | PASSED | Live click on general-install-skill in Settings->General rendered the 'Installing... running in a new terminal tab.' confirmation line immediately in the next forced-repaint capture. | sweep D-MAIN-5, 2026-08-15 |
| `F-SET-10` | PASSED | cargo test -p tiller_ui refresh_click_flips_every_provider_to_loading_before_the_fetch_runs passes; read it — asserts settled != Loading before the click (defeats accidental pass), then reads state synchronously after on_refresh_clicked before run_until_parked lets the fetch run. | sweep E-C-3, 2026-08-16 |
| `F-SET-11` | PASSED | Reran cargo test -p tiller_usage --test usage_tests: 10/10 pass. Read the 3 new tests + fetch_with_env: each spawns a real Pty running bash --noprofile --norc -c claude against an isolated PATH with dotfiles skipped, not a transcript stand-in. Three distinct real subprocess setups (missing binary, fake login-prompt script, fake error script) each drive a distinct correct Unavailable(reason). | sweep D-P2, 2026-08-15 |
| `F-SET-12` | PASSED | Live re-drive (isolated TILLER_CREDENTIALS): Save writes cookie verbatim, status flips signed-in, usage-bar toggle auto-ON, field clears; Clear empties store and flips back; Workspace ID override survives navigation and a fresh-process relaunch. | sweep B4-settings, 2026-08-14 |
| `F-SET-13` | PASSED | Full VERIFY closed: this pass's live click+type+click reached Save -> Signed in (11/12). On-record P111 captures (independently re-viewed) show Clear -> Not signed in, and a real Refresh-now click hitting ollama.com live and landing on status-bar text 'Ollama Cloud error' with a fake cookie (p111-set13-bar-after-refresh), plus fresh-process relaunch persistence. All three named states (not-configured/configured/error) directly observed. | sweep W05-set, 2026-08-14 |
| `F-SET-14` | PASSED | Live: Codex Add Account spawned real detached-session login process (confirmed differing pgid via ps). Cancel click killed it within ~1.6s (ps polling every 0.3s). Clean UI revert, no leftover. PASSED. | sweep C-CHAT-3, 2026-08-15 |
| `F-SET-15` | UNREACHABLE | Reconfirmed live: every provider card shows exactly one hardwired-Active account row across all captured frames; no code path exists for a second row. VERIFY clause unanswerable as designed. UNREACHABLE, same shape as F-SET-21. | sweep C-CHAT-3, 2026-08-15 |
| `F-SET-16` | PASSED | Live: captured 'Refreshed 01:01:08' next to Refresh button, waited, clicked Refresh, captured 'Refreshed 01:01:16' — genuine 8-second advance, not a repeated default. | sweep B4-settings, 2026-08-14 |
| `F-SET-17` | PASSED | Full VERIFY closed across two independently-verified evidence sets: this pass's fresh negative control (isolated 2nd instance, PATH unset, own socket/DB) shows providers:'[]' and the real banner 'Could not load the agent registry: PATH is not set in the environment', Refresh present; positive control (02-15) shows the same code path rendering 5 real non-empty rows. Retry-recovers half independently re-verified this pass by opening the on-record ec53f40 captures directly: p111-set17-registry-error / -recovered-by-retry show a live Refresh click clearing an EACCES-PATH-dir banner and revealing 5 truthful rows. | sweep W05-set, 2026-08-14 |
| `F-SET-18` | PASSED | Forced a genuine not-installed state by excluding nvm from PATH; Agents screen showed OpenCode with a real Install button (Pi/Oh-My-Pi correctly got none). Clicking it showed the confirmation line and panel.list confirmed a real new pane/tab titled 'Install opencode' was created. | sweep D-MAIN-5, 2026-08-15 |
| `F-SET-19` | PASSED | Both halves closed: prior explicit System->Light click (16-fresh3/17-fresh3, on record) plus this pass's System-follows-real-desktop proof, independently re-measured -- flipped host's real COSMIC theme file, confirmed portal ReadOne moved, fresh launch each time (theme:system via socket). Whole-frame grayscale mean (recomputed independently, matches driver's numbers exactly): dark-desktop launches 32.6/33.1 vs known dark baseline 26.7; light-desktop launch 243.8 vs known light baseline 243.9. Genuinely discriminating: same System mode renders two correctly-matched appearances depending solely on live desktop state at launch. Live D-Bus SettingChanged subscription (no re-click) remains untested, flagged as a separate stronger build per triage, not part of this clause. | sweep W06-set, 2026-08-14 |
| `F-SET-20` | PASSED | Static grep confirms main.rs settings_report_pairs now copies translucency (already integrated, commit 71cb625). Live fresh drive: ctl surface.settings.select before a real click on the Translucency toggle returns translucency:false; a click plus the same ctl call after returns translucency:true, in both the top-level result and values array. | sweep D-P2, 2026-08-15 |
| `F-SET-21` | N/A — platform | Live click on the File icons control (single 'Material' button) produced a pixel-identical capture, no menu of any kind. Per census the 2nd option is macOS-only (cfg-gated) — Linux ships exactly 1 theme today, nothing to choose between. Not scored as a defect. shots/90. | sweep A1-P109, 2026-08-14 |
| `F-SET-22` | PASSED | Live real-process restart: picked a non-default coral swatch for Claude Code in Settings->Appearance, killed and relaunched the actual tiller binary against the same DB, reopened Settings and the coral selection (not the tan default) was still selected. | sweep D-MAIN-5, 2026-08-15 |
| `F-SET-23` | N/A — platform | TCC permissions | pass 8 |
| `F-SET-24` | PASSED | Fresh independent live drive (not the builder's): project.add, browser.open, tab.select to bring Browser forward, browser.act driving=true, browser.navigate to an unallowed origin -> reply permission:requested, and a forced-repaint capture shows the real doorhanger 'Allow agent browser access to https://agent-target.example? Allow Deny' plus an Agent driving badge; address bar unchanged confirming navigation was correctly intercepted. | sweep E-C-3, 2026-08-16 |
| `F-SET-25` | N/A — platform | TCC refresh on activate | pass 8 |

### Terminals and agents — app (11)

| id | verdict | evidence | judged |
|---|---|---|---|
| `F-TERM-01` | PASSED | real login shell on pts, breadcrumb | pass 1 |
| `F-TERM-02` | UNREACHABLE | second-critic re-check of P82 (codex11), confirms the 2026-08-14 orchestrator audit still holds: `tiller_terminal/src/lib.rs:1379-1433` renders "No terminal in this pane" plus both real `on_click` actions the clause names, but its only enabling constructor `TerminalView::empty_prompt` (`:637-645`) still has **zero callers anywhere in the workspace** (`grep -rn empty_prompt` across rust/crates) — main.rs never constructs it. SEAMS.md's Open table confirms this is the "No-worktree terminal empty state" seam: Half A (this surface) done by codex11, Half B (the call site) still owned by codex12 and not started. Complete-and-unreachable is not PASSED | critic2, grep evidence + SEAMS.md Open table, 2026-08-14 |
| `F-TERM-03` | PASSED | Live-redrove all four VERIFY cases myself via wayland-drive.sh on a freshly built binary: sleep 30 shows no pill (long-running.png), exit 0 shows pill Process exited successfully + checkmark tab badge (exit0-default-tab.png), exit 7 shows pill Process exited with status 7 + warning badge, reproduced twice (exit7-t0/t2.png), and kill -KILL $$ shows pill Process terminated by signal 9 + signal badge (signal-kill.png). Matches TerminalExitStatus::label() and its lib.rs:1610-1624 bottom-left pill exactly, read by me this pass; one early attempt on a Ctrl+T-created tab under heavy concurrent system load missed the pill, resolved by retrying on the default tab with the same gesture, so I read this as scheduler jitter, not a defect. | sweep F4-terminal, 2026-08-16 |
| `F-TERM-04` | PASSED | X11 lane: click terminal pane, key shift+F10 via linux-drive.sh - full 12-item menu opened (Copy...Close Terminal), no right-click. Frame 07-ctxmenu.png. Confirms already-correct. Separately noted (not graded here): a closed Browser tab's webview keeps painting on top of the window until full app relaunch - out of scope, flag for ledger. | sweep C-P4, 2026-08-15 |
| `F-TERM-05` | PASSED | pass 12: drawn `right_click_resolves_this_terminal_and_draws_all_context_actions` green (tiller_terminal lib.rs:1751) proves delegated typed events; mapping test `terminal_context_app_actions_have_workspace_routes` covers SetTitle; shell routes SetTitle → `set_terminal_title` → `tab.title = "Terminal {id}"` (main.rs:2436-2451); copy-context/clear are terminal-local handlers (lib.rs:718-750) | pass 12 |
| `F-TERM-06` | PASSED | Ran cargo test -p tiller_terminal copy_pane_id_then_paste_round_trips_through_the_context_menu myself: passes. Test is a real GPUI simulate_click against debug_bounds on a live cat PTY, reads clipboard back and asserts Paste echoes the exact pane id — genuine simulated-click evidence, committed to the repo. | sweep D-MAIN-7, 2026-08-15 |
| `F-TERM-07` | PASSED | agent launch/identity real for installed CLIs (pass 4/6); pass 11 live: choosing a missing CLI still creates the terminal and the pane reports the launch error | pass 11 |
| `F-TERM-08` | PASSED | Live: ctl notify session=pane-1 status=running (red tab dot + '1 running' footer badge confirmed it), rightclick dropped once then opened on retry (documented lane trap), clicked Close Terminal... -> real 'This pane has running work. Close anyway?' banner with Close Anyway/Cancel appeared, no immediate close. | sweep E-C-4, 2026-08-16 |
| `F-TERM-09` | UNREACHABLE | title.rs unchanged since prior evidence. Needs a real interactive claude/codex TUI's genuine OSC title sequence, which this sandboxed critic session cannot stage any more than the builder could — a real instrument gap, not absence of effort. | sweep D-MAIN-7, 2026-08-15 |
| `F-TERM-10` | PASSED | LIVE pass10: sleep 300 survived select-workspace away+back via socket; scrollback intact | pass 10 |
| `F-TERM-11` | PASSED | Wayland lane 2026-08-15, fresh DB, zero projects: central terminal surface shows terminal glyph + 'No worktree selected' + 'Add a project, then select a worktree.' (c4b/02-noworktree.png). Fix landed 2026-08-14T17:33 (commit 7289c84), after that day's 'still absent' censuses. Upgrades from FAILED — absent. | sweep C-MAIN-4, 2026-08-15 |

## Package tier — `02-inventory-packages.md` (171)

### F-CORE-ACT — agent activity model (27)

| id | verdict | evidence | judged |
|---|---|---|---|
| `F-CORE-ACT-01` | PASSED | 70 tests green; status.rs 4 states+labels+priority; status_for_panes priority test | pass 10 |
| `F-CORE-ACT-02` | PASSED | PASSED: sidebar half pixel-verified (prior pass); D-Bus half proven via raw dbus-monitor transcripts in reference/linux-progress/p120/logs/ (real Notify call backgrounded, none foregrounded, positive control confirms monitor live) — verified still valid at 4073297 via commit ancestry + empty rust/ diff for tiller_activity/tiller_control between P120's commit and HEAD. Both halves now live-demonstrated. | sweep W01-core, 2026-08-14 |
| `F-CORE-ACT-03` | PASSED | agent_spawned sets running+identity, returns () by construction; test asserts no transition | pass 10 |
| `F-CORE-ACT-04` | PASSED | from_exit_code 0->done else error; apply_exit_result tests incl untracked/closed->None | pass 10 |
| `F-CORE-ACT-05` | PASSED | replayed `panes::tests::real_pty_activity_status_follows_osc_title_then_settled_content` — ok (pass 16, full panes:: run: 16 passed 2 failed; this test among the 16) | pass 16 |
| `F-CORE-ACT-06` | PASSED | Live full cycle on one pane: real /proc-detected foreground child sets Running glyph; a genuine unrelated OSC title mid-life leaves it unchanged; killing the process clears it. Frames 12/23/25-tabglyph-crop.png. Upgraded half-proven to PASSED. | sweep C-P2, 2026-08-15 |
| `F-CORE-ACT-07` | PASSED | Live: ctl notify(needs-input) then a same-window contradicting OSC title left the glyph at NeedsInput (27-within-debounce.png); after the debounce elapsed a fresh title flipped it to Running (28-after-debounce.png). Upgraded NOT EXERCISED to PASSED. | sweep C-P2, 2026-08-15 |
| `F-CORE-ACT-08` | PASSED | replayed `panes::tests::terminal_events_feed_title_and_settled_content_into_the_one_model` — ok (pass 16) | pass 16 |
| `F-CORE-ACT-09` | PASSED | replayed `panes::tests::process_refresh_preserves_process_ownership_until_process_gone` — ok (pass 16, among the 16 passing panes:: tests) | pass 16 |
| `F-CORE-ACT-10` | PASSED | Live wayland-drive: opened a plain Terminal pane, typed the real codex ELF binary as a foreground child, waited 2s (4x 500ms poll cycles). Sidebar flipped from no status dot to the orange Running dot on both the worktree row and the Terminal tab icon (f04-shots 05-d-terminal.png before, 06-e-codex-running.png after). | sweep D-MAIN-2, 2026-08-15 |
| `F-CORE-ACT-11` | PASSED | Code: tab_bar.rs's new-tab dropdown uses deferred(anchored()), which escapes to window root at paint time, a mechanism distinct from the Sidebar's plain .absolute() overlay that P123 documents as confined to its 280px parent. Live wayland-drive.sh (fresh instance, ctl project.add then click at measured + button coords): dropdown opened correctly anchored under the button, drawn over the Files panel (not squeezed into a sidebar column), listing all nine expected items — reproduced on 2 of 2 attempts once the frame was allowed to settle after project.add, matching the builder's and a prior committed critic pass's independent reproduction. | sweep E-P2, 2026-08-16 |
| `F-CORE-ACT-12` | PASSED | overturn pass9 UNREACHABLE: pure classification, every clause case unit-tested (glyphs/pi/pi:/names/bare spinner) | pass 10 |
| `F-CORE-ACT-13` | PASSED | overturn pass9 UNREACHABLE: detect_claude/detect_pi_family; every clause mapping tested | pass 10 |
| `F-CORE-ACT-14` | PASSED | WAITING/IDLE/WORKING keyword lists + boundary negatives ("already","reworking",codex-notes) tested | pass 10 |
| `F-CORE-ACT-15` | PASSED | overturn pass9 UNREACHABLE: content.rs matches Swift ScreenManifest; proceed/esc/y-n/confirm/nonmatch tested | pass 10 |
| `F-CORE-ACT-16` | PASSED | strip_ansi CSI+OSC(BEL+ST) test; content detector uses stripped text | pass 10 |
| `F-CORE-ACT-17` | UNREACHABLE | the pass-14 evidence said "zero app callers — wiring owed" and kept PASSED anyway. Re-swept 2026-08-14, still true: `agent_id_for_panes` (`tiller_activity/src/model.rs:377`) has **0 references** in `crates/tiller/src` or `crates/tiller_ui/src`. Worktree-level agent identity is computed nowhere in the app | orchestrator audit, grep evidence, 2026-08-14 |
| `F-CORE-ACT-18` | UNREACHABLE | the pass-14 evidence was accurate and the verdict contradicted it: "zero app callers — wiring owed" is not a PASSED. Re-swept 2026-08-14, still true — `running_agent_ids` (`tiller_activity/src/model.rs:399`) has exactly two mentions in the workspace, its definition and its own test (`activity_integration.rs:591`). No app path reaches it. Same shape as `F-TERM-02` and `F-TERM-PTY-06`, which critic2 recorded as UNREACHABLE tonight with "complete-and-unreachable is not PASSED" | orchestrator audit, grep evidence, 2026-08-14 |
| `F-CORE-ACT-19` | PASSED | Fresh dbus-monitor capture: real Notify call title="Claude Code — tiller/linux/gpui-waku", agent+worktree-label, not agent+status. | sweep C-MAIN-1, 2026-08-15 |
| `F-CORE-ACT-20` | PASSED | Four dbus-monitor windows: hidden+fresh fires; identical-status, no-agent, and foreground+fresh all zero Notify calls -- all branches of should_notify exercised live. | sweep C-MAIN-1, 2026-08-15 |
| `F-CORE-ACT-21` | PASSED | rows test: terminal+chat kept, doc/diff/browser omitted; None->Idle code-verified (test gap: no unrecognized-pane row) | pass 10 |
| `F-CORE-ACT-22` | UNREACHABLE | the row hedged that only the `urgent_first` half was dead and "the sorted half stays live" — **the hedge is false**. `AttentionSort` (`tiller_activity/src/sort.rs:6`) is the module's *only* export and has **0 app references**; there is no live half. Attention-ordering is never applied to the sidebar. A partial claim reads as more careful than a flat one, which is why this survived four passes unchecked | orchestrator audit, grep evidence, 2026-08-14 |
| `F-CORE-ACT-23` | UNREACHABLE | "zero app callers — wiring owed" is not a PASSED. Re-swept 2026-08-14: `requires_close_confirmation` (`activity.rs:30`) has **0 app references** — no close path consults it. `F-TERM-08` is the consumer-side row and is where the wiring is owed | orchestrator audit, grep evidence, 2026-08-14 |
| `F-CORE-ACT-24` | PASSED | Real quit+relaunch, /proc pstree on the live tiller pid: restored pane's direct child process argv is literally 'claude --resume <ref>'; unmatched ref falls back to bare 'claude'. | sweep C-MAIN-1, 2026-08-15 |
| `F-CORE-ACT-25` | PASSED | Live wayland-drive.sh: project.add of a real 3-worktree fixture with none selected -> workspace.list returned all mounted=false (was 'mount everything' before); then workspace.select main -> mounted flips true for main only, others stay false. Same code path a sidebar click uses (SidebarEvent::SelectWorktree and control door both call TillerWorkspace::select_worktree). | sweep G2-gaps-a, 2026-08-16 |
| `F-CORE-ACT-26` | PASSED | Same drive: enabled 'Limit mounted worktrees' and set 'Keep mounted'=2 via real synthetic clicks on the Settings toggle/stepper (screenshot + surface.settings.read confirm), then selected main, second, third via workspace.select in sequence. After the third selection workspace.list read main:false (evicted, oldest idle), second:true, third:true (selected) -- a real select_worktree -> evict_over_capacity_worktrees -> WorktreeMountPolicy round trip, not just the pure-policy unit test. | sweep G2-gaps-a, 2026-08-16 |
| `F-CORE-ACT-27` | PASSED | quarantines genuinely removed: `grep -c ignore panes.rs` = 0 and the full `panes::` filter executes (16 passed, 2 failed — the 2 failures are ACT-06/11/07's real-PTY tests, named there, not quarantines); `cargo test -p tiller_terminal` 22 green (pass 14/16); the row's stale clauses (SettingsSnapshot blocker, formatting gate) are gone — the bin compiles | pass 16 |

### F-CORE — domain, files, usage, workspace (45)

| id | verdict | evidence | judged |
|---|---|---|---|
| `F-CORE-DOM-01` | PASSED | project + both worktrees round-trip DB across restart | pass 2 |
| `F-CORE-DOM-02` | PASSED | defaults_prefer_explicit_values_then_primary_and_sibling test | pass 10 |
| `F-CORE-DOM-03` | UNREACHABLE | Live click today (cmain2-dom-shots/02) shows the popover open with all 3 items — new proof the popover itself works, correcting the prior 'never seen open' claim. Selecting Open Project closes the menu with no portal window visible (cmain2-dom3-shots/02); per ENVIRONMENT.md the portal picker is invisible to every harness here, not an app defect. UNREACHABLE, not NOT EXERCISED or FAILED. | sweep C-MAIN-2, 2026-08-15 |
| `F-CORE-DOM-04` | PASSED | 5 filter tests: branch match, case-insensitive, empty query, nested tab title | pass 10 |
| `F-CORE-DOM-05` | PASSED | ordering_ignores_unknown_and_noop_moves | pass 10 |
| `F-CORE-DOM-06` | PASSED | tab_order_wraps_and_numeric_selection_validates | pass 10 |
| `F-CORE-DOM-07` | PASSED | Byte-exact vs Swift reference for all three ported adapters; both named tests re-run green per-crate. Live wayland-drive on the exact default route (Chat tab, agent_id None, Summarizer=Claude Code) sent a real user turn and forced the running-to-done transition -- the tab title changed from Chat to a real, transcript-derived Auto-naming in both the strip and sidebar, with the visible assistant reply proving a genuine claude -p call ran, not a hardcoded string. Screenshots: /tmp/i1auto2/04-chat-tab.png (before), /tmp/i1auto2/06-after-notify-pane0.png (after). | sweep I1-autoname, 2026-08-16 |
| `F-CORE-DOM-08` | PASSED | once_gate_runs_only_the_first_callback | pass 10 |
| `F-CORE-WSP-01` | PASSED | legacy_content_exposes_only_terminal_panes_and_chat_tab_activity | pass 10 |
| `F-CORE-WSP-02` | PASSED | ids tests + document_identity_is_worktree_scoped_and_resolves_symlinks | pass 10 |
| `F-CORE-WSP-03` | PASSED | empty layout + InvalidFraction(1001) tested; binary split code-verified | pass 10 |
| `F-CORE-WSP-04` | PASSED | Live single-invocation wayland-drive.sh: right-click tab, click Rename, type text, Enter -> title committed and the immediately-following typed character (no click) landed in the terminal prompt, not dropped. Separate drive with Escape instead of Enter: title reverted and the following typed character again landed in the terminal. Both match the row's exact gesture and discriminate against the pre-fix stranded-focus bug. | sweep H1-gaps, 2026-08-16 |
| `F-CORE-WSP-05` | PASSED | command_classes_distinguish_structural_and_nonstructural_changes | pass 10 |
| `F-CORE-WSP-06` | PASSED | validate() covers full reject list; malformed snapshot falls back empty | pass 10 |
| `F-CORE-WSP-07` | PASSED | snapshots_are_versioned_canonical_and_malformed_data_falls_back_empty | pass 10 |
| `F-CORE-WSP-08` | PASSED | Independently re-drove the restart bar with own draft text across two separate wayland-drive.sh processes sharing one TILLER_DB (same TILLER_WL_LABEL, which kill_ours guarantees is a fresh process, not a kept-alive one): run 1 composed critic_draft_check_ABC999 into default-chat; run 2, with zero compose call, showed the same text already in the composer in its first frame, read purely from SQLite. cargo test -p tiller_persistence --lib --tests: 39 passed, including the new active-flag-ordering regression test. | sweep H1-gaps, 2026-08-16 |
| `F-CORE-FILE-01` | PASSED | tree tests: path hazards, .git excluded, dirs-first localized sort | pass 10 |
| `F-CORE-FILE-02` | PASSED | classify tests: supported/unsupported/oversize/relative/absolute | pass 10 |
| `F-CORE-FILE-03` | PASSED | Builder called this blocked, but integration commit 329a44e (ancestor of HEAD) already wired the exact wanted .on_drag onto right_panel.rs's file rows. Live-verified with the Wayland lane's new `drag` primitive: dragged the real AGENTS.md file row onto an open Terminal pane; the terminal's input line received the real shell-quoted absolute path (file03-shots4/04-after-drag-2.png), a result a wrong-row drag failed to produce moments earlier, confirming discrimination. | sweep D-P3, 2026-08-15 |
| `F-CORE-FILE-03A` | FAILED — defective | Independently reproduced both halves of the VERIFY clause with my own file names in two separate live wayland-drive.sh drives: the ordinary-speed real XDND drop genuinely lands '/tmp/critic-xdnd-alpha.txt' '/tmp/critic-xdnd-beta.txt', in order, in the Terminal pane's prompt through production on_drop::<ExternalPaths>. But the row's own 'provider that resolves slowly' case (--delay-ms 400) silently and totally loses the drop -- DROP_PERFORMED fires, xdnd-source times out waiting for dnd_finished/cancelled, prompt stays empty -- a stronger failure than reordering, confirmed live rather than transcribed from the builder's report. Root cause sits in the vendored pinned zed-industries/zed gpui_linux checkout (Drop handler bails unless Enter's async pipe-read already finished, racing Drop by ~150-350ms), not in any file this repo owns, so nothing here is fixable in this tree, but the row's explicit slow-provider VERIFY clause demonstrably fails. | sweep I2-xdnd, 2026-08-16 |
| `F-CORE-FILE-04` | PASSED | Opened a markdown file in default Preview mode and clicked the rendered link; a new tab opened for the resolved local file, discriminating against wave-D's no-new-tab evidence. Confirmed in source this is the real user path (Workspace::add_file_tab subscribes every FileView it constructs), not a token call site. Instrument: Scripts/wayland-drive.sh, /tmp/ec1-shots/04-after-link-click.png. | sweep E-C-1, 2026-08-16 |
| `F-CORE-FILE-05` | PASSED | wrap/prefix selection-preserving tests | pass 10 |
| `F-CORE-FILE-06` | PASSED | X11 lane: xdotool click --repeat 2 --delay 40 1 (true <100ms double-click) opened note.txt in FileView; typed to dirty it (12-dirty.png); externally overwrote via printf; within 1.5s, zero further clicks, conflict banner 'This file changed on disk.' with Reload/Keep appeared over editor content (13-conflict.png). Confirms already-correct. | sweep C-P4, 2026-08-15 |
| `F-CORE-FILE-07` | PASSED | inotify monitor + real create/modify/remove events test | pass 10 |
| `F-CORE-FILE-08` | PASSED | PASSED: live Files panel shows distinct icons for .md files, git dotfiles (branch glyph), Tiller.xcodeproj, and folders simultaneously in one rendered session; Settings confirms Material icon set live. | sweep A2-P120, 2026-08-14 |
| `F-CORE-TERM-01` | PASSED | xterm byte table tested for all 9 keys | pass 10 |
| `F-CORE-TERM-02` | PASSED | Lane gained `chord` since last sweep. Live: clicked a real running terminal to focus it, then chord shift F10. The full 12-item context menu (Copy...Close Terminal) appeared, identical to a rightclick on the same pane (verified same pass) -- the exact real-keyboard-driven gesture the unit test only simulates in isolation. Both state and gesture halves closed. | sweep D-P2, 2026-08-15 |
| `F-CORE-TERM-03` | PASSED | SplitTree split/remove-with-collapse/leaf enumeration tests | pass 10 |
| `F-CORE-SET-01` | PASSED | Direct binary launch, negative control TILLER_SOCKET_ENABLE=off prints '[control] disabled' and creates no .sock file; positive control (no override) prints '[control] listening on ...' and the socket file exists. Both runs live, discriminating. | sweep D-MAIN-3, 2026-08-15 |
| `F-CORE-SET-02` | N/A — platform | TCC model macOS-only (matches F-SET-23 precedent) | pass 10 |
| `F-CORE-USG-01` | PASSED | from_percent clamps 0-100, non-finite->0; has_any; reducer tests | pass 10 |
| `F-CORE-USG-02` | PASSED | real wham capture + secondary window + labels + malformed + ANSI tests | pass 10 |
| `F-CORE-USG-03` | PASSED | accepts_only_the_expected_usage_percent_field | pass 10 |
| `F-CORE-USG-04` | PASSED | cookie normalization + workspace id + real react-flight capture test | pass 10 |
| `F-CORE-USG-05` | PASSED | Live real-binary drive: backdated CODEX_HOME/auth.json + TILLER_CODEX_TOKEN_URL pointed at a local HTTP fixture; after launch the fixture was hit and auth.json's tokens.access_token changed to the fixture's FIXTURE-NEW-ACCESS value while id_token/account_id/custom_field survived the merge. cargo test -p tiller_usage codex:: 13/13 pass. | sweep D-P3, 2026-08-15 |
| `F-CORE-USG-06` | PASSED | Live: real backdated CODEX_HOME creds drove a genuine 401 from auth.openai.com through the running binary; status bar read 'Codex token expired' not generic 'logged out'. Screenshot 01-usg06.png. Upgraded half-proven to PASSED. | sweep C-P2, 2026-08-15 |
| `F-CORE-USG-07` | PASSED | Source-confirmed tiller_agents/src/codex.rs has no usage/refresh/LoggedOut logic (grep empty); the row's shared owed refresh/merge gap lives in tiller_usage/src/codex.rs, the exact path proven live by USG-05's fixture drive, closing this row's remaining half. | sweep D-P3, 2026-08-15 |
| `F-CORE-USG-08` | PASSED | LIVE pass10: real claude PTY fetch -> Success (session 4%, weekly 30%) in 11.6s; bounded/not-installed paths in code | pass 10 |
| `F-CORE-USG-09` | PASSED | reducer stale-on-timeout + provider catalog/preference tests | pass 10 |
| `F-CORE-AUTH-01` | PASSED | Live: AI Providers frame shows 'Signed in e.palmisano@reply.it' from real claude auth status stdout; Codex card same frame reads 'Not signed in' (negative control). PASSED. | sweep C-CHAT-3, 2026-08-15 |
| `F-CORE-AUTH-03` | PASSED | Independently re-drove the full VERIFY across three separate app processes with its own scratch TILLER_CREDENTIALS: typed a Session cookie into the OpenCode Go card and clicked Save; cat of the file directly (not trusting the UI) showed the cookie at mode 0600; killed and restarted as a real fresh process, and the AI Providers page read 'Signed in' on first paint with zero interaction; Clear emptied the file and flipped it to 'Not signed in', which persisted across a third restart. Deviation carried forward for a human ruling rather than folded into the verdict: the store is plaintext JSON at 0600, not the contract's third PLATFORM option 'an explicitly chosen encrypted store'. | sweep H5-drive, 2026-08-16 |
| `F-CORE-UI-01` | PASSED | appearance_follows_system_only_in_system_mode | pass 10 |
| `F-CORE-UI-02` | PASSED | updater_reaches_every_user_visible_state_and_clamps_progress | pass 10 |
| `F-CORE-AUTH-02` | PASSED | exact non-GUI install command exposed + tested | pass 10 |
| `F-CORE-PLAT-01` | N/A — platform | macOS-15 manifest does not exist in the rewrite; VERIFY itself says reference gap | pass 10 |

### F-CTRL — control socket (34)

| id | verdict | evidence | judged |
|---|---|---|---|
| `F-CTRL-WIRE-01` | PASSED | NDJSON framing, id echo, sorted keys on the wire | pass 6 |
| `F-CTRL-WIRE-02` | PASSED | replayed `oversized_complete_request_line_is_rejected_before_dispatch` green (control_integration, 45 green, pass 14) — cap check before complete-line dispatch | pass 14 |
| `F-CTRL-WIRE-03` | PASSED | fresh connection per round trip; canonical error | pass 6 |
| `F-CTRL-WIRE-04` | PASSED | TILLER_SOCKET wins; XDG default created 0600 | pass 6 |
| `F-CTRL-PANEL-01` | PASSED | create/split/list/write/key/read/wait/focus/close live on the wire | pass 6 |
| `F-CTRL-PANEL-02` | PASSED | create/split/list/write/key/read/wait/focus/close live on the wire | pass 6 |
| `F-CTRL-PANEL-03` | PASSED | create/split/list/write/key/read/wait/focus/close live on the wire | pass 6 |
| `F-CTRL-PANEL-04` | PASSED | create/split/list/write/key/read/wait/focus/close live on the wire | pass 6 |
| `F-CTRL-PANEL-05` | PASSED | create/split/list/write/key/read/wait/focus/close live on the wire | pass 6 |
| `F-CTRL-PANEL-06` | PASSED | create/split/list/write/key/read/wait/focus/close live on the wire | pass 6 |
| `F-CTRL-PANEL-07` | PASSED | create/split/list/write/key/read/wait/focus/close live on the wire | pass 6 |
| `F-CTRL-PANEL-08` | PASSED | create/split/list/write/key/read/wait/focus/close live on the wire | pass 6 |
| `F-CTRL-PANEL-09` | PASSED | create/split/list/write/key/read/wait/focus/close live on the wire | pass 6 |
| `F-CTRL-NOTIFY-01` | PASSED | agent statuses accepted, unknown rejected; user mode listable | pass 6 |
| `F-CTRL-NOTIFY-02` | PASSED | replayed `tillerctl_rejects_ambiguous_notify_modes` green (pass 14) — title combined with agent-mode options errors before socket use | pass 14 |
| `F-CTRL-SESSION-01` | PASSED | session.ref survived relaunch; resumed real claude session | pass 6 |
| `F-CTRL-WORK-01` | PASSED | Real 2-process restart against one scratch DB (wayland-drive.sh, fixed label): boot1 set worktree comment via ctl, boot2 (separate process, no project.add) auto-restored the project and read back comment intact via workspace.list. Confirms the write_catalog-before-restore fix at main.rs:9466/9488-9497. | sweep E-C-2, 2026-08-16 |
| `F-CTRL-SYS-01` | PASSED | replayed `tillerctl_ping_prints_pong` green (tillerctl bin tests, pass 14) — app responds with documented `{pong:true}` | pass 14 |
| `F-CTRL-SYS-02` | PASSED | explicit workspace / TILLER_PANE_ID env / fallback / no-context live | pass 6 |
| `F-CTRL-WORK-02` | PASSED | rows sorted, selected flag live | pass 6 |
| `F-CTRL-WORK-03` | PASSED | non-git project rejected with distinct error | pass 6 |
| `F-CTRL-WORK-04` | PASSED | select by id and exact path, current, no-selection error | pass 6 |
| `F-CTRL-WORK-05` | PASSED | replayed `workspace_close_terminates_process_group_when_worktree_is_missing` green (control_integration, pass 14) | pass 14 |
| `F-CTRL-NOTIFY-03` | PASSED | PASSED — dbus-monitor captured a real Notify call with the sent title/body; source confirms record_notification (main.rs:737) now calls notification_poster directly (commit a5d09d7). Defect no longer reproduces. | sweep W11-ctrl+brw+sid, 2026-08-14 |
| `F-CTRL-SESSION-02` | PASSED | restore-session returns success, no row duplication | pass 6 |
| `F-CTRL-BROWSER-01` | PASSED | Live raw-socket probe (not tillerctl, no tillerctl binary exists for browser anyway): system.capabilities advertises exactly 9 browser.* methods, browser.errors and browser.screenshot both absent and both return ok:false with an explicit unsupported-on-Linux error naming the method; grep of tiller_control source confirms zero browser hits. Row's narrow clause holds, but the ledger's surrounding prose describing all non-open methods as stub refusals is now stale -- browser.get/wait/eval/console/snapshot/permission are real WebKitGTK-backed implementations landed after this row's evidence timestamp. | sweep F3-browser, 2026-08-16 |
| `F-CTRL-BROWSER-02` | PASSED | Live on a fresh instance (no project.add): workspace.current and browser.open both now return 'no current workspace', closing the prior inconsistency where browser.open alone succeeded. wayland-drive.sh, label d3wlcheck. | sweep D-MAIN-3, 2026-08-15 |
| `F-CTRL-BROWSER-03` | PASSED | Live: browser.open then browser.get returns a real BrowserState reply (url/title/loading/canGoBack/canGoForward), not the previously recorded unsupported error. wayland-drive.sh, label d3wlcheck2. | sweep D-MAIN-3, 2026-08-15 |
| `F-CTRL-BROWSER-04` | PASSED | Live drive (Wayland lane): browser.snapshot now returns the real 'Browser child is unavailable' handler error, discriminated from the blanket 'unsupported...not implemented' rejection that browser.screenshot still correctly returns in the same run. Matches the row's own acceptance criterion exactly. | sweep E-C-2, 2026-08-16 |
| `F-CTRL-BROWSER-05` | PASSED | Two independent fresh X11-lane launches via a direct control-socket driver (project.add -> workspace.select -> browser.open -> sleep 5 -> browser.get) both returned {"loading":"false","title":"Example Domain",...} byte-identically -- not the loading:true/title:'' a never-loaded webview shows by default. | sweep G1-browser, 2026-08-16 |
| `F-CTRL-BROWSER-06` | PASSED | Same two independent runs: browser.eval script=document.title returned {"result":"\"Example Domain\""} both times -- real evaluated page content, not 'Browser child is unavailable' and not a dispatch-timeout artifact. | sweep G1-browser, 2026-08-16 |
| `F-CTRL-CLI-01` | PASSED | replayed `tillerctl_accepts_socket_before_command_as_documented` green (pass 14); executed transcript `tillerctl --socket /tmp/p55-cli.sock ping` returned `pong` | pass 14 |
| `F-CTRL-CLI-02` | PASSED | Live today: tab-bar '+' -> Claude Code (no chord) spawned a tab showing the genuine Claude CLI first-run trust prompt ('Quick safety check... 1. Yes, I trust this folder'), text absent from the codebase (grep-confirmed, so it's the real binary). Worktree's .claude/settings.local.json (written by the app's own prepare step) has real notify hooks pointing at the installed XDG tillerctl. Both halves proven; upgraded half-proven -> PASSED. | sweep C-MAIN-2, 2026-08-15 |
| `F-CTRL-PLAT-01` | PASSED | platform-neutral serde/libc manifest; same NDJSON on Linux | pass 6 |

### F-AGENT — agent adapters (20)

| id | verdict | evidence | judged |
|---|---|---|---|
| `F-AGENT-API-01` | PASSED | Live OpenCode tab via rclick->New Tab->OpenCode; panel.list shows pane-2 agent=opencode wired; immediate panel.read id=pane-2 returns {ok:true,data:""}, not unknown-pane -- confirms integrator's dual-map PaneRegistry::read fix (3fb90ec) live, plus tiller_control 46/46 rerun clean | sweep C-P3, 2026-08-15 |
| `F-AGENT-CLAUDE-01` | PASSED | real claude TUI in pane; both hooks fired; ref resumed | pass 6 |
| `F-AGENT-CLAUDE-02` | PASSED | merge semantics: only 5 hook events replaced, rest byte-identical | pass 6 |
| `F-AGENT-CLAUDE-03` | PASSED | claude -p ran live | pass 6 |
| `F-AGENT-CODEX-01` | PASSED | no user-global writes; -c notify override loads in real codex (no TOML trap) | pass 6 |
| `F-AGENT-CODEX-02` | PASSED | codex exec --output-last-message ran live | pass 6 |
| `F-AGENT-OPENCODE-01` | UNREACHABLE — opencode not installed | command -v empty; env verified 14:27 | pass 9 |
| `F-AGENT-OPENCODE-02` | UNREACHABLE — opencode not installed | command -v empty; env verified 14:27 | pass 9 |
| `F-AGENT-OPENCODE-03` | PASSED | PASSED (upgraded from FAILED -- absent). summarizer_command in opencode.rs matches spec, unit test reran green. Independently ran the exact generated command live -- opencode run --pure "summarize this" against installed opencode 1.18.18 -- and got real stdout, a genuine non-interactive response. Both halves of this row's VERIFY (command is X; running it produces inspectable stdout) are proven live. main.rs's missing auto-naming caller is F-SET-05's separately-tracked gap, not this row's clause. | sweep W08-agent, 2026-08-14 |
| `F-AGENT-PI-01` | PASSED | bare launch + resume shape; pi --print ran live | pass 6 |
| `F-AGENT-PI-02` | PASSED | bare launch + resume shape; pi --print ran live | pass 6 |
| `F-AGENT-OMP-01` | UNREACHABLE | Reconfirmed independently: oh-my-pi --version -> SyntaxError: Unexpected token ':' at bin/oh-my-pi.js:176, same as prior passes. Upstream-blocked, not exercisable | sweep C-P3, 2026-08-15 |
| `F-AGENT-OMP-02` | UNREACHABLE | Same upstream SyntaxError reconfirmed via oh-my-pi --version; no hook event ever fireable without a live session. Upstream-blocked, not exercisable | sweep C-P3, 2026-08-15 |
| `F-AGENT-OMP-03` | UNREACHABLE | Reran oh-my-pi --version live: identical upstream SyntaxError (TS annotations run under node), same defect F-AGENT-OMP-01/02 are already UNREACHABLE for. Unit test genuinely validates the generated command string; grep confirms zero production callers of summarizer_command anywhere. | sweep D-MAIN-1, 2026-08-15 |
| `F-AGENT-SAFE-01` | PASSED | Real rclick->New Tab->Claude Code (X11 lane) wrote marker SKILL.md + settings.local.json hooks. Overwrote SKILL.md with unmarked text, repeated gesture: file unchanged byte-for-byte, settings.local.json mtime unchanged, app log shows PrepareError::UnmanagedSkillFile. Caveat: failure not surfaced to user UI, only app log | sweep C-P3, 2026-08-15 |
| `F-AGENT-SAFE-02` | PASSED | Real quit+relaunch, worktree with zero panes opened this run: only the leading tillerctl path changed in .claude/settings.local.json; pane-99 id and a marker field untouched. | sweep C-MAIN-1, 2026-08-15 |
| `F-AGENT-SESSION-01` | PASSED | Positive+negative fixture-file pair through a real restart: matching .jsonl -> --resume fired; missing .jsonl -> fresh claude, no --resume. | sweep C-MAIN-1, 2026-08-15 |
| `F-AGENT-SESSION-02` | PASSED | Live socket call returned exact fixture marker text; unknown-pane and wrong-worktree calls returned typed error strings, not silent ok:true. | sweep C-MAIN-1, 2026-08-15 |
| `F-AGENT-SESSION-03` | PASSED | shell_quote/json_string_literal round-trips, no slash-escaping | pass 6 |
| `F-AGENT-PLAT-01` | PASSED | platform-neutral manifest, plain CLI processes, worktree-local outputs | pass 6 |

### F-GIT — git tier (16)

| id | verdict | evidence | judged |
|---|---|---|---|
| `F-GIT-RUN-01` | PASSED | success/failure/timeout/launch-failure; deadline enforced; no output limits/cancellation | pass 5 |
| `F-GIT-RUN-02` | PASSED | PASSED (was NOT EXERCISED). Critic opened every capture directly, mtime-sorted. Return delivery reconfirmed twice: New Worktree branch prompt (w13-newbranch created on disk) and Clone Repository dialog (URL typed, key Return submitted, GitClone::clone -> GitRunner::run_streaming ran, new project+worktree appeared at /home/enzopalmisano/w13-testrepo, verified on disk then cleaned up). Caveat: raw captures show two earlier rounds of failed text entry before the successful round -- evidence's 'worked every time' phrasing overstates reliability, but the final discriminating proof (04-25-url-typed.png -> 05-26-clone-a.png) is genuine and verdict-supporting. | sweep W13-git+per+persist+win, 2026-08-14 |
| `F-GIT-REPO-01` | PASSED | .git dir/file true, non-repo false, has_head 0/1 | pass 5 |
| `F-GIT-BRANCH-01` | PASSED | Live: ctl git.branches against this repo's own worktree returned exactly the three real local branch names, verified against a direct git branch -a in the same worktree. | sweep D-MAIN-4, 2026-08-15 |
| `F-GIT-WT-01` | PASSED | create with/without base, duplicate refusal, dirty-removal refusal, clean removal | pass 5 |
| `F-GIT-CLONE-01` | PASSED | Live UI drive of the Clone Repository popover (P123 mispositioning accounted for, coordinates read from own screenshots): typed an unreachable TEST-NET-1 URL, watched Destination reactively derive from it, clicked Clone repository and captured the button disabled with status Cloning... 0% -- a state unreachable without a real clone_repository call spawning a real git subprocess. Frames: /tmp/f3crit07/03-url-ready.png, 04-submit1.png. Also confirmed via source read this pass that clone_repository is still imported/invoked from tiller_ui/src/project_forms.rs. | sweep F3-browser, 2026-08-16 |
| `F-GIT-REMOTE-01` | PASSED | Critic drove it live end to end (labels cp1verify4/5): real repo with a github origin remote, ctl project.add, real gear-icon coordinate click, Avatar tab -- GitHub field pre-filled with the literal text octocat, not placeholder styling, contrasted against the still-placeholder Favicon field in the same frame. Clicked Use GitHub Avatar with zero typing and the panel updated to Current: GitHub avatar for octocat. Integrator had landed the requested sidebar.rs fix (fa1a330), confirmed at sidebar.rs:976. | sweep C-P1, 2026-08-15 |
| `F-GIT-STATUS-01` | PASSED | porcelain-v2 -z parse matches v1: MM/rename/UU/spaces | pass 5 |
| `F-GIT-STATUS-02` | UNREACHABLE | "zero app callers" is not a PASSED, and it is still true. Re-swept 2026-08-14: `directory_statuses` and `DirectoryStatusAggregator` (`lib.rs:59`) have **0 app references** — the Files panel never rolls a child's git status up to its ancestor directories. `tiller_git::status` *is* imported (`right_panel.rs:15`), so per-file status is live; the directory aggregation on top of it is not | orchestrator audit, grep evidence, 2026-08-14 |
| `F-GIT-ACT-01` | PASSED | stage/unstage/stage_all/discard/discard_all verified in git after each call | pass 5 |
| `F-GIT-ACT-02` | PASSED | pass 11: stage_refuses_a_conflicted_path_without_changing_git + stage_all_refuses_every_conflicted_path_before_mutation green (worktree unchanged verified); stale+duplicate tested; empty-selection branch code-verified | pass 11 |
| `F-GIT-DIFF-01` | PASSED | tracked/untracked/binary/added/deleted/renamed/no-HEAD hunks correct | pass 5 |
| `F-GIT-DIFF-02` | PASSED | tracked/untracked/binary/added/deleted/renamed/no-HEAD hunks correct | pass 5 |
| `F-GIT-DIFF-03` | UNREACHABLE | **corrects my own 08:00 ruling, which was wrong.** I kept this PASSED after checking `DiffOrigin`/`stats`/`FileDiff` and finding them heavily used — but those live in `diff.rs`, and this row's evidence cites `side_by_side.rs`. Checked the crate instead of the row's own artefact. Re-swept per-symbol: **all six `side_by_side` exports have 0 app references** (`DiffSideBySideLine`, `DiffSideBySideRow`, `GitDiffSideBySide`, `GitDiffSideBySideLine`, `GitDiffSideBySideRow`, `side_by_side_rows`, `lib.rs:63-66`) and nothing outside `tiller_git` names the module at all. The clause requires the user to "compare left/right rows"; `changes.rs:1` states it renders **unified** diffs and holds no side-by-side path. So the model is complete, correct and tested against a real repo, and no surface displays it. `DEAD-MODULES.md` (FABLE-06, pass 13) reached this first — "F-GIT-DIFF-03's 'the display works' is true of changes.rs, not of this module" — and went unapplied for a day | orchestrator audit, grep evidence, 2026-08-14 |
| `F-GIT-DIFF-04` | PASSED | untracked cap is 500,000 bytes, matching the Swift reference exactly (data.count <= 500_000 in GitDiffStats.swift); over-cap files fall back to the diff count (150,000-line file reports its real count, test green); binary -> is_binary | pass 11 |
| `F-GIT-PLAT-01` | PASSED | git via PATH; whole flow on Linux; process-group kill on timeout | pass 5 |

### F-PERSIST — persistence package (13)

| id | verdict | evidence | judged |
|---|---|---|---|
| `F-PERSIST-DB-01` | PASSED | open(path)+in_memory; forward-only v1-v5 tested w/ rows intact; corrupt/empty/truncated/newer-schema refused; 64MiB cap; concurrent first-open+writers tested (22 it-tests) | pass 10 |
| `F-PERSIST-DB-02` | PASSED | persistence suites replayed green (5 unit + 34 integration, pass 14): canonical schema has projects/worktrees/tabs/tab-state/settings/sidebar/session-refs/chat_turn/quarantine; Swift-only legacy fields stay absent by design | pass 14 |
| `F-PERSIST-DB-03` | PASSED | replayed `worktree_metadata_and_exact_path_survive_a_relaunch` green (persistence_integration, pass 14) — comment + nullable created_at/updated_at persist | pass 14 |
| `F-PERSIST-DB-04` | PASSED | SessionTabState scrollback byte-exact round trip + 256KiB bound at encode AND decode (tests) | pass 10 |
| `F-PERSIST-DB-05` | PASSED | Same fix and same restart-roundtrip evidence as F-PER-01 (shared root cause per builder's report): full transcript restored after a genuine process kill+relaunch against the identical DB, confirmed via surface.chat.read and direct sqlite chat_turn row counts. | sweep D-MAIN-4, 2026-08-15 |
| `F-PERSIST-DB-06` | PASSED | Live: account_identity table has real row after discovery (sqlite3 module read). Relaunched with claude unresolvable via PATH (same DB) -- UI still shows cached 'Signed in e.palmisano@reply.it', proving fallback read path works, not just live shell-out. PASSED. | sweep C-CHAT-3, 2026-08-15 |
| `F-PERSIST-DB-07` | PASSED | replayed green (persistence_integration, pass 14): v9 quarantine_record moves malformed tab-state/chat payloads with original bytes, valid siblings survive; physical corruption remains untouched `Corrupt` — **held on re-check 2026-08-14**: fable's half-flag (the `quarantined_records` query has no app consumer) is a true fact that does not reach this clause, which asks only that malformed records "*can be quarantined rather than materialized*" and that valid siblings survive on restart. Both are load-path behaviour and both are exercised; nothing in the clause requires reading the quarantine list back. Recorded so the next pass does not re-open it | pass 14; re-checked orchestrator 2026-08-14 |
| `F-PERSIST-DB-08` | PASSED | replayed `a_duplicate_primary_is_reconciled_and_future_writes_are_rejected` + `exact_path_lookup_does_not_normalize_nearby_paths` green (persistence_integration, pass 14) — v8 one-primary integrity + exact path equality — **held on re-check 2026-08-14**: fable's half-flag (`worktree_by_path` prod=0, exact-lookup half test-only) is a true fact that does not reach this clause. The clause's subject is the store itself — "*`ProjectStore` … performs exact-path worktree lookup*" — so a persistence-integration test **is** the correct instrument, exactly as a unit test is for a pure state machine. This is the boundary that separates these rows from the 14 false PASSED overturned at 07:50: those clauses described user-facing features whose delivery needs app wiring, this one describes a store API. Recorded so the next pass does not re-open it | pass 14; re-checked orchestrator 2026-08-14 |
| `F-PERSIST-DB-09` | PASSED | replayed `a_corrupt_tab_is_skipped_and_quarantined_while_valid_tabs_survive` green (persistence_integration, pass 14) | pass 14 |
| `F-PERSIST-DB-10` | PASSED | session_references_upsert_load_and_delete + survive store reopen (tests) | pass 10 |
| `F-PERSIST-DB-11` | PASSED | New migrations.rs test plants session_ref/tab/chat_turn at v6, migrates to v13, asserts exact backfilled values (agent_id->NULL, updated_at->0) not just row count; independently run and confirmed passing. Closes the row's own gap. (Live chat-UI boot-restore issue is a separate, out-of-scope bug in different files, noted only.) | sweep B7-editor-persist, 2026-08-14 |
| `F-PERSIST-DB-12` | UNREACHABLE — no terminalTab table in the Linux schema | no v17 rename exists; the described mismatch cannot manifest in this lineage | pass 10 |
| `F-PERSIST-PLAT-01` | PASSED | $TILLER_DB->checkout-scoped(XDG)->user-wide tested; creation/migration/concurrency exercised on Linux path (two-process tests) | pass 10 |

### F-TERM — terminal package (17)

| id | verdict | evidence | judged |
|---|---|---|---|
| `F-TERM-PTY-01` | PASSED | spawn/exec/cwd/env/output live (pass 1); resize_reaches_the_child_pty test green; exit reported via panel.state/panel.wait live (0/3/137) | pass 11 |
| `F-TERM-PTY-02` | PASSED | exits 0/3/137 live (pass 4); child_exit_status_preserves_normal_and_signal_termination test green | pass 11 |
| `F-TERM-PTY-03` | PASSED | replayed `terminal_child_receives_the_pane_id_environment` + `stable_identity_is_inherited_by_the_lazily_spawned_pty` green (tiller_terminal suite 22 green, pass 14) — PTY fork delayed until set_identity, pane ID passed as TILLER_PANE_ID; the real-app tillerctl-notify round trip over a live PTY pane remains unexercised by a critic (pass 14) | pass 14 |
| `F-TERM-REG-01` | PASSED | panel create/split/list/write/read/wait/focus/close live | pass 6 |
| `F-TERM-REG-02` | PASSED | wait exit/timeout/unknown live (pass 6); pass 11 live: panel.close then panel.wait returns immediately (6 ms, unknown pane) — close cancels pending registration, no live registration left | pass 11 |
| `F-TERM-SCR-01` | PASSED | 262144 bytes exactly; newest line kept, oldest evicted | pass 4 |
| `F-TERM-SCR-02` | PASSED | Recreated the WINCH-trap fixture independently, drove a real Split Right and a real down/move x4/up divider drag. Own pane scrollback: 6 real SIGWINCH deliveries (cols 41-61-57-84-57-84) for 6 pointer ops, two arriving ~3.2s after the drag ended - live async debounce/settle, not synchronous. | sweep H6-instruments, 2026-08-16 |
| `F-TERM-PTY-04` | PASSED | pass 11 live: app launched with SHELL=/bin/sh spawns an sh prompt in the pane ($SHELL preference real, bash fallback otherwise); TERM fixed xterm-256color; xterm-ghostty + first-resize settle are the macOS-isms the PLATFORM note exempts | pass 11 |
| `F-TERM-PTY-05` | PASSED | Live chord ctrl+shift p -> type Codex -> Return: a new Codex tab spawns, attaches, and renders the real unmodified codex CLI's own banner and Sign in with ChatGPT/Device Code/API key screen, correctly sized. Row's literal ask (child spawns, pane attaches and renders correctly) is satisfied; credential-gated clauses past this screen remain explicitly out of scope per task instructions, same boundary the builder documented. | sweep H6-instruments, 2026-08-16 |
| `F-TERM-PTY-06` | UNREACHABLE | Re-checked current wayland-drive.sh vocabulary: the new drag/rightclick/scroll/chord/modclick primitives are all virtual-pointer/keyboard, not real XDND. drag produces the app's own on_drop::<PathBuf> (proven for F-EDIT-12), not gpui::ExternalPaths. on_drop::<ExternalPaths> remains wired unchanged at lib.rs:1567. ENVIRONMENT.md's XDND-out-of-reach finding still holds; genuinely unreachable. | sweep D-P2, 2026-08-15 |
| `F-TERM-PTY-07` | UNREACHABLE | P82 (codex11) built `TerminalSurfaceHost`/`TerminalPaneCache<T>` in `tiller_terminal/src/lifecycle.rs` (Half A, confirmed present, read in full). But there are **zero production consumers** anywhere in `main.rs`/`panes.rs` — nothing constructs or calls into these types outside the crate's own tests. SEAMS.md's Open table confirms the "Terminal pane composition" seam: Half A done, Half B (the mount into the shell) still owned by codex12 and not started. A complete, unreachable Half A is not PASSED | critic2, SEAMS.md Open table + grep evidence |
| `F-TERM-PTY-08` | UNREACHABLE | Same seam and evidence as PTY-07 — `TerminalPaneCache<T>` (`lifecycle.rs`) has zero production consumers; SEAMS.md's Open table lists Half B as still owned by codex12, not done | critic2, SEAMS.md Open table + grep evidence |
| `F-TERM-SPLIT-01` | PASSED | Same live drive as F-TAB-23: ctl pane.split direction=left correctly placed the new pane left of the marker-bearing original, reproducing the row's own howToExercise with a discriminating frame. | sweep D-MAIN-7, 2026-08-15 |
| `F-TERM-UI-01` | PASSED | Live single-invocation drive (project.add, rightclick terminal pane, click Set Title, shot — no app restart in between): tab bar and sidebar both changed from plain 'Terminal' to 'Terminal terminal-1', a value the app never reaches on its own. Directly contradicts the ledger's current FAILED — defective (stub claim); set_terminal_title at HEAD is not a stub. A separate-invocation first attempt gave a false negative (menu frozen mid-click, then a fresh app instance naturally reset) — discarded, not the real result. | sweep G5-terminal, 2026-08-16 |
| `F-TERM-UI-02` | PASSED | Same-invocation negative/positive pair at the identical pixel (400,111) on a rendered URL: plain click leaves the tab strip unchanged; modclick logo opens a new Browser tab with the URL in the address bar. Independently driven and captured by this critic. | sweep H6-instruments, 2026-08-16 |
| `F-TERM-USG-01` | PASSED | hidden claude PTY fetch ran live (pass 1); pass 11: new test green — PATH-stubbed claude fetch returns TimedOut and the hidden PTY child is killed on drop (termination verified via stub pid) | pass 11 |
| `F-TERM-PLAT-01` | PASSED | gpui + alacritty_terminal only; no webview/HTML renderer | pass 10 |

## Appendix — ACP chat behaviors found by exercising, NOT in the 389 denominator

Pass 17 drove the real ACP chat surface (fixture DB, real `claude` behind
`npm exec @agentclientprotocol/claude-agent-acp`) and catalogued behaviors that no
inventory row owns. **These 14 rows are an appendix by the user's explicit ruling: the
denominator stays 389.** Where a behavior partly overlaps an inventory row, the verdict
lives on that row and this appendix carries the ACP-specific remainder. Progress reports
must count these as "plus 14 newly-found ACP rows not yet in the denominator".

| id | verdict | evidence |
|---|---|---|
| `ACP-01` | PASSED | composer status pill state machine: `● idle` (green, pre-session) → `● working` (amber, during turn — precedes the first streamed chunk) → `● Ask ⌄` (green, post-completion; session mode shown) → `● offline` (gray, transport dead). All four photographed: p17-ah0..ah3, aj1, aj6 |
| `ACP-02` | PASSED | model pill lifecycle: `Opus Plan Mode` pre-session → `Model Opus Plan Mode XHIGH ⌄` once the session starts (p17-ah3) |
| `ACP-03` | PASSED | chat tab indicator lifecycle: ○ idle → ● red filled while working → ✓ done → back to ○ when the transport dies (p17-ah/aj series) |
| `ACP-04` | PASSED | during a turn the composer queues instead of disabling: placeholder "Type to queue for the next turn…" + stop ■ control (p17-aj1); live pixel proof over F-CHAT-06's drawn tests |
| `ACP-05` | PASSED | Files-panel footer shows "Activity 1 running" during a chat turn (p17-aj1) |
| `ACP-06` | PASSED | context ring chip tracks real usage across turns (0% → 5% → 27% in the chat drives; 6% after one counting turn in the terminal drives) |
| `ACP-07` | PASSED | transport-death error entry carries the machine reason JSON and a Retry button; partial streamed output is retained above it (p17-aj6; owning generic row F-CHAT-33) |
| `ACP-08` | FAILED — defective | Retry recovers the session but silently DROPS the failed turn: bridge respawns (new `npm exec` subtree), pill returns to idle, and the prompt is never re-sent — idle at +20s with no response (p17-al2/al3). A user's message vanishes without an error |
| `ACP-09` | half-proven | from `● offline` the composer is inert: the same click+type+Return that worked three times from idle produces nothing, with no feedback (p17-ak4); half-proven because only one input route was tried |
| `ACP-10` | PASSED (measured) | transport-death → UI transition latency in (2s, 13s]: at +2s the stream was still visibly advancing (fresh frame, advancing numbers), at +13s the offline pill and ✓→○ had landed (p17-aj3..aj6) |
| `ACP-11` | PASSED (measured) | bridge architecture: sending in the Chat tab spawns `npm exec @agentclientprotocol/claude-agent-acp@latest` → `sh -c claude-agent-acp` → node, as descendants of the app pid; first-spawn latency ~5-10s (npm resolve) — the paint-lag trap behind two stale frames (p17-artifacts/ah-proctree.txt) |
| `ACP-12` | FAILED — defective | the mode pill's dropdown never opens — display-only pill (owning row F-CHAT-15, moved to FAILED — defective this pass) |
| `ACP-13` | FAILED — absent | the UI chat path never writes `chat_turn` or `session_ref` rows (0 rows after two completed exchanges; WAL-aware read) — the store works, the surface does not call it (owning row F-PER-01, overturned this pass) |
| `ACP-14` | PASSED | kill mid-stream retains the partial streamed content in the transcript above the error entry — nothing already displayed is lost (p17-aj6) |

## Totals

| verdict | count |
|---|---|
| PASSED | **353** |
| half-proven | **1** |
| FAILED — absent | **0** |
| FAILED — defective | **1** |
| UNREACHABLE | **28** |
| N/A — platform | **6** |
| NOT EXERCISED | **0** |
| NOT EXERCISED — blocked on display | **0** |
| builder-claimed, unverified | **0** |
| **total** | **389** |

`UNREACHABLE` groups the three spellings in the body (bare, `— opencode not installed`,
`— no terminalTab table in the Linux schema`); the recount below prints them separately.

Recomputed from the body by counting rows, not by hand, 2026-08-14 — the previous block
summed to 388 and disagreed with the body on five of nine verdicts. Anyone editing totals
should recount rather than adjust:

```bash
python3 -c "import re,io;from collections import Counter;s=io.open('docs/linux-rewrite/INVENTORY-LEDGER.md',encoding='utf-8').read();r=re.findall(r'^\| \`(F-[A-Z0-9-]+)\` \| ([^|]+?) \|',s,re.M);c=Counter(v.strip() for _,v in r);print(len(r));[print(f'{n:>4}  {k}') for k,n in c.most_common()]"
```

Entries never independently judged by the critic: **68**
(**25** never claimed by anyone; **22** builder-claimed, unverified; **8** P56;
**7** P50 builder claim; **6** P55 builder).
`NOT EXERCISED — blocked on display` and `UNREACHABLE` rows *are* critic-judged
(as appearance-blocked / environment-impossible), so they are not counted in
the never-judged figure.

**Do not maintain this block by hand — run `python3 Scripts/ledger-totals.py --write`.**
It has been wrong three times. Twice it was merely stale. The third time is the one that
matters: the critic recomputed it correctly at the end of pass 12 and it was wrong again
within minutes, because builders append and claim rows continuously while only the critic
recomputes. A hand-maintained aggregate over a concurrently-edited table is not
occasionally stale, it is guaranteed stale, and every agent picks its next piece from
these numbers. The script also rejects a row whose evidence contains a raw `|`, which
silently shifts every column after it — one such row (`F-SET-05`) had been hiding the
`judged` column from any count, which is why the never-judged figure read 54 against a
true 68.
