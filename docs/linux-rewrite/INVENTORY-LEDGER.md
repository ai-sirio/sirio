# INVENTORY-LEDGER — per-entry status of all 388 entries

Written by pireview (the critic). Pass 9 built it; pass 10 converted the bulk of
`NOT EXERCISED` into real verdicts (see `CRITIC-baseline.md` PASS 10). Pass 10 snapshot:
whole tree copied to `/tmp/critic-pass10` at **2026-08-13T12:40:06Z**; build clean in
14.14 s; headless launch served `TILLER_SOCKET=/tmp/critic10.sock`; live exercises: real
claude usage fetch (Success 4%/30%), `sleep` pane surviving select-workspace away/back.
Pass 9 snapshot: whole tree copied to
`/tmp/critic-pass9` at 2026-08-13T12:27:17Z (14:27:17 CEST); `cargo build -p tiller
-p tiller_control` clean in 4.87 s; headless launch served `TILLER_SOCKET=/tmp/critic9.sock`
(ping pongs, 42 methods advertised, `browser.*` answers its specific unsupported error,
the P37 mutation doors `surface.changes.stage/unstage/discard/stage_all/discard_all` exist).

Pass 12 snapshot: whole tree copied to `/tmp/critic-pass12`; build clean with shared
`CARGO_TARGET_DIR=/tmp/critic-target`; all suites run from the snapshot. Pass 12 applied
`PASSED-AUDIT.md` (fable): 10 false PASSEDs confirmed and re-marked, 4 unreplayable rows
given named drawn tests or downgraded, 4 doubts resolved, `F-SID-14` and six `F-GIT` stale
FAILEDs flipped, Totals recomputed from the body. New critic tests added in the snapshot:
projects-header/add/rows render, filter narrows+restores, project chevron hide/restore
(all sidebar.rs). Two P50 bin tests fail deterministically (see findings log).
Pass 11 snapshot: whole tree copied to `/tmp/critic-pass11` at **2026-08-13T13:26:20Z**.
The live tree did not compile (builder mid-edit in `main.rs:3596`, split-path refactor —
repaired in the snapshot only, never the live tree). All quiet-crate suites green
(120 tiller_ui lib, 67 tiller bin, 27 tiller_usage, 46 tiller_git, persistence/activity/
project/agents/acp/markdown all green); a mid-pass disk outage invalidated some transient
results, all re-run green afterwards. Headless launches served `TILLER_SOCKET=/tmp/critic11.sock`
with throwaway `TILLER_DB`s. Live exercises: pane launch-error surface for a missing CLI,
close-cancels-registration (6 ms), socket disable effect, scrollback capture->quit->relaunch->replay
(restored pane showed the previous session's 15:54 prompt inside a 15:56 session), `$SHELL`
preference (sh prompt under SHELL=/bin/sh), 1 MiB line-cap boundary pinned. New critic tests
added in the snapshot: usage timeout+PTY-termination, activity-row click/close, remove-project
confirmation prompt, socket disable effect. Pass 11 converted the entire half-proven bucket
(see `CRITIC-findings-log.md` PASS 11).

One row per entry from both inventory files, in inventory order. This ledger consolidates
critic passes 1–8 from `CRITIC-baseline.md`; pass 9 exercised nothing new beyond the
snapshot health check and the environment fact that `opencode`/`omp` are absent from PATH.
**The ledger wins over `INVENTORY-STATUS.md` wherever they disagree.**

Verdict vocabulary: `PASSED` · `FAILED — absent` · `FAILED — defective` ·
`UNREACHABLE` (with reason) · `N/A — platform` · `NOT EXERCISED — blocked on display`
(appearance only) · `NOT EXERCISED` · `half-proven` (with which half).
`judged` names the critic pass that established the verdict, or
`builder-claimed, unverified` / `never claimed` where no independent critic pass has
touched the entry — those rows do **not** count toward done.

## App target — `01-inventory-app.md` (217)

### Window and application shell (12)

| id | verdict | evidence | judged |
|---|---|---|---|
| `F-WIN-01` | PASSED | **wave H live drive (this host)**: gear→Settings→Back round-trip, live, colour-count discriminator | sweep ?, 2026-08-14 |
| `F-WIN-02` | PASSED | **wave H live drive (this host)**: ctrl-t (Chat focused) spawned a real new terminal PTY, live | sweep ?, 2026-08-14 |
| `F-WIN-03` | PASSED | **wave N (live, this host)**: Was half-proven/UNREACHABLE-assumed. Private D-Bus session bus + xdg-desktop-portal confirmed live (FileChooser backend registered, gtk.portal). Full round trip: ctrl+o -> real GTK Open File dialog (independent sway tile) -> Home -> click fixture folder -> Return -> click file -> Return -> editor tab opened with real file content; edited + ctrl+s; saved content confirmed on disk via cat. Screenshots f-win-03-file-opened.png, f-win-03-after-save.png. | sweep ?, 2026-08-14 |
| `F-WIN-04` | PASSED | **wave H live drive (this host)**: ctrl-shift-s hide/restore sidebar, live, colour-count discriminator | sweep ?, 2026-08-14 |
| `F-WIN-05` | PASSED | **wave H live drive (this host)**: ctrl-shift-i hide/restore right panel, live, colour-count discriminator | sweep ?, 2026-08-14 |
| `F-WIN-06` | PASSED | **PASSED 2026-08-19 — CLEARED by a fresh, unrelated critic** (`fullapp/CRITIC-commands.md`, judging `fd0417f5`/`7f41e684`; all in HEAD, verified by `git merge-base --is-ancestor`). Both commands now exist as real actions (`main.rs:101-102`, `:122-123`) bound at `main.rs:152-153` — `ctrl-shift-l` New Browser, `ctrl-l` Focus Address Bar — and `linux_window_shortcuts` now returns **8** entries (counted in HEAD), the 6→8 the P103 merge note predicted. Driven as real `xdotool key --window` chords after `windowfocus` on the critic's own private Xvfb `:3`, never `DISPLAY=:1`: `ctrl-shift-l` opened a fourth tab titled "Browser" that navigated and rendered live (`03-ctrl-shift-l-new-browser-tab.png`), and the '+' menu's own New Browser row still works unchanged with no label collision (`06-plus-menu-still-has-new-browser.png`). **Two controls make the null-result half meaningful:** the identical chord sent *before* `windowfocus` produced no change at all (`02-ctrl-shift-l-nofocus-noop.png`), so the effect is the chord being received and not an ambient auto-open; and `ctrl-l` on a Terminal tab with no browser open left the terminal **uncleared** — Ctrl-L being bash's own clear-screen chord makes this the sharpest available proof that the binding is genuinely global (`KeyBinding::new(.., None)`) and wins over the terminal's raw-input path, rather than falling through. **Superseded record of the failure, kept because it is how the row was proven absent:** **Neither command exists in the port.** The original (`App/TillerApp.swift:76`, read 2026-08-19) puts two File-menu items behind `WorkspaceEngineGate.isEnabled`: `Button("New Browser")` on `⇧⌘L` calling `newBrowserTabInSelected()`, and `Button("Focus Address Bar")` on `⌘L` calling `focusBrowserAddressBar()`. Our action registry is exhaustive and small — `window_commands` (NewTerminalTab, OpenFile, SaveFile, ToggleSidebar, ToggleRightPanel, RestoreLaunchSnapshot), `shell_settings` (CloseSettingsSurface, OpenSettingsShortcut) and `pane_commands` in `panes.rs:77` (splits, focus moves, ClosePane, tab cycling, JumpToTab1-9, tab menu/close/move, ResumeChat) — and contains **no** browser or address action of any kind; a workspace-wide grep for an action or key binding named for Browser/Address returns nothing. New-Browser exists only as a `+`-menu item (`NewTabAction::NewBrowser`, main.rs:7391) with no shortcut; **focus-address-bar has no action, no menu item and no binding at all**, so the row's second half cannot be reached by any input path. The rendering blocker that previously failed this row is genuinely gone (`7d4d2182`, `fullapp/CRITIC-x11-forced.md` CLEARED — browser renders on a Wayland session via XWayland), which is why this is `absent` rather than `defective`: what remains is missing, not broken. The `WorkspaceEngineGate` condition is not the obstacle — the port has no such gate and the workspace engine is unconditional. **Also confirmed live, by a second and independent method** (`786389c5`, critic x11-forced): all four candidate chords — `ctrl-l` and `ctrl-shift-l`, plus the `super` variants in case GPUI maps ⌘ to super — driven into a running instance as real `xdotool key --window` events, each producing zero pixel change and zero effect on `browser.get`. For the address bar the field was deliberately defocused first, then `ctrl-l` sent and a marker typed: nothing landed. **The instrument was then proved alive** — clicking the field directly and typing the same marker put it in (`critic-x11-forced-shots/winb06-09-direct-click-proves-widget-works.png`) — so the null result belongs to the app, not to the harness. That pass also closed the door this cell's registry argument left open: it enumerated every `KeyBinding::new` site in `rust/crates` (30+) and established there is no external keymap path at all — no `load_keymap`, no `Keymap::`, no keymap asset — which makes the enumeration a proof rather than a sample. Its stronger form of the address-bar finding: there is no `FocusAddressBar` action anywhere, only a mouse `on_click` on the field's focus handle (`browser.rs:1429`), so the chord is not merely unbound — the action does not exist. The same pass confirms `WorkspaceEngineGate` has no counterpart in the port (agreeing with the independent F-AUTO-09 finding) and that the gate is not what blocks this row: the Browser tab is unconditionally available without it. | sweep ?, 2026-08-19 |
| `F-WIN-07` | PASSED | **wave H live drive (this host)**: ctrl-shift-o changed active tab matching a restored snapshot, live | sweep ?, 2026-08-14 |
| `F-WIN-08` | half-proven | Live: `swaymsg kill` (real compositor `xdg_toplevel.close`) against the app window; `pgrep -P $APP_PID` showed identical 3 child PIDs before/after, `kill -0 $APP_PID` succeeded after, window kept rendering (`25-24-after-close-attempt.png`) — the "process/pane survives a close" half is proven live. The "reopen it from the Dock" half is UNREACHABLE in this lane: the headless sway config runs no tray/StatusNotifierWatcher host, and source (`tray::TrayRequest::ShowWindow -> window.activate_window()`) confirms reopen is tray-driven only, with nothing to click. Per fullapp/F-WIN.md. | sweep fullapp-9, 2026-08-19 |
| `F-WIN-09` | PASSED | Own live X11-lane drive (linux-drive.sh, DISPLAY=:1): after gsettings set action-double-click-titlebar minimize and a harness-correct window-relative double-click, WM_STATE flipped Normal to Iconic and _NET_WM_STATE gained _NET_WM_STATE_HIDDEN -- genuine ICCCM/EWMH minimize signal, not a pixel diff. A first attempt using raw xdotool --window targeting produced no change at all, independently confirming the harness's documented reason for using WIN_X/WIN_Y absolute-coordinate clicks instead. gsetting restored afterward. | sweep I4-settle, 2026-08-16 |
| `F-WIN-10` | PASSED | **wave N (live, this host)**: Was half-proven. Distinguished control_add_project (socket handler, never calls show_toast) from the real add_project UI path (main.rs:4031, does call show_toast). Live-drove: Projects + -> Open Project -> picked the already-tracked wf-dom3-fixtures folder -> Add without Git confirm -> real bottom-right toast appeared reading the duplicate-project message, auto-dismissed after TOAST_DURATION=4s. Screenshots f-win-10-toast-visible.png, f-win-10-toast-gone.png. | sweep ?, 2026-08-14 |
| `F-WIN-11` | PASSED | **wave H live drive (this host)**: all 5 update-toast states + reset driven live with custom marker text | sweep ?, 2026-08-14 |
| `F-WIN-12` | N/A — platform | **wave H live drive (this host)**: macOS-only TCC sheet, unchanged | sweep ?, 2026-08-14 |

### Projects and sidebar (19)

| id | verdict | evidence | judged |
|---|---|---|---|
| `F-SID-01` | PASSED | **wave F live drive (this host)**: Sidebar header 'Projects' with '+' Add-Project control visible in every screenshot this session, e.g. wf-sid-shots-set2/02-00-settings-opened.png. | sweep ?, 2026-08-14 |
| `F-SID-02` | PASSED | **wave F live drive (this host)**: Filtered to 'fixture-proj', 'emptytest' etc. repeatedly today; nonmatching rows disappeared (wf-sid-shots-final/02-00-filter-emptytest-gone.png shows 0 rows). Fresh unfiltered relaunches always show the full list (equivalent to clearing). Side-finding: filtering to a project hides ALL its worktree rows too (branch names don't literally match filter text, so this is consistent with the literal clause, just surprising UX). | sweep ?, 2026-08-14 |
| `F-SID-03` | PASSED | **wave I live drive (this host)**: Upgraded: the ledger's existing PASSED already covered the typed file:// clone path (P92); this pass closes the explicitly-named remaining gap ('drawn dir-picker dispatch remains for the picker mode') by driving the native-picker mode live under F-PRJ-02/03, and the Create Project mode live under F-PRJ-08/09. All three modes the live + menu offers now have a live, non-synthetic completion with a new project row appearing. | sweep ?, 2026-08-14 |
| `F-SID-04` | PASSED | **wave F live drive (this host)**: fixture-proj expanded/collapsed repeatedly while isolating worktree rows for drag tests, e.g. wf-sid-shots-drag2/02-00-before.png. | sweep ?, 2026-08-14 |
| `F-SID-05` | half-proven | Worktree-row click fully proven: clicking a worktree row highlights it and changes the central pane + status-bar breadcrumb (e.g. `03-19-worktree-row-clicked.png`). But project-row click does not change the central surface at all, only toggles expand/collapse: with `master`/fix1 shown, clicking `sweep1sid-fix2`'s project header (`02-18-project-row-clicked.png`) toggled its disclosure triangle but Files panel and status bar stayed unchanged on `master · /dev/shm/sweep1sid-fix1`. Only the worktree half of the row's "either kind of row selectable" clause holds. Source: fullapp/F-SID.md. | sweep fullapp-9, 2026-08-19 |
| `F-SID-06` | PASSED | **wave I live drive (this host)**: Re-confirmed live: tillerctl notify --session pane-1 --status error against linux/gpui-waku's terminal pane flipped that worktree row's dot to red (Terminal tab also flagged with '!'); collapsing the parent tiller project row then showed the identical red dot on the collapsed project row, absent before the notify. reference/linux-progress/wf-prj/f-sid-06-worktree-error-dot.png, f-sid-06-collapsed-project-error-dot.png | sweep ?, 2026-08-14 |
| `F-SID-07` | PASSED | **wave F live drive (this host)**: Hover-then-click on a project row's gear reliably opened Project Settings throughout, e.g. wf-sid-shots-set2/02-00-settings-opened.png, wf-sid-shots-emoji3/02-00-settings-check.png. | sweep ?, 2026-08-14 |
| `F-SID-08` | PASSED | **wave N (live, this host)**: Sidebar row right-click -> Initialize Git repository, driven live on wf-sid2-nongit; Repository flips Folder->Git. reference/linux-progress/wf-sid2/f-sid-08-*.png | sweep ?, 2026-08-14 |
| `F-SID-09` | PASSED | **wave I live drive (this host)**: Re-confirmed live with a process-tree discriminator: right-click wf-prj-gitfolder project row -> Show in File Manager; ps aux before/after shows a genuinely new process /usr/bin/cosmic-files /home/enzopalmisano/wf-prj-gitfolder with the real project path as argv. reference/linux-progress/wf-prj/f-sid-09-context-menu-show-in-file-manager.png | sweep ?, 2026-08-14 |
| `F-SID-10` | PASSED | **wave N (live, this host)**: Sidebar row right-click -> Remove Project -> confirm, full cycle in one invocation; row disappears, DB-only removal verified safe against the real tiller repo. reference/linux-progress/wf-sid2/f-sid-10-*.png | sweep ?, 2026-08-14 |
| `F-SID-11` | PASSED | **wave N (live, this host)**: tillerctl notify --session pane-1 --status running against a real registered pane; orange activity glyph appears on the worktree row, status bar updates. reference/linux-progress/wf-sid2/f-sid-11-*.png | sweep ?, 2026-08-14 |
| `F-SID-12` | PASSED | **wave I live drive (this host)**: Re-confirmed live: right-click the non-primary linux/gpui-waku worktree row shows Set Primary as the first context-menu item, no chord; clicking it moved the Primary badge from rust/gpui-rewrite onto linux/gpui-waku in the next frame. reference/linux-progress/wf-prj/f-sid-12-context-menu-set-primary.png, f-sid-12-primary-badge-moved.png | sweep ?, 2026-08-14 |
| `F-SID-13` | PASSED | **wave F live drive (this host)**: From the unfiltered sidebar, clicked fixture-proj's 'New Worktree...', filled branch name 'sid-nw', pressed Enter; new worktree appeared under fixture-proj at /tmp/wf-sid-fixtures/fixture-proj-sid-nw, auto-selected, showing empty-terminal state (wf-sid-shots-nw2/03-01-after-enter.png). | sweep ?, 2026-08-14 |
| `F-SID-14` | PASSED | **wave Q fresh critic (live, this host)**: fixed side fully driven live -- all three agent-panel menu items, both directions. Reproduction of the original defect was structural only, not a live pre-fix drive; the critic names this limit itself rather than implying a full two-sided test. See docs/linux-rewrite/CRITIC-waveQ-builder-claims.md. \\| wf-judge3, x86 box, 2026-08-19 | sweep ?, 2026-08-14 |
| `F-SID-15` | PASSED | **wave N (live, this host)**: Full Remove Worktree confirm-and-watch-it-disappear cycle on a real second worktree; git worktree list and ls confirm the directory is actually gone afterward. reference/linux-progress/wf-sid2/f-sid-15-*.png | sweep ?, 2026-08-14 |
| `F-SID-16` | PASSED | **wave F live drive (this host)**: Dragged second-proj from position 2 toward the top; order changed (moved to position 6 instead, before emptytest) - literal clause ('confirm the order changes') satisfied. wf-sid-shots-drag1/03-01-after-drag.png vs 02-00-before-drag.png. Side-finding: landing position did not match the drop point, worth a human look but not scored as a failure since the clause only requires the order to change. | sweep ?, 2026-08-14 |
| `F-SID-17` | PASSED | **wave F live drive (this host)**: Dragged the sid-nw worktree row up to just under master; fixture-proj's worktree order changed from master,sid-feature,sid-nw to master,sid-nw,sid-feature exactly as dropped (wf-sid-shots-drag2/03-01-after-drag-worktree.png). | sweep ?, 2026-08-14 |
| `F-SID-18` | PASSED | **wave F live drive (this host)**: Selecting a worktree with zero tabs showed 'No Terminals' / 'Open a new terminal to get started.' / a New Terminal button (wf-sid-shots-ct6/03-01-...); clicking that button created a real terminal tab (wf-sid-shots-ct4/02-00-after-new-terminal-button.png). | sweep ?, 2026-08-14 |
| `F-SID-19` | PASSED | **wave K fresh critic (live, this host)**: Prior instance drove Ctrl+Shift+P (command palette) from the same zero-tab empty state without ever pressing Ctrl+T, confirming the fix generalizes beyond the one named keybinding. Verified already committed (be2a74a5). [orchestrator note: the critic did NOT reproduce the original defect for this row and said so; the fixed behaviour is proven, the pre-fix contrast is not] | sweep ?, 2026-08-14 |

### Project creation and project settings (18)

| id | verdict | evidence | judged |
|---|---|---|---|
| `F-PRJ-01` | PASSED | **wave I live drive (this host)**: Live re-confirm: sidebar + opens an opaque 3-choice card (Open Project…/Clone Repository…/Create Project…), no bleed-through of Filter field or project rows beneath it. reference/linux-progress/wf-prj/f-prj-01-add-menu-opaque.png | sweep ?, 2026-08-14 |
| `F-PRJ-02` | PASSED | **wave I live drive (this host)**: Upgraded from synthetic-test-only evidence to a real end-to-end drive: + -> Open Project… opened the real GTK 'Open Folder' dialog (private D-Bus bus + portal stood up this pass), navigated Home, single-clicked wf-prj-gitfolder (real pre-existing git repo), clicked Add Project. New project row + master worktree appeared, backed by the real on-disk path with .git present. reference/linux-progress/wf-prj/f-prj-02-gitfolder-added-via-picker.png | sweep ?, 2026-08-14 |
| `F-PRJ-03` | PASSED | **wave I live drive (this host)**: All three named choices driven live via the real native picker (previously only a drawn cargo test simulated this): picked a real non-git folder -> 'This folder is not a git repository' with exactly Initialize Git / Add without Git / Cancel. Initialize Git created a real .git dir + master worktree; Add without Git added the project with no .git and no branch name; Cancel added nothing (no project row appeared). Three separate fresh folders used, one per arm. reference/linux-progress/wf-prj/f-prj-03-nongit-prompt-three-choices.png, f-prj-03-initialize-git-success.png, f-prj-03-add-without-git-success.png, f-prj-03-cancel-no-project-added.png | sweep ?, 2026-08-14 |
| `F-PRJ-04` | PASSED | **wave I live drive (this host)**: Both OR-clause arms proven live with real non-silent errors. Folder-selection fails: before the private portal was up, clicking Open Project… showed a real visible error 'could not open the folder picker: ZBus Error: Failed to connect... Connection refused (os error 111)' -- also disproves the ledger's carried-forward 'no UI door' claim; the door exists. Insertion fails: same collision/invalid-URL errors as F-PRJ-05/07/10. reference/linux-progress/wf-prj/f-prj-04-folder-picker-zbus-error.png | sweep ?, 2026-08-14 |
| `F-PRJ-05` | PASSED | **wave I live drive (this host)**: Re-confirmed live: invalid clone URL produced real git-exit-128 failure text; corrected to file:///home/enzopalmisano/wf-prj-gitfolder and retried -> real clone landed on disk at ~/Tiller/projects/wf-prj-gitfolder with .git present and a master worktree. reference/linux-progress/wf-prj/f-prj-05-07-clone-invalid-url-error.png, f-prj-05-07-clone-retry-success.png | sweep ?, 2026-08-14 |
| `F-PRJ-06` | PASSED | **wave I live drive (this host)**: Re-confirmed live: Clone repository button rendered visibly disabled with the URL field empty. Double-submit guard indirectly confirmed: two rapid clicks on Retry clone (no intervening sleep) produced exactly one project/clone, not two. Direct mid-clone muted-button screenshot not retaken this pass (fast local file:// clone) -- ledger's existing PASSED already carries that frame from an earlier pass. | sweep ?, 2026-08-14 |
| `F-PRJ-07` | PASSED | **wave I live drive (this host)**: Same continuous drive as F-PRJ-05: invalid-URL failure shown, then corrected and retried successfully in the same form instance without closing/reopening -- both arms satisfied in one session. reference/linux-progress/wf-prj/f-prj-05-07-clone-invalid-url-error.png, f-prj-05-07-clone-retry-success.png | sweep ?, 2026-08-14 |
| `F-PRJ-08` | PASSED | **wave I live drive (this host)**: Re-confirmed live: Create Project form has name field, Parent location, live 'Creates ...' preview, Create/Cancel. Typed wf-prj-created-test -> preview live-updated to the full destination path and the Create button visibly brightened from disabled to enabled in the same frame. reference/linux-progress/wf-prj/f-prj-08-create-form-live-preview.png | sweep ?, 2026-08-14 |
| `F-PRJ-09` | PASSED | **wave I live drive (this host)**: Re-confirmed live: clicked Create project -> popover closed, new wf-prj-created-test project row appeared, and /home/enzopalmisano/Tiller/projects/wf-prj-created-test exists as a real directory on disk (checked with ls). reference/linux-progress/wf-prj/f-prj-08-09-create-project-success.png | sweep ?, 2026-08-14 |
| `F-PRJ-10` | PASSED | **wave I live drive (this host)**: Re-confirmed live: reused the same project name -> verbatim visible red 'Creation failed: could not create .../wf-prj-created-test: File exists (os error 17)' with a Retry creation control, no duplicate row created. reference/linux-progress/wf-prj/f-prj-10-create-collision-error.png | sweep ?, 2026-08-14 |
| `F-PRJ-11` | PASSED | **wave F live drive (this host)**: Opened Project Settings for the disposable 'emptytest' project, clicked 'Remove Project', got 'Remove project from Tiller? This only removes the project from Tiller's si[debar]...' dialog (wf-sid-shots-rmproj4/02-00-remove-clicked.png), clicked 'Remove from Tiller'. Confirmed: DB row gone (select ... where id='p-9e09ff1bd85cc908' -> empty), sidebar filter for 'emptytest' returns 0 rows (wf-sid-shots-final/02-00-filter-emptytest-gone.png), and /home/enzopalmisano/Tiller/projects/emptytest still exists on disk via ls -la after removal. | sweep ?, 2026-08-14 |
| `F-PRJ-12` | PASSED | **wave F live drive (this host)**: Display name: typing into second-proj's Display Name field updated the sheet's own header live ('Project Settings · siddisp') and persisted to DB; sidebar showed the new name after relaunch (wf-sid-shots-verify/02-00-sidebar-check.png: siddispsiddisp). Repository type: on Folder-type wf-sid-created, clicking 'Initialize Git' flipped 'Repository: Folder'+button to 'Repository: Git'+icon panel (wf-sid-shots-repotype3 -> repotype4). No reverse (Git->Folder) control found anywhere for a Git project, consistent with the clause's 'where allowed' qualifier. | sweep ?, 2026-08-14 |
| `F-PRJ-13` | PASSED | **wave K fresh critic (live, this host)**: Prior instance reproduced the exact original fixture geometry live (12-worktree decoy project, click at Reset's own pixel position) showing no leak, AND built a pre-fix binary at commit 984defa7 and ran a spliced red/green test pair (genuinely red pre-fix, green post-fix). Verified already committed (d8f35980). | sweep ?, 2026-08-14 |
| `F-PRJ-14` | PASSED | **wave O (live, this host)**: same portal fix applied and verified via dbus-monitor trace of the full OpenFile call chain; real PNG fixture selected with live GTK thumbnail preview, app's Avatar tab shows applied swatch + 'Current: wf-prj14-avatar.png' (commit f88dd7ca) [original defect independently reproduced on a pre-fix binary] \\| wf-sweep2 critic, x86 box, 2026-08-19 | sweep ?, 2026-08-14 |
| `F-PRJ-15` | PASSED | **wave F live drive (this host)**: On second-proj's Project Settings (Icon tab, default-selected), clicked the globe icon glyph; header/preview updated instantly, persisted to DB (icon_kind='icon', icon_value='globe'), and appeared in the sidebar as a blue globe next to siddispsiddisp (wf-sid-shots-verify/02-00-sidebar-check.png). | sweep ?, 2026-08-14 |
| `F-PRJ-16` | PASSED | **wave F live drive (this host)**: Emoji tab confirmed (2nd of 3). Empty-input validation live: clicking Set Emoji with nothing typed shows 'Enter exactly one emoji.' (wf-sid-shots-emoji4/04-02-after-set-single.png). Open Emoji Picker live: opens searchable glyph grid (wf-sid-shots-picker/02-00-emoji-picker.png); clicking a glyph (star) closed the picker and set fixture-proj's icon to it, confirmed via DB (icon_kind='emoji', icon_value='star emoji') and sidebar. Caveat: direct typed-emoji input into the field could not be positively confirmed today because wtype would not deliver either of two tried Unicode emoji into the field at all (field stayed empty) - judged a drive-tool limitation, not an app defect, since the identical underlying accept-a-single-emoji path was proven via the picker moments later. | sweep ?, 2026-08-14 |
| `F-PRJ-17` | PASSED | **wave K fresh critic (live, this host)**: Prior instance clicked the real 'Use Primary' button live, confirmed the field snapped to primary branch and persisted across reopen plus a direct read-only sqlite3 query on the live DB. Verified already committed (0df584f5). [orchestrator note: the critic did NOT reproduce the original defect for this row and said so; the fixed behaviour is proven, the pre-fix contrast is not] | sweep ?, 2026-08-14 |
| `F-PRJ-18` | half-proven | fullapp/F-PRJ.md: no-portal error path driven live twice this pass, confirming 88d8114c's data-layer fix from the prior critic report is real (its own unit test passes); but the pass found and reproduced the error message does not actually reach the user, due to a rendering layering issue in the sidebar-level notice field distinct from the original bug (see Defect 1), a layering bug this report shows is specific to that notice path and does not touch the Chosen/success branch. Success path (portal present, folder chosen) not personally redriven this pass, same picker blocker as F-PRJ-02/03/14 — but unlike those three, it was already driven to completion end-to-end in a prior wave (portal-hang root-caused and fixed, dialog rendered, search/select/accept done, Worktree Location field updated with Restore Default link appearing, commit de508dfa), which this pass did not contradict. | sweep fullapp-9, 2026-08-19 |

### Tabs, panes, navigation (28)

| id | verdict | evidence | judged |
|---|---|---|---|
| `F-TAB-01` | half-proven | Tab-strip decorations (type icon, active state, dirty dot, close control) for Terminal/agent-chat/Browser tabs directly verified live across many screenshots (e.g. `sweep-3-F-TAB/a/06-05-newchat-submenu.png`, `ftabresume-out/07-06-tab-ctxmenu-before-close.png`). But `TabKind::Editor`/`TabKind::Diff` (`rust/crates/tiller_project/src/tab.rs:10-21`) were never opened live — file-open is blocked by the missing xdg-desktop-portal (see F-TAB-09) — so that half is NOT EXERCISED, not confirmed; per fullapp/F-TAB.md. | sweep fullapp-9, 2026-08-19 |
| `F-TAB-02` | PASSED | 02-g2-01-overflow-open.png: chevron opened w/ all 9 tabs listed, checkmark on active Browser tab. \| wave A shard critic, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |
| `F-TAB-03` | PASSED | 03-g1-02-new-terminal.png: plus->New Terminal produced a second active Terminal tab w/ fresh shell prompt. \| wave A shard critic, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |
| `F-TAB-04` | PASSED | Same capture as F-TAB-03 — this port's plus control is the New Tab menu (no separate menu-bar control on Linux). \| wave A shard critic, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |
| `F-TAB-05` | PASSED | 03-g1-09/11/13, 05-g1-04, 06-g1-05, 07-g1-06: all 5 adapters (Claude Code, Codex, OpenCode, Pi, Oh-My-Pi) created+activated tabs; Oh-My-Pi surfaced its launch error per clause's documented branch. \| wave A shard critic, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |
| `F-TAB-06` | PASSED | 05-g1-15-newbrowser-clicked.png: Browser tab+nav chrome created; grep confirms no feature-gate exists (always enabled). Page render failed on GPUI Wayland handle passthrough — noted as environmental, not app defect. \| wave A shard critic, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |
| `F-TAB-07` | PASSED | **wave M live drive (this host)**: New Chat submenu lists exactly the ACP-capable adapters (Claude Code, Codex); clicking each created a distinct new tab in both strip and sidebar, both visible simultaneously with distinct icons. | sweep ?, 2026-08-14 |
| `F-TAB-08` | PASSED | **wave M live drive (this host)**: Booted a second instance with PATH stripped of all agent CLIs; New Chat rendered the no-agent fallback row, clicking it navigated to Settings->Agents showing all 5 adapters tagged Not found on PATH. | sweep ?, 2026-08-14 |
| `F-TAB-09` | PASSED | **wave P (live, this host)**: Real native GTK Open File dialog driven end-to-end via the dbus-update-activation-environment + portal-restart recipe; opened a markdown file and a code file live, each landing in the correct editor mode. Committed pre-compaction (934ed922). \\| wf-rest4, x86 box, 2026-08-19 | sweep ?, 2026-08-14 |
| `F-TAB-10` | PASSED | **wave M live drive (this host)**: Split Right/Down/Left/Above each produced a new independently-live pane group (distinct advancing timestamps/markers per side) confirmed across 4 directions. | sweep ?, 2026-08-14 |
| `F-TAB-11` | PASSED | **wave M live drive (this host)**: Narrowed a pane below 160pt; Split Left/Right greyed out with exact computed reason text ("Npt available, 160pt required") drawn under the label, no click handler on the disabled rows. | sweep ?, 2026-08-14 |
| `F-TAB-12` | PASSED | **PASSED 2026-08-19 by a fresh, unrelated critic** (`fullapp/CRITIC-move-to-this-pane.md`, judging `2ff664bb`; in HEAD): the always-disabled, structurally-unreachable "Move to This Pane" item is deleted for the right reason, pinned by a test at the decision point, with no regression across 54 tab/move tests or the 203-test `tiller` suite. **The surviving twin the same critic flagged is now closed too (`3c470e59`).** `command_palette.rs` offered "Move Tab to This Pane" gated on `has_other_pane`, and that gate is what made it *worse* than the context-menu copy: it asks whether another pane exists, but the command runs `move_selected_tab(MoveTarget::CurrentPane)`, and from the palette `tab_menu_tab` is always `None`, so the tab it moves is the active one — already in the active group by definition. The entry was **enabled and inert** whenever a second pane existed; the deleted context-menu copy at least drew visibly disabled. Removing it showed the whole chain was dead (`TabCommand::MoveTabToCurrentPane` never constructed), so the action, handler, dispatch arm, listener and declaration went with it rather than leaving an unreachable token — the bin suite is 216 before and after, which is what proves nothing depended on it. `MoveTarget::CurrentPane` stays: it lives in `tab_machinery` with its own test. Pinned by `the_palette_does_not_offer_move_tab_to_this_pane`, which asserts `has_other_pane` is true in the fixture **first** (a test built on the false branch would pass on an accidental re-add) and checks the meaningful neighbours survive; positive control confirms it fails when the label returns. See also F-TAB-13, whose cited evidence this deletion invalidated. **Superseded record of the failure:** Move to New Pane and Move to Pane N still confirmed working (ftabsplit-out, prior-run ac/: tab's pane-group membership visibly changed across two independent moves). But Move to This Pane is dead code: source-read of main.rs:8839-8866 (TabContextItem::disabled(..., "no other tab is available")) shows it is built unconditionally disabled, with no if/else branch to ever enable it, unlike neighboring items that correctly branch on live eligibility — making that clause structurally unreachable through the UI in any app state. See fullapp/F-TAB.md. | sweep fullapp-9, 2026-08-19 |
| `F-TAB-13` | half-proven | **Downgraded from PASSED 2026-08-19: this row's cited evidence no longer exists, and nobody would have noticed if the F-TAB-12 critic hadn't said so** (`fullapp/CRITIC-move-to-this-pane.md`, "Collateral"). It passed on `03-h9-02-close-to-right-result.png` — *"with zero eligible tabs, Move to This Pane shows explanatory disabled text 'no other tab is available'"* — which is **exactly the menu item F-TAB-12's fix deleted as unreachable dead code**. The critic checked whether the clause is still satisfiable in any app state and found it is not: every remaining `TabContextItem::disabled(...)` in `tab_context_items()` belongs to Move Earlier/Move Later, Attach to Current Terminal, or Resume Chat; the Move to New Pane / Move to Pane N branch (`main.rs:8925-8938`) is built unconditionally **enabled**. Confirmed live with a single tab in a single pane group: no disabled sibling, no "no tabs available" message anywhere near it. **The half that still stands:** this row also credits itself via Resume Chat's still-gated empty state ("no retained chat is available"), untouched by the deletion. **The half that is now unsatisfiable by design:** the Move Existing Tab clause (VERIFY at `01-inventory-app.md:73`, SRC `SplitContentMenu.swift:292`). This is a **deliberate divergence needing a ruling, not a defect to fix** — Swift disables the degenerate case of moving your only tab; we enable it, which the critic judged "unambiguously correct behavior", since moving your only tab into a fresh pane is always meaningful. Flagged for the user rather than designed around: restoring fidelity here would mean re-adding a disabled item on purpose. | sweep ?, 2026-08-19 |

**A larger divergence found underneath it, 2026-08-19, while reading the SRC this row cites.** `App/Workspace/SplitContentMenu.swift:286` is a **"Move Existing Tab" submenu that lists individual tabs by name**, split into "This Pane" and "Other Panes" sections, with `moveExistingTab(tabID)` per row and the explanatory empty states this row's VERIFY clause quotes ("No other tabs in this pane." / "No other panes in this layout."). You choose *which tab* to bring into the pane you right-clicked. **The port has no such submenu at all.** It has two unrelated commands — "Move to This Pane" (now deleted, both copies) and "Move to Pane N" — that move *the selected tab*, with no tab picker anywhere. That collapse from a per-tab chooser to a single command is why our version was degenerate enough to be dead code. So this row's clause is not merely unsatisfiable by an accident of our gating; the feature it describes was never ported. That is a scope question for the user, not a defect to patch quietly. | sweep ?, 2026-08-14 |
| `F-TAB-14` | PASSED | **wave O (live, this host)**: Pre-fix (wfj2pre14): double-click leaves panel.list title unchanged, no rename box appears. Fix (wfj2tab14d, 2s settle after discovering a load-induced focus-race harness trap): panel.list title -> 'TerminalRENAMETEST'. Context-menu 'silent fail' re-driven with a generous sleep (wfj2tab14f) -> committed cleanly ('TerminalCTXMENURENAME'), reinforcing not overturning the builder's harness-artifact conclusion. [original defect independently reproduced on a pre-fix binary] \\| wf-judge2, x86 box, 2026-08-19 | sweep ?, 2026-08-14 |
| `F-TAB-15` | PASSED | 02-h1->02-h4->02-h5-01-chat-closed-clean.png: clean Chat tab closed via context-menu Close, removed w/ no prompt. \| wave A shard critic, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |
| `F-TAB-16` | PASSED | 02-h2-01-close-dirty-dialog.png (prompt appears) -> 02-h3-01-cancel-clicked.png (Cancel keeps tab) -> 03-h3-02-close-confirmed.png (Close removes it). \| wave A shard critic, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |
| `F-TAB-17` | PASSED | 03-h11/02-h12 (Close Tabs to the Right: bulk dirty-confirm dialog naming Codex/Pi/Claude Code, then 04-h14-03-confirmed.png single tab remains); 03-h15-02/04-h15-03 (Close Others: same bulk-dialog pattern, confirmed). \| wave A shard critic, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |
| `F-TAB-18` | PASSED | **wave M live drive (this host)**: Built a position->pane-id map via click+panel.list, dragged one tab past a sibling, re-mapped positions: pane identity at the two slots swapped exactly matching the drag, confirmed via control-socket reads not screenshots. | sweep ?, 2026-08-14 |
| `F-TAB-19` | PASSED | **wave M live drive (this host)**: Forward already proven by predecessor. Backward Ctrl-Shift-Tab: two real transitions captured via panel.list's active id, including a correct backward wrap (position 1 -> last tab) and a further single backward step. | sweep ?, 2026-08-14 |
| `F-TAB-20` | PASSED | **wave P (live, this host)**: Ctrl-5/Ctrl-9 each hit 14/14 across two trial batches using a panel.list + tab.select hard control-socket discriminator (screenshots alone had reproduced the ledger's flaky 0-hit finding). Ctrl-9's clamp-to-last confirmed structurally via TabSelection::jump. Committed this session (c90e8fdc) after finishing the pending edit from before compaction. \\| wf-rest4, x86 box, 2026-08-19 | sweep ?, 2026-08-14 |
| `F-TAB-21` | PASSED | 03-h9-02/02-h12: Move Earlier/Move Later present+enabled in the tab context menu (this port folds the macOS 'Tab menu' into it, no separate menu bar exists); exercised earlier this same live session with confirmed position change. \| wave A shard critic, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |
| `F-TAB-22` | PASSED | **wave J live drive (this host, recovered)**: horizontal pane-focus cycling (Ctrl-Alt-arrow) proven live with a hard discriminator in both directions | sweep ?, 2026-08-14 |
| `F-TAB-23` | PASSED | **wave M live drive (this host)**: Same one mechanism as F-TAB-10 (terminal context menu's Split Left/Right/Above/Down), confirmed by reading the dispatch code; not re-screenshotted separately. [orchestrator note: passed by equivalence to F-TAB-10, which this agent drove in ALL FOUR directions. The two rows cite different Swift sources (WorkspaceMenuCommands vs SplitContentMenu) and this port folds those two macOS menus into one control -- the same folding already recorded for F-TAB-04/21. The BEHAVIOUR is live-proven; a separate Pane menu was not driven because this port has none.] | sweep ?, 2026-08-14 |
| `F-TAB-24` | PASSED | **wave O (live, this host)**: Pre-fix (wfj2pre24c, positive-control-validated gesture): Escape mid-drag does not stop the reorder, tab order flips despite Escape. Fix (wfj2fix24): order unchanged with Escape, flips without (positive control) -- both 2-tab and 3-tab cases closed, plus drop-clears-snapshot (a post-drop stray Escape is a no-op). Additional finding, not part of this row's own clause: the report's sidebar-drag ('no Escape handling of its own') claim was live-driven anyway (2 gesture recipes x with/without Escape, 4/4 consistent) and did NOT reproduce -- Escape mid-sidebar-drag left no stray reorder in every trial, contradicting the report's characterization; recorded as a finding, not a verdict change. [original defect independently reproduced on a pre-fix binary] \\| wf-judge2, x86 box, 2026-08-19 | sweep ?, 2026-08-14 |
| `F-TAB-25` | PASSED | **wave M live drive (this host)**: Attach to Current Terminal correctly disabled with reason when self-targeting, enabled cross-tab; clicking it merged two independently-live terminal surfaces into one combined view simultaneously. | sweep ?, 2026-08-14 |
| `F-TAB-26` | PASSED | **PASSED 2026-08-19 — driven through this row's own reproduction, not an adjacent one** (`fullapp/BUILDER-verify-tab26-term05.md`, shots `04`-`08`, commit `a22fb87b`). The row failed on the **right-click → "Close Terminal…"** path, and the drive that cleared F-TERM-08 used the `ctrl-alt-w` chord, so the chord's result was not accepted for this row — "same code path therefore same result" is exactly what let F-TERM-08's first critic pass a control from a state the bug could not occur in. Driven properly: a real virtual-pointer right-click on a plain **idle** bash terminal opens the 13-item menu, and "Close Terminal…" raises the "Close terminal?" banner (`05`) — idle being the case the old status gate silently swallowed. Cancel leaves the terminal alive (`06`). **The counterpart was checked in the same session on the same idle terminal, which is what proves the two contracts did not collapse into one:** the Activity row's own close goes straight to "No Terminals"/"No activity" with **no prompt at all** (`07` → `08`), because `request_close_activity` (`main.rs:4987`) keeps its `pane_close_needs_confirmation` gate while `request_close_terminal_at` (`:4950`) is unconditional. Two paths, deliberately different, both observed live. The `whole_tab` half is fixed too (`main.rs:4961`): it was hardcoded `false`, so "Close Anyway" on a single-pane tab hit the F-TAB-13 last-leaf guard and silently did nothing. **Superseded record:** **Fixed by the same change as F-TERM-08 (`c0834159`), and half-proven for a deliberately narrow reason.** The root cause this row named — `request_close_terminal_at` gating on `pane_close_needs_confirmation`, so an idle or finished terminal closed silently — is gone: that function is now **unconditional** (`main.rs:4950`), matching Swift's context menu, which consults no status at all. Its sibling `whole_tab` bug is fixed too and documented in-source at `main.rs:4961`: `close_terminal_at` refuses to remove a tab's last leaf (the F-TAB-13 empty-state guard), so closing a single-pane tab's only pane is really closing the tab; `whole_tab` was hardcoded `false`, which made "Close Anyway" hit that guard and silently do nothing — banner dismissed, tab still there. It is now `tab.panes.leaf_ids().len() <= 1`. The builder's claim that this row "was already fixed independently on `linux/gpui-waku`" was **checked against the merged source rather than taken on trust**, and the code above is what was found. **Why not PASSED:** this row's own reproduction was the **right-click → "Close Terminal…" context-menu** path, driven 3× when it failed. The live drive that cleared F-TERM-08 opened the banner with the `ctrl-alt-w` chord instead. Both reach `request_close_terminal_at`, but "same code path, therefore same result" is precisely the reasoning that let F-TERM-08's *first* critic pass a control from an entry point the bug could not occur in — so it is not being used again here. One drive that opens the context menu on an idle terminal and confirms the banner appears closes this row. **Superseded record of the failure:** Prior multi-pane "Close Anyway" banner fix still holds (3-pane tab: closing one Running pane drops panel.list 3->2, tab survives, banner correctly says "pane" not "tab"). But per fullapp/F-TAB.md, the ordinary "Close Terminal..." context-menu path never asks for confirmation at all: reproduced 3x independently (idle sole terminal, a terminal running `sleep 300`, and a fresh terminal) — each time panel.list goes straight to [] with zero dialog. Root cause (main.rs:4817-4850): pane_close_needs_confirmation gates on ActivityStatus::requires_close_confirmation, the agent-activity model, not on whether the pty has a live child process, so a plain bash terminal is always Idle and never trips the gate. | sweep fullapp-9, 2026-08-19 |
| `F-TAB-27` | PASSED | **wave M live drive (this host)**: Sent a real message to a Claude Code chat tab, got a real agent reply, closed the tab; Resume Chat flipped disabled->enabled in the tab-strip context menu; clicking it reopened the tab showing the exact prior transcript (message + reply + timestamp) verbatim. | sweep ?, 2026-08-14 |
| `F-TAB-28` | PASSED | **wave M live drive (this host)**: Ctrl-W cleanly closed a non-dirty active tab (panel.list pane count dropped by exactly one, twice, across two separate tabs); a further landed press on a terminal with unsaved content raised a distinct "Close dirty tab?" confirmation, and Cancel correctly preserved it. | sweep ?, 2026-08-14 |

### Chat surface over ACP (37)

| id | verdict | evidence | judged |
|---|---|---|---|
| `F-CHAT-01` | PASSED | Real turn transcript shows user bubble, agent markdown reply, and turn timestamp together (02-06-turn1-completed.png: 'PONG' reply, '11:29' stamp). \| wave B, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |
| `F-CHAT-02` | PASSED | **wave F live drive (this host)**: Fix (min_w_0() on the message flex child, chat.rs:4973-4981) re-verified live at both required widths with a click-through discriminator (transcript entry count grows). 1715px: /tmp/wf-chat02d-shots/02-chat-open-1715.png (Retry visible, wrapped text) -> 03-after-retry-click-1715.png (click landed). 2400px: /tmp/wf-chat02g-shots/02-wide-2400-before-click.png -> 03-wide-2400-after-click.png. Committed write-up in docs/linux-rewrite/FINISH-chat.md. | sweep ?, 2026-08-14 |
| `F-CHAT-03` | PASSED | **wave F live drive (this host)**: Fix re-verified with a PID discriminator, not the button caption. Baseline agent PID recorded, agent killed to produce ErrorKind::Disconnected banner (/tmp/wf-chat03b-shots/03-after-disconnect.png, chat-restart-agent selector present, chat-retry absent), 'Restart agent' clicked (/tmp/wf-chat03d-shots/03-after-restart-click.png), foreground process PID after click differs from pre-disconnect PID -- a genuinely new OS process was spawned. | sweep ?, 2026-08-14 |
| `F-CHAT-04` | PASSED | Return sends (03-05-after-return-sent.png); Shift+Return inserts newline without sending (03-02-text-typed.png -> 03-02-after-shift-return.png). \| wave B, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |
| `F-CHAT-05` | PASSED | Re-driven cheaply. Offline composer disabled with 'Agent offline — reconnecting when you send…' (dc-01.png); permission-wait composer shows 'Waiting for permission response…' non-editable (03-02-permA-pending.png). \| wave B, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |
| `F-CHAT-06` | PASSED | Composer placeholder 'Type to queue for the next turn…' with status=working captured mid multi-step real turn (10-09-mention-chip.png). \| wave B, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |
| `F-CHAT-07` | PASSED | Stop-button click and separate Escape-key trial both shown returning the turn to non-active/ready state (03-02-after-cancel-click.png, 03-02-after-escape.png). \| wave B, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |
| `F-CHAT-08` | PASSED | Connecting/offline spinner state (dc-01.png), idle send-arrow state, and working stop-square state (L-124532-01-mid-stream.png) all observed. \| wave B, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |
| `F-CHAT-09` | PASSED | Slash popup open, filtered by prefix, keyboard-navigated, and click-inserted as a chip across 02-01-slash-popup.png, 03-02-slash-filtered.png, 04-03-slash-arrow-down.png, 05-04-slash-inserted.png. \| wave B, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |
| `F-CHAT-10` | PASSED | **wave F live drive (this host)**: Typed '@' in composer (via `type "@"`, the named-key DSL rejects '@'), popup opened fed by the real bounded fs walk, filtered to README.md by typing 'read' (/tmp/wf-chat1011-shots/02-mention-popup-filtered2.png), clicked the README.md row. 03-mention-chip-inserted.png shows a real chip (icon + remove control) replacing the raw @readme text in the composer -- the insert-a-chip half the prior pass could not confirm is now directly screenshotted. | sweep ?, 2026-08-14 |
| `F-CHAT-11` | PASSED | **wave I live drive (this host)**: First-ever complete live exercise of this row (prior passes never got past observing the D-Bus OpenFile call fire, or never reached it at all). Using the same private-D-Bus-bus technique, clicked the composer's + attach control -> real GTK 'Open File' dialog with accept label 'Attach image' (matches the app's own prompt_for_paths call). Accept arm: selected a real 4x4 PNG (hand-built, verified `file`-typed as PNG beforehand) -> a real Image chip with a remove control appeared. Reject arm: selected a plain .txt file -> composer showed 'Only PNG and JPEG images can be attached.' in red, no chip added. Multi-selection rejection not separately driven this pass. reference/linux-progress/wf-prj/f-chat-11-open-file-dialog.png, f-chat-11-image-chip-attached.png, f-chat-11-unsupported-type-rejected.png | sweep ?, 2026-08-14 |
| `F-CHAT-12` | PASSED | **wave F live drive (this host)**: Re-scoped off the unreachable native picker onto the xdnd-drop attach path (reaches the same chip UI). A real xdnd drag attached wf-chat-attach-me.txt as a File chip; clicking the chip's remove control produced /tmp/wf-chat1213-shots/03-chip-removed.png showing the composer back to its empty pre-attach state. | sweep ?, 2026-08-14 |
| `F-CHAT-13` | PASSED | **wave F live drive (this host)**: Real xdnd drag (Scripts/xdnd-source, not GPUI's simulated drag) of a supported file completed the full compositor handshake and produced a chip (/tmp/wf-chat13-shots/02-after-xdnd-drop.png). Unsupported-file half driven per the codebase's own framing (dropping_external_files_attaches_chips_and_rejects_the_oversized_one test names oversized image as the drop path's one rejection route; non-image files are never rejected, only accepted as generic File chips): dropping an 11MB dummy PNG produced the transient 'wf-chat-big.png is too large (max 10 MB)' rejection banner, /tmp/wf-chat1213-shots/04-after-oversized-drop.png. | sweep ?, 2026-08-14 |
| `F-CHAT-14` | PASSED | Overflow menu with Follow Edited Files toggle (L-073-follow-toggled.png); New Conversation resets transcript to empty and composer to a fresh state (L-123904-02-after-newconv.png). \| wave B, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |
| `F-CHAT-15` | PASSED | Mode pill lists all six modes (Auto, Manual, Accept Edits, Plan Mode, Don't Ask, Bypass Permissions) with current mode highlighted (03-02-mode-pill-options.png); selecting a different mode updates the pill label (02-01-manual-mode-set.png). \| wave B, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |
| `F-CHAT-16` | PASSED | Independently re-driven with real claude this pass: search filters results, no-match state shown, Recommended labels present, selection updates pill (L-125946/47-mp-02/03*, 03-mp-15-sonnet-selected.png). \| wave B, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |
| `F-CHAT-17` | PASSED | Independently re-driven this pass: effort sub-picker opens, LOW selection lands in the composer pill (05-mp-17-effort-low.png, 03-ef-02-low-selected.png). \| wave B, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |
| `F-CHAT-18` | PASSED | **wave F live drive (this host)**: chat_fixture.py composer mode's usage_update(170000,200000) on session/new opened a popover with fraction/remaining shown (/tmp/wf-chat1819c-shots/03-ring-popover.png). Full breakdown exercised with a wire-level fixture (/tmp/wf-chat-usage-breakdown.sh) sending a real ACP PromptResponse.usage object (camelCase totalTokens/inputTokens/outputTokens/cachedReadTokens, matching agent-client-protocol v2's unstable_end_turn_token_usage schema) -- /tmp/wf-chat18d-shots/02-breakdown-popover.png shows populated Input/Output/Cache read rows and a cost line via the context-usage-breakdown selector. | sweep ?, 2026-08-14 |
| `F-CHAT-19` | PASSED | **wave F live drive (this host)**: Same composer fixture's 85% fill (170000/200000, above the 80% threshold) turns the ring warning color in /tmp/wf-chat1819-shots/02-after-connect.png and /tmp/wf-chat1819c-shots/02-idle-85pct.png. Settled the brief's whose-fault question: Tiller correctly implements/renders both the session/update ring path and the ACP-unstable usage breakdown extension end to end (proven by the wire-level fixture in F-CHAT-18); the real claude-agent-acp bridge simply never populates the unstable usage field in practice -- an upstream agent-bridge gap, not a Tiller defect, and irrelevant to this row's own ring-color VERIFY clause which only needs the session/update path (proven). | sweep ?, 2026-08-14 |
| `F-CHAT-20` | PASSED | **wave F live drive (this host)**: chat_fixture.py staged mode gated on a go-file, real-size (1715x972) transcript padded with turns for genuine overflow (small artificial viewports tried first, found unreliable, discarded rather than reported). /tmp/wf-chat20n-shots/02-scrolled-away.png (scroll stops following), 03-after-marker-still-scrolled.png (new distinct-marker content while scrolled away does not force back down), 04-scrolled-back-to-bottom.png -> 05-after-repin-marker.png (manual scroll-to-end re-pins Tail-follow, next turn followed again). | sweep ?, 2026-08-14 |
| `F-CHAT-21` | PASSED | **wave F live drive (this host)**: chat_fixture.py's existing staged mode already emits a real thought_chunk ('thinking hard') -- no fixture extension was needed (none made, per the brief's disclosure requirement). /tmp/wf-chat21b-shots/02-collapsed.png -> 03-expanded.png -> 04-collapsed-again.png show the Thinking row toggling on repeated clicks against the thought-toggle-0 selector. | sweep ?, 2026-08-14 |
| `F-CHAT-22` | PASSED | Re-driven; WorkGroup expand/collapse (05-03/06-04-workgroup-click*.png) and older-turn fold/unfold (04-03-workgroup-refolded.png) both confirmed live. \| wave B, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |
| `F-CHAT-23` | PASSED | **wave F live drive (this host)**: Both halves re-driven. Half A (real claude CLI): /tmp/wf-chat23c-shots/02-before-toggle.png -> 03-after-toggle-collapsed-or-expanded.png -> 04-after-toggle-back.png (tool card expand/collapse), 05-after-location-click.png (location link navigates). Half B (chat_fixture.py permission-unrenderable, two turns): dismissing turn 1's permission (/tmp/wf-chat23e-shots/02-after-turn1-dismiss.png) and, separately, turn 2's permission (04-after-turn2-dismiss.png, /tmp/wf-chat23h-shots/03-after-turn2-dismiss.png), each checked via ctl surface.chat.read -- exactly the dismissed entry disappeared, rest of transcript survived. No whole-transcript wipe reproduced. | sweep ?, 2026-08-14 |
| `F-CHAT-24` | PASSED | Real turn produced a pending Plan card with option buttons (02-01-plan-pending.png), decision applied and plan state changed (04-03-plan-completed.png). \| wave B, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |
| `F-CHAT-25` | PASSED | **wave P (live, this host)**: AskUserQuestion text/option/cancel arms all covered by named drawn tests. Committed pre-compaction (dc3c2970). \\| wf-rest4, x86 box, 2026-08-19 | sweep ?, 2026-08-14 |
| `F-CHAT-26` | PASSED | Scrolled away from a pending question (02-show-01-scrolled-up.png), then Show in the pending bar scrolled the transcript back to the question card (03-show-02-after-show-click.png). \| wave B, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |
| `F-CHAT-27` | PASSED | Via chat_fixture.py question-expire mode (real claude will not reliably let a turn time out unanswered within a short test window, so the fixture was used deliberately) — card read 'No answer — turn ended' after scripted expiry. \| wave B, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |
| `F-CHAT-28` | PASSED | Real turn spawning a subagent produced a task card (02-01-subagent-completed.png) that expands to nested tool-call detail on click (03-02-subagent-click1.png/-expanded.png, 04-03-subagent-click2.png). \| wave B, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |
| `F-CHAT-29` | PASSED | Independently re-driven: hover-revealed Copy control on a real assistant message fires its transient confirmation state (02-cp-03-assistant-copy-clicked.png). \| wave B, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |
| `F-CHAT-30` | PASSED | Independently re-driven: code-block header Copy control fires its confirmation state (02-cp-01-codecopy-clicked.png). \| wave B, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |
| `F-CHAT-31` | PASSED | Re-driven; clicking a diff-preview file opens/expands it with numbered added/removed lines visible (L-123405-01-diffpreview-click.png through L-123531-01-expand-edit-diff.png). \| wave B, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |
| `F-CHAT-32` | PASSED | Open file, Revert click, and confirmed-reverted state all shown on a real edited file (L-123541-01-after-revert-click.png through L-123747-01-revert-alpha-retry.png). \| wave B, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |
| `F-CHAT-33` | PASSED | **wave Q critic (live, this host)**: genuine two-sided test. `git archive 407f7c9b` (83be7a14^) into a scratch tree, the fix's own two tests transplanted verbatim, and both PANICKED there -- 'the MCP warning must render its own banner' and 'a retryable turn error must also offer OK to dismiss'. The same two tests at HEAD: 2 passed. The pre-fix contrast an earlier pass called 'not independently driveable without reverting the fix' was obtained without touching the working tree. \\| wave Q, x86 box, 2026-08-19 | sweep ?, 2026-08-14 |
| `F-CHAT-34` | FAILED — defective | Downgraded from PASSED. Chat History listing a still-open sibling tab's session with a working Delete control still holds (rci/05-04-history-from-second-tab.png). But the VERIFY clause's actual claim — browsing a past/finished session — is structurally unreachable: closing a tab cascades a session-layout save that deletes its `chat_turn` rows, so a completed conversation shows "No past chats" the instant its tab closes, confirmed both via live UI (rce/06-05, rcg/05-04) and direct sqlite reads of the running DB. fullapp/F-CHAT.md. | sweep fullapp-9, 2026-08-19 |
| `F-CHAT-35` | PASSED | **wave F live drive (this host)**: Brand-new worktree (/tmp/wf-chat-project, sqlite state wiped) opened, Chat History checked before any turn was sent. /tmp/wf-chat35b-shots/02-chat-history-empty.png shows the 'No past chats' empty state (chat-history-empty selector) with no accumulated session. | sweep ?, 2026-08-14 |
| `F-CHAT-36` | PASSED | **wave F live drive (this host)**: chat_fixture.py plain mode never calls advertise(), so no config_option_update ever arrives -- a genuinely no-model-list agent over the wire. /tmp/wf-chat36-shots/02-no-models-composer.png shows the composer's model control in its no-models fallback state; 03-after-click-agent-badge.png confirms it degrades gracefully rather than erroring on click. | sweep ?, 2026-08-14 |
| `F-CHAT-37` | PASSED | Freshly opened chat tab shows zero transcript entries with a focusable composer and all action controls present before any send (02-cp-00-fresh.png, 02-mp-00-fresh.png). \| wave B, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |

### Files, changes, activity (22)

| id | verdict | evidence | judged |
|---|---|---|---|
| `F-CHG-01` | PASSED | Wayland lane (fin-chgit): surface.changes.open renders Changes as a tab beside Chat/Terminal, distinct from the always-present Files panel; right_panel.rs re-grepped, Files header still hardcoded, no mode switch. docs/linux-rewrite/FINISH-changes-git.md \| wave A shard critic, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |
| `F-CHG-02` | PASSED | **wave O (live, this host)**: Pre-fix (wfj2prechg02): empty catalog (project.list/workspace.list both []), center pane shows placeholder, Files panel shows a stray real tree of this repo -- reproduced independently. Fix: matching-fallback case confirmed via workspace.current returning real data; builder's named gap (non-matching project) closed via a throwaway scratch git repo (wfj2chg02b) -- workspace.current errors 'no current workspace' and both center pane and Files panel correctly show placeholders in sync, no divergence. [original defect independently reproduced on a pre-fix binary] \\| wf-judge2, x86 box, 2026-08-19 | sweep ?, 2026-08-14 |
| `F-CHG-03` | PASSED | **wave J live drive (this host)**: Isolated as the Files panel's own root-traversal error (chmod 000 on worktree root /tmp/wfchg-err2, not .git — read_tree explicitly skips .git). Full round trip in one continuous wayland-drive.sh session: error state 'Files unavailable: Permission denied (os error 13)' + Retry link (02-error-fresh-boot.png), then chmod 755 + click on Retry recovers to normal a.txt listing (03-after-retry-same-session.png). Path: /tmp/wf-chg-shots/49-chg03-round-trip/. | sweep ?, 2026-08-14 |
| `F-CHG-04` | PASSED | Live click on the sub directory row expands (chevron down, deep appears) and collapses (chevron right, deep gone) repeatedly; frame 17-files/02-04.png. Backed by right_panel::tests::clicking_a_drawn_directory_row_expands_and_collapses_it (green, re-run today). \| wave A shard critic, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |
| `F-CHG-05` | PASSED | Live pixel-sampled keyboard nav: click selects, Down/Down moves selection, Space no-ops on a file row but collapses a directory row, Down/Down/Return opens image.bin as a new editor tab. Frames 18-keys, 19-keys2. right_panel::tests::arrow_keys_select_and_space_expands_the_files_tree + return_opens_the_selected_file_row green. \| wave A shard critic, x86 box, 2026-08-18 \| **Correction 2026-08-19: the first of those two named tests was not running between `d015d752` and `82da42e9`.** An F-CORE-FILE-01 test was inserted between `#[gpui::test]` and the function it decorated, so `arrow_keys_select_and_space_expands_the_files_tree` lost its attribute and silently became dead code — the suite kept reporting green while covering one fewer thing. Restored in `82da42e9`, verified by the lib count moving 371 → 377 (5 new + this one). **The row stays PASSED**, because its primary evidence is the live pixel-sampled keyboard drive above, which predates the breakage and never depended on the test. Recorded so the "tests green" clause is not read as continuously true. | sweep ?, 2026-08-14 |
| `F-CHG-06` | PASSED | **PASSED 2026-08-20 — the one outstanding half was a live look, and it now exists in the repo** (`fullapp/VERIFY-chg06-file01.md`, frame `02-files-and-changes-agree.png`, driven against `c392542c`). A single frame holds both views of the same worktree — Changes tab in the centre column, Files tree in the right panel — on a fixture whose `git status` is `AM zz-staged-and-modified.txt`, i.e. exactly the staged-then-further-edited case this row exists to make visible; `surface.changes.read` on the same instance confirms it sits in **both** the `Staged (3)` and `Changed (2)` sections. The Files-tree dot reads `#7AC791` green and the Changes-list icon reads `#4C7458`, and those are **the same colour**, not merely similar ones: the tree paints a filled dot while the list paints an antialiased glyph, so recovering the alpha separately from R, G and B gives 0.521 from all three channels with a spread of 0.0008 — an agreement only possible if both resolve to the identical theme token, which is what routing both through `tiller_ui::git_status_style` was meant to guarantee. The frame carries a **discriminating control**: `zz-plain-modified.txt` (modified, never staged) reads amber `#F2B847` / `#856830`, so "everything is green" is excluded as an explanation. `zz-untracked.txt` reads blue. The staged-and-modified file reads green in the **Changed** section too, which is correct — staged beats modified in the colour regardless of which section lists the file, matching Swift. Per `WAYLAND-LANE.md`:102-107 this closes the **render** half, which is the whole of what this row's clause claims; no gesture is claimed. **Independently corroborated the same night** by `fullapp/BUILDER-triple-observation.md` (`9409cfb0`, merged as `bb703f41`), whose drive was committed at 00:29 against this row's frames at 00:44 — neither conclusion could have followed the other, checked against the commit times rather than assumed. Its fixture is a *different git state*: `MM`, a staged modification plus a further unstaged one, where this one used `AM`, a staged add plus an edit; both must resolve to staged-green and both do, in the Changed-section row as well as the Staged one. The two runs report different Changes-list numbers — `#4C7458` here, `#67A57A` there — which is **not** a disagreement: that run's own zoom holds both values (2100 px and 1050 px), so the two passes sampled different points of one antialiased glyph, and per-channel alpha recovery returns 0.521 and 0.804 against the *same* staged token. **The limit of that agreement, written down so it is not over-read:** both passes used the same harness (`wayland-drive.sh`) and the same ImageMagick sampling against the same binary tree. That is two independent fixtures and two independent observers, not two independent *instruments* — a harness that mis-rendered colour would have fooled both identically. **Superseded record — the half-proven verdict this replaces:** **Half-proven 2026-08-19 by a fresh, unrelated critic** (`fullapp/CRITIC-staged-marker.md`, judging `0db25a26`+`98b91fb2`; both in HEAD). **The half that is genuinely fixed and was verified live, pixel-by-pixel:** the Files-tree marker can now represent "staged" — `DirectoryGitStatus` gained a `Staged` variant and a *second* resolver `for_file()` (conflicted > untracked > staged > modified, `tiller_git/src/directory_status.rs:62`, consumed at `right_panel.rs:318`), deliberately kept separate from the unchanged directory roll-up `for_entry()` which still never returns `Staged`, matching Swift's `DirectoryStatusAggregator`. The critic checked that precedence against Swift's `GitStatusStyle.color` (`App/RightPanel/GitPanelTypes.swift:36-41`) itself rather than trusting the builder's citation. The critic's **Defect 1** (the new test failed to compile, `E0596`) **is closed in HEAD** — it was the same missing `mut` at `right_panel.rs:2894` that broke the whole `tiller_ui` test target and was fixed during the merge. **Defect 2 is now fixed too (`82da42e9`), which leaves only a live look outstanding.** It was: `changes.rs`'s own `status_color` ordered precedence **backwards**, testing `has_worktree_changes()` *before* `is_staged()`, so a file that is staged **and further modified** rendered amber in the Changes list while the just-fixed Files tree rendered it green — the two views now visibly disagree on the exact state this row exists to make visible. Swift has no "worktree changes" branch at all: staged always beats modified, in **one shared function**, factored out precisely because (its own doc comment) the two views "had drifted into two copies". Rust has re-created that drift, just moved. Proven live on `staged-and-modified.txt`, which `ctl surface.changes.read` shows in both the `staged` and `changed` arrays, pixel-cropped amber in both, sitting in the `Staged (3)` section beside two genuinely green pure-staged siblings. The existing test `a_staged_and_modified_file_appears_in_both_sections` (`changes.rs:2209`) asserted **presence only, never colour**, which is why this never tripped. **The fix does not merely reorder the branches** — reordering would cure the symptom and leave the mechanism that produced it. Both views now resolve through one new module, `tiller_ui::git_status_style`, so the drift Swift's shared function was written to prevent is structurally impossible here too; `entry_color` keeps the neutral no-status fallback that `for_file` alone would not give, so the shared resolver does not silently repaint an unrelated case on its way. Proven by the previously colour-blind test, now asserting the colour through `status_color` itself, plus 5 precedence/agreement tests, with a **positive control**: reverting the precedence makes it fail `rgba(0xf2b747)` vs `rgba(0x7ac691)` — the same amber-vs-green the critic pixel-sampled live. `tiller_ui` lib suite 371 → 377 (5 new, and 1 recovered — see below). **What still keeps this off PASSED, and it is now only this:** nobody has *seen* the corrected Changes list. The defect was proven present live; the fix is proven only in tests. One drive opening the same worktree's Files tree and Changes list side by side on a staged-then-edited file, confirming they now agree, promotes this row. **Collateral worth knowing:** this same commit revived `arrow_keys_select_and_space_expands_the_files_tree` (F-CHG-05), which had been silently dead — an earlier F-CORE-FILE-01 test was inserted between `#[gpui::test]` and the function it decorated, leaving that test unattributed and never run. It was the compiler's `dead_code` warning that surfaced it, not a review. **Superseded record of the failure:** FAILED — defective per fullapp/F-CHG.md: the Changes-list half still holds (staged/changed/untracked render distinct colours — green/amber/blue — matching their section), but the Files-tree marker dot cannot show "staged" at all — live pixel sampling found staged-rename, staged-add, and two genuinely unstaged-modified files all render the identical amber `#F2B847` dot. Root cause: `DirectoryGitStatus` (`tiller_git/src/directory_status.rs:10`) has only `Conflicted`/`Changed`/`Untracked` variants, no `Staged`, consumed by `git_status_color` at `right_panel.rs:1313`. Contradicts the Swift original's four-colour model (staged=green, modified=amber, untracked=blue, conflict=red). | sweep fullapp-9, 2026-08-19 |
| `F-CHG-07` | PASSED | Live: Changes on a clean worktree (fin-chgit-clean) renders centred No changes with Local changes (0). Frame 29-clean3/03-cc2-*.png. \| wave A shard critic, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |
| `F-CHG-08` | PASSED | Live, repeated across a dozen captures: Staged/Changed/Untracked headers track exact counts through every mutation this pass. \| wave A shard critic, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |
| `F-CHG-09` | PASSED | **wave J live drive (this host)**: Isolated as the Changes tab's own git-status error: project added to catalog while .git was readable (persisted to sqlite), then chmod 000 on .git only, then rebooted. Degraded-catalog fix keeps project/worktree selectable through the fault. Opening Changes shows 'Git is unavailable: git exited with status 128...' + Retry, while Files panel on the same frame correctly still shows a.txt normally (proving independence from F-CHG-03). Full round trip: chmod 755 + click Retry recovers to 'No changes', confirmed both visually and via ctl surface.changes.read returning error:''/ready:'true'. Paths: /tmp/wf-chg-shots/52-chg09-error/02-changes-error-fresh.png, /tmp/wf-chg-shots/53-chg09-roundtrip/. | sweep ?, 2026-08-14 |
| `F-CHG-10` | PASSED | Live, git-verified round trip both directions: staging file.txt moved Changed->Staged (git status M in index col); unstaging staged.txt moved Staged->Untracked (??). Frames 04-07-stage/unstage*. \| wave A shard critic, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |
| `F-CHG-11` | PASSED | Live, 3 trials, git-verified: toolbar Stage all staged all 5 files; Discard all cleared Staged+Changed but left Untracked (matches git restore --worktree); section Unstage all on Staged undid all 5 back to Changed/Untracked. Frames 13-bulk, 14-16-discardall*, 58-secaction2. \| wave A shard critic, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |
| `F-CHG-12` | PASSED | Live: clicking file.txt's row expands it in place with a real hunk (@@ -1 +1,2 @@, numbered old/new, +changed line2 green); second click collapses. Frame 03-expand/02-expanded-file.png. \| wave A shard critic, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |
| `F-CHG-13` | PASSED | Live: Open file icon opens a genuine new editor tab (README2.md, real content); Open diff opens another Changes-surface tab pre-expanded on that file (changes.rs:100-121 confirms this build's intentional routing, distinct from a dedicated diff view). Frames 11-opendiff2, 12-openfile/03. \| wave A shard critic, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |
| `F-CHG-14` | PASSED | **wave J live drive (this host)**: GPUI's Wayland prompt() always returns None (gpui_linux window.rs:1561-1569), triggering GPUI's own in-window FallbackPromptRenderer — a real drawn modal with white background, 'Discard'/'Cancel' buttons, easy to miss but genuinely present and hit-testable. Two full git-state-verified round trips: text file Discard-confirm mutates the worktree (Local changes 5→4, file.txt leaves Changed section: /tmp/wf-chg-shots/20-full-cycle/03-dialog-open.png, 04-after-discard-confirmed.png); binary file Cancel leaves the file untouched, still listed under Changed(1) afterward (/tmp/wf-chg-shots/21-cancel-then-discard/04-after-cancel.png). | sweep ?, 2026-08-14 |
| `F-CHG-15` | half-proven | fullapp/F-CHG.md: Binary half still PASSED — dirtying `image.bin` renders inline "Binary diff unavailable" per its row. Diff-load-fail half now FAILED — defective: `chmod 000` on one already-tracked, already-modified file (`git diff` exits 128, "cannot hash") collapses the *entire* Changes tab into a whole-repo "Git is unavailable" error, hiding every other healthy file, instead of a per-file "diff unavailable" row. Root cause: `tiller_git::diff::stats()` (`diff.rs:333`) runs one whole-repo `git diff --numstat -z HEAD` via `run_accepting(&[0])`, accepting only exit 0, so one unreadable file poisons the whole snapshot before per-file `diff_errors` isolation ever runs. | sweep fullapp-9, 2026-08-19 |
| `F-CHG-16` | PASSED | Built a real UU merge conflict from scratch. Conflicted row shows real conflict-marker diff (<<<<<<< HEAD / ======= / >>>>>>> left) plus a Resolve in terminal action present only on conflicted rows; clicking it opened a focused terminal tab with real diff --cc conflict.txt output and a live shell prompt. Frame 26-resolve2/03-s2.png spot-checked directly. \| wave A shard critic, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |
| `F-CHG-17` | PASSED | Same expanded-diff evidence as F-CHG-12: @@ -1 +1,2 @@ header with legible 1/2 old/new numbered lines rendered and selectable. \| wave A shard critic, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |
| `F-CHG-18` | PASSED | **wave O (live, this host)**: Reconstructed the report's down/move/chord-ctrl-shift-Tab/move/up drag gesture from scratch (including the +-menu Changes-tab-open step). Pre-fix (wfj2prechg18): 'Dropped diff' toast appears, panel.list stays 3 panes, no new tab. Fixed binary (wfj2chg18b, repeated wfj2chg18c): panel.list 3->4, new Changes tab active showing the exact dragged file's diff -- reproduced twice for flake-checking, closing the builder's own gap 1. [original defect independently reproduced on a pre-fix binary] \\| wf-judge2, x86 box, 2026-08-19 | sweep ?, 2026-08-14 |
| `F-CHG-19` | PASSED | Live, 5 separate expansions: clicking the Activity header toggles its chevron and reveals rows for every open tab of the current worktree (3x Changes, Resolve conflict.txt, Terminal, each with subtitle and a close x), collapses on second click. Single-open-worktree model caveat applies as previously accepted. Frames 49/51/55-activity*. \| wave A shard critic, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |
| `F-CHG-20` | PASSED | cargo test -p tiller_ui --lib right_panel today: activity_section_states_no_activity_when_empty and activity_section_states_the_running_count both green (21/21 suite), plus F-CHG-19's live confirmation the section renders and reacts to real state. \| wave A shard critic, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |
| `F-CHG-21` | PASSED | **wave J live drive (this host)**: Both focus and close mechanisms proven live at precise click coordinates, contradicting the wave-A 'close always collapses the whole section' finding (very likely their own coordinate imprecision — the Activity section anchors .absolute().bottom_0() and grows upward with row count). Close half: 4 Activity rows (3x Changes + 1x Terminal) reduced to 3 after clicking one row's x — other rows/tabs unaffected, section stayed expanded, tab strip dropped from 4 to 3 in lockstep. Frames: /tmp/wf-chg-shots/43-close-precise/02-expanded-precise.png, 03-after-close-precise.png. CloseActivity(index) confirmed wired to a real request_close_activity call in main.rs. | sweep ?, 2026-08-14 |
| `F-CHG-22` | PASSED | cargo test -p tiller_ui --lib right_panel today: activity_section_draws_needs_input_as_distinct_from_idle green, plus running/idle/error states visually distinct across F-CHG-19's live rows (Terminal glyph vs Changes glyph vs conflict-terminal icon). \| wave A shard critic, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |

### Documents and editors (13)

| id | verdict | evidence | judged |
|---|---|---|---|
| `F-EDIT-01` | PASSED | **wave H live drive (this host)**: Preview/Code mode switch renders raw markers correctly | sweep ?, 2026-08-14 |
| `F-EDIT-02` | PASSED | **wave H live drive (this host)**: Bold/Italic toolbar wraps exactly the selected word, disk-confirmed | sweep ?, 2026-08-14 |
| `F-EDIT-03` | PASSED | **wave H live drive (this host)**: large.md (400KB) shows manual-preview bar, Preview renders on click | sweep ?, 2026-08-14 |
| `F-EDIT-04` | PASSED | **wave H live drive (this host)**: typed marker survives ctrl-s, confirmed via external cat | sweep ?, 2026-08-14 |
| `F-EDIT-05` | PASSED | **wave H live drive (this host)**: clean auto-reload silent; dirty conflict banner exact text; Keep/Reload both correct | sweep ?, 2026-08-14 |
| `F-EDIT-06` | PASSED | **wave H live drive (this host)**: external delete shows exact recreate banner; ctrl-s recreates file on disk | sweep ?, 2026-08-14 |
| `F-EDIT-07` | PASSED | **wave H live drive (this host)**: Python syntax highlighting; word-replace-then-save preserves indentation | sweep ?, 2026-08-14 |
| `F-EDIT-08` | PASSED | **wave H live drive (this host)**: double-open same file never duplicates the tab | sweep ?, 2026-08-14 |
| `F-EDIT-09` | PASSED | **wave H live drive (this host)**: Files-panel double-click opens editor tab with mode pills | sweep ?, 2026-08-14 |
| `F-EDIT-10` | PASSED | **wave H live drive (this host)**: context menu Reveal in File Manager spawns a real external COSMIC Files window | sweep ?, 2026-08-14 |
| `F-EDIT-11` | PASSED | **wave H live drive (this host)**: Copy Path round-trips through the real Wayland clipboard (via terminal Paste menu) | sweep ?, 2026-08-14 |
| `F-EDIT-12` | PASSED | **wave H live drive (this host)**: drag from Files panel onto Terminal inserts shell-quoted path | sweep ?, 2026-08-14 |
| `F-EDIT-13` | PASSED | **wave H live drive (this host)**: missing-file link and binary-file open both show exact live messages | sweep ?, 2026-08-14 |

### Persistence and lifecycle (8)

| id | verdict | evidence | judged |
|---|---|---|---|
| `F-PER-01` | FAILED — defective | fullapp/F-PER.md downgrades this row. Chat half still PASSED: created a real ACP chat tab via New Chat -> Claude Code, sent a message over `surface.chat.send`, confirmed the row in `chat_turn` before quitting, and the identical transcript rendered after a genuine restart. Terminal scrollback half now FAILED: in an isolated lane, typed a marker, waited 20s idle (rules out debounce), sent SIGTERM, relaunched against the same sqlite -- `tab_state.state` read back as `{"pane_events":[],"scrollback":{"0":[]}}`, empty; the restored tab shows a brand-new shell, not the marker. Same root cause as F-PER-03/06 (F-PER-04's remount-corruption bug is a distinct defect where the underlying data does survive); source `rust/crates/tiller/src/main.rs`, `Workspace::layout()`/`capture_scrollback()`. | sweep fullapp-9, 2026-08-19 |
| `F-PER-02` | PASSED | Verdict unchanged, citation repaired. fullapp/F-PER.md re-drove this row live in two independent lanes: after a genuine fresh-process restart against the same DB and with zero further clicks, the previously-selected worktree came back pre-highlighted with its parent project row pre-expanded (sweep-7-F-PER clean2 and inv3, 02-01-boot-zero-interaction.png). The previous cell cited fin-set2-postper/03; that directory does not exist in this checkout, tracked or untracked -- the report checked with find and git ls-files. The replacement captures live under /dev/shm and are themselves outside the repo, so this row is proven but not yet replayable from the tree. | sweep fullapp-9, 2026-08-19 |
| `F-PER-03` | FAILED — defective | Multi-worktree half PASSED: second worktree (`workspace.create`) with its own terminal/marker survived a restart, `workspace.list` still lists both. Split-layout half FAILED: built a real 2-pane split (`pane.split direction=right`), confirmed both markers live via `panel.read`, but after restart the tab came back as a single unsplit empty pane — `pane_events` persists as `[]`, same root cause as F-PER-01. Also found: a Browser-kind tab's entry survives restart but WebKit content never re-renders (XCB/Wayland handle error), matching F-WIN-06. Source: fullapp/F-PER.md. | sweep fullapp-9, 2026-08-19 |
| `F-PER-04` | FAILED — defective | Data half still PASSED: typed a marker into worktree A's terminal, switched to B and back within one session; the marker text is present after remount (fullapp critic live re-drive, clean1/04-03-after-remount-same-session.png -- the old ledger's fin-set2-postper/03 citation does not exist anywhere in the repo). But rendering on return is corrupted -- pfetch banner duplicated/overlapping, prompt boxes misaligned and colliding with banner text, box-drawing fragments detached from prompts -- so content is present but not legibly visible as the row requires. Downgraded per fullapp/F-PER.md.</corrected_cell> | sweep fullapp-9, 2026-08-19 |
| `F-PER-05` | PASSED | **wave N (live, this host)**: Was NOT EXERCISED. Closed a dirty Terminal tab (confirm dialog handled), ctrl+shift+o to restore_launch_snapshot, confirmed merge_launch_snapshot_tabs only appends past current.len() -- the closed tab came back without duplicating an untouched tab. Screenshots f-per-05-after-close.png, f-per-05-after-restore.png. | sweep ?, 2026-08-14 |
| `F-PER-06` | FAILED — defective | Child-process-termination half PASSED: started real 'sleep 600' job in a terminal pane, captured its bash job PID (178606), sent SIGTERM at ~12:41:49; host `ps -p 178606` at 12:45:13 found it gone, well before its natural 600s expiry. Flush-state half FAILED: same quit path does not flush terminal scrollback to disk -- `tab_state.state` shows `"scrollback":{"0":[]}`/`"pane_events":[]` after quit regardless of idle wait, tab/project/settings state flush correctly but terminal content does not (rust/crates/tiller/src/main.rs, capture_scrollback/on_app_quit). Per fullapp/F-PER.md. | sweep fullapp-9, 2026-08-19 |
| `F-PER-07` | half-proven | Name half PASSED cleanly: right-click project row -> Project Settings -> typed new display name -> closed -> genuine restart -> new name rendered in sidebar with zero further clicks, confirmed against SQLite `project.display_name`. Icon/colour half is NOT EXERCISED, not PASSED: two attempts with different coordinate estimates for the icon-glyph and colour-swatch rows in the settings sheet neither demonstrably changed persisted `icon_kind`/`icon_value`/`color_hex`, and there was no independent read of the true pre-edit default to compare against, so it cannot be told whether clicks missed the controls or nothing changed — a harness coordinate-targeting gap, not a confirmed app failure. Per fullapp/F-PER.md. | sweep fullapp-9, 2026-08-19 |
| `F-PER-08` | PASSED | Across THREE separate genuine process restarts against the same DB, all of chatRetention/autoNaming/limitMountedWorktrees+mountedWorktrees/controlSocketEnabled/translucency/font sizes/resumeAgentSessions AND the https://agent-target.example browser-origin grant survived every time with zero further interaction (fin-set2-postper/04). \| wave B, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |

### Browser (9)

| id | verdict | evidence | judged |
|---|---|---|---|
| `F-BRW-01` | PASSED | Live X11-nested lane: browser.open to a local http.server page (#ff2d95, 96px text), tab.select. Capture shows real toolbar, address field with exact URL, globe glyph, and the actual page content rendered (not chrome alone), 8842 colours. \| wave B, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |
| `F-BRW-02` | PASSED | Real click gestures: Back (357,89) reverted example.com->probe.html; Forward (393,89) returned to example.com; Reload (429,89) re-fetched Example Domain (loading glyph changed). Stop: clicked reload/stop toggle mid an 8s-delayed load, loading flipped false, address reverted to last-committed page, and the slow page never appeared even 9s later (positive control confirmed it does appear without Stop). \| wave B, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |
| `F-BRW-03` | PASSED | Real gesture: click address field x2, ctrl+a, xdotool-typed 'http://127.0.0.1:8982/', Return. ctl browser.get 0.5s later shows loading:true, url matching the typed text exactly -- real navigation from real typed input. \| wave B, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |
| `F-BRW-04` | PASSED | Real typed 'not a valid url ###'+Return -> red banner 'Enter a valid HTTP or HTTPS address'. Real typed 'http://127.0.0.1:8989/' (nothing listening)+Return -> content shows 'Could not connect to 127.0.0.1: Connection refused'. Both live UI renders from real keystrokes. \| wave B, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |
| `F-BRW-05` | PASSED | ctl browser.act driving=true renders an orange 'Agent driving' pill next to Stop, absent in every prior frame; driving=false clears it on the next frame. Clean two-state toggle captured both ways. \| wave B, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |
| `F-BRW-06` | PASSED | driving=true + navigate to ungranted origin returns permission:requested and renders real 'Allow agent browser access to https://agent-target.example?' banner with Allow/Deny. A real xdotool click on Allow dismissed it; repeat navigate to same origin then attempted real DNS resolution (no re-prompt), proving the click resolved the grant. \| wave B, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |
| `F-BRW-07` | PASSED | After granting an origin, the entire tiller process was killed and relaunched (not just UI reload) against the same TILLER_DB/TILLER_SOCKET. Fresh driving=true+navigate to the same origin in the new process skipped the doorhanger entirely (real DNS failure only) -- grant survived a genuine process restart via SQLite. \| wave B, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |
| `F-BRW-08` | half-proven | fullapp/F-BRW.md downgrades this to half-proven: the revoke action itself is proven live -- real click on a single origin's Revoke button (1054,202) in Settings->Permissions dropped it, and re-navigating under driving=true immediately re-prompted, confirming the grant was truly removed, not just hidden. But the 'from Permissions settings' half is defective: the native browser webview never unmaps behind Settings (see Defects #1), rendering on top of the Browser-origin-grants card so the origin's URL text was completely unreadable in every screenshot -- a real user cannot see which origin they are revoking. Revoke-all was not independently clicked. | sweep fullapp-9, 2026-08-19 |
| `F-BRW-09` | PASSED | Scratch ACP script (/tmp/fin-brw-link-agent.py, not committed, same category as chat_fixture.py) streamed two real markdown links via TILLER_ACP_PROGRAM. Plain click opened a new internal Browser tab at the exact URL with real rendered content. Held Super+Shift click on the second link opened no new tab (transcript/tab-count unchanged) -- matches chat.rs:822's exact branch, now exercised live. \| wave B, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |

### Status bar / usage (6)

| id | verdict | evidence | judged |
|---|---|---|---|
| `F-USE-01` | PASSED | Live baseline screenshot shows gear icon, refresh icon, and bottom-right 'linux/gpui-waku · ~/Scrivania/Progetti/tiller-linux'. Clicked refresh at (58,952): next frame showed both segments flip to loading ellipsis, confirmed by pixel diff of before/after screenshots. \| wave D, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |
| `F-USE-02` | PASSED | Visibility: same live toggle as USG-09. Tooltips: worked around a shot()-clamp lane defect (documented in the file) using a raw grim capture — hovering the Claude segment produced tooltip text identical to the segment ('Claude 38% 5h · 33% wk'), hovering the Unavailable Codex segment produced 'Codex logged out' — two distinct tooltips over two distinct segments, each matching its own content. \| wave D, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |
| `F-USE-03` | PASSED | Configured/loading/logged-out states live (see USG-08/09 evidence); timed-out live via TILLER_USAGE_CLAUDE_TIMEOUT_MS against the real claude binary; stale via the drawn a_real_timeout_after_a_real_success_dims_the_live_entity test (production Loaded->Stale, dimming asserted); all 4 unavailable reasons plus Loaded/Stale text distinctness via unavailable_reasons_render_distinct_text — all pass today. \| wave D, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |
| `F-USE-04` | PASSED | Live busctl --user list found the real registered org.kde.StatusNotifierItem-<pid>-1 against this machine's actual cosmic-applet StatusNotifierWatcher. GetLayout via com.canonical.dbusmenu on /MenuBar returned a real menu reflecting the live needs-input transition: item 1 'linux/gpui-waku — tiller (needs input)', separator, 'Quit Tiller'. \| wave D, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |
| `F-USE-05` | PASSED | Switched active worktree away via the socket, then sent a genuine com.canonical.dbusmenu.Event(1,'clicked',...) D-Bus call directly at the real SNI service (not the ctl tray.jump shortcut). workspace.current afterward reported back on linux/gpui-waku with panel.list showing the needs-input Claude Code pane active:true — the real menu-click entry point, not a convenience method. \| wave D, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |
| `F-USE-06` | PASSED | Opened a real spawn-owned Claude Code agent pane, switched away (Chat became active, making it genuinely not visible), sent ctl notify status=needs-input. dbus-monitor on the real session bus (sanity-checked first with a manual notify-send) captured a genuine Notify method call from app-name 'Tiller', title 'Claude Code — tiller/linux/gpui-waku', body 'linux/gpui-waku · tiller' — matching the production payload format exactly. A control run against a plain (non-agent) terminal pane correctly produced no notification, confirming the agent_id guard works as designed rather than being a silent bug. \| wave D, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |

### Control socket automation (9)

| id | verdict | evidence | judged |
|---|---|---|---|
| `F-AUTO-01` | PASSED | **wave I live drive (this host)**: Live click on the real Settings General toggle at (1288,843): ctl system.ping right after failed with 'No such file or directory' (socket file unlinked, not just refused); clicked again, ping returned pong again. Screenshot shows the toggle knob moved and the socket-path label stayed displayed throughout. | sweep ?, 2026-08-14 |
| `F-AUTO-02` | half-proven | fullapp/F-AUTO.md downgrades this row to half-proven: `panel.create` still returns a real pane id (`pane-569928-1`), confirmed live via `panel.list` and, in the F-AUTO-03 exercise, via `panel.read` returning genuine shell output — the control-plane half holds. But it produces zero UI trace: `04-03-new-terminal-clicked.png` and `05-04-after-control-panel-create.png` are pixel-identical, same single "Terminal" tab, no new tab, no sidebar entry, no error either, so the app-tier half of the row's VERIFY text ("a terminal pane/tab is created, or a visible protocol error is returned") is unmet. | sweep fullapp-9, 2026-08-19 |
| `F-AUTO-03` | PASSED | **wave I live drive (this host)**: split/list/write/key(lowercase 'enter')/read/state/wait(both timeout and real exit)/focus/close all exercised against one real pane; write+read round-tripped an exact marker string; wait --timeout-ms 4000 returned exitCode:0 only after a real 'exit' was written. | sweep ?, 2026-08-14 |
| `F-AUTO-04` | PASSED | **wave I live drive (this host)**: worktree-set changed current-workspace's comment live; session-ref produced a new row read directly from the live SQLite session_ref table; notify queued a real ControlAction::Notify without error. | sweep ?, 2026-08-14 |
| `F-AUTO-05` | PASSED | **wave I live drive (this host)**: list/select/current round-tripped the real fixture worktree; panel.create only succeeded after select (failed with 'unknown worktree' before), live proof of the mount gate. | sweep ?, 2026-08-14 |
| `F-AUTO-06` | PASSED | **wave I live drive (this host)**: Real D-Bus Notify(app_name='Tiller', summary=..., body=...) call captured by a stub notification daemon on a private session bus (real bus is down per ENVIRONMENT.md); list-notifications showed the entry, clear-notifications emptied it, confirmed by re-listing empty. | sweep ?, 2026-08-14 |
| `F-AUTO-07` | PASSED | **wave I live drive (this host)**: ping->pong, identify with a selected workspace returned real workspaceId/path/branch, capabilities enumerated the live 61-method dispatcher table. | sweep ?, 2026-08-14 |
| `F-AUTO-08` | PASSED | **wave I live drive (this host)**: Closed a real dirty Terminal tab through its confirm dialog, screenshotted a genuine 'No Terminals' empty state, sent session.restore -> restoredCount:1, screenshotted the Terminal tab back running a freshly-spawned bash (new timestamp, distinct from the closed session). | sweep ?, 2026-08-14 |
| `F-AUTO-09` | half-proven | **wave I live drive (this host)**: control-socket surface itself is honest — `browser.open`/`navigate`/`get`/`act(driving=true)` produced real changing state (including a real "Agent driving" pill) and `screenshot`/`errors` ("unsupported on Linux") and `eval`/`console`/`snapshot` ("Browser child is unavailable") returned explicit typed errors, never a silent ok:true. But every URL opened via `browser.open`/`browser.navigate`, including a curl-verified-working local page, renders nothing but a permanent red banner ("XCB build failed... Wayland(WaylandWindowHandle)") in the Browser tab's content area — the same by-construction Wayland gap in `rust/crates/tiller_ui/src/browser.rs` (Xcb/Xlib-only handling) independently confirmed at source by F-WIN-06. See fullapp/F-AUTO.md. | sweep fullapp-9, 2026-08-19 |

### Settings (25)

| id | verdict | evidence | judged |
|---|---|---|---|
| `F-SET-01` | PASSED | Live real clicks on all 5 category rows; each rendered correct content + highlight. fin-set2-nav/02-06. \| wave B, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |
| `F-SET-02` | PASSED | Live Back click (44,51) and separately Escape both returned to workspace (colour count matched pre-Settings baseline). fin-set2-nav/07,09. \| wave B, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |
| `F-SET-03` | PASSED | Version 0.1.0 shown; no Check-for-Updates control anywhere; settings.rs:3465 absence test still asserts it. fin-set2-general2. \| wave B, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |
| `F-SET-04` | PASSED | Live toggle click true->false; DB-backed fresh-process restart read back resumeAgentSessions:false with zero further clicks. \| wave B, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |
| `F-SET-05` | PASSED | Live: auto-rename toggled on, summarizer picker opened a real dropdown (Claude Code checked/Codex/OpenCode/Pi); named test summarizer_picker_is_gated_on_auto_naming_and_selects passes; autoNaming:true survived a fresh-process restart. \| wave B, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |
| `F-SET-06` | PASSED | Live stepper 100->97 clicks; chatRetention:97 survived a fresh-process restart. \| wave B, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |
| `F-SET-07` | PASSED | Live toggle-on + stepper 6->7; limitMountedWorktrees:true/mountedWorktrees:7 survived restart. Eviction behavior itself out of scope, tracked separately under F-CORE-ACT-26 per prior text, not re-opened. \| wave B, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |
| `F-SET-08` | PASSED | Live click turned control-socket off; NEXT real process launch (same DB) printed '[control] disabled' with no .sock file created -- UI-driven, stronger than an env-var test; re-enabling via DB edit + relaunch printed '[control] listening...' again. Copy-install-command confirmed absent, matching absence test. \| wave B, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |
| `F-SET-09` | PASSED | Earlier-today capture fin-set-tall2/02-general-install-skill-clicked.png: live click on Install Skill produced 'Installing... running in a new terminal tab.' immediately. \| wave B, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |
| `F-SET-10` | PASSED | Earlier-today fin-set-shots7/02-04: live show-in-bar toggle off, refresh interval stepper 5->7min, Refresh now click, all real gestures. \| wave B, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |
| `F-SET-11` | PASSED | cargo test -p tiller_usage --test usage_tests: 10/10 pass, re-run fresh today. Named tests spawn real PTY subprocesses under isolated PATH covering not-installed/logged-out/error/stale/bounded-timeout -- all clause states. \| wave B, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |
| `F-SET-12` | PASSED | Earlier-today fin-set-tall5/02-06: live cookie type/Save -> Signed in, workspace-ID override typed, Clear on both -> reverts to Not signed in and usage-bar auto-off. \| wave B, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |
| `F-SET-13` | PASSED | Earlier-today captures show live Ollama cookie field type/save cycle and a Refresh click; live network refresh-error state not independently re-hit against ollama.com this exact verification pass. \| wave B, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |
| `F-SET-14` | PASSED | **wave O (live, this host)**: signing-in/cancel UI transition reproduced live (committed pre-compaction, commit 5c71efeb) [original defect independently reproduced on a pre-fix binary] \\| wf-sweep2 critic, x86 box, 2026-08-19 | sweep ?, 2026-08-14 |
| `F-SET-15` | PASSED | **wave O (live, this host)**: Pre-fix (wfj2preset15): only one 'System default' row per provider card; ctl settings.account.add errors 'unknown control method' at the protocol level. Fix (wfj2set15): settings.account.add creates durable SQLite agent_account rows plus real config-dir directories on disk (verified via a fresh sqlite3 read outside any live process). Builder's named gap 2 closed: live-clicked Codex's own System-default row (not just the add via socket), Active badge moved back, both providers' activeId setting rows correctly cleared (0 rows) after. Also live-confirmed (DB row count before/after, not just re-read code) that clicking the production 'Add Account' button spawns a real login attempt but creates no new account row. [original defect independently reproduced on a pre-fix binary] \\| wf-judge2, x86 box, 2026-08-19 | sweep ?, 2026-08-14 |
| `F-SET-16` | PASSED | Earlier-today fin-set-tall2/03,04: typed 'zzzznonexistent' into Search agents, list genuinely emptied (real filter) vs the 5-row positive control in the same session. \| wave B, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |
| `F-SET-17` | PASSED | Earlier-today negative control fin-set-nopath2/02 (separate instance, PATH unset): live banner 'Could not load the agent registry: PATH is not set in the environment'. \| wave B, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |
| `F-SET-18` | PASSED | **wave O (live, this host)**: full install success path driven live end to end (commit 7eaf8b77) [original defect independently reproduced on a pre-fix binary] \\| wf-sweep2 critic, x86 box, 2026-08-19 | sweep ?, 2026-08-14 |
| `F-SET-19` | PASSED | Live real clicks on System/Light/Dark segments; whole window genuinely recolored each time (fin-set2-appear/02-04), discriminating, not a socket-only switch. \| wave B, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |
| `F-SET-20` | PASSED | Live: Translucency toggle click moved+colored; Interface font stepper 13->15->14pt, Terminal font stepper 13->14pt, all visibly changed text size (fin-set2-appear/05-08). \| wave B, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |
| `F-SET-21` | N/A — platform | Live click on the single 'Material' File-icons button produced a pixel-identical frame, no menu (fin-set2-appear/09) -- Linux ships exactly one file-icon theme. \| wave B, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |
| `F-SET-22` | PASSED | **wave O (live, this host)**: accent-color decoupling from brand color proven live (commit 2e1ce891) [original defect independently reproduced on a pre-fix binary] \\| wf-sweep2 critic, x86 box, 2026-08-19 | sweep ?, 2026-08-14 |
| `F-SET-23` | N/A — platform | Re-verified against App/Permissions/SystemPermissionProbe.swift today: all 6 modeled kinds (.notifications/.screenRecording/.accessibility/.fullDiskAccess/.automation/.localNetwork) resolve through macOS-only APIs (UNUserNotificationCenter, CGPreflightScreenCaptureAccess, AXIsProcessTrusted, TCC.db check, AppleEvents, System Prefs URLs). Linux Permissions page live-confirmed to carry none of them, only Browser origin grants. \| wave B, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |
| `F-SET-24` | PASSED | Full live round trip today: empty state confirmed on a fresh instance; real browser.navigate to an unallowed origin produced genuine permission:requested, browser.permission action=allow granted it, Settings->Permissions listed https://agent-target.example with its own Revoke button, live click on Revoke removed it back to empty (fin-set2-revoke/02-after-revoke.png). Grant also survived a genuine process restart (see F-PER-08). \| wave B, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |
| `F-SET-25` | N/A — platform | Same TCC-only mechanism as F-SET-23, re-verified against the Swift original today -- the activation-refresh clause only re-polls those same macOS-only probes, none of which exist on Linux. \| wave B, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |

### Terminals and agents — app (11)

| id | verdict | evidence | judged |
|---|---|---|---|
| `F-TERM-01` | PASSED | **wave F live drive (this host)**: Live-driven: selected an empty pane, chose New Terminal, a live shell prompt rendered immediately with a working PTY (ran a command, saw output). | sweep ?, 2026-08-14 |
| `F-TERM-02` | half-proven | Live: confirmed the *other* empty state (`group.id==0`, "No Terminals" / single "New Terminal" button) is a different surface from this row's target. Live: created a genuine second pane group via the tab's "Move to New Pane" context-menu item — confirmed it renders as an independent, fully live pane (its own PTY, own neofetch banner, own prompt), proving the underlying group-creation mechanism is real. Could **not**, within budget, pixel-locate the second tab-strip precisely enough to blind-click "Move to Pane 0" and vacate it live (harness/coordinate limitation — see Traps). Corroborated instead by running `tests::drawn_detached_pane_group_offers_the_real_empty_prompt` fresh this pass (`cargo test -p tiller --bin tiller`, **1 passed**) — a full GPUI render+simulated-click test that performs exactly this sequence and asserts both `terminal-new` ("New Terminal") and `terminal-new-command` ("New…") are drawn, then simulates a click on New Terminal and confirms it lands in the correct group. The **"New…" action's own resulting surface was never actually invoked/observed** by this pass or by the test. `/dev/shm/sweep-12-F-TERMapp/phase{4,5,6,7}/*.png`. | sweep ?, 2026-08-19 |
| `F-TERM-03` | half-proven | Freshly live-drove all three *exit* legs via `exec sh -c 'exit 0'`, `exec sh -c 'exit 7'`, and `exec sleep 100` + host-side `kill -9`: each produced a correct, distinct tab badge (`✓ exit` / `! exit 7` / `! signal 9`) and bottom-left status text (`Process exited successfully` / `Process exited with status 7` / `Process terminated by signal 9`). `/dev/shm/sweep-12-F-TERMapp/cont/{02,03,05}-*.png`. **But**: traced the "running" leg through source (`tiller_terminal/src/lib.rs:1977-1985`) — the status overlay only ever renders `when_some(self.exit_status...)`; nothing is drawn while a command is merely executing. The tab's own activity dot is agent-identity-gated (`AgentCatalog`/Layers A-D per CLAUDE.md), not "a child process is alive" — confirmed live: a `sleep 20` run to completion never changed the tab from a plain hollow "○", even while genuinely running (`/dev/shm/sweep-12-F-TERMapp/main2/06-05-running-state.png`). So 3 of 4 VERIFY states are solid; the "running" state has no positive visual distinguishing it from "nothing has run yet" for an ordinary command — only the *absence* of a pill. | sweep ?, 2026-08-19 |
| `F-TERM-04` | PASSED | **wave F live drive (this host)**: Live sweep /tmp/wf-term-block2a (frames 04-07): selected output, Copy, pasted into another location; copied external text, Paste affected the terminal. | sweep ?, 2026-08-14 |
| `F-TERM-05` | PASSED | **PASSED 2026-08-19 — the missing half was evidence, and it now exists in the repo** (`fullapp/BUILDER-verify-tab26-term05.md`, shots `11`-`15`, commit `a22fb87b`). Re-driven live because the earlier critic deleted its own screenshot directory and said so, leaving a verdict nobody could replay. Observed this time and committed: the modal opens prefilled with the tab's current title; typing lands in the field and not the terminal underneath; OK updates the tab strip, the sidebar row **and** the Activity row at once; Escape leaves the title unchanged everywhere. **The deliberate check is the reopen** — reopening Set Title on the just-renamed tab prefills with `Terminal renamed-by-verify2`, not the original, so the field reads live state rather than echoing a stale value; the modal body reads `Enter the new title for "Terminal renamed-by-verify2:"`, keeping the Swift original's own odd colon-inside-the-quote wording (`TerminalContextMenuProvider.swift:76`) verbatim. The frame also shows **OK before Cancel**, which is the visible reason Enter commits here while Enter *cancels* the close-confirm prompt — each follows its own alert's button order, not one blanket rule. Enter was not re-driven this pass, deliberately: it is covered by `set_title_prompt_opens_prefilled_and_enter_commits_the_edit` and was never the point of doubt. **Superseded record:** **Set Title is implemented and merged 2026-08-19 (`c0834159`), and the remaining gap is evidence, not behaviour.** The port now opens a real prefilled modal built on the shared `tiller_ui::modal` primitive. A critic drove it live end to end — right-click → "Set Title…" opened a modal prefilled with the tab's actual current title and focused, typing landed in the field rather than the terminal underneath, OK updated both the tab strip and the sidebar row, reopening showed the *just-renamed* title (so the prefill reads current state, not a stale value), and Escape left the title unchanged. Body text keeps the Swift original's own odd colon-inside-the-quote wording verbatim, checked against `TerminalContextMenuProvider.swift:76`. Five in-repo tests drive the same path through `simulate_keystrokes` and all pass (`set_title_prompt_opens_prefilled_and_enter_commits_the_edit`, `clicking_ok_commits_the_set_title_prompts_draft`, `set_title_prompt_treats_a_blank_committed_draft_as_a_no_op`, `escape_cancels_the_set_title_prompt_without_changing_the_title`, `modal::tests::set_title_variant_draws_a_text_field`). **Why this is not PASSED:** the critic deleted that session's screenshot directory before copying anything into the repo and said so itself — so the live half is a first-hand account that **cannot be replayed**, and this project has been burned specifically by promoting on evidence that could not be re-run. Unit tests are not a substitute here; F-CORE-FILE-01 is the standing proof that a fix can pass its tests while the UI never calls it. One screenshot-backed re-drive of the Set Title path closes this. **Superseded record of the failure:** Copy Context: PASSED live (Paste after Copy Context inserted real surrounding multi-line text — `cont/07-06-copy-context-pasted.png`). **Set Title: confirmed broken**, both live and in source. Live: clicking "Set Title" instantly renamed the tab to `Terminal terminal-0` with **no text-entry prompt of any kind**; my subsequent typing landed as literal shell input instead (`cont/08-07-set-title-opened.png`, `cont/09-08-after-set-title.png`). Source: `rust/crates/tiller/src/main.rs` `set_terminal_title()` — `tab.title = format!("Terminal {terminal_id}")`, with the code's own comment: *"The Linux context event carries identity, not text. Use that stable identity as the app-owned title **until a text-entry prompt is added**."* This is a self-documented incomplete stub, not a working custom-title feature — the row's "confirm... title changes [to what was typed]" clause cannot be satisfied because there is no way to type a title at all. Clear Terminal: inconclusive in my run (see Not Exercised below) — not the basis for this verdict. One of three required sub-actions is source-confirmed non-functional, so the row fails as a whole. | sweep ?, 2026-08-19 |
| `F-TERM-06` | PASSED | **wave F live drive (this host)**: Live sweep /tmp/wf-term-block2a frames 13-16 plus standalone /tmp/wf-term-paneid-clean, /tmp/wf-term-termid-clean: Copy Pane ID and Copy Terminal ID each followed by Paste produced a nonempty identifier. | sweep ?, 2026-08-14 |
| `F-TERM-07` | PASSED | **wave F live drive (this host)**: Fresh live drive this pass (/tmp/wf-term-agentmenu, /tmp/wf-term-agent1): '+' menu offers New Terminal, Changes, New Browser, Claude Code, Codex, OpenCode, Pi, Oh-My-Pi, Split Claude Code, New Chat. Chose Claude Code; a real claude process launched, progressing through its actual first-run trust prompt to 'Welcome back Enzo!'. Sidebar/Activity panel showed '1 running' immediately. | sweep ?, 2026-08-14 |
| `F-TERM-08` | PASSED | **PASSED 2026-08-19, merged as `c0834159`** — and it took two rounds, because the first fix passed a critic that had tested it from the wrong entry point. The row's original defect (the context menu closing a running terminal with **zero** confirmation) is fixed by making `request_close_terminal_at` (`main.rs:4950`) **unconditional**, while `request_close_activity` (`:4987`) **keeps** its `pane_close_needs_confirmation` gate — the two distinct Swift contracts P103 established, verified still separate in the merged tree rather than collapsed into one. **The second round is the one that matters.** A refuter overturned the first PASSED: the banner was a plain div with no `.track_focus()`, so opening it via the documented `ctrl-alt-w` chord (with the terminal focused — the ordinary case) left every keystroke, Enter included, falling through into the live PTY while the user believed a blocking confirmation was up. The first critic had checked for a keystroke leak, but from the **mouse-driven** path, which never put keyboard focus on the terminal — a control that passed by exercising a state the bug cannot occur in. Fixed in two layers: a real GPUI focus claim following the Set Title prompt's already-correct pattern, plus a root-level swallow in `handle_root_key_down` mirroring the existing `palette_open` case, which holds regardless of focus-claim timing. **Enter cancels rather than confirms, and that was read from primary source, not assumed**: `NSAlert` gives Return to the *first-added* button and every close-confirm alert in the Swift original adds Cancel before Close (`TerminalContextMenuProvider.swift:58`, `SidebarView.swift:580`, `:627`); Set Title adds OK first, which is why the two prompts intentionally differ. **Live evidence, driven from the failing entry point this time** (`fullapp/BUILDER-modal-focus-fix.md`, shots committed under `builder-modal-focus-shots/`): typed into a real PTY first so it genuinely held focus (this doubles as the **positive control** — it proves typed text is detectable at all before any "did not leak" claim is trusted), opened the banner with the real `ctrl-alt-w` chord, typed `LEAK_1981730` which **never appears** in the prompt line, Escape cancelled and focus returned (the post-cancel canary lands right after the preamble), Enter cancelled without closing, and the mouse path still works via a real virtual-pointer click. One capture-race artefact (`07-04`, shot taken before the async Escape was processed) is called out in the doc rather than smoothed over. Regression suite on the merged tree: `tiller` bin 215 passed, `tiller_ui` lib 380 passed. **Superseded record of the failure:** Live-reproduced cleanly: started `sleep 300` in a plain (non-agent) terminal, confirmed it actively running on screen, right-clicked → "Close Terminal…". **The pane closed instantly, with zero confirmation of any kind** — no prompt, no "Close Anyway" banner — straight back to the "No Terminals" empty state in one click. `/dev/shm/sweep-12-F-TERMapp/phase8/02-01-menu-open.png` (menu open, `sleep 300` visibly running) → `phase8/03-02-close-confirm-dialog.png` (instantly empty). Traced to source: `request_close_terminal_at` (`rust/crates/tiller/src/main.rs:4817`) gates the confirm banner entirely on `pane_close_needs_confirmation(status)`, and `status` comes from `pane_activity_status` — the **agent-recognition** activity status (`AgentCatalog`-based Layers A-D), not "does this pane have a live child process." A generic shell command that isn't a recognized agent CLI can never reach `ActivityStatus::Running/NeedsInput/Error`, so the confirmation gate is permanently closed for ordinary terminal use. This directly contradicts the row's VERIFY ("Start a command that remains running... confirm the prompt, accept it") and the ledger's own current evidence text, which claims exactly the opposite ("confirm affordance appeared, accepted"). I could not find any confound in my repro (single deterministic click, no race) — this is a genuine, source-confirmed regression. | sweep ?, 2026-08-19 |
| `F-TERM-09` | half-proven | Live: while Claude Code was starting and then actively processing a real prompt, observed a solid/working status indicator, a live "Sautéed for Ns" elapsed-time readout, growing context/token counters, and "Activity · 1 running" — a real, observed working state (`agent3/03-02-sent-working.png`). Did **not** cleanly close the loop back to idle within budget: the response stayed stuck mid-turn for the rest of my observation window under heavy host contention (6+ concurrent `tiller`/`claude` processes measured via `pgrep`/`free` during this pass) rather than completing — recorded as environment load, not a defect (see Traps). The working→idle *transition* itself is separately corroborated by a same-host screenshot I directly reviewed today (`reference/linux-progress/wf-sweep2/f-term-10-marked-before-switch.png`, showing a solid "Claude Code ●" next to hollow "Terminal ○" tabs in the same frame) and by the documented Layer A-D model in CLAUDE.md, but I did not personally close a single continuous before/after capture. | sweep ?, 2026-08-19 |
| `F-TERM-10` | PASSED | **wave O (live, this host)**: pre-fix contrast driven live (commit dbe14bc0) [original defect independently reproduced on a pre-fix binary] \\| wf-sweep2 critic, x86 box, 2026-08-19 | sweep ?, 2026-08-14 |
| `F-TERM-11` | PASSED | **wave F live drive (this host)**: Live-driven from a genuinely fresh boot (deleted /tmp/wf-term.sqlite*/.sock after confirming no live holder, cold launch): central surface explained no worktree selected, captured before any ctl call reconstructed state. | sweep ?, 2026-08-14 |

## Package tier — `02-inventory-packages.md` (171)

### F-CORE-ACT — agent activity model (27)

| id | verdict | evidence | judged |
|---|---|---|---|
| `F-CORE-ACT-01` | PASSED | status::tests::{priority_order_is_error_needs_input_running_done,highest_priority_picks_the_winner,human_labels_match_swift,exit_codes_map_to_done_and_error} replayed green today (cargo test -p tiller_activity). \| wave D, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |
| `F-CORE-ACT-02` | PASSED | Live: ctl notify session=pane-0 status=running held through a same-instant contradicting title on a real pane (shot acta-second2/04-03); the resulting Claude Code notification fired on real D-Bus. \| wave D, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |
| `F-CORE-ACT-03` | PASSED | agent_spawned_sets_running_and_agent_id_without_transition passed; live: every real spawn today (codex/decoy/pi) went straight to Running+identity, no spurious notification. \| wave D, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |
| `F-CORE-ACT-04` | PASSED | exit_results_map_codes_and_respect_tracking passed; live: kill %1 on decoy codex cleared status/identity immediately (shot acta-master2/05). \| wave D, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |
| `F-CORE-ACT-05` | PASSED | panes::tests::real_pty_activity_status_follows_osc_title_then_settled_content (real PTY) passed; live: bare title alone assigned Claude+needs-input, no hook/process signal (shot acta-second2/02). \| wave D, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |
| `F-CORE-ACT-06` | PASSED | Live both halves: unmatched title cleared title-owned Claude (acta-second2/02); a genuine echoed OSC-title tamper left process-owned decoy codex untouched, cleared only by kill (acta-master2/04-05). \| wave D, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |
| `F-CORE-ACT-07` | PASSED | Live: hook beat an instant contradicting title (acta-second2/04); >1.5s later a fresh title took effect (acta-second2/05). Debounce unit tests replayed green. \| wave D, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |
| `F-CORE-ACT-08` | PASSED | content_match_overrides_stale_title_even_inside_debounce_window (exact clause) + real-PTY terminal_events test replayed green. \| wave D, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |
| `F-CORE-ACT-09` | PASSED | Live: real spawned child comm-named codex (symlink to /bin/sleep) -> Running/process-owned; kill %1 -> cleared (acta-master2/03,05). Real-subprocess /proc-walk test replayed green. \| wave D, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |
| `F-CORE-ACT-10` | PASSED | Live: real codex/decoy ELF detected via /proc on master; real pi (node cli.js) launched on third, structurally invisible to Layer D (exec'd comm is 'node', not in CATALOG_IDS). Depth/count-bound unit test replayed green. \| wave D, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |
| `F-CORE-ACT-11` | PASSED | Ledger's recorded evidence was for an unrelated feature (tab-bar dropdown), corrected here. each_ownership_kind_is_cleared_only_by_its_own_condition + pane_closed_clears_all_state_and_is_idempotent replayed green; live corroboration via today's rows 06/09. \| wave D, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |
| `F-CORE-ACT-12` | PASSED | title identity unit tests (glyphs, pi family, name tokens, bare spinner) replayed green; live title matched. \| wave D, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |
| `F-CORE-ACT-13` | PASSED | title status unit tests (claude idle/working, pi spinner/bare) replayed green; live matched. \| wave D, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |
| `F-CORE-ACT-14` | PASSED | word-boundary generic title unit tests replayed green. \| wave D, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |
| `F-CORE-ACT-15` | PASSED | content detection unit tests (claude prompt/streaming, generic y/n, boundary negatives) replayed green. \| wave D, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |
| `F-CORE-ACT-16` | PASSED | ANSI/OSC strip unit tests replayed green. \| wave D, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |
| `F-CORE-ACT-17` | PASSED | **wave P (live, this host)**: Corrected a stale DEAD-MODELS.md dead-code claim (agent_id_for_panes is live-wired at main.rs:5765). Added tiller_activity's first test module: agent_id_for_panes_breaks_a_tie_by_pane_order_not_by_agent_identity proves the identity-tie-break-by-pane-order clause via an order-reversal assertion, after a live pixel-tie attempt revealed NeedsInput's colour is brand-independent by construction. Committed (4bb12a41). \\| wf-rest4, x86 box, 2026-08-19 | sweep ?, 2026-08-14 |
| `F-CORE-ACT-18` | PASSED | **wave M live drive (this host)**: Live drive with a real duplicated-agent scenario (two codex tabs + one opencode tab) showed the running-agent badge row de-duplicated and in fixed CATALOG_IDS order (codex, opencode), not launch order -- reference/linux-progress/wf-rest3/05-06. | sweep ?, 2026-08-14 |
| `F-CORE-ACT-19` | PASSED | Live today: real dbus-monitor capture of Notify title 'Claude Code — tiller/linux/gpui-waku', body 'linux/gpui-waku · tiller' from this session's own transition. Payload-format unit test replayed green. \| wave D, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |
| `F-CORE-ACT-20` | PASSED | **wave K fresh critic (live, this host)**: Prior instance closed the mandatory gap with a real, installed GNOME notification-daemon (not a stub) on a private D-Bus bus — real GetServerInformation handshake, real Notify call captured, real suppression confirmed on refocus. Verified already committed (86ae6b33). | sweep ?, 2026-08-14 |
| `F-CORE-ACT-21` | PASSED | activity-rows unit test replayed green; live: Activity panel showed the real Terminal row labelled fixture-repo/master (shot acta-22b/02). \| wave D, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |
| `F-CORE-ACT-22` | PASSED | attention-sort unit test (both modes, ties) replayed green. Wiring re-confirmed live (both sorted and urgent_first called from main.rs, contra stale DEAD-MODELS.md 'whole module dead' claim). Did not independently re-drive the live multi-worktree reorder gesture today; relies on tests + confirmed-live wiring + prior sweep's gesture evidence. \| wave D, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |
| `F-CORE-ACT-23` | PASSED | Live today: real click on the Activity row's own close (x) control on a running pane produced the exact banner 'This tab has running work. Close anyway?' with Close Anyway/Cancel, dimming the whole window (shot acta-23b/03, 14210->6481 colours). Close-confirmation unit test (all 5 statuses) replayed green. \| wave D, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |
| `F-CORE-ACT-24` | PASSED | **wave P (live, this host)**: Real restart cycle (separate isolated instance, CODEX_HOME redirected, scratch git worktree, real session.ref control-socket call, real process kill+relaunch) produced a /proc/<pid>/cmdline showing the exact production `codex -c notify=[...] resume <ref>` command -- no OAuth needed since classification/launch never touches auth. Committed (af73eaa1). \\| wf-rest4, x86 box, 2026-08-19 | sweep ?, 2026-08-14 |
| `F-CORE-ACT-25` | PASSED | Live today, repeatedly: every one of ~10 fresh app boots this session auto-mounted only the persisted-selected worktree first (workspace.list confirmed). Bootstrap-order unit test replayed green. \| wave D, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |
| `F-CORE-ACT-26` | PASSED | Fully re-driven live today: real clicks toggled 'Limit mounted worktrees' on and stepped 'Keep mounted' to 3 (surface.settings.read confirms it); five real workspace.select calls across two projects show the mounted set held at exactly 3, oldest-idle evicted first each time exceeded. Mount-policy unit test replayed green. \| wave D, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |
| `F-CORE-ACT-27` | PASSED | grep -c '#[ignore]' panes.rs = 0; panes:: filter now 19/19 passed (0 failed, up from ledger's last-recorded 16/2); tiller_activity 73/73 passed; cargo build --workspace exit 0. \| wave D, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |

### F-CORE — domain, files, usage, workspace (45)

| id | verdict | evidence | judged |
|---|---|---|---|
| `F-CORE-DOM-01` | PASSED | **PASSED 2026-08-19 by a fresh, unrelated critic** (`fullapp/CRITIC-worktree-base.md`, judging `dd31b281`; in HEAD), live, with a **genuine kill+relaunch** — not a same-session re-read. Root cause was narrower and stranger than "not persisted": the persistence layer was always correct (`session.rs:2389`'s round-trip test predates both fixes). `main()`'s one-time `cx.open_window` closure called `set_project_identity` for every project but never `set_project_worktree_defaults`, unlike `refresh_sidebar` which always called both — so `project_worktree_defaults` stayed an **empty `HashMap` until the next unrelated refresh**. Reopening Project Settings before any such refresh — i.e. exactly a real restart with nothing happening in between, exactly what this row observed — read the empty map while the DB held the right value the whole time. The fix extracts `seed_sidebar_identity_and_worktree_defaults`, shared by boot and `refresh_sidebar`, so the two call sites structurally cannot drift apart again. Evidence: `07-dom01-fresh-boot-after-restart.png` (first frame of the **new** process, proving a fresh boot) → `08-dom01-reopened-after-restart-both-persisted.png` ("feature"/"Pinned" and `/dev/shm/domrestarttest` both intact). **The second half of the VERIFY clause is closed too**, which is why this is PASSED and not half-proven: `ctl project.list` on the restarted process now emits `defaultWorktreeBase`, `worktreeLocationOverride`, `color`, `iconKind`, `iconValue` — the fields it previously exposed none of. Regression: `cargo test -p tiller --bin tiller` 204 passed, run by the critic from its own clean build. **Superseded record of the failure:** Live restart test. Project display name ("MyFixture") and icon colour (green) survive a genuine kill+relaunch (same DB) correctly. But "Default Worktree Base" pinned to `feature` (UI showed "feature"/"Pinned") and "Worktree Location" override set to `/dev/shm/domrestarttest` (UI showed the literal path, no "Restore Default" residue) both **silently reverted to their unset defaults** after a clean restart — reopening Project Settings showed "master / Following primary branch (master)" and the bare `/dev/shm` placeholder again. Two of the eight listed persisted fields do not survive a restart. Screenshots: `22-both-set-preclose` (both fields set) vs `24-reopened-after-restart` (both reset). Additionally `ctl project.list` exposes only `id/isGit/name/path/worktreeCount/worktrees` — display name, colour, icon, default-worktree-base and location-override are **not visible through the control listing at all**, so the "…and visible through the corresponding domain/control listing" half of the VERIFY clause fails even for the fields that do persist. | sweep ?, 2026-08-19 |
| `F-CORE-DOM-02` | PASSED | **PASSED 2026-08-19 by a fresh, unrelated critic** (`fullapp/CRITIC-worktree-base.md`, judging `1f70f38b`; in HEAD), live, in a single uninterrupted session so no restart confound. **The dispatch hypothesis that this and F-CORE-DOM-01 were one defect wearing two symptoms was wrong, and the critic established that before writing anything** — they are two independent bugs sharing one map (`project_worktree_defaults`). This one was "read but ignored" by exactly one path: `begin_worktree_prompt` (the New Worktree dialog's open handler) never consulted the map at all, always seeding blank drafts, so `confirm_worktree_prompt` sent `None`/the sibling directory whenever the dialog's fields were left blank. The value was in memory correctly the whole time — Project Settings, reading the *same* map, showed "Pinned" immediately. The fix snapshots the pin onto `WorktreePrompt` at open time and falls back to it before reaching for `None`, mirroring the Swift original's `WorktreeDefaults.resolveBase`/`resolveParentDirectory` (`Packages/TillerCore/Sources/TillerCore/WorktreeDefaults.swift`), which the critic read rather than trusting the builder's citation. **Superseded record of the failure:** Live, single uninterrupted session (no restart confound). Fixture: `master`=HEAD (only `init`), `feature`=`init`+`feature-commit`. Set project's "Default Worktree Base" to `feature` (UI confirmed "feature"/"Pinned" immediately). Closed settings. Opened "New Worktree…" (its own placeholder literally reads *"base branch (optional, defaults to HEAD)"*), typed only a branch name `cleanbase1`, left the dialog's own Base field blank, pressed Enter. Result on disk: `git -C /dev/shm/MyFixture-cleanbase1 log --oneline` → only `init` — **no `feature-commit`**, proving the new branch was cut from HEAD, not from the pinned project base. The persisted project pin has zero effect on worktree creation; only the one-off dialog field (defaulting to HEAD) matters. | sweep ?, 2026-08-19 |
| `F-CORE-DOM-03` | PASSED | **wave N (live, this host)**: Was half-proven (unit test only). Live Create Project dialog with TILLER_PROJECTS_DIR unset: Parent location reads exactly /home/enzopalmisano/Tiller/projects, the documented $HOME fallback. Genuine relaunch with TILLER_PROJECTS_DIR=/tmp/wf-dom3-custom-projects-dir exported into the app's real environment: reopened dialog shows Parent location = /tmp/wf-dom3-custom-projects-dir exactly. Both VERIFY-clause halves through the real dialog, not the unit test alone. Screenshots f-core-dom-03-default-parent.png, f-core-dom-03-override-parent.png. | sweep ?, 2026-08-14 |
| `F-CORE-DOM-04` | PASSED | **wave H live drive (this host)**: unit tests green plus repeated live sidebar Filter use this pass | sweep ?, 2026-08-14 |
| `F-CORE-DOM-05` | PASSED | **wave N (live, this host)**: Was half-proven, citing tiller_project::move_item (domain.rs:41). Grep confirms move_item is dead (zero app callers) -- but unlike DOM-08 the feature lives under a different name: Sidebar::reorder_rows (sidebar.rs:758), wired to real .on_drag/.on_drop handlers, with its own unknown-id and no-op guards. Three named VisualTestContext drag tests (project/worktree/tab scopes) re-ran green fresh this pass. This lane's own hard discriminator: a genuine OS-level `drag` through wayland-drive.sh on the wf-term-clean sidebar row visibly moved it in the live app (before/after screenshots f-core-dom-05-before-drag.png, f-core-dom-05-after-drag.png), not a simulated event. | sweep ?, 2026-08-14 |
| `F-CORE-DOM-06` | PASSED | **PASSED 2026-08-19 — CLEARED by a fresh, unrelated critic** (`fullapp/CRITIC-commands.md`, judging `5e6c1715`; in HEAD). The duplicated wired path (`panes.rs`'s `TabSelection::jump`, which did `saturating_sub(1).min(count-1)` and therefore *clamped*) is deleted, and the previously-dead, already-unit-tested `numeric_tab_selection` is now the single implementation — confirmed in HEAD at `main.rs:7839`, with a post-deletion grep finding no dangling caller. The critic checked the equivalence claim **before** trusting the deletion and found the two were deliberately *not* equivalent, which is precisely the defect. Driven live through real chords on five real tabs: the decisive `ctrl-7` (out of range) now leaves tab 1 active with a pixel-identical tab bar (`07-…-baseline.png` → `08-ctrl7-outofrange-unchanged.png`, reproduced twice to rule out stale focus), `ctrl-2`/`ctrl-5` jump correctly, and `ctrl-9` still means "last" past the literal count. **The second caller was exercised independently rather than inferred** — raw NDJSON `tab.select` over the control socket: index 7 → no change, index 3 → Browser becomes active. **And the `usize`→`u8::try_from` boundary was closed after the critic's own first draft named it an untested gap:** index `100000` (past `u8::MAX`, inside `usize`) selected nothing and, critically, did **not** wrap into a small valid-looking position the way a truncating cast would. **Superseded record of the failure:** Live. With 4 open Terminal tabs, isolated single key-presses `ctrl-1`, `ctrl-2`, `ctrl-3`, `ctrl-9` all correctly jump to the matching 1-indexed tab (`ctrl-9` correctly selects the last tab). **Decisive test**: from tab 1 active, an isolated `ctrl-7` (out of range: not ≤4 tabs, not 9) moved the active tab to **tab 4 (the last tab)** instead of leaving tab 1 unchanged. Screenshots `50-before-c` (tab1) → `51-after-solo-ctrl7` (tab4). This is a real "clamp to last" behaviour, contradicting the contract's "invalid numbers ignored" and contradicting the already-correct, already-unit-tested `tiller_project::domain::numeric_tab_selection` (returns `None` for the same input) — which, like `move_item` in `F-CORE-DOM-05`'s prior finding, is dead code with zero live callers; the wired implementation (`panes.rs`'s `TabSelection::jump`) reimplements the same feature with different, wrong-per-contract behaviour. wrap/cycle (`ctrl-tab`/`ctrl-shift-tab`) not independently re-driven this pass (already PASSED live in wave H, and `TabSelection::cycle`'s wrap logic matches `domain.rs`'s `move_tab` on inspection). | sweep ?, 2026-08-19 |
| `F-CORE-DOM-07` | half-proven | **Not live-driven this pass** — would require a real multi-minute agent conversation (real API cost) to exercise the 30s/200-char boundaries properly, which this pass's time budget did not allow. Code inspection: real production wiring exists beyond the pure unit test — one `AutoNamingThrottle` per tab id in `main.rs` gates a genuine summarizer subprocess spawn and tab-title rewrite, with dedicated tests that spawn real subprocesses (`run_summarizer_command_trims_and_truncates_real_process_output`) and cover the exact "default chat tab, no tab agent" regression this row's history names. I did not re-run those tests nor drive a live agent turn myself this pass, so I cannot upgrade this past half-proven. | sweep ?, 2026-08-19 |
| `F-CORE-DOM-08` | PASSED | **wave Q fresh critic (live, this host)**: fixed side proven by a named regression test plus a structural read. Reproduction is structural only, and the critic argues the row has no live-observable defect by its own nature: the hand-rolled bool it replaced behaved identically, so wiring OnceGate changes the contract, not the behaviour. Recorded with that limit intact rather than as a live drive. See docs/linux-rewrite/CRITIC-waveQ-builder-claims.md. \\| wf-judge3, x86 box, 2026-08-19 | sweep ?, 2026-08-14 |
| `F-CORE-WSP-01` | PASSED | **wave P (live, this host)**: New adversarial gpui::test drawn_tab_status_distinguishes_split_terminal_chat_and_document (main.rs, commit 6abef2ac) draws a split terminal (worst-of-2-leaves), a chat tab, and a document tab with IDENTICAL activity data registered under all three pane ids (adversarial for the document case); asserts via debug_bounds that only terminal+chat surface a status glyph and the document tab's tab_status() returns None even with matching activity data present. cargo test -p tiller --bin tiller -- drawn_tab_status_distinguishes_split_terminal_chat_and_document: 1 passed. \\| wf-wsp2, x86 box, 2026-08-19 | sweep ?, 2026-08-14 |
| `F-CORE-WSP-02` | PASSED | **wave Q fresh critic (live, this host)**: original defect reproduced live on a pre-fix binary, fix confirmed on a HEAD binary. Note: this row was previously PASSED by its own builder, which had forgotten authoring commit e846ca44; this pass is the first genuinely independent judgment of it. See docs/linux-rewrite/CRITIC-waveQ-builder-claims.md. \\| wf-judge3, x86 box, 2026-08-19 | sweep ?, 2026-08-14 |
| `F-CORE-WSP-03` | PASSED | **wave J wf-wsp decision (live, this host)**: real route is `panes.rs::PaneNode<T>` — live-driven real split this pass. The tiller_project::layout model this row was previously scored against has zero app callers; see docs/linux-rewrite/WSP-LAYOUT-DECISION.md | sweep ?, 2026-08-14 |
| `F-CORE-WSP-04` | PASSED | **wave H live drive (this host)**: live rename via context menu, title persisted, focus-return proven | sweep ?, 2026-08-14 |
| `F-CORE-WSP-05` | half-proven | **The divider leg's evidence debt is PAID, 2026-08-20 — and the row still is not PASSED, for a reason worth reading before promoting it** (`fullapp/BUILDER-wsp05-divider-drag.md`, `fa9a24cc`, merged as `b6f8b363`). A fresh drive did exactly what this cell asked for: probe, drag the divider, probe again, all zero-click, read back through `panel.scrollback` — both markers land in `pane-1` in order, neither appears in `pane-0`, and `panel.list` still reports `pane-1` active afterwards. The read-back is what carries the claim, because it comes from the control socket rather than from pixels. **The anti-vacuity check was made and matters:** "focus unchanged" is trivially true of a gesture that did nothing, so the drag was confirmed to have really moved the split; verified here independently of the builder's own figure by differencing the pre- and post-drag frames, which changed across a 1198x861 px region. (Their specific 485→618 px measurement is theirs, not re-derived — a luminance probe cannot isolate a divider in a uniformly dark UI, and saying so is cheaper than a false precision.) **What keeps this half-proven is now one thing only, and it is not evidence:** the row's other leg, Insert and Move, still has no identified real-app analog and must be settled as a scoped port decision or found. No drive can close that; it is the user's call. Also recorded from that pass, in `WAYLAND-LANE.md`'s trap table: `wayland-drive.sh` starts its virtual pointer and keyboard only when the *action string* mentions an input verb, so a `ctl`/`shot`-only invocation left running under `TILLER_WL_KEEP=1` has no input devices at all, and a later `chord`/`type` against it silently reaches nothing. **Superseded record of the build:** **orchestrator build, 2026-08-19 (commit follows FIX-PLAN-wsp05-divider-focus.md)**: the divider focus defect is FIXED. Split and Close focus behaviour were already proven live. The divider leg -- the row's fraction/nonstructural class, which must leave focus alone -- ended the gesture with window focus on NOTHING, saved only by F-SID-19's root reclaim. New method refocus_focused_pane restores focus, called from the divider's on_drop. THE OBVIOUS FIX WAS WRONG AND A PROBE PROVED IT: an on_mouse_down listener does fire with the correct tab_index and pane id (instrumented and observed), yet focus was still blurred, because GPUI's drag machinery clears it after the listeners run and even a window.on_next_frame re-focus loses that race; on_drop is the first moment the gesture is over. Two-sided evidence: the pre-existing test drawn_divider_drag_blurs_focus_to_workspace_root, which ASSERTED the defect, FAILED against the change -- that failure is the proof the behaviour moved -- and it is inverted to drawn_divider_drag_leaves_pane_focus_untouched, green. Built and run with CARGO_TARGET_DIR on tmpfs because the root disk was minutes from ENOSPC. STILL HALF-PROVEN, on two counts: the orchestrator built it, and the row's OTHER open leg is untouched -- Insert and Move still have no identified real-app analog and must be settled as a scoped port decision or found. A fresh critic also owes the live zero-click drive (probe, drag the divider, probe again, both landing in the same pane via panel.scrollback). \\| orchestrator, x86 box, 2026-08-19 | sweep ?, 2026-08-14 |
| `F-CORE-WSP-06` | PASSED | **wave Q fresh critic (live, this host)**: original defect reproduced live on a pre-fix binary, fix confirmed on a HEAD binary. Same independence note as WSP-02 (builder had forgotten authoring ea9026da). See docs/linux-rewrite/CRITIC-waveQ-builder-claims.md. \\| wf-judge3, x86 box, 2026-08-19 | sweep ?, 2026-08-14 |
| `F-CORE-WSP-07` | PASSED | **wave Q (this host)**: the schema-version/canonical-JSON half is no longer argued but TESTED -- two new named tests in session.rs prove the additive-evolution claim (an old blob still decodes after a field is added the #[serde(default)] way, and a reordered-key blob decodes identically), both green, and re-run fresh this pass inside `cargo test -p tiller --bin tiller` (197 passed). Malformed-to-empty fallback was already proven via SessionTabState::decode + quarantine. \\| wave Q, x86 box, 2026-08-19 | sweep ?, 2026-08-14 |
| `F-CORE-WSP-08` | PASSED | **wave H live drive (this host)**: draft byte-exact in SQLite, survived a real process kill+relaunch | sweep ?, 2026-08-14 |
| `F-CORE-FILE-01` | PASSED | **PASSED 2026-08-20 — the missing half was the live one, and it has now been read off the screen** (`fullapp/VERIFY-chg06-file01.md`, frame `01-files-tree-natural-sort-and-status-dots.png`, driven against `c392542c`). The Files panel, opened on a fixture containing all four of `file1.txt`/`file2.txt`/`file10.txt`/`file20.txt`, renders them in that order — `file10.txt` **after** `file2.txt`. The fixture is known to be able to fail: `ls` on the same directory prints `file10.txt file1.txt file20.txt file2.txt`, the exact lexicographic order this row was originally failed for, so a panel that had not been wired to the comparator would have shown it. This is the **render** half per `WAYLAND-LANE.md`:102-107, which is the entirety of what this row's "sorts names localized" clause claims. The accented-name (`é-note.md`) sub-case noted below was **not** re-driven and is not part of this verdict. **Independently corroborated** by `fullapp/BUILDER-triple-observation.md` (`9409cfb0`, merged as `bb703f41`), driven and committed before this row's own frames existed, on a different fixture: the panel rendered `zdir, file1.txt, file2.txt, file9.txt, file10.txt`, an ordering where plain lexicographic would have put `file10.txt` second — a sharper discriminator than this row's own fixture, since there the wrong answer differs from the right one at the *second* position rather than the third. Same shared-harness caveat recorded under `F-CHG-06`. **Superseded record — the half-proven verdict this replaces:** **Half-proven 2026-08-19 — fixed and unit-proven, but never driven live through the UI, and the reason that distinction matters is this row's own history.** A fresh critic (`fullapp/CRITIC-files.md`) failed commit `c3b9e7ac`'s attempt with the verdict "the fix exists, **was never wired to the UI**, and is now deleted" — a natural-sort comparator that no rendering path called. The landed fix wires it where the UI actually reads: `tiller_project::file_sort::compare_file_tree_names` (three-tier, ported against Swift's `localizedStandardCompare` in `FileTree.swift:82`) is now called from **both** `tiller_project/src/file.rs:218` and `tiller_ui/src/right_panel.rs:1321`'s `read_tree` — the second being the one the Files panel renders from, and the one the deleted attempt missed. Proven by a test driving `read_tree` directly (`the_files_tree_sorts_names_in_natural_numeric_order`) plus a **positive control**: the test was re-run against the pre-fix comparator and failed, so it is known to be able to fail. **The missing half is the live one** — no critic has yet opened the Files panel on a fixture containing `file2.txt`/`file10.txt` and read the rendered order off the screen, which is the only evidence that would satisfy this row by the standing rule. Promote only on that. **Superseded record of the failure:** Dirs-before-files and case-insensitive alphabetic sort both confirmed live: `.github, a-dir, docs, node_modules, src, tests, Z-dir` (dirs) then `.env, a-note.md, app.lock, ...` (files) in that order, `.git` never listed (`/dev/shm/sweep-15-F-CORE-FILE/02-02-tree-full.png`). But the row's own "sorts names localized" clause is live-disproved: with `file2.txt` and `file10.txt` both present, the tree renders `file10.txt` **before** `file2.txt` (`/dev/shm/sweep-15-F-CORE-FILE/02-02-tree-full.png`, rows at y=601/631) — plain lexicographic order, not the natural/numeric-aware order a real "localized" (Finder-style) sort produces. Root cause read in `Packages/TillerCore/Sources/TillerCore/FileTree.swift:82`: the original explicitly calls Swift's `localizedStandardCompare`; the Rust port's `read_directory` in `rust/crates/tiller_project/src/file.rs:216-223` sorts on a plain `.to_lowercase()` string comparison, which has no natural-number or diacritic-folding behaviour. (Same root cause also means the accented `é-note.md` sorts after every ASCII name by raw codepoint rather than folding near unaccented siblings — read from the same comparator, not independently re-driven this pass beyond the file2/file10 case.) | sweep ?, 2026-08-19 |
| `F-CORE-FILE-02` | PASSED | **wave H live drive (this host)**: real XDND onto Chat composer: image accept, oversized reject (exact message), in-tree and outside-tree file chips all confirmed live | sweep ?, 2026-08-14 |
| `F-CORE-FILE-03` | PASSED | **wave H live drive (this host)**: cross-referenced to F-EDIT-12 (identical production path) | sweep ?, 2026-08-14 |
| `F-CORE-FILE-03A` | PASSED | **wave P (live, this host)**: New test dropping_external_files_preserves_the_drop_order in chat.rs drops BRAVO then ALPHA and asserts mention_paths preserves literal drop order, not alphabetical. Committed pre-compaction (76df0822). \\| wf-rest4, x86 box, 2026-08-19 | sweep ?, 2026-08-14 |
| `F-CORE-FILE-04` | PASSED | **wave P (live, this host)**: Real markdown link clicked live in the running app; resolved via resolve_file_link and opened a new tab with the target file's content. Committed pre-compaction (a453305e). \\| wf-rest4, x86 box, 2026-08-19 | sweep ?, 2026-08-14 |
| `F-CORE-FILE-05` | PASSED | **wave H live drive (this host)**: cross-referenced to F-EDIT-02 (identical production path) | sweep ?, 2026-08-14 |
| `F-CORE-FILE-06` | PASSED | **wave H live drive (this host)**: cross-referenced to F-EDIT-05/06 (identical production path) | sweep ?, 2026-08-14 |
| `F-CORE-FILE-07` | PASSED | **wave H live drive (this host)**: same inotify watcher exercised for F-CORE-FILE-06/F-EDIT-05/06 | sweep ?, 2026-08-14 |
| `F-CORE-FILE-08` | PASSED | **PASSED 2026-08-19 by a fresh, unrelated critic** (`fullapp/CRITIC-files.md`, judging `c3b9e7ac`; in HEAD) — "real, wired, and correct where it claims to be". `tiller_project::file_icon::FileIconKey` now carries **57 variants** with `for_file_name`/`for_directory_name`, ported 1:1 against the Swift `FileIconKey.swift`'s three tables (exact-name, extension, directory-name), including the `NSString.pathExtension` vs Rust `Path::extension()` divergence. Counted in HEAD: 56 enum variants, 58 name/extension match arms, consumed at `right_panel.rs:1274`. Driven live on the critic's own `Xvfb :3` against a fixture built with one file per extension family and one directory per named-folder convention; screenshots committed under `fullapp/critic-files-shots/`, not left in `/dev/shm`. The row's directory half — previously **0 of 15** folder mappings — is the part that changed most. **Superseded record of the failure:** Live Files-panel screenshot of the fixture (25 files across 18 distinct extensions/exact-names, 7 named directories) shows only **three** distinct file glyphs total — one generic document icon shared by `a-note.md, app.lock, archive.zip, config.json, data.yaml, database.db, doc.pdf, Dockerfile, format-me.md, index.html, intree-photo.png (a real PNG!), LICENSE, link.md, notes.log, photo.png, run.sh's siblings, script.py, song.mp3, style.css` — plus a distinct gear glyph for `.env` and a distinct terminal glyph for `run.sh` (`/dev/shm/sweep-15-F-CORE-FILE/02-02-tree.png`, cropped in `/dev/shm/crop-tree-icons.png`) — and **one** generic folder glyph shared by every one of `.github, a-dir, docs, node_modules, src, tests, Z-dir` with zero name-based distinction. Confirmed at the code level, `rust/crates/tiller_ui/src/right_panel.rs`'s `file_glyph()`: it special-cases only shell scripts, `.git*`-family dotfiles, and `.env`/config dotfiles; every other extension — and every directory regardless of name — falls through to one shared `Icon::File` / `Icon::FolderFill`. The original `Packages/TillerCore/Sources/TillerCore/FileIconKey.swift` defines **~40** distinct per-extension icon keys (swift, python, rust, json, yaml, image, video, audio, pdf, archive, sql, database, docker, log, lock, makefile, …) and **15** distinct named-folder icon keys (`folderSrc, folderTests, folderDocs, folderNodeModules, folderDist, folderScripts, folderConfig, folderAssets, folderPublic, folderPackages, folderVscode, folderGit, folderLib, folderTools, …`); this port implements 3 of the ~40 file mappings and 0 of the 15 folder mappings. The row's own contract text — "selects exact filename or extension icons for files **and named icons for directories**" — is not met for directories at all, and is met for a small minority of files. | sweep ?, 2026-08-19 |
| `F-CORE-TERM-01` | PASSED | **wave F live drive (this host)**: Live-confirmed earlier in this continuous pass; corroborated by passing test terminal_actions_are_local_and_pane_actions_are_delegated (rust/crates/tiller_terminal/src/context_menu.rs:261). | sweep ?, 2026-08-14 |
| `F-CORE-TERM-02` | PASSED | **wave F live drive (this host)**: Chord Shift+F10 opens the terminal context menu — confirmed live incidentally during the exit-0 pill test, plus the full block2a menu sweep. | sweep ?, 2026-08-14 |
| `F-CORE-TERM-03` | PASSED | **wave F live drive (this host)**: Live-confirmed earlier in this continuous pass (menu dispatch structure). | sweep ?, 2026-08-14 |
| `F-CORE-SET-01` | PASSED | Closed twice: direct-binary TILLER_SOCKET_ENABLE=off/on negative+positive control, AND the live UI toggle (F-SET-08) causing the next real process launch to print '[control] disabled' -- same boundary, exercised through both the env var and a real user gesture. \| wave B, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |
| `F-CORE-SET-02` | N/A — platform | Re-verified today against App/Permissions/PermissionsModel.swift's counterpart SystemPermissionProbe.swift -- every modeled permission kind is macOS TCC-only, matching F-SET-23's re-check. \| wave B, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |
| `F-CORE-USG-01` | PASSED | cargo test -p tiller_usage model::tests:: today: percent_is_clamped_on_construction, success_replaces_the_previous_state, unavailable_replaces_everything — 3/3 pass. from_percent (model.rs:29) routes non-finite to 0 before clamping. \| wave D, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |
| `F-CORE-USG-02` | PASSED | cargo test -p tiller_usage claude::tests:: + usage_tests.rs today: strip_ansi_removes_csi_and_osc, label_matching_follows_the_swift_patterns, percent_token_handles_used_left_and_bare, failure_markers_classify, a_well_formed_state_file_parses_into_the_expected_windows (real captured /usage transcript), a_real_capture_with_carriage_return_line_separators_parses, a_truncated_state_file_never_panics_and_loses_nothing_complete — all pass. \| wave D, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |
| `F-CORE-USG-03` | PASSED | cargo test -p tiller_usage ollama::tests:: today: accepts_only_the_expected_usage_percent_field, extracts_the_first_unquoted_usage_percent_from_page_text, page_percents_above_100_are_clamped, garbage_is_not_usage — pass. \| wave D, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |
| `F-CORE-USG-04` | PASSED | cargo test -p tiller_usage opencode_go::tests:: today: normalizes_bare_and_prefixed_cookies, extracts_a_workspace_id_from_the_server_body, parses_a_react_flight_usage_page, garbage_is_not_usage, workspace_override_requires_content_after_trimming — pass. \| wave D, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |
| `F-CORE-USG-05` | PASSED | cargo test -p tiller_usage codex::tests:: + p99_codex_locations.rs today, incl. auth_file_precedence_load_refresh_and_missing_at_both_locations covering both $CODEX_HOME and ~/.codex, and the 8-day refresh gate — pass. Also live-confirmed CODEX_HOME resolution in the real running binary via F-CORE-USG-06's drive. \| wave D, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |
| `F-CORE-USG-06` | PASSED | Live today against the REAL auth.openai.com (curl confirmed real egress, HTTP 403 not a network error): garbage OAuth tokens under isolated CODEX_HOME, launched pinned binary, clicked refresh — real 401 from the real backend, status bar read 'Codex token expired' (distinct copy). Screenshot /tmp/wd-actb-shots/codex-401-zoom2.png. Plus unit tests token_refresh_failure_routes_to_distinct_usage_reasons and UI-tier status_bar::tests::token_refresh_reasons_render_distinct_text, all pass. \| wave D, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |
| `F-CORE-USG-07` | PASSED | **wave Q critic (live, this host)**: the valid-credentials/200 leg, previously recorded UNREACHABLE on this host, was driven against a real local one-shot HTTP fixture and a real local CODEX_HOME/auth.json, through the production CodexUsageFetcher::fetch() (confirmed reachable from tiller_ui/status_bar.rs by grep). No user-global config was written. ORCHESTRATOR NOTE: the critic added a test seam to production code to do this -- usage_url()/TILLER_CODEX_USAGE_URL in tiller_usage/src/codex.rs (commit b0f6cc7b), mirroring the existing token_url() precedent. The fetch path under test pre-existed; only the URL override is new. \\| wave Q, x86 box, 2026-08-19 | sweep ?, 2026-08-14 |
| `F-CORE-USG-08` | PASSED | not-installed/logged-out/error/bounded-timeout: usage_tests.rs live-shell tests pass today. Success: live app status bar showed real changing values across refreshes ('Claude 36-40% 5h / 32-34% wk'). Timed-out: TILLER_USAGE_CLAUDE_TIMEOUT_MS=1200 against the real claude binary produced 'Claude timed out' live (timeout-check-zoom.png). PTY termination measured by PID: a spawned claude child (usage-fetch PTY) appeared then was confirmed gone via ps -p 6s later, while a separate genuine interactive Claude Code pane's own claude child stayed alive the whole time. \| wave D, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |
| `F-CORE-USG-09` | PASSED | provider_catalog_and_preferences_are_stable, status_bar::tests::usage_bar_consumes_visibility_and_interval_preferences and a_real_timeout_after_a_real_success_dims_the_live_entity (drawn, production entity method) all pass today. Live: toggled Claude's Show-in-usage-bar off/on in AI Providers, confirmed via ctl surface.settings.read socket readback AND visually (segment vanished/reappeared). \| wave D, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |
| `F-CORE-AUTH-01` | PASSED | **wave I live drive (this host)**: Live Settings AI Providers frame: Claude Code reads 'Signed in e.palmisano@reply.it' from the real credentials file, Codex in the same frame reads 'Not signed in' (negative control); AgentAccountIdentity::parse_claude_json/parse_codex_identity confirmed as the real functions behind the displayed state. | sweep ?, 2026-08-14 |
| `F-CORE-AUTH-03` | PASSED | **wave I live drive (this host)**: Independently re-drove the read/persistence half live: fresh scratch credential store read 'Not signed in' with zero interaction; a value written directly in the exact production JSON format at mode 0600 was then read as 'Signed in' by a genuinely fresh app process with zero interaction. Write-via-UI half rests on the prior sweep's (H5-drive) documented three-process methodology since the literal Save/Clear field could not be scrolled to this pass; the plaintext-vs-encrypted-store deviation is carried forward unchanged for a human ruling. | sweep ?, 2026-08-14 |
| `F-CORE-UI-01` | PASSED | cargo test -p tiller_project ui:: today: appearance_follows_system_only_in_system_mode ... ok. Pure state-machine test (AppearanceMode::resolve), executed not read. \| wave B, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |
| `F-CORE-UI-02` | PASSED | Same run: updater_reaches_every_user_visible_state_and_clamps_progress ... ok. Exercises every UpdateEvent transition including 150->100 progress clamping. \| wave B, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |
| `F-CORE-AUTH-02` | PASSED | **wave I live drive (this host)**: Real caller chain confirmed live (Settings on_install_skill -> InstallSkill action -> direct-exec TerminalShell, no shell interpolation) plus cargo test green; the exact generated argv was independently run for real and reached real GitHub, failing only on private-repo auth - proves the command is genuine and well-formed even though the literal Settings button could not be clicked this pass due to a scroll-instrument gap in this lane. | sweep ?, 2026-08-14 |
| `F-CORE-PLAT-01` | N/A — platform | Re-checked today (not carried from stale pass): Packages/TillerCore/Package.swift still declares platforms: [.macOS(.v15)] only; no Linux manifest replaces it. VERIFY clause itself says this is a reference gap not a build step. \| wave B, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |

### F-CTRL — control socket (34)

| id | verdict | evidence | judged |
|---|---|---|---|
| `F-CTRL-WIRE-01` | PASSED | 56/56 tiller_control tests green today over real UnixStream sockets (framing/id-echo); 60+ live NDJSON round trips via wayland-drive ctl today. docs/linux-rewrite/FINISH-control.md \| wave A shard critic, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |
| `F-CTRL-WIRE-02` | PASSED | Live: socket at 0600 (srw-------), stale-socket + oversize/malformed tests green today (oversized_request_line_is_rejected_not_buffered etc.), concurrent_clients_are_all_served green. \| wave A shard critic, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |
| `F-CTRL-WIRE-03` | PASSED | Fresh-connection-per-round-trip proven by dozens of independent ctl calls today; idle_client_does_not_block_other_clients, request_split_across_reads_is_assembled green today. \| wave A shard critic, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |
| `F-CTRL-WIRE-04` | PASSED | Live both halves today: with TILLER_SOCKET (all drives) and without (manual launch resolved to $XDG_RUNTIME_DIR/TillerRust/control.sock, tillerctl ping succeeded against it). \| wave A shard critic, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |
| `F-CTRL-PANEL-01` | PASSED | Live panel.create with cmd and without cmd both returned live ids, listed correctly (title defaults to 'Panel'). No legacy-mode concept exists in the Rust rewrite (grep confirms) so that half is N/A, not a defect. \| wave A shard critic, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |
| `F-CTRL-PANEL-02` | PASSED | Live panel.split from=<id> direction=right returned new id, appeared in panel.list titled '...(right)'. \| wave A shard critic, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |
| `F-CTRL-PANEL-03` | PASSED | panel.list called 7+ times through the drive, rows matched live pane state each time (create/split/close reflected). \| wave A shard critic, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |
| `F-CTRL-PANEL-04` | PASSED | panel.write+panel.key(enter) then panel.read decoded (base64) to b'CTRL-MARKER-2WRITE-OK-MARKER\r\n' -- written text landed, enter appended CRLF. \| wave A shard critic, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |
| `F-CTRL-PANEL-05` | PASSED | All 9 symbolic keys sent to a real byte-echo (cat) pane; base64-decoded scrollback hex-verified byte-for-byte against terminal_key_bytes() for enter/tab/escape/backspace/delete/up/down/left/right. \| wave A shard critic, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |
| `F-CTRL-PANEL-06` | PASSED | panel.read on a printf'd marker pane decoded exactly to 'CTRL-MARKER-1'. \| wave A shard critic, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |
| `F-CTRL-PANEL-07` | PASSED | Direct probe with correct timeoutMs key: 1000ms/2000ms waits returned 'pane timed out' at 1.001s/2.005s elapsed; unknown pane -> distinct error; fast-exit pane -> exitCode 7 promptly. (My first attempt used the CLI flag spelling 'timeout-ms' by mistake, causing an unbounded wait -- documented in FINISH-control.md, not a product bug.) \| wave A shard critic, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |
| `F-CTRL-PANEL-08` | PASSED | panel.focus returns ok:true on a live pane; same underlying registry visibly mounts/unmounts the real Claude Code UI tab via workspace.close/session.restore (see SESSION-02). \| wave A shard critic, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |
| `F-CTRL-PANEL-09` | PASSED | panel.close on a live pane and a timed-out pane both ok:true; subsequent panel.list no longer lists them. \| wave A shard critic, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |
| `F-CTRL-NOTIFY-01` | PASSED | Agent mode (running/needs-input/done) queued:true, unknown status rejected; user mode (title/body) confirmed end-to-end via dbus-monitor; agent mode also exercised through a REAL Claude Code SessionStart hook that wrote a genuine session UUID via the installed tillerctl shim. \| wave A shard critic, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |
| `F-CTRL-NOTIFY-02` | PASSED | tillerctl_rejects_ambiguous_notify_modes green today; --stdin-json path exercised for real by the genuine Claude hook config which uses exactly that flag. \| wave A shard critic, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |
| `F-CTRL-SESSION-01` | PASSED | session.ref round-tripped and persisted in sqlite session_ref table; a real Claude Code SessionStart hook independently wrote a genuine session UUID to the same table; restore_tabs()/resumable_session_refs (main.rs:10071) demonstrably consumes it. \| wave A shard critic, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |
| `F-CTRL-WORK-01` | PASSED | worktree.set by UUID and by exact path both echoed correctly, unknown selector errors; comment survived a genuine full app-process restart (workspace.list on the new process still showed the comment). \| wave A shard critic, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |
| `F-CTRL-SYS-01` | PASSED | ping->pong and capabilities returned the full real 62-method roster plus socketEnabled:true and real socketPath, live today. \| wave A shard critic, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |
| `F-CTRL-SYS-02` | PASSED | All four resolution branches driven live: explicit params, env TILLER_WORKTREE_ID/TILLER_PANE_ID, no-context-with-selection fallback, and true no-context on an empty instance ('no current workspace' error). \| wave A shard critic, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |
| `F-CTRL-WORK-02` | PASSED | workspace.list stayed correctly ordered and selected-flagged across multiple live worktree/project additions and selections. \| wave A shard critic, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |
| `F-CTRL-WORK-03` | PASSED | Live: explicit-branch create (real git worktree add -b on disk), no-branch create (generated wt-<epoch> branch), and rejection against a genuine non-git project ('project nongit-project is not a Git repository'). \| wave A shard critic, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |
| `F-CTRL-WORK-04` | PASSED | Selected by UUID and by exact path, workspace.current reflected each; no-selection error reproduced live on a fresh empty-DB instance. \| wave A shard critic, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |
| `F-CTRL-WORK-05` | PASSED | workspace.close on a worktree with a live real Claude Code pane returned closed:true; panel.list then errored 'unknown worktree' (unmounted) while workspace.list kept the row available for reopening. \| wave A shard critic, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |
| `F-CTRL-NOTIFY-03` | PASSED | dbus-monitor captured a real org.freedesktop.Notifications.Notify call with the exact sent title/body DBUS-TEST-TITLE-XYZ/DBUS-TEST-BODY-XYZ, live today, plus a bonus real first-launch notification in the same capture. \| wave A shard critic, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |
| `F-CTRL-SESSION-02` | PASSED | Live: workspace.close unmounted a real Claude Code tab, session.restore returned restoredCount:1 and remounted it (new pane, same tab), a second session.restore returned restoredCount:0 with no duplication (still exactly 1 panel). \| wave A shard critic, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |
| `F-CTRL-BROWSER-01` | PASSED | system.capabilities lists exactly the 9 documented browser.* methods; browser.errors absent from the list and returns ok:false 'unsupported on Linux'. All 9 advertised methods individually called this session and returned real (non-stub) behavior. \| wave B, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |
| `F-CTRL-BROWSER-02` | PASSED | Fresh instance, no project.add: ctl workspace.current and ctl browser.open both return {"ok":false,"error":"no current workspace"} -- identical failure, closing the row's inconsistency check. \| wave B, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |
| `F-CTRL-BROWSER-03` | PASSED | browser.open then browser.get returns a real BrowserState (canGoBack/canGoForward/loading/title/url reflecting actual page state), repeated across multiple live sessions today. \| wave B, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |
| `F-CTRL-BROWSER-04` | PASSED | browser.screenshot returns the blanket unsupported error (excluded by design, wry 0.56 has no WebKitGTK pixel-capture entry point). browser.snapshot on the same live surface returned real structural data: title/url/elements array with real h1 and a-tag entries -- discriminated live today. \| wave B, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |
| `F-CTRL-BROWSER-05` | PASSED | browser.act verb=click selector=a on live Example Domain actually clicked 'Learn more'; follow-up browser.get shows url=iana.org/help/example-domains, title='Example Domains', screenshot confirms real IANA page rendered. browser.wait condition=url value=iana returned loading:false, timedOut:false against real state. \| wave B, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |
| `F-CTRL-BROWSER-06` | PASSED | browser.eval script=document.title on live Example Domain returned {"result":"\"Example Domain\""} -- real evaluated JS value, not the 'Browser child unavailable' failure mode. \| wave B, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |
| `F-CTRL-CLI-01` | PASSED | Source matches documented subcommand list exactly; live today invoked nearly the entire surface (ping, capabilities, identify, project, panel.*, notify, session-ref, worktree-set, workspace.*, notification.*, restore-session) with observed state changes, including --socket before the subcommand. \| wave A shard critic, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |
| `F-CTRL-CLI-02` | PASSED | Shim confirmed installed at ~/.local/share/TillerRust/bin/tillerctl (symlink, functional); live UI spawn of real Claude Code wrote settings.local.json with 5 hooks all pointing at exactly that path, and its real SessionStart hook fired it for real (wrote a genuine session UUID). \| wave A shard critic, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |
| `F-CTRL-PLAT-01` | PASSED | grep confirms zero Darwin imports, only cfg(unix)/std::os::unix; cargo build --workspace exit 0 native Linux today; identical NDJSON contract carried every live call and all 56 tests today. \| wave A shard critic, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |

### F-AGENT — agent adapters (20)

| id | verdict | evidence | judged |
|---|---|---|---|
| `F-AGENT-API-01` | PASSED | **wave I live drive (this host)**: Real right-click context menu lists Claude Code/Codex/OpenCode/Pi/Oh-My-Pi in order; settings snapshot returned real resolved PATH for every adapter; catalog_has_the_five_adapters_in_order test green; real callers of prepare/command/resume_command confirmed at 3 sites in main.rs. | sweep ?, 2026-08-14 |
| `F-AGENT-CLAUDE-01` | PASSED | **wave I live drive (this host)**: Real v2.1.234 TUI, real prompt sent, Stop hook fired live (visible in the tab glyph and inside the real .jsonl), session id captured live into SQLite from the hook's own stdin JSON, and a real `claude --resume <same id>` PID resumed showing the full prior turn on screen. | sweep ?, 2026-08-14 |
| `F-AGENT-CLAUDE-02` | PASSED | **wave I live drive (this host)**: Live settings.local.json has exactly the five documented hooks with correct statuses and the real resolved tillerctl path; independently re-verified merge preservation by seeding a stale path + unrelated key/hook and confirming only the path was rewritten on relaunch. | sweep ?, 2026-08-14 |
| `F-AGENT-CLAUDE-03` | PASSED | **wave I live drive (this host)**: Ran claude -p '<prompt with $(...), quotes, &>' in a real shell; output echoed the prompt literally, no injection; shell_quote tests (6) green. | sweep ?, 2026-08-14 |
| `F-AGENT-CODEX-01` | PASSED | **wave P (live, this host)**: F-CORE-ACT-24's captured command line word-for-word satisfies this row's own clause text (notify override + resume <ref>, needs-input in the override); structural proof that command()/resume_command() share one notify_override() call site closes the 'that same override' claim. Corrected a draft error citing unverified corroborating evidence before committing. Committed (5c81c7c4). \\| wf-rest4, x86 box, 2026-08-19 | sweep ?, 2026-08-14 |
| `F-AGENT-CODEX-02` | PASSED | **wave I live drive (this host)**: Ran the exact generated 'codex exec --output-last-message /dev/stdout' command with a $(id)-laden prompt for real; no injection; reached the real OpenAI endpoint (401, same account limitation). | sweep ?, 2026-08-14 |
| `F-AGENT-OPENCODE-01` | PASSED | **wave I live drive (this host)**: Real OpenCode TUI, real session.updated captured via the live plugin, and a genuine `opencode --session <same id>` PID resumed after quit/relaunch. | sweep ?, 2026-08-14 |
| `F-AGENT-OPENCODE-02` | PASSED | **wave I live drive (this host)**: prepare() wrote exactly .opencode/plugin/tiller-session.js matching the documented template; real session.updated event fired tillerctl session-ref exactly once for the new id, persisted in SQLite. | sweep ?, 2026-08-14 |
| `F-AGENT-OPENCODE-03` | PASSED | **wave I live drive (this host)**: opencode run --pure '<prompt with $(id), quotes>' executed for real; stdout echoed the prompt literally, no injection. | sweep ?, 2026-08-14 |
| `F-AGENT-PI-01` | PASSED | **wave I live drive (this host)**: Real Pi v0.84.1 TUI launched; zero files written anywhere in the worktree by prepare(), matching 'performs no preparation' exactly. | sweep ?, 2026-08-14 |
| `F-AGENT-PI-02` | PASSED | **wave I live drive (this host)**: pi --print --no-tools '<prompt with $(id), quotes>' executed for real (piped stdin to avoid an unrelated stdin-read hang); reached Pi's real backend and returned a genuine backend usage-limit error, not an injected result. | sweep ?, 2026-08-14 |
| `F-AGENT-OMP-01` | PASSED | **PASSED 2026-08-19 by an independent UI-drive critic** (`fullapp/F-AGENT-OMP-uidrive.md`), driven through Tiller's real UI with synthetic X11 input inside a private nested Xwayland -- never `DISPLAY=:1`. The critic verified the binary's provenance before trusting it (`strings` shows only `omp --hook`, mtime 20:31 postdates commit `6e6ccdd0` at 19:40, clean worktree, and it read `omp.rs` directly rather than only the binary). Root cause of the five-day `UNREACHABLE`: npm carries two unrelated packages, and Tiller launched the wrong one -- `oh-my-pi` (acidsugarx) is a broken VS Code extension; the agent is `@oh-my-pi/pi-coding-agent`, binary `omp` 17.3.8, which is what the Swift original always spelled. Fully live: real menu click, real pane, real omp TUI, real child process, real `.tiller/omp-hook.ts` written, and a real before/after diff of the user-global files confirming none was touched. Also proven at adapter level by `omp_live.rs`, which runs `prepare()`/`command()`'s own output rather than a typed lookalike. \| independent UI-drive critic, x86 box, 2026-08-19 | sweep ?, 2026-08-14 |
| `F-AGENT-OMP-02` | PASSED | **PASSED 2026-08-19 by an independent UI-drive critic** (`fullapp/F-AGENT-OMP-uidrive.md`), driven through Tiller's real UI with synthetic X11 input inside a private nested Xwayland -- never `DISPLAY=:1`. The critic verified the binary's provenance before trusting it (`strings` shows only `omp --hook`, mtime 20:31 postdates commit `6e6ccdd0` at 19:40, clean worktree, and it read `omp.rs` directly rather than only the binary). Root cause of the five-day `UNREACHABLE`: npm carries two unrelated packages, and Tiller launched the wrong one -- `oh-my-pi` (acidsugarx) is a broken VS Code extension; the agent is `@oh-my-pi/pi-coding-agent`, binary `omp` 17.3.8, which is what the Swift original always spelled. Fully live: real prompt, real turn, real badge transition (running -> needs-input), real tab close, real process exit, real desktop-notification side effect. Adapter-level `omp_live.rs` separately captures all four events reaching `tillerctl notify`. KNOWN GAP, confirmed independently by the critic rather than repeated from the claim: `ctl panel.list` gave the omp pane `pane-0`, and the run's SQLite `session_ref` table holds no row for it -- omp's real `SessionStartEvent` is `{type:"session_start"}` and carries none of `session.id`/`sessionId`/`session.file`, so `--agent-session` is never sent. Inherited verbatim from Swift; the id is reachable via `pi.sessionManager` (`hooks/types.ts:186`). No row covers it. \| independent UI-drive critic, x86 box, 2026-08-19 | sweep ?, 2026-08-14 |
| `F-AGENT-OMP-03` | PASSED | **PASSED 2026-08-19 by an independent UI-drive critic** (`fullapp/F-AGENT-OMP-uidrive.md`), driven through Tiller's real UI with synthetic X11 input inside a private nested Xwayland -- never `DISPLAY=:1`. The critic verified the binary's provenance before trusting it (`strings` shows only `omp --hook`, mtime 20:31 postdates commit `6e6ccdd0` at 19:40, clean worktree, and it read `omp.rs` directly rather than only the binary). Root cause of the five-day `UNREACHABLE`: npm carries two unrelated packages, and Tiller launched the wrong one -- `oh-my-pi` (acidsugarx) is a broken VS Code extension; the agent is `@oh-my-pi/pi-coding-agent`, binary `omp` 17.3.8, which is what the Swift original always spelled. Live for the summarizer command itself (`omp --print --no-tools '<prompt>'` run in a real shell, plus `omp_live.rs` executing `summarizer_command()`'s own output) and for enabling auto-naming in Settings. NOT live for the picker selection: the Settings "Summarizer agent" picker did not open under synthetic click across four attempts, so the critic used the sanctioned shell fallback and said so. That picker is now its own defect -- see `F-SET-*` follow-up; suspected missing `cx.stop_propagation()` in `settings.rs::render_summarizer_trigger`'s `on_click`, by analogy with `PLUS-MENU-INVESTIGATION.md`. \| independent UI-drive critic, x86 box, 2026-08-19 | sweep ?, 2026-08-14 |
| `F-AGENT-SAFE-01` | PASSED | **wave I live drive (this host)**: Overwrote the real SKILL.md with unmarked text, md5 recorded; triggered a second live prepare(): md5 unchanged, and the app log shows the exact live refusal message 'refusing to overwrite unmanaged skill at ...SKILL.md'. | sweep ?, 2026-08-14 |
| `F-AGENT-SAFE-02` | PASSED | **wave I live drive (this host)**: Seeded a stale leading tillerctl path plus an unrelated key/hook into the real settings.local.json, quit and relaunched (the real migration call site in main.rs's boot path): the stale path was rewritten to the real resolved binary path on every managed hook; pane id, statuses, unrelated hook, and unrelated key all survived unchanged. | sweep ?, 2026-08-14 |
| `F-AGENT-SESSION-01` | PASSED | **wave I live drive (this host)**: The live Claude resume above succeeded specifically because AgentSessionValidator found the real .jsonl file; session_validator_checks_claude_and_codex_files_but_trusts_other_agents test green. | sweep ?, 2026-08-14 |
| `F-AGENT-SESSION-02` | PASSED | **wave I live drive (this host)**: Raw session.transcript socket call against the real live pane returned the exact real joined user+assistant text from the real .jsonl; unknown-pane and wrong-worktree negative controls both returned typed errors, not silent success. | sweep ?, 2026-08-14 |
| `F-AGENT-SESSION-03` | PASSED | **wave I live drive (this host)**: Exercised via four separate real adapter executions (Claude/Codex/OpenCode/Pi) each with metacharacter-laden prompts and confirmed no shell interpretation; shell_quote/json_string_literal unit tests (6) also green. | sweep ?, 2026-08-14 |
| `F-AGENT-PLAT-01` | PASSED | **wave I live drive (this host)**: tiller_agents/Cargo.toml's only dependency is serde_json, no macOS API anywhere; every adapter driven this pass launched as a plain CLI process and wrote only worktree-local files, confirmed by direct filesystem inspection across all four real adapters. | sweep ?, 2026-08-14 |

### F-GIT — git tier (16)

| id | verdict | evidence | judged |
|---|---|---|---|
| `F-GIT-RUN-01` | PASSED | **PASSED 2026-08-20 — the live drive is done and the workspace build ran** (`fullapp/BUILDER-shell-fallback-and-git-cap.md`, `cc7d39e3`, merged as `f1567056`; workspace suite run by the integrator the same night, 50 suites / 1212 passed / 0 failed at load 26). **The fixture was chosen for the property most likely to break, and that is what makes this more than a truncation check:** a 3,540,144-byte unstaged diff against a lowered 4096-byte cap is far past a 64 KiB pipe buffer, so a reader that stopped at the cap instead of draining would leave git blocked mid-write forever. The Changes tab reached `ready:true` in **0.634 s** against a 10 s `DEFAULT_GIT_TIMEOUT` — that timing is the proof of the drain; a cap-only test would have passed either way. Both files' stats are correct in the same frame (`huge.txt` −1/+60000, `normal.txt` −0/+1), the `OutputTruncated` text renders verbatim against `tiller_git/src/error.rs:156` (checked in the source here, not taken from the report), and `normal.txt`'s diff renders correctly right below the truncated one, so the failure is confined to the file that caused it. **Scope stated rather than implied:** only the **buffered** path was driven. The streaming runner's other reporting mode — `GitCommandResult::truncated`, which sets a flag and still returns the capture instead of raising — was not exercised, and neither was a clone. Also still open and deliberately untouched by that pass: the **separate finding** below, `tiller_project`'s duplicate git runner with no output cap at all. **Superseded record of the build:** **orchestrator build, 2026-08-19 (commit 18e2a7a9)**: the missing output-limit clause is now BUILT. read_capped replaces read_to_end with a per-stream byte cap (DEFAULT_OUTPUT_LIMIT_BYTES = 10 MiB, overridable via TILLER_GIT_OUTPUT_LIMIT_BYTES on the existing TILLER_GIT_TIMEOUT_MS precedent; a zero or unparseable value falls back to the default rather than disabling the cap). The reader keeps DRAINING past the cap rather than returning early, because a pipe nobody drains fills its kernel buffer and git then blocks forever on its next write -- that drain is what the test pins down. Truncation is reported differently per path on purpose: the streaming runner sets GitCommandResult::truncated and still returns the capture, while the buffered path raises the new GitError::OutputTruncated, because every buffered caller parses its output as a complete document and a truncated `status --porcelain` would read as a short but valid listing. cargo test -p tiller_git: 69 passed (3 new: output_limit_from_env_parses_override_and_falls_back, read_capped_retains_a_prefix_and_drains_the_rest, run_streaming_reports_a_truncated_capture). Blast radius checked statically since a full workspace build was unaffordable: no exhaustive match on tiller_git::GitError exists anywhere, and the match arms in tiller_project/src/error.rs belong to that crate's OWN separate GitError. HALF-PROVEN BECAUSE THE ORCHESTRATOR BUILT IT: a fresh critic owes a live drive through the app (a real diff or clone whose output exceeds a lowered cap) and a workspace build, which this pass could not run. SEPARATE FINDING for a future pass, not part of this row: tiller_project carries a DUPLICATE git runner (its own run/run_with_timeout and its own 3-variant GitError) which has no output cap at all. \\| orchestrator, x86 box, 2026-08-19 | sweep ?, 2026-08-14 |
| `F-GIT-RUN-02` | PASSED | cargo test -p tiller_git today: streaming_lines_arrive_incrementally_before_completion and a_real_git_clone_streams_progress_and_completes both green, proving stderr lines split on CR/LF arrive before process completion. \| wave A shard critic, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |
| `F-GIT-REPO-01` | PASSED | non_git_directory_is_refused + unborn-HEAD cases green in worktree_integration.rs; has_head exercised true/false throughout git_integration.rs. Wrote+ran a throwaway test today confirming is_git_repository(true) on a real linked worktree's .git FILE (the suite only tested the directory case before), then deleted it, not committed. \| wave A shard critic, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |
| `F-GIT-BRANCH-01` | PASSED | cargo test -p tiller_git today: branch_listing_returns_exact_names_and_git_refuses_spaced_names and branch_listing_preserves_spaces_in_names both green. \| wave A shard critic, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |
| `F-GIT-WT-01` | PASSED | All 11 tests in worktree_integration.rs green today: create with/without base, duplicate refusal, dirty-removal refusal, slash/unicode/space branch names, porcelain round-trip. \| wave A shard critic, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |
| `F-GIT-CLONE-01` | PASSED | cargo test -p tiller_git today: a_real_git_clone_streams_progress_and_completes, clone_from_local_repository_reports_receiving_progress, clone_from_file_url_creates_the_requested_destination all green. \| wave A shard critic, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |
| `F-GIT-REMOTE-01` | PASSED | cargo test -p tiller_git today: remote_parsing_supports_github_ssh_https_and_project_suffixes green. \| wave A shard critic, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |
| `F-GIT-STATUS-01` | PASSED | status.rs unit tests (modified/untracked/rename/unmerged/malformed/non-ASCII paths) plus status_and_diff_handle_spaces_and_non_ascii_paths and parse_status_round_trips_real_porcelain_output all green today. \| wave A shard critic, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |
| `F-GIT-STATUS-02` | PASSED | directory_statuses_mark_every_ancestor_with_precedence_and_both_rename_sides + for_entry_classifies_leaves_exactly_as_it_marks_their_ancestors green today; live conflict-repo drive independently showed correct precedence (a UU file in both Staged and Changed, distinct conflict icon colour). \| wave A shard critic, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |
| `F-GIT-ACT-01` | PASSED | actions_stage_unstage_and_discard_single_paths and actions_stage_all_and_discard_all green today; independently reproduced live throughout F-CHG-10/11 (every click git-verified). \| wave A shard critic, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |
| `F-GIT-ACT-02` | PASSED | stage_refuses_a_conflicted_path_without_changing_git, stage_all_refuses_every_conflicted_path_before_mutation, mutation_validation_rejects_stale_and_duplicate_entries all green today. \| wave A shard critic, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |
| `F-GIT-DIFF-01` | PASSED | git_integration.rs's rename/staged-and-worktree/untracked/binary/unborn-HEAD/CRLF/conflicted tests all green today. \| wave A shard critic, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |
| `F-GIT-DIFF-02` | PASSED | diff.rs unit tests (hunks, CRLF-strip, single-number and zero-zero hunk headers, binary marker, multi-hunk accumulation) all green today. \| wave A shard critic, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |
| `F-GIT-DIFF-03` | PASSED | side_by_side_rows_pair_context_zip_replacements_and_pad_pure_runs, side_by_side_preserves_hunks_pairs_runs_and_drops_metadata, side_by_side_handles_real_rename_binary_and_large_context all green today; Unified/Split segmented control also visibly switchable in Changes-tab screenshots this pass. \| wave A shard critic, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |
| `F-GIT-DIFF-04` | PASSED | parses_numstat_with_binary_and_rename, empty_numstat_is_empty, stats_count_staged_files_in_an_unborn_head_repo, stats_fall_back_to_the_diff_for_uncountable_untracked_files all green today. \| wave A shard critic, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |
| `F-GIT-PLAT-01` | PASSED | All 64 tiller_git tests ran today against the real Linux git executable resolved via PATH (git.rs), exercising repository/worktree/status/diff/action flows end-to-end on this box. \| wave A shard critic, x86 box, 2026-08-18 | sweep ?, 2026-08-14 |

### F-PERSIST — persistence package (13)

| id | verdict | evidence | judged |
|---|---|---|---|
| `F-PERSIST-DB-01` | PASSED | **wave H live drive (this host)**: DB survived 5 real restarts; 47/47 persistence tests reran green | sweep ?, 2026-08-14 |
| `F-PERSIST-DB-02` | PASSED | **wave H live drive (this host)**: all documented tables present and populated, direct DB read | sweep ?, 2026-08-14 |
| `F-PERSIST-DB-03` | PASSED | **wave H live drive (this host)**: project/worktree fields correct in DB, is_primary exclusive | sweep ?, 2026-08-14 |
| `F-PERSIST-DB-04` | PASSED | **wave H live drive (this host)**: scrollback bytes round-tripped exactly across a real restart | sweep ?, 2026-08-14 |
| `F-PERSIST-DB-05` | PASSED | **wave H live drive (this host)**: tab kind/title/active-state all survived a real restart | sweep ?, 2026-08-14 |
| `F-PERSIST-DB-06` | PASSED | **wave H live drive (this host)**: account_identity row matches the live signed-in Claude account | sweep ?, 2026-08-14 |
| `F-PERSIST-DB-07` | PASSED | **wave H live drive (this host)**: deliberate corruption test: quarantine_record captured original bytes+reason, siblings untouched, no crash | sweep ?, 2026-08-14 |
| `F-PERSIST-DB-08` | PASSED | **wave H live drive (this host)**: primary exclusivity + duplicate rejection confirmed live and in DB | sweep ?, 2026-08-14 |
| `F-PERSIST-DB-09` | PASSED | **wave H live drive (this host)**: same corruption test: valid siblings and active-tab selection survived | sweep ?, 2026-08-14 |
| `F-PERSIST-DB-10` | PASSED | **wave H live drive (this host)**: session_ref row matches the live ACP chat session UUID | sweep ?, 2026-08-14 |
| `F-PERSIST-DB-11` | PASSED | **wave H live drive (this host)**: v6→current migration test reran green live | sweep ?, 2026-08-14 |
| `F-PERSIST-DB-12` | N/A — platform | **reclassified from UNREACHABLE, 2026-08-18**: the row asks to verify a compatibility RISK created by GRDB migration v17 renaming `terminalTab` to `legacyTerminalTab_v15` while legacy save/load paths still name `terminalTab`. The Rust migrator is written from scratch (migrations.rs migrate_v1 creates the tab table with current column names) and never had a `terminalTab` table; `grep -rn 'terminalTab\|legacyTerminalTab' rust/crates` is empty workspace-wide. The risk is structurally absent here, so the row does not apply — UNREACHABLE wrongly implied a critic might reach it later. | sweep ?, 2026-08-14 |
| `F-PERSIST-PLAT-01` | PASSED | **wave H live drive (this host)**: DB path resolution is cleanly cfg(target_os)-gated, no leakage; live-exercised throughout | sweep ?, 2026-08-14 |

### F-TERM — terminal package (17)

| id | verdict | evidence | judged |
|---|---|---|---|
| `F-TERM-PTY-01` | PASSED | **wave F live drive (this host)**: Named tests real_pty_emits_osc_title_and_settled_output (rust/crates/tiller_terminal/src/lib.rs:3060) and resize_reaches_the_child_pty (line 2365), run this pass, green. | sweep ?, 2026-08-14 |
| `F-TERM-PTY-02` | PASSED | **wave F live drive (this host)**: Named test child_exit_status_preserves_normal_and_signal_termination (rust/crates/tiller_terminal/src/lib.rs:2200), run green; corroborated live by F-TERM-03's exit-0/exit-7 drives. | sweep ?, 2026-08-14 |
| `F-TERM-PTY-03` | PASSED | **wave F live drive (this host)**: Named test terminal_child_receives_the_pane_id_environment (rust/crates/tiller_terminal/src/lib.rs:2138) plus a fresh live drive: echo TERM=$TERM SHELL=$SHELL PANE=$TILLER_PANE_ID printed 'TERM=xterm-256color SHELL=/bin/bash PANE=pane-0' (/tmp/wf-term-envcheck3). | sweep ?, 2026-08-14 |
| `F-TERM-REG-01` | PASSED | **wave F live drive (this host)**: Named tests in rust/crates/tiller_control/tests/control_integration.rs, run green this pass: pane_registry_runs_a_real_command_and_returns_output_and_exit_code, pane_registry_writes_input_to_a_live_command, pane_registry_close_terminates_process_group, pane_registry_lists_live_application_panes_for_their_worktree, pane_registry_reports_live_state_and_distinguishes_closed_from_unknown, pane_registry_shutdown_terminates_live_children. The 'unknown pane' behavior was live-corroborated by accident: a mistaken pane id sent over the real socket returned exactly {"ok":false,"error":"unknown pane: <id>"}. | sweep ?, 2026-08-14 |
| `F-TERM-REG-02` | PASSED | **wave F live drive (this host)**: Same pane_registry_* close/shutdown test suite (asserts no live child survives a close), run green this pass. | sweep ?, 2026-08-14 |
| `F-TERM-SCR-01` | PASSED | **wave F live drive (this host)**: Live socket transcript captured this pass (/tmp/wf-term-scr01.json): wrote >256 KiB numbered output, read the pane back, buffer was exactly 262144 bytes with newest content retained, matching SCROLLBACK_CAPACITY in rust/crates/tiller_control/src/panel.rs. | sweep ?, 2026-08-14 |
| `F-TERM-SCR-02` | PASSED | **wave O (live, this host)**: 6 real pointer-driven resize ops collapsed to exactly 1 delivered WINCH, 204ms after gesture-end, via panel.read burst-counting (commit 1188dbf4) [original defect independently reproduced on a pre-fix binary] \\| wf-sweep2 critic, x86 box, 2026-08-19 | sweep ?, 2026-08-14 |
| `F-TERM-PTY-04` | PASSED | **PASSED 2026-08-20 — both things this cell asked a fresh critic for are done** (`fullapp/BUILDER-shell-fallback-and-git-cap.md`, `cc7d39e3`, merged as `f1567056`). With `$SHELL` unset for the whole app process (`env -u SHELL` around the launch) and `/bin/zsh` confirmed absent on this box, `ctrl-t` — the real shortcut, same `open_action(NewTabAction::NewTerminal)` path the `+` menu uses — opened a working, interactive pane. `ps -p $$ -o comm=,args=` printed `bash  /bin/bash -il`: the shell naming its own argv, which is `default_system_shell()`'s Linux arm exactly. **Two independent sources in one frame** — fastfetch's own `/proc`-based `Shell: bash 5.2.21` line corroborates it without going through the shell at all. (Cosmetic caution for a later reader: that frame's prompt decoration reads `in zsh` at top right. It is a prompt segment, not a shell identity; `ps` and `/proc` both say bash and they are the authorities here.) **The judgement leg the cell also asked for, answered rather than dodged:** the critic's view is that bash-then-sh is not wrong but is not the most idiomatic Linux answer — most emulators consult the passwd entry before a hardcoded binary — and recommends `$SHELL` → passwd entry (when present and not `nologin`-style) → bash → sh, while ranking it **a legitimate follow-up, not a defect**, since a passwd read carries its own failure modes (locked accounts, stale entries, no NSS in a minimal container) and would still need this same chain underneath. The current code fully fixes the crash this row exists for. **Superseded record of the build:** **orchestrator build, 2026-08-19**: the ungated /bin/zsh fallback is FIXED. tiller_terminal/src/lib.rs now resolves it through default_system_shell(), gated per platform and naming each target explicitly (macOS keeps /bin/zsh; other unixes prefer /bin/bash falling back to POSIX-guaranteed /bin/sh; Windows resolves COMSPEC and passes no -il) rather than a single cfg(unix) arm, following the warning already in this file that cfg(unix) silently hands macOS the Linux branch. The pre-existing test asserted the defect as correct behaviour; its discriminator was kept and inverted, so spawning must now SUCCEED with $SHELL unset. Red direction proven by construction (`ls /bin/zsh` -> no such file on this box, so the old literal fails the new test's first assertion); green: cargo test -p tiller_terminal --lib -> 47 passed, and cargo check -p tiller clean. HALF-PROVEN BECAUSE THE ORCHESTRATOR BUILT IT: a fresh critic must still drive a real System pane live with $SHELL unset and confirm a working shell appears, and should judge whether preferring /bin/bash over the passwd-file shell is the right Linux answer. The row's terminfo leg is N/A - platform and its scrollback-restore and settled-resize legs were proven by wave Q's lane. \\| orchestrator, x86 box, 2026-08-19 | sweep ?, 2026-08-14 |
| `F-TERM-PTY-05` | PASSED | **wave F live drive (this host)**: Live-driven fresh with a real claude CLI across F-TERM-07/09's session plus a dedicated exit test: connect (real trust-prompt flow), sent 'reply with exactly the single word: PONG' and observed live 'Thinking…' streaming, status glyph transitions (orange→outline '?'), then chord ctrl+d twice in one invocation (CLI's own 'Press Ctrl-D again to exit' appeared after the first) produced 'Process exited successfully' and a checkmark+exit badge on the tab (/tmp/wf-term-agentB/03-01-settle.png). | sweep ?, 2026-08-14 |
| `F-TERM-PTY-06` | PASSED | **wave F live drive (this host)**: Live-driven fresh via wayland-drive.sh's real compositor-delivered xdnd action (genuine text/uri-list Wayland drag, not synthetic paste) onto a live, mid-turn Claude Code pane: dropped /tmp/wf-term-clean2/README.md; composer received the literal quoted path with no trailing newline (not submitted), focus stayed on the pane (/tmp/wf-term-dnd/03-01-after-drop.png). | sweep ?, 2026-08-14 |
| `F-TERM-PTY-07` | PASSED | **wave F live drive (this host)**: Right-clicked live pane -> real 13-item menu incl. Restart Terminal -> clicked it. Screen went fully black (real TerminalState::Pending clear) then a fresh banner rendered. Marker PID before=1020401, after=1025304 (different PID = fresh spawn, the externally-observable effect of the generation bump; surface_generation() itself isn't exposed over the control socket). All in one continuous wayland-drive.sh invocation (separate invocations reboot the app from the shared DB, discovered the hard way). | sweep ?, 2026-08-14 |
| `F-TERM-PTY-08` | PASSED | **wave F live drive (this host)**: Right-clicked Terminal tab -> Move to New Pane (real 2nd pane-group split). Marker MOVEMARK_BEFORE 1078166 visible unbroken in the moved pane's scrollback; typed again post-move: MOVEMARK_AFTER 1078166 -- identical PID, proving the PTY was never torn down. ctl panel.list was tried but is too coarse (unaffected by group membership) -- screenshot+PID pair is the real evidence. | sweep ?, 2026-08-14 |
| `F-TERM-SPLIT-01` | PASSED | **wave F live drive (this host)**: Live-driven earlier in this continuous pass: nested left/right/above/below splits created, resized to the documented 160px MIN_SPLIT_PANE_SIZE, a leaf closed, surviving panes kept live PTYs and correct focus. | sweep ?, 2026-08-14 |
| `F-TERM-UI-01` | PASSED | **wave F live drive (this host)**: Comprehensive 24-frame live sweep /tmp/wf-term-block2a, one continuous invocation, invoked all 10 menu items in order (Copy, Paste, Copy Context, Copy Pane ID, Copy Terminal ID, Set Title, Clear Terminal, Restart Terminal, Close Terminal…) each with the matching effect confirmed on screen. | sweep ?, 2026-08-14 |
| `F-TERM-UI-02` | PASSED | **wave O (live, this host)**: modclick on two distinct URLs in two distinct panes each opened its own correctly-addressed Browser tab (commit ea716a1b) [original defect independently reproduced on a pre-fix binary] \\| wf-sweep2 critic, x86 box, 2026-08-19 | sweep ?, 2026-08-14 |
| `F-TERM-USG-01` | PASSED | **wave F live drive (this host)**: Named tests in rust/crates/tiller_usage/tests/usage_tests.rs, all run green this pass: not_installed_is_reachable_through_the_real_shell_when_claude_is_absent_from_path, logged_out_is_reachable_through_the_real_shell_with_a_fake_claude_on_path, error_is_reachable_through_the_real_shell_with_a_fake_claude_on_path, the_fetch_is_bounded_and_single_attempts_do_not_hang — using a real executable fake-claude script over a genuine PTY, matching the VERIFY text exactly. | sweep ?, 2026-08-14 |
| `F-TERM-PLAT-01` | PASSED | **wave F live drive (this host)**: rust/crates/tiller_terminal/Cargo.toml's full dependency list inspected — no webview/HTML renderer crate (wry/webkit/cef/etc.) present; native GPUI-drawn terminal surface only. | sweep ?, 2026-08-14 |

## Appendix — ACP chat behaviors found by exercising, NOT in the 389 denominator

Pass 17 drove the real ACP chat surface (fixture DB, real `claude` behind
`npm exec @agentclientprotocol/claude-agent-acp`) and catalogued behaviors that no
inventory row owns. **These 14 rows are an appendix by the user's explicit ruling: the
denominator stays 389.** Where a behavior partly overlaps an inventory row, the verdict
lives on that row and this appendix carries the ACP-specific remainder. Progress reports
must count these as "plus 14 newly-found ACP rows not yet in the denominator".

| id | verdict | evidence |
|---|---|---|
| `ACP-01` | PASSED | composer status pill state machine: `● idle` (green, pre-session) → `● working` (amber, during turn — precedes the first streamed chunk) → `● Ask ⌄` (green, post-completion; session mode shown) → `● offline` (gray, transport dead). All four photographed: p17-ah0..ah3, aj1, aj6 |
| `ACP-02` | PASSED | model pill lifecycle: `Opus Plan Mode` pre-session → `Model Opus Plan Mode XHIGH ⌄` once the session starts (p17-ah3) |
| `ACP-03` | PASSED | chat tab indicator lifecycle: ○ idle → ● red filled while working → ✓ done → back to ○ when the transport dies (p17-ah/aj series) |
| `ACP-04` | PASSED | during a turn the composer queues instead of disabling: placeholder "Type to queue for the next turn…" + stop ■ control (p17-aj1); live pixel proof over F-CHAT-06's drawn tests |
| `ACP-05` | PASSED | Files-panel footer shows "Activity 1 running" during a chat turn (p17-aj1) |
| `ACP-06` | PASSED | context ring chip tracks real usage across turns (0% → 5% → 27% in the chat drives; 6% after one counting turn in the terminal drives) |
| `ACP-07` | PASSED | transport-death error entry carries the machine reason JSON and a Retry button; partial streamed output is retained above it (p17-aj6; owning generic row F-CHAT-33) |
| `ACP-08` | FAILED — defective | Retry recovers the session but silently DROPS the failed turn: bridge respawns (new `npm exec` subtree), pill returns to idle, and the prompt is never re-sent — idle at +20s with no response (p17-al2/al3). A user's message vanishes without an error |
| `ACP-09` | half-proven | from `● offline` the composer is inert: the same click+type+Return that worked three times from idle produces nothing, with no feedback (p17-ak4); half-proven because only one input route was tried |
| `ACP-10` | PASSED (measured) | transport-death → UI transition latency in (2s, 13s]: at +2s the stream was still visibly advancing (fresh frame, advancing numbers), at +13s the offline pill and ✓→○ had landed (p17-aj3..aj6) |
| `ACP-11` | PASSED (measured) | bridge architecture: sending in the Chat tab spawns `npm exec @agentclientprotocol/claude-agent-acp@latest` → `sh -c claude-agent-acp` → node, as descendants of the app pid; first-spawn latency ~5-10s (npm resolve) — the paint-lag trap behind two stale frames (p17-artifacts/ah-proctree.txt) |
| `ACP-12` | PASSED | the "dropdown never opens" finding is stale, not live: it predates `ac696cb1` ("wire the mode pill to the live ACP session-mode catalog", 2026-08-15) by about a day. Re-confirmed live in fullapp/F-CHAT.md — clicking the pill after a completed turn opens a real dropdown listing all six modes (Auto, Manual, Accept Edits, Plan Mode, Don't Ask, Bypass Permissions) with the current one highlighted, and selecting one updates the pill label. Its owning row F-CHAT-15 is PASSED, which this row previously contradicted. |
| `ACP-13` | FAILED — absent | the UI chat path never writes `chat_turn` or `session_ref` rows (0 rows after two completed exchanges; WAL-aware read) — the store works, the surface does not call it (owning row F-PER-01, overturned this pass) |
| `ACP-14` | PASSED | kill mid-stream retains the partial streamed content in the transcript above the error entry — nothing already displayed is lost (p17-aj6) |

## Totals

| verdict | count |
|---|---|
| PASSED | **362** |
| half-proven | **15** |
| FAILED — absent | **0** |
| FAILED — defective | **5** |
| UNREACHABLE | **0** |
| N/A — platform | **7** |
| NOT EXERCISED | **0** |
| NOT EXERCISED — blocked on display | **0** |
| builder-claimed, unverified | **0** |
| **total** | **389** |

`UNREACHABLE` groups the three spellings in the body (bare, `— opencode not installed`,
`— no terminalTab table in the Linux schema`); the recount below prints them separately.

**Recounted from the body 2026-08-19** after promoting the merged fixes, again by measuring the
rows rather than adjusting by hand — `grep -oE "^\| \`F-[A-Z0-9-]+\` \| [^|]+ \|" | sort | uniq -c`
returns 355 / 19 / 8 / 7 = 389, which is what this block now says. That pass moved six rows to
PASSED (F-WIN-06, F-CORE-DOM-01, F-CORE-DOM-02, F-CORE-DOM-06, F-TAB-12, F-CORE-FILE-08), two to
half-proven (F-CORE-FILE-01, F-CHG-06), and **downgraded one that had been PASSED** (F-TAB-13,
whose cited evidence F-TAB-12's own fix deleted). **`FAILED — absent` reaches 0 for the first
time**: every remaining failure is a feature that exists and misbehaves, not one that is missing.

**Recounted again the same evening**, after merging the close-confirm focus fix (`c0834159`):
356 / 21 / 5 / 7 = 389. **Then again after the two follow-up drives: 358 / 19 / 5 / 7 = 389**, as
F-TERM-05 and F-TAB-26 moved to PASSED — both were half-proven for want of *evidence* rather than
code, and both were closed by driving the thing the row itself names rather than an adjacent path. That merge moved F-TERM-08 to PASSED and F-TERM-05 and F-TAB-26 to
half-proven. Note the shape of what is left: **five defective rows and twenty-one half-proven
ones**, and most of the half-proven bucket is now waiting on *evidence* rather than on code — a
drive that was never run, or one whose screenshots were lost. That is a different kind of debt
from a broken feature, and it should not be read as twenty-one bugs.

**Recounted 2026-08-20: 360 / 17 / 5 / 7 = 389.** F-CHG-06 and F-CORE-FILE-01 moved to PASSED
(`fullapp/VERIFY-chg06-file01.md`). Both had been half-proven for exactly the reason the paragraph
above describes — code landed and unit-proven, the *live look* never taken — and both were closed
by one drive on a single fixture built to serve them together, carrying a discriminating control so
that "everything renders green" could not pass as agreement. The recount also repaired two rows the
totals script had been unable to parse for some time: `F-TAB-13` was missing its `judged` column and
closing pipe outright, and `F-CHG-05` carried a raw `|` that split its evidence into a fifth cell —
introduced by the 2026-08-19 orphaned-test annotation itself. Both are mechanical, neither changed a
verdict, and `Scripts/ledger-totals.py` now parses all 389 rows without complaint. Worth noting how
they were found: not by reading the table, but because the script says out loud which rows it could
not count. A tally that silently skipped them would have been wrong in a way nobody could see.

**A coverage gap the counts cannot show, found the same evening.** All twenty `F-CHAT-*` rows were
`PASSED` while the chat surface rendered **completely blank after any restart or worktree switch**
(fixed in `e6b3816c`; see `fullapp/BUILDER-chat-empty.md`). The rows are not wrong and are not being
downgraded — each exercises a chat *behaviour*, and those behaviours work once the surface is
visible. But not one of the twenty could have caught this, because none of them asks the prior
question: *does this surface still appear after the app restarts?* The inventory tests what a
feature does, rarely whether it survives a restart. `F-CORE-DOM-01` failed for the same shape —
persisted state that was correct in the database and invisible after boot — and was found only
because someone restarted the app on purpose. Worth a deliberate pass: restart, then look at every
surface, before trusting any row that was only ever verified inside one session.

Recomputed from the body by counting rows, not by hand, 2026-08-14 — the previous block
summed to 388 and disagreed with the body on five of nine verdicts. Anyone editing totals
should recount rather than adjust:

```bash
python3 -c "import re,io;from collections import Counter;s=io.open('docs/linux-rewrite/INVENTORY-LEDGER.md',encoding='utf-8').read();r=re.findall(r'^\| \`(F-[A-Z0-9-]+)\` \| ([^|]+?) \|',s,re.M);c=Counter(v.strip() for _,v in r);print(len(r));[print(f'{n:>4}  {k}') for k,n in c.most_common()]"
```

Entries never independently judged by the critic: **68**
(**25** never claimed by anyone; **22** builder-claimed, unverified; **8** P56;
**7** P50 builder claim; **6** P55 builder).
`NOT EXERCISED — blocked on display` and `UNREACHABLE` rows *are* critic-judged
(as appearance-blocked / environment-impossible), so they are not counted in
the never-judged figure.

**Do not maintain this block by hand — run `python3 Scripts/ledger-totals.py --write`.**
It has been wrong three times. Twice it was merely stale. The third time is the one that
matters: the critic recomputed it correctly at the end of pass 12 and it was wrong again
within minutes, because builders append and claim rows continuously while only the critic
recomputes. A hand-maintained aggregate over a concurrently-edited table is not
occasionally stale, it is guaranteed stale, and every agent picks its next piece from
these numbers. The script also rejects a row whose evidence contains a raw `|`, which
silently shifts every column after it — one such row (`F-SET-05`) had been hiding the
`judged` column from any count, which is why the never-judged figure read 54 against a
true 68.
