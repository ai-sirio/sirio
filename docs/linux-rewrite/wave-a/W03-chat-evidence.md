# W03-chat evidence

## F-CHAT-13 — ledger line 162

- **Claim:** exercised-broken (code-absent; supports triage reclassify to FAILED — absent)
- **Drove:** `grep -rn ExternalPaths rust/crates/` (repo-wide, no hits) and `grep -n "on_drop\|ondrop" rust/crates/tiller_ui/src/chat.rs` (no hits, confirmed chat.rs is at `rust/crates/tiller_ui/src/chat.rs`). Confirmed the attach UI is picker-only: `attach_image` (chat.rs:1960) and `attach_button` (chat.rs:5108) exist, wired to a click-driven image picker, not a drop target.
- **Observed:** No `ExternalPaths` type or drag-and-drop handling anywhere in `rust/crates/`. `chat.rs` has zero `on_drop` callbacks. This corroborates the record's independent finding that `wayland-virtual-pointer.c` has no drag-offer/`wl_data_device` call, and the prior live attempt where the '+' control was reached (cursor confirmed) but produced no popup/dialog. Three independent checks (two live-drive passes on different lanes, one direct source grep) now agree: the feature is source-absent, not merely unreached by this lane's tooling.
- **Discriminating:** yes — this is a source-level absence check, not a default-state capture; a `grep` hit for `ExternalPaths` or `on_drop` would have falsified the "absent" claim.

## F-CHAT-18 — ledger line 167

- **Claim:** partially-exercised
- **Drove:** confirmed `context_ring` div (chat.rs:4755) is unconditionally rendered with `.on_click` -> `toggle_context_popover` (chat.rs:1859), regardless of `context_usage` being `None` (defaults to 0%). Wayland-lane drive: `project.add`, `tab.select index=1` (Chat tab foregrounded — checked, matching the F-CHAT-26/27 lesson), then several blind clicks at candidate coordinates near the composer's bottom-right toolbar (the ring is the last child of a `flex_1`-terminated row, per source layout, so it sits near the right edge of the composer) — `click 1690 930`, `click 1660 930`, `click 1620 940` with forced-repaint shots between each (`02..05-f18-scan-*.png`).
- **Observed:** Grayscale stddev/mean of a 400x300 region above the composer toolbar (x1300,y600) measured near-identical before/after click (base: stddev 24.75/mean 30.37; after-click: stddev 23.25/mean 29.96) — no large new content block appeared. This lane has no element-bounding-box introspection (`system.capabilities` was not re-checked for one; `panel.list`/`pane.surfaces` return no coordinates), so a 16x16px click target could easily be missed entirely; a null result here does not distinguish "popover doesn't open" from "the click missed the ring". Confirmed via source: `ContextUsage` (tiller_acp/src/lib.rs:164) carries only `used: u64`, `size: u64`, `cost: Option<ContextCost>` — no separate input/output/cache breakdown — corroborating triage's identified residual gap independent of the gesture question.
- **Discriminating:** the source-struct check is discriminating (a breakdown field would have falsified the gap claim). The click-scan is not discriminating — inconclusive due to lack of pixel-target certainty, not a proof of absence. State-mechanism half remains proven only by the existing drawn test (`context_ring_shows_reported_usage_and_escape_dismisses_popover`, chat.rs:7831), not freshly by this drive.

## F-CHAT-20 — ledger line 169

- **Claim:** could-not-reach (this lane's tooling has no scroll/axis primitive)
- **Drove:** `grep -n "scroll\|axis" Scripts/wayland-drive.sh` and the pointer-injector source (repo-wide grep for `wayland-virtual-pointer.c`) — no scroll or axis event support anywhere in this lane's action DSL or its virtual pointer client. Confirmed source mechanism independently: `list_state.set_follow_mode(FollowMode::Tail)` (chat.rs:940), `push_entry` checks `self.list_state.is_following_tail()` before re-pinning (chat.rs:994-1000) — the follow-tail half of the mechanism is real and present.
- **Observed:** No `click`/`move`/`type`/`key`/`title` action in `wayland-drive.sh`'s DSL can generate a wheel/axis event, so the manual-scroll-ownership half of this row cannot be exercised through this instrument. Per this task's routing rules the X11 lane (`linux-drive.sh`, `DISPLAY=:1`) is explicitly off-limits to this slice ("Never run Scripts/linux-drive.sh and never use DISPLAY=:1"), so that natural next attempt is not available to me either. Confirms triage's own conclusion verbatim, independently re-derived rather than copied.
- **Discriminating:** the grep is a direct absence check (a `scroll`/`axis` hit would have falsified the "unreachable" claim); it is not a default-state capture.
